# ============================================================
# Text-to-SQL 專案依賴套件
# ============================================================

# --- LLM / LangChain 核心 ---
langchain
langchain-core
langchain-openai
langgraph

# --- 資料庫 ---
sqlalchemy
pymysql

# --- SQL 解析 ---
sqlglot

# --- 工具 ---
python-dotenv
loguru
pyyaml
pandas

# --- 嵌入與數值（直接 import，先前漏列）---
# embedding.py 用 requests 打 NIM /v1/embeddings；table_retriever 用 numpy 算餘弦。
requests
numpy

# 精確版本見 migrate/requirements.lock.txt（換機請用 lock 檔，不要用這一份）
