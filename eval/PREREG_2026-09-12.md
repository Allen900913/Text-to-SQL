# 判準（寫在跑之前）—— 2026-09-12 開發集迴歸 ＋ 驗收集 v4

## 這一輪改了什麼

    c7bc770  拿掉 12 個「宣告了、資料 0 筆」的 enum 值，2 個承重的改成 dead_ok 登記
    0de3fec  補 12 個欄位的 enums 宣告（三條通道全斷的低基數字串欄）
    dfd5ab8  閘門 [4b] 的前提改成會爆的檢查（只影響工具，不影響架構）
    4e1b0ef  payments 的 kind:enum 子句與 enums 區塊本來互相矛盾 —— 改子句

**射程**：`enum_text()`（→ 生成器）＋ payments 的 catalog／retrieval 文件（→ 選表）。
enums 區塊有變的表共 14 張：

    audit_log campaign_profiles customer_profiles employee_profiles
    payment_profiles payments product_specs product_variants
    return_profiles review_profiles shipments store_profiles
    supplier_profiles support_ticket_profiles

開發集 309 題裡，GT SQL 碰到這 14 張表的有 **63 題（20.4%）**。其餘 246 題射程外
—— 但**不當對照組用**：檢索本身會抖（20.5%），射程外的題 Prompt 不是逐位元相同
（memory: retrieval-is-nondeterministic）。

## 開發集（309 題）

基準：六輪 **96.8 ~ 98.7%**（299 ~ 305 / 309）。這組已經飽和，這一跑是**迴歸檢查**，
不是求進步。

    落在 96.8 ~ 98.7%   → 沒有可偵測的回歸。不逐題追。
    低於 96.8%（≤ 298） → 算回歸。只查那幾題在不在上面那 63 題裡；
                          射程外的退掉不追（那是硬幣池）。
    高於 98.7%          → 不宣稱是這次改動的效果（n=1 分不出）。

硬條件（違反就算失敗，與分數無關）：

    · 防禦題維持 4/4
    · 殘留 llm_api_error = 0（重試到底再判分）

## 驗收集 v4（123 題，hash 64199b1b0560b914）

基準：v4 從未跑過。同一把尺的前一版 v3 兩跑 **79.7% / 80.5%**，但 v4 收窄了 3 題
並列歧義，母體不同 —— **v3 的數字只能當量級參考，不是基準線**。

事前登記可以看的切面（`testset_run.py` 預設就只印這些）：

    總分 · 表數切面 · 形狀切面 · 寬窄切面

**不看逐題**（`--show-failures` 不下，`viewed_failures` 必須是 false）。
不論分數如何，**這一跑的結果不拿來改任何東西**。要迭代回開發集。

---

# 結果

## 開發集（2026-09-12，commit 500a240）

    307 / 309 = 99.4%      防禦題 4/4      過度拒答 0/305
    llm_api_error 殘留 0（首跑 23 題 → 四輪 --resume 清零）
    eval_result_1789186397.json

判準說「落在 96.8~98.7% 就是沒有可偵測的回歸」。99.4% **高於窗口上緣**，
照事前寫好的第三條讀：**不宣稱這是這次改動的效果**。那條帶子是六輪疊出來的，
單輪高於上緣分不出是效果還是擲到好的一面。

結論只有一個：**沒有回歸**，可以跑驗收集。

錯的兩題 #64 #296 都是「系統 1 列 / GT 多列」，GT SQL 都不碰那 14 張表 ——
射程外，照判準不追。

旗標（不是判決，留給之後在開發集上處理）：

    僥倖候選 #285   模型只用 review_profiles，GT 要 reviews + review_profiles
                    答案對。要補 alt_sql 還是修 GT 待查。
    檢索漏給 8 題   其中 5 題是同一個形狀：問「總消費金額」給了
                    customer_profiles 沒給 orders —— 寬表快照 vs 母表現算
                    （閘門 [10b]）在檢索層的投影。與答案對錯無關。
