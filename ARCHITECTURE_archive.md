# 現行架構

只寫**今天上線的東西**與它為什麼這樣設計。被否決的方案、實驗過程、歷史數字
一律不在這裡 —— 那些在 `ARCHITECTURE.md`。

```
繁體中文 Text-to-SQL｜LangGraph on NVIDIA NIM
93 張表 / 1071 欄位｜全庫 DDL 77,474 字元
309 題 GT（含 4 題防禦題、11 張表宣告 never_answered）
六輪基準 96.8 ~ 98.7%｜穩定錯 0 題｜約 30s/題
```

---

## 1. Pipeline

```
context_retriever → sql_generator → ast_validator → db_validator
       ↑ (retry)                          ↓ (通過)
       └──────────────────  executor_voter → final_summarizer → END
```

| Node | 職責 | 模型 |
|---|---|---|
| `context_retriever` | 決定這題要看哪幾張表，組 Prompt | 見 §2 |
| `sql_generator` | 產生候選 SQL | gpt-oss-120b |
| `ast_validator` | sqlglot 靜態快篩（三層，見 §5） | — |
| `db_validator` | MySQL `EXPLAIN` 驗證 | — |
| `executor_voter` | 執行、對 `valid_sqls` 投票取結果 | — |
| `final_summarizer` | 結果轉繁體中文 | gpt-oss-20b |

**三個關鍵設計：**

- **重試回 `sql_generator`，不回 `context_retriever`。** 重試是因為 SQL 寫錯不是表選錯，
  重跑檢索只會多花一次 LLM 呼叫拿到完全相同的表。
  代價要知道：**表的集合一旦定了整題就凍住了，選錯表沒有第二次機會。**
- **錯誤訊息會加工**（`db_validator._augment_schema_error`）：EXPLAIN 報欄位不存在時，
  把那張表的真實欄位附回去。模型猜錯欄位時不是被打回票，是**拿到正確答案再寫一次**。
- **`ast_validator` 是 fail-open**：解析不出歸屬的欄位一律放行。
  誤擋的代價遠高於漏擋 —— 漏擋後面有 EXPLAIN 與實際執行接住，誤擋直接吃掉重試預算
  （`MAX_RETRIES = 2` 是跨節點共用的）。

---

## 2. 檢索層：三段漏斗

全庫 DDL 是 77,474 字元，而平均每題只需要 **2.0 張表**（中位數 1 張）。

```
93 張表
   │
   ├─① 相似度收斂    → CANDIDATE_N = 40 張            table_retriever.py
   │                   （每題砍掉 53 張，候選涵蓋 43%）
   │
   ├─② LLM 選表      從 40 張候選裡「選」出必要的表    table_filter.py
   │                   錨點 = LLM 的選擇 ∪ 相似度第 1 名
   │                   候選順序依問題 CRC32 打散
   │
   └─③ KMB 補橋      Steiner tree 近似，補中介表       schema_graph.py
```

**結果：召回 99.2%、平均帶進 2.0 張表、淨度 86.1%。**
對照純相似度 K=3 是召回 86.6% / 3.7 張表 —— **召回更高而表數更少**，
兩者在固定 K 的世界裡是對立的，能同時改善是因為換掉的不是參數，
是**憑什麼決定要幾張表**。

而且 schema 從 41 張長到 93 張的過程中，**每題進 prompt 的表數一直是 2.0 張**。

### 2.1 為什麼是「選」不是「剪」

**相似度知道「這題關於客戶」，但不知道「算總消費必須 JOIN orders」。
那是結構必要性，不是文字相似度，相似度在原理上看不到它。**

所以任何純粹在相似度分數上做的切法都救不了。最難的那一群題（需要 4 張表）
在 K=8 的召回還是 0% —— **剪枝沒辦法把不在名單裡的表加回來**，
正確的形狀是「LLM 從候選裡選」。

同理 `CANDIDATE_N` 是**固定 40**，不是表數的函數：這一段漏斗存在的目的就是
「不管資料庫多大，送進 LLM 的表數有上限」，隨表數成長的公式等於這一層不存在。

### 2.2 ∪ 相似度第 1 名：保險要的是誤差獨立

LLM 偶爾漏掉問題裡**明講**的主體，而那恰恰是相似度最不會錯的部分。
值 +1.2pp，代價每題多 0.2 張表。

> **冗餘元件要的是誤差獨立，不是個別準確度；而誤差獨立來自機制不同，不是輸入不同。**
> 餘弦只比對相似度，LLM 會推理「物流狀態 ≠ 訂單狀態」—— 同一份文字，兩種讀法。

### 2.3 檢索文件只放「表名 + 表註解」

中位數 **60 字元**，**長度與表寬完全脫鉤** —— 最寬的 57 欄表跟 4 欄表一樣短。

理由是兩條互為鏡像的機制：

> **往所有文件加字 → 大家都變像**（所有表對任何查詢都有點像）。
> **往一份文件加字 → 那一份到處排第一。**

所以檢索文件長度必須與表寬無關。這讓寬表問題從「被壓小」變成**結構上不可能發生**。

**推論**：提升檢索只剩一條路 —— 把 `TABLE_COMMENT` 寫好。
檢索向量、LLM 候選目錄、DDL 共用這同一個權威來源。

三條寫註解的硬規則：

1. **舉例只能取自資料，不能取自題目。** 兩種寫法都「符合資料」，
   差別只在有沒有看過測試集。**每一版都要跑保留組**（換掉例子再量一次，
   差多少就是過擬合了多少）。
2. **不要舉具體實體名。** 出現在任何註解裡的值會被踢出值索引（§2.5），
   那句註解等於在扯值索引的後腿。
3. **劃邊界只能用「我是什麼」，不能用「我不是什麼」。**
   嵌入模型不做否定 —— 寫「供應商的名稱不在這裡」只會把「供應商」加進這份文件，
   讓它在問供應商的題上排得**更前面**。

**兩支方向相反的註解偵測器**（都不擋流程，輸出是待查清單）：

| | 驅動來源 | 抓得到 |
|---|---|---|
| `check_comment_blindspot.py` | **問句**（GT SQL 就是「哪張表才對」的答案） | **誘餌**：關鍵詞字面出現在錯的表上 |
| `check_comment_coverage.py` | **欄位註解**（離線 LLM 歸納概念群） | **盲區**：整組概念在表註解裡不存在 |

⚠️ **有缺口不等於要補。** 判準是**有沒有失敗證據** ——
全補 17 個缺口與只補有證據的那一個，買到的東西一模一樣而代價差 13 倍。
而且一張表一行註解裝不下 20~50 個欄位的概念（全庫 302 筆盲區集中在 15 張寬表），
**這個上限不可能靠改註解清完**，那是 §2.4/§2.5 存在的理由。

### 2.4 選表投三票取聯集（`FILTER_VOTES=3`）

**這一層的錯是單向的**：漏一張表這題就死了，多一張表只是 Prompt 長一點。
所以取聯集不取多數決 —— 多數決會把硬幣題的少數正確票丟掉，
而**聯集只動得了硬幣題**（三輪都漏同一張的穩定題它救不了）。

```
漏斗 +KMB 97.1% → 98.5%（逐題配對救回 9 弄丟 0）
e2e  95.96% → 97.38%｜平均表數 2.35 → 2.57（+9.4%）｜選表呼叫 1 → 3 次/題
```

`_vote_seeds(query, n)` 的契約是**第 0 顆 seed 與 `votes=1` 走的那一顆位元相同**，
所以 `FILTER_VOTES=1` 是位元還原的舊行為，隨時可以當對照臂。

### 2.5 值索引（`VALUE_BETA=0.05`、`VALUE_EVIDENCE=1`）

**解的是「問句點名實體的值」**：問「買過 iPhone 15 的客戶」時問句裡沒有任何概念詞，
嵌入不知道 iPhone 15 是商品 —— **但資料庫知道，那就是 `products.name` 裡的一個值。**

它是**第四條通道，不是任何一條的超集**：

| 層 | 解的是什麼 |
|---|---|
| dense 表向量 | 問句用**概念詞**（營收、客訴、備貨天數） |
| LLM 選表 | **結構必要性**（算營收必須 JOIN `order_items`） |
| KMB 補橋 | 橋接表在問句裡**一個字都沒有** |
| **值索引** | 問句點名**實體的值** |

**兩個接點**：dense 層 `+β·min(命中數, 3)` 把表撈進候選；
選表層在候選目錄那一行後面附上事實。第二個接點**連欄位一起記**（不是猜的，
是那個值真的存放的地方），實測「表對的時候欄位 100% 也對」。

三個設計要點：

- **報事實，不報建議 —— 把命中位置全部列出來，讓 LLM 自己選。**
  「運動鞋」確實同時在 `products.name` 與 `search_logs.keyword` 裡，
  **每一條看起來像雜訊的證據都是真的**。只挑一個報就變成有主張的錯。
- **概念詞不進索引**：`這個值如果出現在任何一則 schema 註解裡，它就是概念詞，不收。`
  判準用既有的權威來源，不引進新常數 —— **概念詞本來就是 dense 那一層的工作。**
- **不設基數上限、不做磁碟快取。** 基數上限是一道無聲的懸崖（商品目錄長到 61 項
  整條通道就消失且不報錯），而快取要正確就得有一個能偵測「資料被 UPDATE 過」的指紋
  —— **一個會無聲失效的快取比沒有快取糟**。建索引 5.7 秒，記在行程裡就夠。

**成本天生稀疏**：只有 4.3% 的題有值命中，目錄中位增量 +34 字元。

### 2.6 欄位提示（`COLUMN_HINT_K=2`）

選表那一步原本**看不到欄位**，模型得從一句散文去猜寬表裡有沒有某個欄位。
而全庫 1071 個欄位 100% 都有註解，且註解與問句幾乎逐字對應
（「跟對帳單勾稽」← `is_reconciled`「是否已與金流對帳單勾稽」）。

在候選目錄每張表加一行「本題可能相關的欄位」，依問句餘弦挑 2 欄。
**K=2 就夠，不要往上調** —— 實測正解欄位幾乎都是自己表內的第 1 名，
而目錄從 3,096 漲到 5,568 字元，這條曲線會咬人。

收益有兩條路徑：

```
召回   選表從 [invoices, payments]（漏表）→ [payment_profiles, payments]（涵蓋）
精準   看到 order_returns ▸ quantity（退回數量）之後**丟掉**近義干擾表 order_items
```

⚠️ **這條通道有已知的毒性**：它會把「刻意埋的快照陷阱」變成明顯的捷徑
（`total_spent（累計消費金額（每日結算快照…））` 對「總消費金額」的餘弦
比 `orders.total_amount` 高）。**修好與弄壞來自同一個機制，不存在只留好處的參數設定。**
今天的模型上這個陷阱不再發作，但**它是模型的性質不是提示的性質** ——
換模型之後要重測那一族。

### 2.7 失效時的降級

檢索層是加分項，不是單點故障：

| 失效 | 行為 |
|---|---|
| 嵌入 API 掛掉 | 跳過相似度，**LLM 選表照跑**（它讀表註解不需要向量），只損失 ∪ 第 1 名 |
| LLM 回不出合法 JSON / 選了不存在的表 | 退回純相似度 Top-K |
| 兩者都失效 | 退回全部 93 張表 —— 退化成 Prompt 變長，不是答不出來 |

另外 DDL 一律附上「其他表的表名」（只有名字，約 250 字元）。
**模型只要知道那張表存在就能用它。真正的風險是它不知道，然後拿看得到的表硬湊一個答案。**

---

## 3. KMB：補橋接表要用圖論

「陳先生上個月買了哪些手機」語意上只命中 customers 與 products，
但少了 orders、order_items 就接不起來 —— 而問題裡**沒有任何字詞會命中這兩張表**。
橋接表只能靠外鍵結構找。

用 KMB 而不是 SPH：近似保證相同（皆 2−2/k），搜尋次數 O(k) vs O(k²)。
兩者今天都是微秒級 —— **這是為變大做的準備，不是現在的優化。**

規律：**錨點召回愈低，KMB 補得愈多**（K=3 時補 +10.3pp，production 只補 +1.6pp）。
所以要砍它時看的指標是**錨點召回**，不是端到端。

它今天只救 4 題，但那 4 題是**其他任何一層都救不回來的** ——
`warranty_claims` 沒有 `customer_id`，要接到客戶只能經由 `orders`，
那條路徑在任何語意相似度裡都不存在。

**它救不了「問題主體」**：葉節點（掛在別人旁邊、不在兩個錨點之間）補不進來。
中介表可以外包給 KMB，問題主體不行。

⚠️ **KMB 會遮住上游的缺陷** —— 候選召回測的是 LLM 的選單，不是最終送進生成的表集合，
兩者被 KMB 隔開。閘門 [12] 就是為此而設。

### 3.1 權重記在「表」上而不是邊上

「經過 browse_logs 很貴」本來就是**表**的性質，設定量也從 O(邊) 降到 O(表)。

10 張**行為表**成本 3（browse_logs、reviews、carts、cart_items、wishlists、
wishlist_items、product_qna、search_logs、campaign_clicks、product_recommendations）：
看過 ≠ 買過、放進購物車 ≠ 買過。沒有權重時 KMB 會抄近路，
把「購買」的問題接到「瀏覽」的表上。

14 張 `*_profiles` 維持成本 1 —— 它們是掛在主體旁的一對一檔案表，不是那一族。

**W=3 是唯一可行的值**，兩個邊界夾出來：要大到輸給 3 跳的購買路徑（3 < 3+1），
要小到 carts 自己當錨點時跳數決勝仍會選對橋。W=4 在真實 schema 上就壞了。
`test_schema_graph.py` 有 17 條迴歸檢查鎖住它。

---

## 4. Prompt 層

每題送進去的是：**剪裁後的 DDL ＋ 外鍵提示 ＋ 其他表的表名 ＋ 該範圍的 enum ＋
business_rules ＋ few-shot**。

### 4.1 只用 Few-Shot，不加業務規則

**硬規定。** 規則表達不了情境相依的邊界，而且會跟既有的 few-shot 打架
（規則說「總營收用 `SUM(total_amount)`」而 few-shot 對同一個詞用逐項計算 ——
**Prompt 自我矛盾**）。

⚠️ **但「補既有規則的射程」不等於「加規則」。**
補一則規則的適用邊界是允許的，而且實測是零代價的；**刪掉一則規則的處方會咬人。**

### 4.2 遷移性有兩層，只有一層會遷移

| 類型 | 例子 | 會遷移嗎 |
|---|---|---|
| **謂詞級** | `is_deleted = 0`、狀態值對應 | **會** |
| **結構級** | JOIN 後主體收斂去重的形狀 | **只在形狀完全對齊時** |

寫 few-shot 的三條規矩：**不用評估題本身當範例**（用不同的表靠形狀遷移）、
**把錯的寫法也寫進 `reasoning`**（這類錯誤「看起來完全合理」，
只說正解不足以讓模型認出自己正在犯它）、**接在同族既有範例後面**。

### 4.3 每一則 few-shot 都要寫射程邊界

`reasoning` 會進 Prompt。而**教訓一旦真的送達，就會往相鄰形狀溢出** ——
「『最高』要用 `DENSE_RANK()` 不可用 LIMIT 1」被套到了「前 5 位」上。

所以邊界寫成成對的形式，寫在 `reasoning` 裡：

```
極值（最高/最多/哪一個最…）→ 問的是第一名這個位置，WHERE rnk = 1
top-N（前 5 位/前 10 名）  → 問的是一份 N 筆的清單，ORDER BY …, <唯一鍵> LIMIT N
rnk <= N 當成 top-N 是錯的：DENSE_RANK 排的是「不同的值」。

個體全稱量詞（他的每一張訂單都…）→ 預設他有訂單，用 MIN/MAX 聚合
群組全稱量詞（哪些城市的成員全都…）→ NOT IN 整組排除
NOT IN／NOT EXISTS 會把「一張訂單都沒有的人」也選進來。
```

同樣的成對寫法也用在「問句沒指定來源時，事實表才是權威來源」，
並劃出射程 —— `tier`／`risk_flag` 這些寬表獨有、事實表算不出來的欄位不在射程內。

### 4.4 `enum_fields`：代碼欄位的合法值

註解把「代碼 中文」黏成一串（`下單通路：WEB 官網／APP 行動應用`），
模型會讀成一個值寫出 `channel = 'APP 行動應用'`（實際值 `'APP'`）。

`tools/gen_enum_fields.py` 從 `INFORMATION_SCHEMA` ＋ 真實 `DISTINCT` 值產生 52 項
（手寫 6 ＋ 生成 46）。三個設計決定：

- **納入與否由註解決定，值域由資料決定。** 純看資料會收進人名、銀行名 ——
  那是種子池的產物不是欄位的語意。
- **值域 = 資料 ∪ 註解宣告。** 資料決定**字面形式**（病灶是 `'APP 行動應用'` 不是 `'APP'`），
  註解決定**值域**（某個值現在 0 筆不代表它不合法）。
  **後半是承重的** —— 剪成只取資料會把兩題 `expect: empty` 的題無聲改難度。
- **只收 46 欄不收 74 欄**：只收「註解把代碼與中文黏在一起」（44 欄）與
  「資料有、註解沒提」（2 欄）。另外 28 欄是 `工單狀態 (OPEN/PENDING/CLOSED)` 這種
  有分隔符、沒有可黏的中文，enum 只是把 DDL 已有的資訊再抄一遍。
  **代價是 72% 的題一個字都不加**（中位增量 0）。

`get_enum_text(scoped)` 依選中的表剪裁 —— 剪掉了 DDL 卻留著全部的 enum，
等於在告訴模型有一些它看不到 schema 的欄位可以用。

### 4.5 防禦題：拒答是未解問題，動作必須窄

四題斷言「93 張表的 schema 裡沒有這個概念」：運費、利潤率、庫存週轉率、信用評分。
研究界說這個失效未解（推理型模型**反而更不知道何時該不答**），所以不要期待修到 100%。

漏接的形狀是模型自己發明 `SUM(quantity) / capacity AS turnover_rate` ——
**SQL 合法、EXPLAIN 會過、使用者看不出那個數字是憑空來的。**

處理方式是**窄規則 + 正向邊界**。全庫有 19 題可回答的題目提到相鄰詞
（手續費率、平均評分、總消費金額），任何粗暴的關鍵詞拒答都會誤傷它們：

```
擋的是「週轉」這個沒有定義的比率，不是庫存與出貨這兩個有資料的量。
「每個倉庫的容量是多少」「哪些商品庫存是 0」→ 直接答。
```

**只在觀測到漏接的那兩題加規則**，另外兩題沒加 —— 加規則是純推測還要付誤觸風險。

---

## 5. 驗證器：三層，性質完全不同

| 層 | 來源 | 會不會錯 | 處置 |
|---|---|---|---|
| **3a 全庫白名單** | `INFORMATION_SCHEMA` | 人定的，不會錯 | **硬擋**（安全邊界） |
| **3b 欄位存在性** | `INFORMATION_SCHEMA` | 不會錯 | 硬擋 ＋ 把真實欄位附回去 |
| **3d 檢索範圍** | **機器猜的**（召回 98.4~99.3%，已知會漏） | **會漏** | **聯集，不淘汰** |

**3d 是 `SCOPE_MODE=union`**：模型用了一張真實存在、但這一題沒給它 DDL 的表時，
**把那張表聯集進 scope、補上它的 DDL、記進 `scope_extra`，SQL 照常通過。**

為什麼不淘汰：

- **拿安全層的嚴格度去執行一個會漏的猜測，是把「檢索的錯」升級成「生成的死路」。**
- 實測越界的 SQL **`retry` 全是 0 且全部答對** —— 淘汰的是**已經正確的 SQL**，
  而 `MAX_RETRIES` 是共用的，一次攔截吃掉一半。
- schema drift 由 3b 擋住（它拿真實 `INFORMATION_SCHEMA` 比對並列出合法欄位），
  3d 在那一項上沒有多買到任何東西。

兩個記錄上的規矩：

- **`scope_extra` 無條件記錄**（union 模式下 SQL 通過才是常態，
  只在淘汰時記等於指標永遠是空的）。
- **刻意不寫回 `retrieved_tables`** —— 那一欄是檢索層的輸出，
  混進來會把「檢索漏了、生成端救回來」記成檢索沒漏。

補的 DDL 是給**之後的輪次**用的；補過那張表就在 scope 裡，不會再次觸發，不會迴圈。

---

## 6. 資料與 schema 的產生鏈

**每一項 Prompt 內容都要能追到一個權威來源，而且中間那一步要有閘門守著。**

```
init_db*.py 的 CREATE TABLE（表註解／欄位註解的唯一來源）
   │  tools/sync_table_comments.py --apply     純 ALTER，不動一位元資料
   ▼
MySQL INFORMATION_SCHEMA  ← 白名單、欄位存在性、可檢索性都問這裡
   │  tools/gen_ddl.py --write                 產 semantic_layer.yaml 的 ddl
   │  tools/gen_enum_fields.py --write         產 enum_fields
   ▼
utils/semantic_layer.yaml   ddl ／ enum_fields ／ business_rules ／ few_shot_examples
   │  langgraph_sql/utils/schema_parser.py
   ▼
Prompt
```

`SEMANTIC_LAYER_PATH` 可以覆寫 yaml 路徑 —— **prompt 層的 A/B 兩臂要落得進同一個 commit。**

### 6.1 十三項閘門

**預設 `exit 1`。只警告不阻斷的檢查等於沒有檢查。**

```
check_schema_pipeline.py
  [1] DDL 覆蓋  [2] DDL 新鮮度  [3] 註解同步  [4] regex 可解
  [5] KMB 成本  [6] 覆蓋與宣告  [7] 沒有空表  [8] prompt 鏈
check_derived_consistency.py    [9]  縱向：寬表衍生欄 vs 從母表現算
check_question_ambiguity.py     [10] 題目層：同一個問題兩條路、答案不同
check_predicate_collisions.py   [11] 橫向：兩張平行表對同一件事各擲各的骰子
check_table_retrievability.py   [12] 逐表：被需要時排第幾名
check_question_shape.py         [13] 問句決定得了 GT 的 SELECT 清單嗎
```

前八項守 schema 鏈，第八項守 prompt 鏈（每個欄位挑最長的一非空行當探針，
看它在不在 renderer 的輸出裡；**不比對完整內容** —— 常態誤報的檢查等於沒有檢查）。

後五項各守前面看不到的東西：

- **[9] 的 `DERIVED` 名單一律紅燈**，不管有沒有題目引用。
  黃燈的語意是「還沒有人負責這一欄」，有人負責的欄位對不上只有一個意思：**那一步沒有跑**。
  （`customer_profiles` 的 6 個衍生欄結構上無法在建表時算完 ——
  母表是後面的腳本才建的，所以對齊注定是獨立的一步，而獨立的一步注定會有人忘記跑。）
- **[10] 的判準是執行，不是相似度。** 實際跑兩條 SQL 比對結果，
  不一致本身就是訊號，不需要門檻（門檻會變成憑感覺調的常數）。
- **[11] 的候選用問句驅動，判決用資料**：互補／包含放行，互斥／獨立（|phi| < 0.25）報警。
  裁決寫進 `ADJUDICATED`（含理由），之後只守迴歸。**修好了會自動放行**——
  沒有這一步，修完之後閘門永遠紅燈，三天後被人忽略。
- **[12] 的紅燈條件是「最差排名」不是平均。** 缺陷藏在尾巴
  （某張表中位排名 5.0 正常、最差 62 掉出候選）。另有一級 🟠 **橙燈**：
  production 過得去但純 dense 掉出候選（靠值索引撐著），不 `exit 1` 但一定點名 ——
  **問題從來不是「有救援」，是沒有人把救援印出來。**
- **[13] 量的是「暴露」不是「缺陷」**，暴露會不會發作是擲硬幣。
  **任何讓命中清單變短的規則都是在對某一組快照過擬合**，所以裁決逐題寫理由，不改判準。

**新閘門上線前要餵已知的壞例子確認它會響。** 第一次報全綠時先懷疑工具。

### 6.2 配題與資料的準則（`seed_distractor_data.py` 的 assert）

**「干擾表」不是表的屬性，是「表 × 題目」的二元關係。**
同一張 `payment_attempts`：問「成功付款金額」它是干擾，問「哪些訂單刷卡失敗過」
它是唯一正解而 `payments` 變成干擾。

所以兩種表都要，比例 **8:2**（82 張有題目、11 張 `never_answered`），
而且**每加 10 張表，8 張要有題目**（閘門 [6] 強制）——
**加表不配題等於沒加**（沒有題目會去要那些欄位，量測完全不動）。

零列的表有兩個壞處：`expect: empty` 的題**必然假通過**（任何對零列表的查詢都回 0 列），
以及「用資料當訊號」的方法會**假性滿分**。全庫空表 0 張。

十條 assert 的重點：

1. 列數比例合理且**不能一律偏小** —— 干擾表全小的話，列數本身就是免費的答案洩漏。
2. 值域與正解表**重疊**（否則值檢索一秒分辨得出來）。
3. 參照完整性、時序一致（子事件不早於父事件）。
4. 查得出「像樣但錯」的答案，不是靜悄悄的空集合。
5. **小表不能靠機率** —— 8 家門市抽三個 p≈0.5 的旗標期望 1.0，實際抽到 0。
   題目要問的條件要用保證式配置寫進去（在寫入前調整已生成的列，**不動亂數序列**）。
6. **非空不等於有鑑別力**：交集要同時小於兩邊，單一條件涵蓋率**上限 75%**。
7. 跨表狀態欄位由母表決定，不能各自隨機（否則會生出「在職卻有離職日」）。
8. **歧義從資料端消掉**比事後補 `alt_sql` 乾淨。
9. 鑑別力要分兩層查：**條件的、以及輸出欄位的**（兩個不同實體投影到同一組值也會假通過）。
10. 雙寬表題要驗「單表捷徑會不會給出不同答案」。

順序必須是**先宣告值域 → 稽核 → 才灌**（判歧義需要知道值域，而值是灌的時候才決定的）。

### 6.3 資料衛生：冗餘可以，矛盾不行

> 兩張表記同一件事、**值相同** = 好的檢索測試（93 張表的設計目的）。
> 兩張表記同一件事、**值不同** = 壞掉的 benchmark，因為「對」沒有定義。

這條界線也回答了「反向指標算不算作弊」：
**讓模型選對表的提示是作弊，讓兩張表講同一句話是資料衛生。**

處置判準：

```
母表唯一決定           → 對齊資料（fix_derived_consistency.py，只下 UPDATE）
兩個量本來就不同       → 收窄問句，不對齊（對齊會毀掉語意）
母表算不出唯一答案     → profile 欄位就是真相來源
落差是刻意的設計       → 宣告不動，但要有可驗證的不變量
```

**真相來源怎麼判：有佐證的贏；都沒佐證則原始窄表贏**
（後加的寬表是獨立擲的骰子）。

**刻意的落差要升級成不變量**：快照欄位對齊到**同一個截止點**而不是即時值
（對齊到即時值會讓同一張表一半快照一半即時，那是更難發現的矛盾），
並且**另外印一行「與即時值不同 27/50 位」，歸零就警告** ——
那個差距就是快照題的鑑別力來源。

⚠️ **對齊要用 post-pass，不要改 `gen_*` 裡那一行** ——
`R.randint(...)` 必須留在原地，**少抽一次整條亂數序列就平移，GT 全毀**。

---

## 7. 評估

```bash
# 零 LLM，幾秒跑完，紅了不要往下走
python tools/check_schema_pipeline.py
python tools/check_derived_consistency.py     # [9]
python tools/check_question_ambiguity.py      # [10]
python tools/check_predicate_collisions.py    # [11]
python tools/check_table_retrievability.py    # [12]
python tools/check_question_shape.py          # [13]
python eval/eval_gt_check.py                  # GT 自檢

# 打 LLM
python eval/test_runner.py                    # 完整 e2e（309 題）
python eval/eval_score.py                     # 對帳 + 防禦題稽核 + 僥倖偵測
python eval/eval_stability.py --ids <錯題>    # 重複取樣分層
```

**重建資料庫之後必跑**（順序不能反）：
`fix_derived_consistency.py --apply` → `check_derived_consistency.py` → `check_predicate_collisions.py`。

### 7.1 報告格式與判準

```
穩定通過 N 題｜硬幣池 M 題（各 k/8）｜穩定錯 K 題｜防禦題 x/4
```

**不報單輪百分比。** MoE 在 temperature=0 仍然不確定，同臂各輪錯題重疊只有 38%~67%
—— **單輪報「錯 13 題」時，其中七八題下一輪就會自己好。**

解析度：n=8 分得出 3/8 → 7/8，分不出 7/8 → 8/8。
**在硬幣題上做改動，驗收比改動貴得多** —— 這本身就是「先不要動」的理由。

四條紀律：

1. **GT 永遠先修正才跑測試。** GT 錯了，模型的錯與 GT 的錯會混在一起無法歸因。
   GT 只選「回答問題所需的最少欄位」（比對對多回寬容、對少回不寬容，
   過度指定一定會製造假錯誤）。
2. **任何用「哪些題錯了」挑出來的子集合，不能再用「那些題錯幾次」去檢定。**
   合法的例外是**事前按治療分組**（按輸入而非按結果選）。
3. **探針（手挑靶題）只能否決，不能放行。** 靶題是依失敗機制挑的，
   任何反轉該機制的介入在它們身上必然大勝 —— 那是選題方式決定的。
   對照組必須隨機抽樣（固定 seed）並**按歷史正確率分層**。
4. **A/B 的臂要釘在版本上，不是釘在環境變數上。** 環境變數相同不代表行為相同 ——
   Python 匯入後模組就定住。**assert 要驗行程內的實際生效模式**，
   而且**永遠不要在 A/B 跑到一半改被測的檔案**。

### 7.2 兩個免費的對照組

- **共用輸入的那一半。** 兩臂共用同一組呼叫時，「輸入逐字相同」的題理論上必須平手 ——
  它是唯一能回答「這個顯著是不是雜訊撿到的」的東西。
- **單層介入自己會生出對照組。** 先確認介入只影響哪一層（grep 它的文字進到哪個 prompt），
  用那一層的輸出把題目切兩半：**輸出相同的那半是免費的雜訊地板**，
  落在裡面的題機制假說再漂亮都不用查。
  ⚠️ 只對單層介入成立 —— 同時進兩個地方的介入不能這樣切。

### 7.3 評估框架自己的四個洞

| 洞 | 工具 |
|---|---|
| 防禦題會被加表**靜默作廢**（它斷言的是整個 schema 的補集） | `tools/check_defence_gt.py` |
| 端到端**低報**檢索與生成失敗（答案對、schema 不對） | `eval/eval_lucky.py` |
| 單輪計分**掩蓋**擲硬幣的題 | `eval/eval_stability.py` |
| 欄位裁剪沒有守門員 | `eval/eval_column_recall.py` |

前兩支併進 `eval_score.py`，而防禦題稽核放在**對帳之前** ——
它最便宜，且**它報警時後面的分數要重讀**。

⚠️ 防禦題那一行**單獨印，不平均進正確率** —— 那是誠實性指標，不該被稀釋掉。
而且它**看不到「守衛該開火卻沒開火」**，也看不到過度拒答。

---

## 8. 改動時的硬約束

- **重跑 `db/init_db.py` 會重新隨機生成資料，309 題 GT 整份失效。**
  schema 要改請走 metadata-only（`ALTER TABLE … COMMENT`）或純新增
  （只 CREATE + INSERT，事後跑逐表 SHA 指紋確認既有表一位元都沒動）。
- **新增資料要用獨立亂數源**（`RNG_LATE = random.Random(...)`）。
  生成迴圈走的是 **YAML 的順序不是程式碼的順序**，「新的 random 加在最後」不夠。
- **不要新增會命中防禦題探針的欄位／表**：運費/freight/shipping_fee、
  利潤/profit/margin/supply_price、週轉/turnover/inventory/stock/capacity、
  信用評分/credit_score/額度/risk/score。
- **改資料前先查它是不是在撐一道題。** 「註解宣告了某個值、資料 0 筆」不一定是缺陷 ——
  有題目考的就是「欄位註解列了某個值 ≠ 資料裡真的有」。判定要拿
  `eval_ground_truth.yaml` 的 `expect` 對帳：`expect: empty` 打在它上面就是**撐題**。
  GT 是活 SQL 現跑，所以改資料不會弄壞計分 —— **傷害是題意，而那不會有任何腳本喊。**
- **以題號為鍵的 YAML，append 是一個會沉默覆寫的動作**（PyYAML 只留最後一個）。
- 改檔 → `py_compile` → 手動驗兩臂真的不同 → 才送背景。
