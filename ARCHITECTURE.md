# 架構與設計決策

這份文件記錄的是**為什麼不是別的做法** —— 每個結論附實測數字，包含被否決的方案。
沒有數字支撐的理由不寫。這個專案裡「聽起來很有道理」的想法，實測後約一半是反的。

> 📏 **系統今天是 93 張表 / 309 題。** 2026-08-21 加了六張寬表與 22 題（80 → 86），
> 2026-08-25 又加了七張刻意語意重疊的寬表與 30 題（86 → 93），
> 所以本文有**三個**量測基準，各段落都會寫明是哪一個：
> **93 張表 / 305~309 題**（§0、§2.7e~§2.7h、§7.8、§7.9 —— 最新重量過的）、
> **86 張表 / 275 題**（§2.6、§2.7~§2.7d、§7.1~§7.6 —— 第一批擴表後量的）、
> **80 張表 / 253 題**（§2.1~§2.4、§3 —— 要 LLM 呼叫、還沒重跑的）。
> 更早只在小 schema 量過的結論一律標成
> ⚠️ **未在 80 張表重測**，並附上重測指令。
> 逐輪的歷史過程（21 → 41 → 60 → 80 的演進、每一題的診斷）在
> `ARCHITECTURE_archive.md` §1~§11，本檔不再引用那些數字。
> **例外是該檔的 §12** —— 那是 93 張表「選表投票聯集」那一輪的原始逐題資料，
> 因為量太大才擺過去，本檔 §2.7i 直接引用它。

**怎麼讀**：§0 是今天的狀態，§1~§4 是設計，§5~§6 是評估紀律，§7 是規模與天花板，
**§8 是這份文件最貴的部分（反覆咬人的四個失敗形狀）**，§9 是今天的缺陷，
§10 是被否決方案總表 —— **想提新方案先查這張表**。

---

## 0. 現況（2026-08-28）

第二批七張**刻意語意重疊**的 1:1 檔案表（shipment / review / payment /
support_ticket / subscription / promotion / return_profiles）已建立、灌完資料、
30 題 GT 已補齊。**既有 86 張表指紋全部一致 —— 279 題 GT 沒有被動到。**

```
schema   93 張表 / 1071 欄位｜全庫 DDL 77,474 字元
         >30 欄 14 張（第一批 7 + 第二批 7）｜最寬 57 欄
         擴充前：86 張 / 763 欄 / DDL 55,580；再之前：80 張 / 482 欄 / 36,654
GT       309 題｜11 張宣告 never_answered｜防禦題 4 題｜alt_sql 50 題
prompt   few-shot 24 則 / 12,472 字元｜business_rules 10 條 / 6,221 字元
基準     單輪 297/309 = 96.1%｜防禦題 4/4（修矛盾前是 298/309 = 96.4%）
         穩定錯 3：#284 #292 #308（n=8 實測；#279 已修、#287 已降級為硬幣）
         擲硬幣：#282 1/8、#287 3/8、#286 5/8；其餘見 §9
         alt_sql 50 題｜GT 全檔無重複鍵（嚴格載入器掃過）
閘門     八項全綠（KMB 成本已為七張新表做過決定）
         第 [9] 項衍生量一致性（縱向）：一致 29｜已宣告 2｜黃燈 0｜紅燈 0
         第 [10] 項題目層多來源歧義：分歧名單 11 題（迴歸測試用，不是缺陷清單）
         第 [11] 項橫向謂詞衝突：待修 0｜已修 1｜未裁決 0｜靜音 7｜一致 8
                （2026-09-01 新增，§7.11；2026-09-02 全庫掃描再清一輪，§7.12）
         第 [12] 項逐表可檢索性：紅燈 0｜黃燈 0｜量不到 11 張（GT 從不需要）
                （2026-09-03 新增，§2.7m；判準是「最差排名」不是平均 —— products
                 被需要時中位數只有 5.0，平均型判準抓不到它）
         全庫同義欄位掃描：19 條對齊全部下完，93 張表有 12 張指紋變動、列數 0 變動
驗收     修完資料後，引用被改欄位的 17 題 × n=8：穩定過 16｜硬幣 1（#302 7/8）｜穩定錯 0
         **`#258` 5/8 → 8/8** —— §7.10 的起點結案，沒有動模型／規則／問句（§7.12 尾）
選表     FILTER_VOTES = 3（投三票取聯集，2026-08-28 上線，見 §2.7i）
```

> ⬆️ **上面那一格 96.1% 是 `FILTER_VOTES=1` 的舊基準。**
> 2026-08-28 把選表改成投三票取聯集之後，三輪 e2e 是
> **97.7% / 97.4% / 97.0%（平均 97.4%，舊臂同三輪平均 96.0%）**，
> 逐題配對在「兩臂輸入真的不同」的 102 題子集上 **7 救 0 丟，p = 0.0156**，
> 防禦題 6 個細胞全部 4/4。
> **§9 的名單已用新臂重跑（2026-09-01，21 題 × n=8）**：
> 穩定錯從 3 題降到 **2 題（`#284` 1/8、`#308` 0/8）**，
> `#282` 1/8 → **8/8 穩定通過**、`#292` 0/8 → 4/8，
> `#40`／`#62` 兩題舊檢索缺陷在 93 張表上各 8/8，已從清單移除。

> 🔁 **修完資料之後的三輪重取（2026-09-02，同臂 `FILTER_VOTES=3`）**：
> **97.4% / 97.7% / 97.1%（平均 97.4%）**，防禦題三輪 12 個細胞全 4/4。
> **與修資料前的三輪平均一模一樣（97.4%）—— 總分位移 0.0pp。**
>
> 這不是「白做」，而是**總分從來就對這件事不敏感**：被改到的 17 題在修之前
> 單輪 e2e 也全部 `ok`，真正的差別只在重複取樣下看得到（`#258` 5/8 → 8/8）。
> §5.2 平常用來擋「分數變好了所以有效」，這裡擋的是反方向的
> **「分數沒變所以沒用」**——同一條紀律，兩個方向都要守。
>
> ```
> 三輪錯題         R1 8 題  #89 #119 #138 #260 #284 #287 #296 #308
>                  R2 7 題  #20 #71 #260 #282 #284 #308 #309
>                  R3 9 題  #20 #34 #248 #284 #287 #292 #296 #308 #309
> 三輪都錯（2 題）  #284 #308        ← 與 §9 的 n=8 名單完全吻合
> 錯兩輪（5 題）    #20 #260 #287 #296 #309
> 只錯一輪（8 題）  #34 #71 #89 #119 #138 #248 #282 #292   ← 純雜訊
> ```
>
> **聯集 15 題、交集 2 題**：單輪報「錯 7 題」時，其中 5 題下一輪就會自己好。
> 這是 §5.2 最直觀的一張表 —— **單輪錯題名單有一半以上不是缺陷。**

**修掉衍生量矛盾之後的 n=8 實測（單輪 297 vs 298 是雜訊，這才是訊號）：**

```
#279  0/8 → 8/8  ✅ 修好了。收窄問句「行銷活動檔案上點擊率最高」就夠，
                    沒有動模型、沒有動資料、沒有加規則
#287  0/8 → 0/8  但病換了：8 次全部選對 review_profiles、8 次全部回 COUNT，
                    值是 34 而 GT 是 36 —— 差的正是 2 則已刪除的評價。**現在是 GT 的問題**
#308  0/8 → 0/8  但病換了：6/8 觸發 SCHEMA_UNSUPPORTED（選表層沒給 return_profiles）、
                    2/8 回 4 列（＝對齊後的正確列數）但欄位不符。資料矛盾修好了，
                    露出底下的選表問題
#284  0/8 → 0/8  8/8 都回 0 列，沒變。真·字面吸引（`product_tags`），加註解解不掉
#292  0/8 → 0/8  5/8 拒答、3/8 回 8 列。沒動過它
#282  1/8 → 1/8  7/8 拒答。沒動過它
```

> **兩題「分數沒變但診斷變了」比分數本身重要。** `#287` 與 `#308` 都還是 0/8，
> 但它們已經不在同一個失敗族裡了 —— 一個變成 GT 缺陷、一個變成選表缺陷。
> 只看總分會以為什麼都沒發生。

**`#287` 露出的是整個 review 家族的 GT 不一致**：走 `reviews` 進來的 6 題
（`#142`／`#143`／`#144`／`#145`／`#188`／`#189`）**全部**濾 `is_deleted = 0`，
走 `review_profiles` 進來的 4 題（`#285`／`#286`／`#287`／`#288`）**全部不濾**。
模型 join 回母表之後套用了題庫自己的多數慣例，然後被判錯。
**只修 `#287` 是 teaching to the test，要修就修整個家族的慣例。**

已統一成「一律濾掉已刪除」（與多數慣例一致）。四題 GT 都改成
`JOIN reviews r ON r.id = p.review_id … AND r.is_deleted = 0`，n=8 實測：

```
#285  8/8 → 8/8  穩定過（誘餌仍成立：is_hidden 8 列與 is_deleted 9 列零重疊）
#286  3/8 → 5/8  擲硬幣（3 次失敗全是「系統 33 列 / GT 32 列」＝沒濾）
#287  0/8 → 3/8  穩定錯 → 擲硬幣（5 次失敗全是「系統 1 列 / GT 34 列」＝沒濾）
#288  8/8 → 8/8  穩定過
```

> **統一慣例沒有把它們變成穩定過，因為模型自己就是搖擺的** ——
> `#286` 與 `#287` 的失敗全部是「這一輪沒濾」，也就是說換成另一個慣例
> 只會換掉哪幾輪過，總票數差不多。
>
> **真正的收穫是結構性的，不是分數：** 原本 6 題要求濾、4 題要求不濾，
> **任何行為一致的模型都不可能十題全對**。統一之後才有可能。
> 這種缺陷不會出現在任何單題的診斷裡，只有把同一族擺在一起看才會現形。

#### 一般化檢查：這次真的只有 review 一族

照 §9 那條教訓（修完一題要問它是不是一整類），掃了全部 14 個 profile 家族的
母表狀態欄位。結果乾淨，三個理由各不相同：

| 家族 | 判定 | 依據 |
|---|---|---|
| `employee_profiles`／`store_profiles` | 本來就對 | GT **已經** `JOIN employees`／`JOIN stores`，不是孤立查 profile 表 |
| `promotion_profiles` | 濾了反而錯 | 加 `promotions.is_active = 1` 會讓 `#302`／`#303`／`#304` 變成 **3→0、2→0、3→0** 全空。這裡 `is_active = 0` 是「檔期結束」不是「不存在」 |
| `orders`／`shipments`／`support_tickets`／`subscriptions`／`order_returns` | 不適用 | `status` 是多值業務維度（`COMPLETED`／`SHIPPED`／`OPEN`…），不是軟刪除，濾掉才是錯的 |

**檢查沒抓到新東西，但那本身就是結果。** 上一輪的問題正是「修完 `#261` 沒有人問還有幾個」。

#### 順帶：YAML 重複鍵靜默吃掉修改

補 `#305` 的 `alt_sql` 時插在 `note` 前面，而該題**本來就有一個 `alt_sql`**（在 `note` 後面）——
PyYAML 取最後一個，我補的那份被靜默丟掉，而且「兩式都 10 列」看起來還很正常。
這與 memory 記過的 `ambiguity_verdicts.yaml` 同一類坑。
已合併成單一清單，並加了一支嚴格載入器掃全檔：**309 題無重複鍵、題號無重複**。

`#305` 那條 `alt_sql` 本身是實測後承認的等價路徑，不是放寬 GT：
稽核把「模型只用 4 張表、沒用 `orders`」標成僥倖候選，但
`order_promotions.order_id` 與 `order_profiles.order_id` 都指向 `orders.id`、
`order_promotions` 沒有孤兒列（0 筆），所以 `orders` 純屬穿透，
兩式逐列相同；鑑別力也沒變（只查到其中一檔仍然只有 5 列）。

**這一輪買到的答案：擴表的代價 100% 落在 LLM 選表層。**

| 層 | 86 → 93 張表的代價 |
|---|---|
| dense 候選@40 | 98.8% → **98.8%**，被擠出候選 **0 題**，平均位移 +0.17 名 |
| LLM 選表 | 新增 **5 題**全滅，全部是「挑了字面更像的舊窄表」 |

| 題 | 問句裡的字面誘餌 | 模型選了 | 答案其實在 | 落點 |
|---|---|---|---|---|
| `#282` | 改過**地址** | `addresses` | `shipment_profiles` | 1/8 |
| `#284` | **標**成易碎品 | `product_tags` | `product_profiles` | 0/8 |
| `#287` | **評價** | `reviews` | `review_profiles` | 0/8 |
| `#292` | **對帳** | `invoices` | `payment_profiles` | 0/8 |
| `#308` | **退貨** | `order_returns` | `return_profiles` | 0/8 |

`#282` / `#308` 直接誠實拒答（拿不到表就答不出來），`#292` 把「對帳」讀成
「沒有發票」查出 0 列，`#287` 用 `reviews.created_at` 減 `shipments.delivered_at`
自己算出 22（正解 36），`#284` 把「易碎」當成 `product_tags` 的行銷標籤 —— 沒有那個標籤，回 0 列。

**這是第三組指向同一個結論的獨立證據**（§7.6 的漏斗剖析、§2.7d 的探針、這一輪的 e2e）：
**dense 層還有大量餘裕，瓶頸在候選目錄那一次 LLM 呼叫。**
路線 ②（在候選目錄加一行概念群）要修的正是這五題 ——
靶子從第一批的 4 題誘餌變成 9 題，而且這五題有 e2e 證據，不只探針分數。

### 0.1 第一批擴表（86 張表）留下的量測，仍然成立

三項零 LLM 量測（確定性，可重跑）：

| | 80 張表 / 253 題 | 86 張表 / 275 題 |
|---|---|---|
| dense K=3 錨點／+KMB | 76.3% / 86.6% | **72.4% / 84.7%** |
| dense K=40 錨點／+KMB | 99.2% / 100.0% | **98.9% / 100.0%** |
| 欄位裁剪 K=4／W=0（全中／召回） | 100% / 100% | **95.2% / 98.0%** ← 破了 |

**檢索層幾乎沒有代價，欄位裁剪破功了 —— 兩件事都是這次擴充要買的答案。**

K=40 的候選天花板從 99.2% 只掉到 98.9%（多了 22 題、多了 6 張表），
+KMB 仍然 100.0%：**寬表不會讓表級檢索變難，因為表數才是檢索的維度，欄位不是。**

而 `eval_column_recall` 第一次不是滿分。K=4 漏欄的 13 題**全部**是新題，
既有 248 題一題沒漏 —— 也就是舊題庫根本測不到這件事（§7.3 的「配題才算數」）：

```
#258 漏 name_en          #259 漏 package_height_cm、is_air_restricted
#261 漏 has_manual_pdf   #262 漏 os_name          #263 漏 reviewed_at
#265 漏 landing_page     #266 漏 work_city        #269 漏 district、floor_area_ping
#270 漏 offers_installation                       #272 漏 quality_grade
#274 漏 clicks、ctr_pct  #275 漏 approved_at       #276 漏 suspended_at、quality_grade
```

K=5 也救不回來（96.7% / 98.7%）。**固定 K 就是 §2.3 那個死法搬到欄位層級**，
只是這次有題目證明了。詳見 §2.6（該節的結論不變，但理由從「省太少」
換成「連正確性都守不住」）。

第三項：每題進 Prompt 的 scoped DDL（GT need-set，零 LLM）

```
既有 1~257    p50   633｜p90 1,688｜max 4,987
新增 258~279  p50 3,337｜p90 6,836｜max 7,971   ← 5 倍，但仍在 10,000 以下
```

**所以兩層 DDL（精簡層＋完整層）今天還不用做**，觸發條件是 p90 超過 10,000。

檢索層：

```
【86 張表 / 275 題，零 LLM，這次重量過】
候選@40             錨點召回 98.9%｜+KMB 100.0%   ← 錨點漏 3 題，KMB 全數補回
候選@20             錨點召回 97.1%｜+KMB  99.3%
候選@10             錨點召回 89.8%｜+KMB  97.1%
純相似度 K=3        錨點召回 72.4%｜+KMB  84.7%   ← 沒有 LLM 過濾的下限
每題 scoped DDL     p50 724｜p90 3,337｜max 7,971 字元（GT need-set）

【80 張表 / 253 題，需要 LLM，尚未在新題上重跑】
漏斗（production）   錨點 97.6%｜**+KMB 99.2%**    ← 漏 #40（候選層）、#62（LLM 層）
∪Top-1 這道保險      值 3 題（98.0% → 99.2%），代價每題多 0.2 張表
dense Top-1 命中     84.6%｜干擾表搶 Top-1 = 0
檢索文件            中位 60 字元（最短 24、最長 167）—— 與表寬無關，見 §2.5
```

> ⚠️ **漏斗那一列有跑次變異。** 同一份設定連跑兩次是 97.2% 與 99.2%（差 2 題），
> 所以要當成「約 99%、漏 2~4 題」而不是一個定值（§5.2）。
> 其餘各列都是確定性量測，可以直接比。

**「穩定錯 0 題」是這個專案第一次做到。**

> ⚠️ **不要報單輪百分比。** 報「穩定通過 N｜硬幣池 M（各 k/8）｜穩定錯 K」。
> 理由見 §5.2 —— 三輪 e2e 分數一模一樣，錯的題卻每輪換一批。

目錄與環境：

```
根目錄  eval_ground_truth.yaml、eval_questions_v2.json、ARCHITECTURE.md、README.md
langgraph_sql/  pipeline（進入點 langgraph_sql/main.py）
eval/           評估腳本 + results/（跑分產出，gitignore）
db/             init_db.py、init_db_ext.py
tools/          八項閘門、建表腳本、gen_ddl、seed
test_script/    單元測試      utils/  semantic_layer.yaml
```

GT 留在根目錄（8 支程式讀它）。**跨目錄用 sys.path bootstrap，不做成套件** ——
改成 `python -m` 會讓文件與 memory 裡幾十條已記錄的指令全部要重寫。
專案裡有兩個 virtualenv（`venv/`、`.venv/`），指令一律用 `venv/Scripts/python.exe`；
系統 python 沒有 `yaml`。

---

## 1. Pipeline 總覽

```
context_retriever → sql_generator → ast_validator → db_validator
       ↑ (retry)                          ↓ (通過)
       └──────────────────  executor_voter → final_summarizer → END
```

| Node | 職責 | 模型 |
|---|---|---|
| `context_retriever` | 從 93 張表裡決定這題要看哪幾張，組出 Prompt | 見 §2 |
| `sql_generator` | 產生 SQL | gpt-oss-120b |
| `ast_validator` | sqlglot 靜態快篩（安全性 + 欄位白名單 + 反模式） | — |
| `db_validator` | MySQL `EXPLAIN` 驗證 | — |
| `executor_voter` | 執行、取結果 | — |
| `final_summarizer` | 結果轉繁體中文 | gpt-oss-20b |

三個非顯然的設計：

- **重試一律回 `sql_generator`，不回 `context_retriever`。** 重試是因為 SQL 寫錯，
  不是表選錯；重跑檢索只會多花一次 LLM 呼叫得到完全相同的表。
- **錯誤訊息會加工**（`_augment_schema_error`）：EXPLAIN 報欄位不存在時，把那張表的
  真實欄位附回去。模型猜錯欄位時不是被打回票，是拿到正確答案再寫一次。
- **`ast_validator` 是 fail-open**：解析不出歸屬的欄位一律放行。誤擋的代價遠高於
  漏擋 —— 漏擋後面有 EXPLAIN 與實際執行接住，誤擋直接吃掉重試預算。
  （`SELECT p.*` 曾被 sqlglot 解析成 `Column(name='*')` 而誤判成幻覺欄位，
  三次重試被同一個誤判吃光。已修；`test_ast_validator.py` 9 項的主體是**誤判**不是漏判。）

---

## 2. 檢索層：為什麼是三段漏斗

### 2.1 核心問題

全庫 80 張表的 DDL 是 36,654 字元，而 253 題**平均每題只需要 1.58 張表**
（中位數 1 張，55.7% 的題只需要一張）。

不做檢索，模型每次要讀 **28 倍**於它需要的 schema。
（**93 張表的今天是 77,474 字元** —— 分子翻倍而每題送進去的仍是 2.0 張表，
倍數只會更大。這一段的論證不需要重測，重測只會讓它更強。）

### 2.2 設計

```
使用者問題
   │
   ├─① 相似度收斂    93 張 → CANDIDATE_N = 40 張                table_retriever.py
   │                 （每題砍掉 53 張，候選只涵蓋 43%）
   │
   ├─② LLM 選表      從 40 張候選裡「選」出邏輯上必要的表        table_filter.py
   │                 錨點 = LLM 的選擇 ∪ 相似度第 1 名
   │                 候選順序依問題雜湊**打散**（不照相似度排，§2.7）
   │
   └─③ KMB 補橋      Steiner tree 近似，補上串接用的中介表      schema_graph.py
```

80 張表的實測：

| | 召回 | 平均帶進的表 | 佔全庫 | 淨度 |
|---|---|---|---|---|
| 純相似度 K=3（＋KMB） | 86.6% | 3.7 | 4.6% | 39.8% |
| **三段漏斗** | **99.2%** | **2.0** | **2.5%** | **86.1%** |

**召回高 12.6pp，同時表數少 46%。** 這兩件事在固定 K 的世界裡是對立的
（K 開大兩者一起上升），能同時改善是因為換掉的不是參數，是**憑什麼決定要幾張表**。

而這一層真正買到的東西是：**schema 是 36,654 字元，每題進 prompt 的只有 1,295
字元（3.5%）** —— 而且這個數字在表數從 41 張長到 80 張的過程中沒有變大。

### 2.3 為什麼不是固定 K、動態門檻、或剪枝 —— 三個都死於同一個根

**相似度知道「這題關於客戶」，但不知道「算總消費必須 JOIN orders」。
那是結構必要性，不是文字相似度，相似度在原理上看不到它。**
所以任何純粹在相似度分數上做的切法都救不了 —— 包含門檻、elbow、加權。

#### 固定 K：同一個 K 同時犯兩個相反的錯

253 題按「實際需要幾張表」拆開（80 張表實測）：

| 實際需要 | 題數 | K=3 召回 | K=5 召回 | K=8 召回 | K=14 召回 | K=5 給的浪費倍數 |
|---|---|---|---|---|---|---|
| 1 張 | 144（56.9%） | 95.8% | 98.6% | 99.3% | 100.0% | **5.0×** |
| 2 張 | 86（34.0%） | 62.8% | 75.6% | 86.0% | 96.5% | 2.5× |
| 3 張 | 11（4.3%） | 9.1% | 27.3% | 54.5% | 81.8% | 1.7× |
| 4 張 | 12（4.7%） | **0.0%** | **0.0%** | **0.0%** | 25.0% | 1.2× |

**K=5 對超過一半的題目慷慨五倍，同時對最難的 12 題召回是 0。**
要讓那 12 題全中得開到 K≈40，那時前面 141 題各拿到 40 倍於需要的 schema。

**K 開大改善下面惡化上面，K 縮小反之 —— 沒有任何 K 值能同時解決。**
這不是調參問題，是「需要幾張表」本來就因題而異，而固定 K 是一個與題目無關的常數。

（K 曾經被 3 → 6 → 4 反覆調整，每次都是在同一個資料集上挑最高分。
那不是設計，是過擬合 —— 這是本專案第一條方法論教訓。）

#### 動態門檻 / Elbow：讓分數自己決定切在哪

80 張表實測（`eval_retrieval.py --ratio`，零 LLM 成本）：

| 設定 | 錨點召回 | +KMB | 平均錨點數 | 淨度 |
|---|---|---|---|---|
| r=0.85 | 64.0% | **69.2%** | 1.7 | 76.3% |
| r=0.75 | 71.9% | **78.7%** | 2.6 | 60.6% |
| 對照 K=3 | 76.3% | **86.6%** | 3.0 | 39.8% |

**餘弦分數根本沒有 elbow。** r=0.85 用 1.7 個錨點換到 69.2%，而 K=3 用 3.0 個錨點
拿到 86.6% —— 每一個軸都輸。

更關鍵的是**它漏的是第二個錨點，而那連帶關掉了第三段**。單一錨點時
KMB 在數學上不可能作用（單一終端點的 Steiner tree 就是那個點自己）。
80 張表的 K=1 是最乾淨的證據：

```
K=1   錨點召回 49.4%  →  +KMB 49.4%   ← 一張表都補不回來
K=3   錨點召回 76.3%  →  +KMB 86.6%   ← 兩個以上錨點，KMB 補回 10.3pp
```

#### 剪枝（Top-6 再讓 LLM 刪）

**剪枝沒辦法把不在名單裡的表加回來** —— 而上表那 12 題需要 4 張表的難題，
問題正是需要的表從沒進過 Top-K（K=8 召回還是 0%）。
所以正確的形狀是「LLM 從候選裡**選**」，不是「從短名單裡**刪**」。

> **這個認識就是三段漏斗的由來：既然缺的是推理，就補一層會推理的東西。**

### 2.4 ∪ 相似度第 1 名：保險要的是誤差獨立，不是自己準

LLM 偶爾漏掉問題裡**明講**的主體，而那恰恰是相似度最不會錯的部分。
兩者錯誤型態互補，所以取聯集。

**80 張表實測它值多少**（`--funnel` 的兩臂共用同一組 LLM 呼叫，所以這個對照是免費的）：

| | 錨點召回 | +KMB | 平均表數 | 淨度 |
|---|---|---|---|---|
| LLM 選表單獨 | 96.4% | 98.0% | 1.8 | 90.7% |
| **∪ 相似度 Top-1** | **97.6%** | **99.2%** | 2.0 | 86.1% |

**值 3 題（+1.2pp），代價是每題多帶 0.2 張表、淨度 −4.6pp。** 救回的三題長這樣：

```
#127 已經停產的商品有哪些？        LLM 只選 products     → Top-1 補上 product_specs
#180 有多少位客戶登記了已驗證的聯絡方式  LLM 選 customers(+profiles) → 補上 customer_contacts
#173 列出曾經被取消過的訂單編號與客戶姓名  LLM 選 orders,customers  → 補上 order_cancellations
```

三題的形狀完全一致：**LLM 選了「主體表」，而問題真正要的是掛在它旁邊那張更具體的表。**
餘弦剛好相反 —— 它對字面最貼近的那張表最敏感。**這就是誤差獨立在做事。**

⚠️ **∪ Top-2 / Top-3 仍未在 80 張表重測**（要另一組 LLM 呼叫）。
當時的結論是 Top-2 之後召回不再上升、只是變胖；今天看的是**淨度**那一欄。

**這條原則後來反覆用到。** 欄位級嵌入（§2.5）Top-1 更準，但它把表註解複製進每個
欄位 chunk，讀的東西跟 LLM 高度重疊 —— 更準，但**錯得更一致**，保險失效。

> **冗餘元件要的是誤差獨立，不是個別準確度。**
> 而誤差獨立來自**機制不同**，不是來自輸入不同：餘弦只會比對相似度，
> LLM 會推理「物流狀態 ≠ 訂單狀態」—— 同一份文字，兩種讀法。

### 2.5 檢索文件與候選目錄裡放什麼：只放表名 + 表註解

**今天的結果**：檢索文件中位數 **60 字元**（最短 24、最長 167）——
**長度與表寬完全脫鉤**。全庫最寬的表是 57 欄，它的檢索文件跟一張 4 欄的表一樣短。

這是刻意的。文件長度一旦是表寬的函數，兩條互為鏡像的機制就會同時發作：

> **往所有文件加字 → 大家都變像**（稀釋定律：所有表對任何查詢都有點像）。
> **往一份文件加字 → 那一份到處排第一**（57 欄的寬表誤選 24/139，
> 而肇事者 100% 是餘弦，LLM 一次都沒被騙）。

⚠️ **底下五個實驗是在 21~22 張表的年代量的，未在 80 張表重測**
（要重跑得把 80 張表用不同的文件配方各嵌入一次）。**保留它們的理由是
今天的配置就是它們的產物**，而且五個獨立實驗收斂到同一點：

| 實驗 | 加了什麼 | 當時的結果 |
|---|---|---|
| 稀釋定律 ① | LLM 合成的「典型問法」 | K=3 87.1% → 82.7% |
| 稀釋定律 ② | enum 中文狀態值對照 | 87.1% → 86.3% |
| 欄位進檢索文件 | 全部欄位名 + 註解 | Top-1 84.9% → 76.3%，寬表誤選 9 → 24 |
| 欄位進候選目錄 | 六種呈現方式都試過 | 錨點召回 94~95% → 87.7~92.9%，**沒有一種對寬表題有幫助** |
| 概念短語列進嵌入 | 含限定詞的概念短語 | Top-1 83.9% → 80.6%，寬表誤選 10 → 22 |

**重測的觸發條件寫的是中位數，而中位數沒有動**：稀釋定律的射程是
「文件長到會互相稀釋」，80 張表時**只有 2 張超過 12 欄、1 張超過 30 欄**，
中位表寬 5 欄 —— 根本沒有東西可以稀釋。**表寬中位數超過約 30 欄時要回來重測。**

⚠️ **93 張表的今天：中位表寬仍然是 5 欄（79 張窄表壓著），但 >12 欄的有 15 張、
>30 欄的有 14 張，最寬 57 欄。** 觸發條件字面上沒成立而分布已經變了 ——
§2.7f 就是用「另一個理由」回來重測的，兩者的差別見該節的方法論教訓 ②。

三個從中長出來、與規模無關的結論：

1. **檢索文件長度必須與表寬無關。** 第一版修法是「替寬表的欄位清單設上限」，
   那只是壓小症狀還多一個憑感覺的常數。根治是只放表註解 ——
   **寬表問題從「被壓小」變成「結構上不可能發生」**。
2. **限定詞在向量空間裡是加強誘餌，不是警告牌。** 加上「（非即時，落後 30 天）」
   之後誤選反而增加 —— 模型只看到我又多塞了幾個「消費金額」token。
3. **描述寫得更好能救「盲區」，救不了「誘餌」。**
   盲區＝概念根本沒被提到，加字就能解；誘餌＝概念被提到了但不是你要的那一個，
   加字只會讓誘餌更顯眼。**這是機制性的上限，不是措辭問題。**

**所以提升檢索只剩一條路：把 `TABLE_COMMENT` 寫好。**
檢索向量、LLM 候選目錄、DDL 現在共用這同一個權威來源。
註解的唯一來源是 `init_db*.py` 的 `CREATE TABLE`，用
`tools/sync_table_comments.py --apply` 同步（純 metadata，不動一位元資料）。

#### 盲區補強：一個可重複的程序

`#104`（付款失敗）與 `#154`（Email 驗證）是同一個形狀：
**問句的關鍵詞字面出現在錯的那張表上，正解表的註解裡沒有。**
兩次都用同一套動作修好（0/6 → 6/6、4/8 → 8/8）：

1. 印出實際餵給生成器的 `schema_ddl`，確認缺的是**表**還是**欄位**
2. 比對正解表與競爭表的 `TABLE_COMMENT`
3. 改 `init_db*.py` 的 COMMENT，`sync_table_comments.py --apply`
4. **補概念家族，不補那一題的關鍵詞**（避免 teaching to the test）
5. 跑稀釋鏡像檢查：新舊註解各嵌入一次頭對頭，看 Top-1／誤選／進前 3／搶 Top-1
6. `eval_stability.py --ids <目標題> + <同表其他題>`，確認修好且無回歸

**往干擾表加指路標沒有用** —— `payment_attempts` 的註解本來就寫著
「成功的付款結果在 payments」而完全無效。**要補的永遠是正解表的盲區。**

**兩支偵測器，方向相反，缺一不可：**

| | 驅動來源 | 抓得到 | 抓不到 |
|---|---|---|---|
| `check_comment_blindspot.py` | **問句**（GT SQL 就是「哪張表才對」的答案） | **誘餌**：關鍵詞字面出現在錯的表上 | 還沒被問到的概念 |
| `check_comment_coverage.py` | **欄位註解**（離線 LLM 歸納概念群） | **盲區**：整組概念在表註解裡不存在 | 誘餌（概念寫了但別張表更像） |

blindspot 上線第一次就獨立重新發現了已知的汙染 —— 指出的五題與另一份實測的
干擾清單**逐題吻合**，而它沒看過那份清單。
**用法是門檻不是清單：誘餌數應維持 0，加新表時重跑。**
（它第一版是表驅動的，不知道哪張表才對，40 個候選大半是誤報。）

coverage 是 2026-08-22 補的，起因是 blindspot **照設計看不見 `#263`**：
`order_profiles` 有四個 `fraud_review_*` 欄位而表註解一字未提，
但問句寫的是口語的「被系統擋下來、要人工看過」，字面上跟註解沒得比。
coverage 不看題目，只問「欄位裡有的概念，表註解漏了哪些」——
所以它能在題目被寫出來之前就發現缺口。第一次跑就點名了那一組。
**它的輸出是候選清單不是處方**，因為下面這條容量上限仍然成立。

#### 一張表一句註解的容量上限

**（下面這段寫於 80 張表時代，當時 `customer_profiles`(57 欄) 是全庫唯一超過
30 欄的表；今天有 14 張。結論沒有變，而 §2.7f 後來把這個上限量成了數字：
把 869 個欄位註解各當一句問句，排不進候選前 40 的只有 5.9%，
而且寬表沒有比較慘 —— 5.5% vs 窄表 6.6%。）**

`customer_profiles`（57 欄）是這個上限第一次現形的地方。補完它的一個概念群
（會員系統時間戳）就發現下一個概念（Email 驗證）又漏了，而每加一個概念，
它就在更多不相干的問題上排第一。

> **一張 57 欄的表大約有 5 個概念群，而表註解在被過度選中之前大概只塞得下 3~4 個。**
> 這不是措辭問題，是「一張表一句註解」這個資料結構在寬表上的容量上限。

**2026-08-22 直接量到這個上限。** `check_comment_coverage.py` 在 11 張寬表上
報了 17 個缺口，兩個補法頭對頭（275 題，零 LLM）：

| 設定 | @3 | @5 | @20 | Top-1 | 寬表誤選 | `#263` 排名 | 既有題排名 |
|---|---|---|---|---|---|---|---|
| 現行 | 72.4 | **82.2** | **97.1** | **82.9%** | **16** | 10 | — |
| **窄補**（只補 `#263` 那一組） | **72.7** | **82.5** | **97.1** | 82.5% | 18 | **1** | 變好 6 / **變差 2** |
| 全補（17 個缺口全補） | 72.4 | 80.4 | 96.4 | 81.8% | 17 | **1** | 變好 6 / **變差 27** |

**買到的東西一模一樣（`#263` 都是排名 1），代價差 13 倍。**
「有缺口就補」是錯的做法 —— 判準是**有沒有失敗證據**，不是有沒有缺口。
窄補的寫法也刻意是**壓縮而不是追加**：把「客服接觸紀錄」改寫成
「客服接觸與風控人工覆核」，註解只長 5 個字。

`#104` 與 `#154` 兩題同根。真要解的方向是給寬表**額外的概念群摘要**
（而不是塞進同一句）或階層式選表 —— 都是設計改動，要另外量。

### 2.6 欄位裁剪：86 張表上「沒有一個 K 是安全的」

`eval/eval_column_recall.py`，270 可評估題、86 張表 / 763 欄（鍵 182 個，24%）。
掃「非鍵 Top-K × 寬度閘門 W」的組合：

| K | W | 會裁的表 | 題目欄位全中 | 欄位召回 | 每題省 |
|---|---|---|---|---|---|
| 3 | 0 | 86 | 92.6% | 95.5% | 572 字元 ← **會漏** |
| 4 | 0 | 86 | 95.2% | 98.0% | 517 字元 ← **會漏** |
| 5 | 0 | 86 | 96.7% | 98.7% | 473 字元 ← **會漏** |
| 4 | 12 | 8 | 95.2% | 98.0% | 480 字元 ← **會漏** |

**80 張表時 K=4 是 100.0% / 100.0%，現在沒有任何一組是滿分。**
變的不是演算法，是題目：22 題寬表題進來之後，K=4 漏欄的 13 題**全部**是新題。

這件事本身比裁剪的成敗重要 —— **舊題庫給的 100% 是「沒人問過那些欄位」，
不是「裁剪安全」。** 一個沒有反例的守門員，量到的是題庫的形狀不是系統的能力
（§7.3 的「加表不配題等於沒加」，這裡是它的欄位版）。

四個不做的理由（第 ②、④ 兩條因為上面的數字而改寫）：

**① 失敗模式比表裁剪糟得多，而且是靜默的**

```
表沒給   → 答不出來 / EXPLAIN 報錯 / 空結果      ← 會叫
欄位沒給 → 模型挑一個看起來像的，SQL 合法、
           EXPLAIN 過、結果錯                    ← 不會叫
```

而欄位層級的「語意相近替代品」遠比表層級密集（`created_at`/`paid_at`、
`price`/`unit_price`/`amount`）。

**② 固定 K 已經死了，不是「將來會死」。**
80 張表時的說法是「K=4 剛好貼齊邊界、證明不了 K 該是多少」。
現在有反例了：K=4 漏 13 題、K=5 仍漏 9 題，而**要漏的題只要問四個以上欄位就會漏**
（`#259` 一題就要 5 個非鍵欄位，`#274` 要 6 個）。
**這是 §2.3「固定 K 對不同題型是不同難度」原封不動搬到欄位層級。**
真正的修法只有兩類：讓 K 隨題目走（要準確率量測，不是常數），
或讓漏掉這件事有聲（下面那個兩層架構）。

**③ 沒有預算危機。** 判準不是每張表幾欄，是 DDL 有沒有大到傷害正確率 ——
今天每題 scoped DDL 的 p90 是 3,337 字元（新寬表題也只到 6,836）。而「欄位給太多
模型會挑錯」這個前提**被否證過三次**，最直接的一次是 57 欄的寬表整份丟給生成器
（約 4,500 字元）而那 14 題一題沒錯。

**④ 省下來的是「不會出事的字元」。** 每題省約 500 字元，聽起來比 80 張表時的
278 多 —— 但多出來的節省**正好來自新寬表**，也正好是那 13 題漏欄的地方。
**省得多與漏得多是同一件事**，因為兩者的來源都是「這張表的欄位很多而題目只用幾個」。
所以節省的成長不能當成做這件事的理由。

真要做的那一天，已經量清楚的三件事：

- **鍵一律保留，只對非鍵欄位評分。** 全庫 763 欄有 182 個是鍵（24%），
  而 `customers.id` 跟任何中文問題都沒有語意相似 —— 它的必要性是結構性的。
  **結構性資訊不能交給語意評分決定**（同 §2.3，只是從表層級搬到欄位層級）。
- **加寬度閘門**：W=12 只碰 8 張表（80 張表時是 2 張）就拿到 480/517 的節省。
  風險面積小，但**它守不住新寬表** —— 那八張正是漏欄的來源，閘門擋不到重點。
- **不要用 Elbow**（同 §2.3：餘弦分數沒有斷崖，欄位層級只會更碎）。

而正確的架構不是把賭注押在命中率，是**讓失敗變得有聲**：精簡層（鍵全留 + 非鍵
Top-K + 一行「本表另有 N 欄未列出」）＋ 完整層（模型可主動索取完整清單，
`_augment_schema_error` 已經是它的被動版雛形）＋ AST 白名單不跟著裁。
守門員就是上面那支（欄位召回不是 100% 就 exit 1）。

### 2.7 三個檢索變體在 86 張表的實測（零 LLM）

`eval/eval_retrieval_variants.py`，**275 題、86 張表、值索引 1,488 個相異值**：

| 變體 | @3 | @10 | @20 | @40 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|---|---|
| **baseline（純表向量）** | 72.4 | 89.8 | 97.1 | 98.9 | 83.3% | **0** |
| A 列數先驗 α=0.05 | 71.6 | 89.5 | 97.1 | 98.9 | 84.7% | 0 |
| **C 值檢索 β=0.05** | **73.8** | **91.3** | **97.5** | **100.0** | 84.0% | 0 |
| D 欄位優先 M=20 γ=1.0 | 69.8 | 89.8 | 97.1 | 98.9 | 81.1% | 0 |

**C 在 86 張表上仍然是唯一把候選天花板補到 100.0% 的方案**，而且每一個 K 都不輸。

> ⚠️ **D 的 γ=0.5 兩臂是無效臂，不是「量到沒效果」。**
> 合成規則寫的是 `max(s, γ × own[t])`，而 `s` 是餘弦（0.4~0.8）、
> `0.5 × own` 幾乎永遠更小 —— `max()` 永遠取 `s`，那兩列**在建構上等於 baseline**
> （數字也確實一模一樣）。A 與 C 用加法、D 用 max，三臂的合成規則不一致。
> 真正有作用的只有 γ=1.0，而它讓 @3 掉 2.6pp。
> 這是 §8 ④「對照組與檢查工具本身也要驗」的又一次 —— 三個數字一模一樣就該起疑。

底下是 80 張表 / 253 題那一輪的原始紀錄（值索引 733 個相異值），保留是因為
兩輪的結論一致，而且它記著各方案「當初的賣點」為什麼失效：

| 變體 | @3 | @8 | @14 | @40 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|---|---|
| **baseline（純表向量）** | 76.3 | 88.1 | 94.5 | 99.2 | **84.6%** | **0** |
| A 列數先驗 α=0.05 | 75.5 | 87.7 | 94.5 | 99.2 | 86.6% | 0 |
| A 列數先驗 α=0.1 | 74.3 | 86.2 | 93.7 | 99.2 | **87.4%** | 0 |
| **C 值檢索 β=0.05** | **77.9** | **89.3** | **94.9** | **100.0** | **85.8%** | 0 |
| C 值檢索 β=0.1 | 77.9 | 88.5 | 94.1 | **100.0** | 85.8% | 0 |
| D 欄位優先 M=20 γ=1.0 | 77.1 | 88.1 | 93.7 | 99.2 | 83.0% | 0 |

α/β/γ 是**刻意掃的曲線，不是選出來的參數** —— 看的是有沒有方向，不是哪個數字最高。

**① 干擾表搶 Top-1 現在是 0，整欄都是 0。** 這個汙染通道在 80 張表已經關掉了
（表註解補強的累積效果），所以它**不再是任何方案的賣點** ——
過去 A 與 D 最大的賣點就是把這個數字壓下來。

**② A（列數當熱度）否決。** Top-1 換到 +2.8pp，但**候選召回每一個 K 都變差**
（@3 −2.0pp、@8 −1.9pp）。它在賣一個已經是 0 的東西，卻要付召回。
更根本的是本專案**沒有 query log，列數不是熱度** ——
LinkedIn 用的是存取熱度，而這裡 `payment_attempts`(239 列) 比 `payments`(164 列)
還多，列數先驗幫的是干擾表。

**③ C（值／實體檢索）從「擱置」升級成「候選天花板的解」。**
這是這一輪唯一改變判決的結果：

```
baseline  候選@40 = 99.2%   ← 2 題的必要表落在第 41 名之外，後面兩段救不回來
C β=0.05  候選@40 = 100.0%  ← 補起來了，而且 @3/@8/@14 全部同時上升
```

當時擱置 C 的理由是「深層召回 +2.6pp 但買不到 Top-1，而候選召回已經 100%」——
**兩個前提今天都翻了**：候選召回不再是 100%（§7.2），而 C 現在連 Top-1 都加
1.2pp。它從「方向對但沒東西可買」變成「有東西可買了」。
**仍然不採用，但理由換成「今天損失的是 2 題，還沒有大到值得多維護一個值索引」** ——
觸發條件寫在 §7.2。

**④ D（欄位優先並聯）否決。** 80 張表上它已經不做事了：@3 +0.8pp、
其他 K 全部持平或更差，Top-1 掉 1.6pp。它當初唯一的賣點（把寬表盲點的排名從
4 拉到 1、把干擾搶 Top-1 從 17 壓到 5）在今天分別已由表註解補強與 0 汙染取代。
**86 張表 / 22 題寬表題之後重測，結論不變**（唯一有效的 γ=1.0 讓 @3 掉 2.6pp）——
「寬表盲點」這個賣點現在真的存在了，D 還是買不到它。

### 2.7b 概念群文件：一張表拆成 N 份短文件 —— 否決

**要解的問題**：一張 47~52 欄的寬表有 5~6 個概念群，而**一句表註解塞不下**。
§2.5 量到兩個極端都不行 —— 只放表註解會漏概念（`#263` 的「風控覆核」
整組四個欄位在檢索文件裡完全不存在），放全部欄位則讓文件長度變成表寬的函數。

**想法**：既然問題出在「一份文件要裝五個概念」，那就拆成五份，
每份講一個概念群，表分 = 各群文件的最大值。**每份文件的長度與表寬無關**，
理論上兩個極端都避開了。欄位用嵌入自動指派到最近的群名（不手寫對照表 ——
手寫等於把「更好的文案」偷渡進實驗，量到的就不是結構的效果）。

275 題、86 張表，三臂對照：

| 設定 | 文件數 / 中位長度 | @3 | @5 | @10 | @20 | Top-1 | **寬表誤選** |
|---|---|---|---|---|---|---|---|
| **現行（一表一句）** | 86 / 62 | **72.4** | **82.2** | **89.8** | **97.1** | **82.9%** | **6** |
| 全欄位（§2.5 已否決） | 86 / 106 | 69.5 | 81.1 | 88.4 | 93.8 | 80.0% | 21 |
| 概念群 | 108 / 66 | 71.6 | 78.2 | 89.1 | 96.0 | 80.0% | 19 |

**否決。概念群的代價幾乎等於「全欄位」（寬表誤選 6 → 19 vs 21），
收益卻更小（@5 反而掉 4.0pp）。**

**為什麼失敗 —— 稀釋定律的第四種形式：**

> **稀釋不是來自文件長度，是來自「機會數」。**
> 拆成五份短文件之後，每一份都很短（中位 66 字元，跟現行的 62 幾乎一樣），
> 但那張表對每個查詢有**五次機會**取最大值。
> `max` 是有偏的：抽五次的最大值系統性地大於抽一次。
> 所以寬表照樣到處贏 —— 換了機制，同一個病。

真正買到的東西只有一個，而且很具體：

```
#263（風控覆核）  正解表排名 10 → 1      ← 覆蓋缺口確實被補上了
#264              2 → 1
#278              6 → 4
代價：#258 4 → 6、#266 4 → 6，以及上表那 13 個新的寬表誤選
```

**這正好把兩種病分開了**（§2.5 結論 3 的實例）：
`#263` 是**盲區**（概念根本沒被提到），加字就能解；
`#264`/`#279` 是**誘餌**（概念提到了，但另一張舊表更像），加字解不了 ——
它們在概念群這一臂的排名已經是 1，e2e 卻仍然選 `order_notes` / `campaign_clicks`。

**所以寬表的概念覆蓋不該用檢索結構解，該用「把那一組概念寫進表註解」解**，
而那要一次一張表、走 §6.4 六步、附稀釋鏡像檢查。
`#263` 目前刻意保留不修（§9），它是這個形狀唯一的活例子。

### 2.7c Hubness 校正：把 max 的偏差扣掉 —— 否決，而且理由很有價值

§2.7b 說概念群輸在「機會數」。那就有一個明顯的後續問題：
**機會數的偏差是統計問題，統計問題有標準解，扣掉不就好了？**

`max over N` 讓「對所有查詢都有點像」的文件（hub）更容易被選中。
檢索文獻的標準解是 CSLS / z-normalisation —— 每份文件先扣掉它自己的平均相似度：

```
score(t) = max_i [ cos(q, d_i) − λ · mean_q'( cos(q', d_i) ) ]
```

λ 掃 0.25 / 0.5 / 1.0，**平均值用 split-half 算**（奇數題號算平均、偶數題號評估，
再反過來）—— 用同一批題目算平均等於拿測試集去校正。
**現行那一臂也套同樣的校正**，否則量到的是「校正有沒有用」而不是「結構有沒有用」。

| 設定 | @3 | @5 | @20 | @40 | Top-1 | 寬表誤選 |
|---|---|---|---|---|---|---|
| **現行** | **72.4** | **82.2** | **97.1** | 98.9 | **82.9%** | **6** |
| 現行+CSLS λ=0.25 | 72.4 | 79.6 | 94.5 | 98.5 | 82.5% | 8 |
| 現行+CSLS λ=1.0 | 68.0 | 74.2 | 88.4 | 91.6 | 77.1% | 6 |
| 全欄位 | 69.5 | 81.1 | 93.8 | **99.3** | 80.0% | 21 |
| 全欄位+CSLS λ=1.0 | 68.4 | 76.0 | 88.4 | 91.3 | 81.8% | **0** |
| 概念群 | 71.6 | 78.2 | 96.0 | 98.9 | 80.0% | 19 |
| 概念群+CSLS λ=0.25 | 71.3 | 78.9 | 93.8 | 98.2 | 81.1% | 17 |
| 概念群+CSLS λ=1.0 | 69.1 | 73.8 | 88.7 | 92.0 | 75.6% | 12 |

**沒有任何一組 (臂, λ) 贏過現行。** λ 越大，寬表誤選越少、召回也越差，
單調到沒有中間地帶 —— 校正確實在做它該做的事（`全欄位+CSLS λ=1.0`
把寬表誤選壓到 **0**，全表唯一一個），代價是把召回打到不能用。

**為什麼 —— 這才是這個實驗真正買到的東西：**

> **CSLS 假設 hub 是偽陽性；在這個 schema 裡 hub 大多是真陽性。**
> 「對所有查詢都有點像」的表是 `orders`、`customers`、`products` ——
> 它們**本來就被大多數題目需要**。中心性與真實相關性在這裡是同一件事，
> 所以任何懲罰中心性的校正都會把訊號一起扣掉。

最直接的證據是 `#263`（風控覆核）在**現行**那一臂的排名隨 λ 惡化：

```
λ=0    排名 10
λ=0.25 排名 16
λ=0.5  排名 30
λ=1.0  排名 70      ← 被校正推走的是「需要的表」
```

**結論：一表一票不只是迴避偏差的權宜之計，它是唯一可用的形式。**
「N 份文件 + 去偏」這條路已經量過，關掉了。
剩下的問題也因此定義得更清楚：**一表一票 + 文件長度與表寬無關 ⟹
每張表的資訊量有上限，寬表必然漏概念。**
這個上限不能在 dense 這一層解決，只能換層解決（§7.6：壓力在 LLM 選表層）。

### 2.7d 候選目錄按 query 組裝：機制成立，但還沒過回歸關（進行中）

§2.7b / §2.7c 關掉了「在 dense 層給寬表更多容量」這條路。但**壓力本來就不在
dense 層**（§7.6：漏的三題正解表全都在候選裡），所以容量該加在**候選目錄**——
那裡每張候選表只列一次，**不存在取最大值，也就沒有 max 偏差可付**。

做法：檢索維持一表一票；目錄那一段對寬表多加一行
`▸ 本題可能相關：<概念群>（<代表欄位>）`，概念群由問句向量挑選。

只打**選表**那一次呼叫（比整條 pipeline 便宜約 5 倍，而且直接量在出問題的那一層）。
四題誘餌 + 六題對照，每臂 8 票：

| 配方 | 那一行寫什麼 | 四題合計 | 六題對照 |
|---|---|---|---|
| A 現行 | 沒有 | 14/32 | 全 8/8 |
| B 只列相關度第 1 的群 | 做一次強主張 | 21/32 | 全 8/8 |
| C 列出全部群名，不挑 | **不做主張** | 16/32 | 全 8/8 |
| **D 列相關度前 2 的群** | 放寬的主張 | **25/32** | 全 8/8 |

（B 與 A/C/D 不同輪，只能各自跟同輪的 A 比。）

**三個發現，每個都推翻一個直覺：**

**① 價值不在資訊，在判斷。** C 是「純補容量、不做任何相關性主張」——
我原本以為它最安全，結果它幾乎沒用（16/32），`#279` 還掉到 0/8。
把五個群名全列出來，等於告訴 LLM「這張表什麼都做一點」，
**那讓它看起來更泛用，不是更相關**。B 與 D 都做了判斷，兩者都把 `#279` 修到 8/8。

**② 那一行是主張，不是資訊 —— 主張錯了比不說更糟。**
B 只給第一名，猜錯就把正解表推走（`#263` 6/8 → 2/8、`#264` 7/8 → 3/8）。
D 給前二名，第一名猜錯時第二名接得住，`#263` 回到 6/8 而 `#279` 保住 8/8。
**這一層有旋鈕可調，dense 層那條路沒有 —— 這是兩個實驗合起來才看得出來的。**

**③ 回歸關卡報了紅燈 —— 但紅燈是關卡自己造成的。**
隨機抽 40 題（固定 seed、排除前面那 10 題）A vs D 各 **3 票**：

```
合計 A 115/120、D 114/120   變好 2 題、退步 1 題
#152「同意接收 Email 行銷的客戶有幾位？」  A 3/3 → D 0/3
```

當時我把它讀成崩壞，寫下的根因是「概念群只宣告在六張新表上，
那一行變成打在錯的表上的聚光燈」。**2026-08-22 的反向聚光燈探針推翻了這個結論。**

同一題、同一組候選、同一個 shuffle seed，四臂各 **8 票**：

| 臂 | 候選裡帶標註的表 | `#152` |
|---|---|---|
| A 不標註 | 0 張 | **8/8** |
| D 舊 mapping（嵌入自動指派） | 4 張 | 6/8 |
| D 新 mapping（LLM 宣告） | 4 張 | 7/8 |
| S **只標正解表** | 1 張 | **8/8** |

`0/3` 沒有重現。真值約 6~7/8，而從 p≈0.75 抽三次全錯的機率是 1/64 ——
40 題裡出現一次幾乎是必然。

> **⚠️ 這是 §5.2 被自己的工具違反的實例。** `route2_regress.py` 的註解寫著
> 「n=3 是刻意的：要偵測的是 3/3 → 0~1/3 這種級距的崩壞」——
> 但那個級距**正是 p≈0.75 的雜訊會產生的形狀**。
> 回歸關卡要偵測「掉多少」，就必須先問「不動它的時候會抖多少」。
> §8 ④「對照組與檢查工具本身也要驗」適用於關卡本身，不只適用於對照組。

扣掉雜訊之後，真正留下來的訊號是兩個，都比原本的敘事小、但都是真的：

**聚光燈效應存在，量級是「掉 1~2 票」，不是崩壞。**
替不相干的表加標註會讓正解表從 8/8 掉到 6~7/8；只標正解表（S）完全無害。
方向與原本的直覺一致，**規模差一個數量級**。

**嵌入自動指派在毒害那一行 —— 這才是可回收的損失。**
舊 mapping 在 `#152` 實際印出來長這樣：

```
order_profiles    ▸ 本題可能相關：客服接觸（screen_size、gift_message、support_contact_count）
product_profiles  ▸ 本題可能相關：法規合規（id、product_id、name_en）
```

`screen_size` 被塞進「客服接觸」、`id`／`product_id` 被塞進「法規合規」。
改用 `check_comment_coverage.py` 的 LLM 歸納之後（它本來就回 name + cols），
`customer_profiles` 歸出 7 群、正解需要的「行銷偏好與同意設定」有 8 個欄位，
`#152` 從 6/8 回到 7/8。

**所以 D 的 25/32 是帶著壞掉的 mapping 拿到的 —— 先前是低估，不是高估。**

**下一個開發週期要做的（不是這一輪）：**

1. **群 → 欄位的 mapping 改用 LLM 宣告**（已驗證優於嵌入自動指派）。
   這是四項裡唯一有實測支持的，其餘三項是假設。
2. 概念群宣告到所有寬表 —— `customer_profiles`、`product_specs` 都要有。
   **理由從「不對稱是根因」降級為「S 臂顯示標對表無害、標錯表有小害」**，
   所以對稱化的期望收益是回收那 1~2 票，不是回收一個崩壞。
3. 加相似度門檻：最佳概念群不夠像就**不要標註**。
4. ~~回歸關卡的票數要從 3 提到 8，否則量到的是雜訊。~~
   **2026-08-26 推翻：這是把偏誤誤診成變異數（§5.5）。** 提高票數不會讓手挑靶題的
   結論變得可遷移；要換的是**對照組**（隨機抽樣，不是手挑窄表題）。
   上 production 的驗收仍然是全庫 `--funnel`（當時 275 題，今天 305 題），探針只能否決、不能放行。

**e2e 上的真實靶（但要照 §5.2 讀）**：2026-08-25**第二批擴表前**的 86 張表 / 279 題基準
（**277/279、防禦題 4/4**）裡，唯一答錯的新表題是 `#264`
「有多少客人下單的時候有特別交代說送到之前要先打電話給他？」——
檢索漏給 `order_profiles`，模型改用字面更像的 `order_notes`。**它正是誘餌四題之一。**

但 `eval_stability --ids 89,264` 顯示 **`#264` 是 6/8 的硬幣，不是穩定錯**
（`#89` 也是 6/8）。所以路線 ② 在 e2e 上的可回收空間是「6/8 → 8/8」的那 2 票，
**不是一題全錯**。這與探針量到的量級（掉 1~2 票）一致 ——
兩個獨立的量測第一次對上了。

**因此不在擴表前重跑路線 ② 的完整量測。** 誘餌探針與 40 題回歸都要重跑
（壞 mapping + n=3），但擴到 100 張會讓這些數字全部作廢，順序上該先擴表。

**順帶一個 `∪ Top-1` 的活例子**：`#180`「有多少位客戶登記了已驗證的聯絡方式？」
兩臂都是 0/3 —— **選表層每次都漏**，而 `customer_contacts` 的 dense 排名是 **1**。
這題今天 e2e 會過，靠的正是 §2.4 那道保險。誤差獨立在這裡看得見。

### 2.7e 路線 ② 重測：靶子有三分之一是髒的（2026-08-25）

把 §2.7d 列的四項做掉三項（LLM 宣告 mapping、93 張表全標註、票數 3 → 8），
三臂各 8 票：**A** 現行無標註、**C** 只列群名（不做相關性主張）、
**D** 依問句餘弦挑前二群（放寬的主張）。

```
誘餌 9 題   A 23/72   C 26/72   D 32/72      目錄 3,382 → 5,341 → 6,128 字元（+81%）
對照 6 題   A 46/48   C 48/48   D 48/48
合計       A 69/120  C 74/120  D 80/120
D vs A：變好 [258 264 282 292 152]、退步 [284 308]
```

**但這組數字要重讀 —— 九題誘餌裡有三題（`#279`／`#287`／`#308`）
根本不在測選表**，它們是我自己造的資料矛盾（§7.9）。扣掉之後：

```
乾淨的 6 題   A 21/48 (44%)   C 23/48 (48%)   D 32/48 (67%)
退步只剩 #284（#308 是髒靶）
```

**D 在真正的選表題上是 +11/48，比污染版本（+9/72）好得多。**
但立刻要打的折扣是：**11 票裡有 8 票來自 `#292` 一題**（0/8 → 8/8）。
扣掉它是 21/40 → 24/40，也就是 +3 —— 在 n=8 的解析度內（§5.2）。

**判決：仍然不上線，但理由改了。**
不是「效果小」，而是**單題效應撐起了整個增益，而樣本只有 6 題**。
`#292` 是真的被修好還是取樣運氣，n=8 分不出來；要分得出來得先把 §7.9 的
三題髒靶清掉、重跑 e2e，拿到乾淨的誘餌清單再決定要不要投 275 次 `--funnel`。

> **這一節本身就是 §8 ④ 的例子：探針的靶子也要驗。**
> 三輪路線 ② 量測都把 `#279` 當誘餌題在打，而它根本沒有唯一答案。
> 「對照組與檢查工具本身也要驗」要往上延伸一層 —— **連你在測什麼都要驗。**

### 2.7f 欄位級標註：把欄位放進**候選目錄**（不是檢索文件）—— 待 e2e 驗收

**先講清楚它和 §2.7b 差在哪，因為兩者看起來很像而結論相反：**

```
§2.7b（否決）  把一張表拆成 N 份**檢索文件** → N 份取 max → 寬表有 N 次機會 → 寬表誤選 6 → 21
§2.7f（本節）  檢索層完全不動，只在**候選目錄**替每張表加一行欄位 → 每張表仍只出現一次
```

候選目錄裡不存在取最大值，所以目錄只會變長，不會變偏。**加容量的位置決定成敗。**

#### 起點：選表那一步看不到欄位

`get_table_briefs()` 的 docstring 自己寫著「給 LLM 看的候選清單，**只有註解，不含欄位**」。
於是模型得從一句散文去猜 `shipment_profiles` 裡有沒有「中途改地址」這個欄位。
而全庫 **1071 個欄位 100% 都有註解**，且註解與問句幾乎逐字對應：

```
#282 有沒有哪些出貨是「中途改過地址」的   ← shipment_profiles.is_redirected「是否中途改過送件地址」
#292 付款「還沒跟對帳單勾稽完成」          ← payment_profiles.is_reconciled「是否已與金流對帳單勾稽」
#308 「超過可退期限」才申請退貨            ← return_profiles.is_over_policy_window「申請時是否已超過可退貨期限」
```

零 LLM 的前置量測（869 個欄位文件，排除 `id`／`*_id`）：**八題裡七題，
正解欄位在全庫排 1~2 名，而且都是自己表內的第 1 名** —— 所以 K=2 就夠。
`#284` 的前 8 名全是 `shipment_profiles`／`shipments`，
那張把模型騙走的 `product_tags`**一個都沒進來**。

#### 三臂 + 窄表對照，共 17 題 × 8 票

| 臂 | 目錄多出來的那一行 | 有沒有做相關性主張 |
|---|---|---|
| **A** | 無（現行） | — |
| **F** | 每表列 2 個「最有辨識度」的欄位（離全庫欄位重心最遠者），**與問句無關** | 無 |
| **E** | 每表列 2 個與問句餘弦最高的欄位 | 有 |

```
誘餌   4 題   A   3/32   F   0/32   E  32/32
寬表對照 3 題  A  14/24   F  14/24   E  24/24
窄表對照 10 題 A  66/80     ——       E  72/80     （E 誤拉進寬表的次數：0）
合計  17 題   A  83/136 (61%)        E 128/136 (94%)
E vs A：變好 [282 284 292 308 263 280 24 25]、**退步 []**
目錄長度：A 3,325 → F 6,183 → E 5,966 字元（+79%）
```

**F 臂是本節最有價值的一欄。** 不做主張、同樣加兩欄文字，誘餌題是 **0/32**，
還把 `#282`／`#308`／`#263` 推得更差。

> **收益不是來自「多了欄位文字」，是來自「挑對了欄位」。**
> 這也解釋了 D 臂（§2.7e）為什麼只有 +11/48：D 挑的是「概念群」，
> 再取該群宣告時的**前三個欄位**（`cols[:3]`）—— 跟問句無關。
> D 的主張是半盲的；E 的主張準確度是 1/869。

**§2.7b 擔心的寬表污染沒有發生。** 10 題純窄表題，E 一次都沒有把
`*_profiles` 拉進答案，而且 `#24` 6/8 → 8/8、`#25` 4/8 → 8/8。

#### 全庫驗收：**平手，而且是雙向的 —— 不上線**

上面那組 17 題的數字**沒有在全庫複現**。實作接進 production
（`column_hints.py`＋`format_catalog(..., query=)`）之後跑 305 題 `--funnel`：

```
A 基準（COLUMN_HINT_K=0）  錨點召回 95.7%   +KMB 97.4%   淨度 80.2%
E 欄位提示（K=2）          錨點召回 95.4%   +KMB 97.4%   淨度 80.3%

A 漏：[21, 40, 62, 151, 270, 282, 285, 308]
E 漏：[21, 30, 62,  94, 100, 189, 285, 287]
```

**錨點召回差 0.3pp（305 題裡的 1 題），+KMB 完全一樣。**
把兩臂不同調的 7 題各跑 8 票（走 production 的 `format_catalog`）：

```
#282  1/8 → 8/8 ✅     #94   8/8 → 0/8 ❌
#308  2/8 → 7/8 ✅     #100  8/8 → 0/8 ❌
#287  4/8 → 7/8 ✅     #30   8/8 → 7/8（雜訊）
                       #189  8/8 → 6/8（本來就是硬幣）
合計  A 39/56   E 35/56
```

**`#94`／`#100` 的 8/8 → 0/8 是可歸因的，不是雜訊：**

```
#94「找出消費金額高於所在城市平均消費的客戶」    需要 customers + orders
customer_profiles  0.302  total_spent（累計消費金額（每日結算快照，不含最近數日訂單…））
customers          0.239  city（居住城市）
orders             0.131  total_amount（訂單總金額）
→ E 選了 [customer_profiles, customers]，不 JOIN orders
```

而 `customer_profiles.total_spent` 正是 `seed_customer_profiles` **刻意埋的快照落後陷阱**
（50 位有 27 位對不上，閘門 [9] 已宣告為刻意）。

> **欄位提示會把「刻意的陷阱」變成「明顯的捷徑」。**

那段欄位註解**自己就寫著**「與即時 `SUM(orders.total_amount)` 可能不同」，模型照樣走捷徑
—— 所以不是「警語不夠」，加字沒有用。門檻也切不開：0.302（錯）vs 0.239（對）。
**而且修好與弄壞來自同一個機制**（寬表是正解時命中、寬表是陷阱時也命中），
所以不存在「只留好處」的參數設定。

#### 三個方法論教訓（比結論本身貴）

**① 我提案前沒查 §10。** 那張表第 393 列早就寫著「欄位進候選目錄｜六種呈現方式都試過｜
錨點召回 94~95% → 87.7~92.9%」。§10 的存在理由就是「提新方案前先查這張表」，我漏了。
（那組數字是 80 張表時代的，但這不是我沒查的藉口。）

**② 重測的觸發條件字面上沒有成立。** §10 寫的是「表寬中位數超過約 30 欄時回來重測」，
而 93 張表的中位表寬**還是 5 欄**（79 張窄表壓著）。真正變的是
**>30 欄的表從 1 張變成 14 張**。我的重測是**用另一個理由**站得住，不是觸發條件到了 ——
這個差別要記清楚，否則觸發條件會被當成可以模糊解釋的東西。

**③ 「零退步」是「我測過的 17 題零退步」。** 那 17 題的誘餌四題正是研究最久的題。
我在同一段裡自己寫了「樣本有偏」當保留事項，然後標題還是下「打穿了」——
**保留事項寫在文件裡但沒有影響結論，等於沒寫。**

> **2026-08-26 補上機制**：「樣本有偏」講得太輕。誘餌四題是**依失敗機制挑出來的**，
> 而 E 臂正是反轉那個機制 —— 所以 3/32 → 32/32（p = 0.00001）**在建構上就不可能不出現**。
> 那不是雜訊，是選擇偏誤；提高票數解決不了。完整推論與由此定下的硬規定見 **§5.5**。

#### 追問已經有答案了：不是題目的錯，所以這條路是**永久關閉**，不是暫緩

當時的追問是：`#94`／`#100` 問「總消費金額」，而 `total_spent` 是已宣告的刻意不一致，
那「從快照答」算不算另一個合法答案？如果算，該修的是問句。

閘門 [10]（`tools/check_question_ambiguity.py`）把整族掃出來了 ——
手寫 14 條「快照路線」的等價 SQL，實際執行比對：

```
兩條路答案不同 11 題：[4, 26, 30, 44, 45, 46, 58, 60, 93, 94, 100]
兩條路答案相同  3 題：[5, 56, 74]
```

**但這 11 題不是缺陷，兩個獨立證據：**

1. **11 題在 e2e 全部答對**（不開欄位提示時）。模型自己就走了 `orders` 那條路。
2. 欄位註解**明說**了：
   `total_spent`「累計消費金額（每日結算快照，不含最近數日訂單，與即時 `SUM(orders.total_amount)` 可能不同）」

也就是說 **schema 自己已經裁決了哪一個算數，而模型讀得懂**。這是
`seed_customer_profiles` 刻意埋的快照陷阱在正常運作，`#146`／`#147`／`#148`
三題就是專門在問這個落差 —— **改掉這 11 題會把那三題的鑑別力一起毀掉。**

於是欄位提示的定位變了：

> 它不是「踩到有缺陷的題目」，而是**把一個模型本來 11/11 通過的陷阱，
> 變成模型會掉進去的捷徑**。提示只放大「這個欄位很像你要的東西」，
> 不放大「但它是快照」。

所以 `HINT_K` 預設 0 是**終局判決**，不是等修完再議。實作保留的理由改成：
它是這個機制唯一的活體示範，而且一行環境變數就能重現。

**這也讓 §7.8 的判準長出第三支**（見該節）：
兩讀法都有答案、但 schema 明說了哪個算數 → **不改問句**。

#### 閘門 [10]：題目層的多來源歧義

`check_derived_consistency.py`（閘門 [9]）檢查**欄位**，而且是用 GT 的 SQL 判斷
「有沒有題目引用」。`#94` 的 GT 走 `SUM(orders.total_amount)`，**根本沒提 `total_spent`**，
所以閘門 [9] 是綠的 —— 危險的正是 GT **沒**用、而模型會用的那條路。

判準必須是**執行**，不能是相似度。第一版用
`cos(問句, 雙來源欄位) >= max(GT 需要的表的最像欄位)`，在 305 題上標出一堆
差 0.001~0.003 的雜訊（「三體這本書總共賣出幾本」vs `prior_ticket_count`），
而**用已知的那一題去調門檻正是專案禁止的調常數**。
改成 [Soma-SQL](https://arxiv.org/html/2606.11424v1) 那條路：實際跑兩條 SQL 比對結果，
不一致本身就是訊號，不需要門檻。代價是替代路徑要手寫（14 條，可接受）。

閘門 [10] 今天的角色是**迴歸測試**而非缺陷清單：分歧名單少了一題，
代表快照陷阱被誰對齊掉了，`#146`/`#147`/`#148` 會失去鑑別力，要有人知道。

#### 順帶量掉了「一表一票的資訊天花板」有多高

使用者提的測法是「出一題問大表裡沒被表註解涵蓋的概念」。
全覆蓋、零成本的版本是：**把 869 個欄位註解各當成一句問句**，
量它自己的表排第幾 —— 欄位註解是問到該欄位所能寫出的最貼題措辭，所以是保守下界。

```
全部         869 欄｜中位 3｜第 90 百分位 27｜排不進前 40 的 51 欄 = 5.9%
寬表 profile 613 欄｜中位 5｜第 90 百分位 28｜排不進前 40 的 34 欄 = 5.5%
其餘 79 張   256 欄｜中位 1｜第 90 百分位 23｜排不進前 40 的 17 欄 = 6.6%
```

**天花板是真的，但只有 5.9%，而且寬表沒有比較慘（5.5% vs 6.6%）。**
後者推翻了「45 欄的表一句註解代表不了全部概念」這個直覺 ——
寬表的註解比較長（91~140 字），反而蓋得比較廣。
排不進去的多半是邊緣欄位（`dry_ice_used` 92/93、`route_code` 90/93、
`sync_version`、`checksum`）或本質上沒有區辨訊號的通用欄位
（`created_at`、`is_active`、`postal_code`）。

逐表最糟的是 `review_profiles`：**40 欄裡有 7 欄的概念問了就檢索不到**（表註解 106 字）。
要手寫題目驗證天花板，那 7 欄是最好的靶。

### 2.7g 三個不同層的介入，同一個雙向 —— 這才是結論（2026-08-26）

一天之內在三個不同的地方各試了一次，每次都量了 n=8：

| | 介入的地方 | 誘餌題 | 窄表題 | 判決 |
|---|---|---|---|---|
| §2.7e **概念群** | 候選目錄加一行群名／群+欄位 | +11/48（8 票來自單一題） | — | 不上線 |
| §2.7f **欄位提示** | 候選目錄加一行「本題可能相關的欄位」 | `#282` 1→8、`#308` 2→7 | `#94` 8→**0**、`#100` 8→**0** | 不上線 |
| §2.7g **第 5 條原則** | 選表 system prompt（不動目錄） | **2/32 → 17/32** | **35/40 → 28/40** | 不上線 |

第 5 條原則的全文（刻意寫成「哪一類屬性住在哪一類表」，不是「哪一題用哪張表」）：

> 5. 同一個概念可能同時存在於「明細表」與「檔案寬表」兩個地方。明細表記的是
>    這筆紀錄本身（誰、什麼、多少、什麼時候），檔案寬表記的是它的**狀態旗標、
>    作業細節與設定值**。問題問的是後者時，明細表裡通常沒有那個欄位。

```
誘餌     4 題   A   2/32   G  17/32     ← #282 1→8、#284 0→4、#292 0→2、#308 1→3
寬表對照 3 題   A  16/24   G  19/24
窄表對照 5 題   A  35/40   G  28/40     ← #24 8→5、#25 5→2、#132 7→6
合計    12 題   A  53/96   G  64/96
```

**三次介入的位置完全不同 —— 嵌入層、目錄層、指令層 —— 而失敗形狀一模一樣。**

> **誘餌題與窄表題在同一條軸上，方向相反。**
> 誘餌題失敗是因為模型不肯用寬表；窄表題成功是因為模型不亂用寬表。
> 任何把「寬表更值得看」這個訊號調高的手段，都會同時推動這兩端。
> 這不是實作沒調好，是這個訊號本身分不開這兩件事。

要贏就得有一個**能區分「這一題該用寬表」與「這一題不該用寬表」**的訊號，
而目前三種代理（表註解相似度、欄位餘弦、屬性類型的文字描述）**都不是那個訊號**。

`#284` 是個有用的例外：0/8 → 4/8，它需要四張表（`order_items`＋`product_profiles`
＋`shipment_profiles`＋`shipments`），原則讓模型願意多選寬表因而多對了一半。
但它同時也是最脆的 —— 多選寬表正是窄表題退步的原因。

### 2.7h 關係宣告句拆出檢索層：分欄做得到，收益沒兌現（2026-08-26）

**起點是使用者的一個指控**：「表註解感覺都是跟著題目設計的，換題目不就錯了。」
先量再談 —— 把註解切成 2~3 字的中文 n-gram，找**同時滿足四件事**的指紋：
在註解裡、不在自己的 schema 裡、在「GT 會用到這張表」的題目裡、
且**不**在其他題目裡（最後一條擋掉常用中文）。

```
觀測指紋 192 個 ｜ 隨機置換虛無 7.6（30 次）｜ 25.3×  ｜ 52/82 張表有指紋
```

**耦合是真的。** 但「註解抄題目」與「出題時看著註解寫」留下一模一樣的指紋，
方向分不開。分得開的是**內容**：

```
訂單主表：一張訂單一列，記錄下單客戶、下單時間、訂單狀態與總金額。   ← 業務定義
訂單狀態 PENDING/PAID/SHIPPED/COMPLETED/CANCELLED                  ← 值域
—— 查待處理、已付款、已出貨、已完成、已取消的訂單都用這張表           ← 複述值域，零資訊
   訂單張數、客單價、期間營收等訂單層級的統計用這張表                  ← 衍生指標歸屬
```

**可逐句套用的判準：「如果題庫從來不存在，這句話還成不成立？」**
「商品層級的營收用 `order_items` 算」成立（`order_items` 只有 `quantity`/`unit_price`，
「營收」是任何欄位註解都沒寫的衍生指標）；「查付款成功、付款失敗的都用這張表」不成立。
純路由句全庫只有 **220 字（4.4%）**，而且集中在被修最多次的樞紐表上。

#### 「只寫核心業務」在檢索層是免費的

機械規則（只留第一句，與題目無關）頭對頭，零 LLM：

```
臂            總字數    @40      @5      @3   Top-1   寬表誤選
A 現行         6654   99.0%   83.0%   72.8%   81.0%      172
B 首句         4098   99.0%   83.9%   72.8%   80.0%      138   ← 少 20%
C 去冗餘       6547   99.0%   83.6%   73.4%   79.3%      171
```

砍掉 38% 的字，`@40` 不動、`@5` 還變好、**寬表誤選降兩成**。
C 臂（丟掉「內容能從自己欄位註解推得」的句子）只砍掉 107 字 ——
**表註解裡幾乎沒有一句是欄位註解的複述**，所以「只寫核心業務」不是刪冗餘，
是刪資訊；能承受是因為候選收 40/93 張，天花板離現在很遠（§7 的 5.9%）。

而 `#55`/`#98` 掉出前 40，砍掉的正是
「商品銷量、商品層級的營收、熱賣排行都要用這張表計算」—— **語氣像路由指令，
內容是衍生指標歸屬**。所以正確的做法不是變短，是**分欄**。

#### 真正的發現：寬表註解結尾那句在把模型推離寬表

每張 `*_profiles` 的註解都以「這是…，`<母表的東西>` 在 X」收尾。拆掉之後，
12 題定向探針（n=8）三組全贏，這是今天唯一看起來不雙向的介入：

```
              A 現行    C 砍尾    C' 只砍指路標
誘餌  4 題     3/32     7/32      9/32
寬表  3 題    21/24    24/24     23/24
窄表  5 題    34/40    33/40     40/40
```

C 與 C' 的差把機制切開了：C 多砍的是 `customer_profiles` 的概念涵蓋
（`#154` 的 Email 驗證那段），窄表反而 34 → 33。
**收益來自移除指路標，不是來自「註解變短」** —— 這是 §2.7f 的 F 臂
（固定列欄位、誘餌 0/32）的減法版：**挑對了東西，不是多／少了文字。**

#### 全庫六輪配對驗收：平手

```
anchor  變好 14  變差 14  持平 277   符號檢定 p = 0.5747
kmb     變好  9  變差  9  持平 287   符號檢定 p = 0.5927
```

（有一輪撞 429 降級 59 題；拿掉之後 p = 0.40 / 0.50，只取乾淨的兩輪 p = 0.30 / 0.50。）

退步的機制乾淨，而且**還是 §2.7g 那條軸**：

```
指路標在   → 跑去母表，忘了寬表     #282 #292
指路標不在 → 留在寬表，忘了母表     #286 #287
```

只是粒度變了 —— 不是「寬表 vs 窄表」，是**「這一題需要幾張表」**。
配對之後兩端恰好相抵，與欄位提示（§2.7f）、第 5 條原則（§2.7g）同一個結局。

**保留的是做法不是收益。** `_strip_relations()` 留在
`table_filter.py`（`BRIEF_RELATIONS=strip` 開啟，預設關閉），因為它證明了
「同一段 TABLE_COMMENT 拆給不同職務看」實作上是零成本的：`get_table_briefs()`
只餵檢索文件與選表目錄，DDL 走 `semantic_layer.yaml`，兩條路本來就是分開的。

### 2.7i 選表投三票取聯集：五次不兌現之後，第一個在 e2e 兌現的介入（2026-08-28，**已上線**）

**這一節推翻了 §2.8 與 §10 裡「多次取樣取聯集（MCS-SQL）—— 聯集治隨機不治系統，
3× 呼叫零增益」那一列。** 舊判決是在 80 張表、`--funnel` 單輪、只看 `+KMB` 總分下的；
它錯在兩件事：**（一）當時的 schema 裡沒有「系統性漏掉的那一群表」可漏** ——
`*_profiles` 寬表是 86 → 93 那兩批才進來的；**（二）單輪總分量不出雙向抵銷**（§5.2）。

#### 起點：使用者對 A/B 框架的質疑，以及它為什麼是對的

我原本主張這個實驗沒有頭寸可賺：B（聯集）夾在 A（現行）與**天花板**
（完全不做 schema linking，整份 93 張表全送）之間，而 A 與天花板實測是**平手**，
所以中間的東西不可能贏。使用者反對：「檢索完雜訊變少，B 搞不好能選中那些需要的表。」

回頭拆天花板那次平手的逐題資料，使用者是對的，而我錯得很具體：

```
天花板 vs 現行，全庫 309 題          總分：平手
逐題拆開                            救回 19 題 / 弄丟 14 題 / 持平 276
```

**那不是「沒有東西在動」，是 33 題在動然後互相抵銷。**
「送更多表」同時買到兩件相反的事 —— 買回被漏掉的表，也買到更多可湊的雜訊。
一個介入如果只買前者不買後者，就有頭寸。聯集正是這種形狀：它**只加**
「另外兩次取樣選到而第一次沒選到」的表，加的量遠小於「全送」。

> **記給下次：總分平手有兩種，一種是 null，一種是抵銷。**
> 在拿逐題配對看過之前，不可以用「端點平手」推論「中間沒有頭寸」。

#### 為什麼是聯集不是多數決

這一層的錯是**單向**的 —— 漏一張表這題就死了，多一張表只是 Prompt 長一點。
逐題拆開，`+KMB` 之後仍失手的題分成「三輪都漏同一張」（穩定）與
「三輪漏一次兩次」（硬幣）兩群，**聯集只動得了後者**。
多數決會把硬幣題的少數正確票丟掉，剛好丟掉唯一有價值的那一票。

#### 實驗設計：兩臂共用同一次選票採收

`_vote_seeds(query, n)` 的契約是「第 0 顆 seed 與 `votes=1` 走的那一顆位元相同」
（`scratchpad/pool_check.py` 的 PC4 守住這條）。所以一次採收三張選票之後：

```
A 臂 = 選票 0                 ← 與現行 production 位元相同
B 臂 = 選票 0 ∪ 1 ∪ 2
```

**兩臂從同一份票庫展開，選表層零 API 成本、零細胞間漂移。**
票庫存成 `scratchpad/vote_ballots.json`（3 輪 × 305 題 × 3 張票），
e2e 時把 `filter_tables_union` 換成查表版本重播。

這個設計還附贈一個平常買不到的東西，見下面的雜訊對照組。

#### 第一關：漏斗層（零 e2e，只量選表召回）

```
              錨點      +KMB     平均表數
A votes=1    95.1%     97.1%      2.35
B votes=3    97.4%     98.5%      2.57   （+9.4%）

逐題配對（3 輪 × 305 題）
  anchor   救回 16  弄丟  0   p = 0.0000
  kmb      救回  9  弄丟  0   p = 0.0020
死票 0 張
```

⚠️ **`anchor` 那一行不是證據。** 聯集 ⊇ 選票 0，所以 anchor 召回**只可能上升**，
「弄丟 0」是算術保證的。有資訊量的是 `kmb`：Steiner 樹在終端點改變時會換掉橋接節點，
**`+KMB` 不是單調的**，它的「弄丟 0」才是量出來的。

#### 第二關：e2e 逐題配對 + 防禦題（使用者設的驗收條件）

使用者早就指定過驗收不能只看漏斗：「**多選表的成本落在漏斗量不到的地方**」——
表變多，模型更容易從無關的表裡湊出一個看起來合理的答案，而漏斗只量召回，量不到這個。

```
輪    A votes=1    B votes=3
 1      96.1%        97.7%
 2      96.4%        97.4%
 3      95.4%        97.0%

逐題配對（全 305 題）：持平 290｜救回 12｜弄丟 3｜雙尾 p = 0.0352
防禦題：6 個細胞全部 4/4
```

#### 但全集的 12:3 有一半是雜訊 —— 要看的是「輸入真的不同」的那個子集

兩臂只在「聯集 ≠ 選票 0」的題上收到不同的輸入；其餘的題兩臂逐字相同，
只能貢獻 MoE 雜訊。**按治療分組（不是按結果分組）做限制，這是事前決定的，
不違反 §5.4。** 305 題裡符合的有 102 題 / 173 組 (題, 輪) 配對：

```
子集（輸入真的不同）：持平 95   救回 7   弄丟 0   雙尾 p = 0.0156

  #152   A 1/3 → B 3/3   多給 customer_profiles, customers
  #184   A 0/3 → B 3/3   多給 warehouse_transfers            ★ 穩定救回
  #224   A 0/1 → B 1/1   多給 customers
  #263   A 2/3 → B 3/3   多給 order_profiles, orders, audit_log, support_ticket_profiles
  #281   A 1/2 → B 2/2   多給 delivery_attempts, shipments
  #282   A 0/2 → B 1/2   多給 shipment_profiles, addresses, orders
  #292   A 0/1 → B 1/1   多給 payment_profiles
```

**全集那三題「弄丟」（`#89` `#189` `#260`）逐輪查票庫，兩臂輸入是逐字相同的**
（`#260` 有一輪不同，而那一輪兩臂都對）。它們不是聯集的代價，是雜訊。
同理，全集 12 題救回裡有 6 題（`#71` `#119` `#147` `#225` `#249` `#287`）
也是輸入相同的雜訊 —— **兩邊都要扣，不能只扣對自己不利的那邊。**

#### 雜訊對照組：這個設計最值錢的地方

共用票庫讓「輸入逐字相同」的那 283 題變成一個**現成的對照組**，
它理論上必須是平手，不平手就代表 MoE 雜訊本身有偏、主結果要打折：

```
【雜訊對照組】283 題輸入逐字相同：B 好 6 題 / A 好 4 題   雙尾 p = 0.7539
```

平手。所以 7:0 不是雜訊的形狀。

> **記給下次：只要兩臂共用輸入，就順手把「輸入相同」那一群報出來。**
> 它幾乎免費，而且是唯一能回答「這個顯著是不是雜訊撿到的」的東西。

#### 防禦題閘門差點被自己的優化弄假

票庫只有 305 題，4 題防禦題（`#68` `#70` `#140` `#141`）不在裡面，會落回真實選表。
第一版程式碼兩臂都用預設 `votes=1` —— **那道閘門就完全沒有測到 B 臂**，
而防禦題正是「多給表」最危險的地方（表愈多，模型愈容易湊出東西＝誤觸暗號）。
改成照臂給 `votes=(1 if A else 3)`，代價只有 4 題 × 3 輪 × 2 臂 = 48 次呼叫。

**6 個細胞全部 4/4。** 加表沒有讓守衛失守。

#### 上線的帳

```
選表呼叫    1 → 3 次／題（三票平行，延遲幾乎不變，配額吃三倍）
進 Prompt   2.35 → 2.57 張表（+9.4%）
e2e         95.96% → 97.38%（三輪平均，+1.4pp）
```

`FILTER_VOTES` 預設已從 `1` 改成 `3`（`table_filter.py`）。
`FILTER_VOTES=1` 仍然是位元相同的舊行為，隨時可以退回去當對照臂。

#### 沒修好的：`#284`

三輪六格全滅，`product_profiles` **從頭到尾沒有任何一張選票選中過**。
它與 `#282` / `#292` 是同一種病：`*_profiles` 的表註解寫的是**類目層級**的業務範圍，
而問句用的是**欄位層級**的詞彙。三題的必要表都在 top-40 候選裡，不是 dense 的問題。

```
#282  「中途改過地址」  → shipment_profiles.is_redirected / redirect_reason
#284  「標成易碎品」    → product_profiles.is_fragile / package_type
#292  「跟對帳單勾稽」  → payment_profiles.is_reconciled / reconciled_at
```

`#292` 尤其乾淨：註解裡的「對帳結算」在字面比對上輸給 `invoices` 的「發票」，
而問句寫的是「對帳單」。**聯集救得動 `#282` / `#292`（它們是硬幣題），
救不動 `#284`（它是穩定題）** —— 完全符合上面「聯集只動得了硬幣題」的預測。

**下一個實驗：只讓三張選票裡的一張帶欄位提示（`COLUMN_HINT_K=2`）。**
（✅ 已做，**否決** —— 漏斗 4:1 但 e2e 5:7，`#258` 三輪全輸。見 §2.7j。）
欄位提示自己跑全庫是雙向平手（§2.7f、§2.7g），因為它救回一些題同時擠掉另一些表。
放進聯集裡，救回的表會被聯集吃到，而被擠掉的表由另外兩張沒帶提示的票補回來 ——
**把一個雙向的平手變成單向的收益。** 這是「聯集的錯是單向的」這條性質的第二次應用。

> 📂 **原始資料在 `ARCHITECTURE_archive.md` §12**：天花板那次 19:14 抵銷的逐題拆解、
> 三輪逐輪數字、全集 12:3 的逐題名單與各自的「兩臂輸入同／異」標記、
> 雜訊對照組、以及過程中我判斷錯的三個地方。
> ⚠️ 量測工具（`vote_funnel.py` / `e2e_union.py` / `e2e_subset.py`）住在 session
> scratchpad 裡沒有進 repo，會被清掉 —— 檔案裡的數字是唯一紀錄。

### 2.7j 只讓三張選票裡的一張帶欄位提示 —— 第六次，一樣沒兌現（2026-09-01，否決）

§2.7i 上線之後最自然的下一步。**假說**：欄位提示（`COLUMN_HINT_K=2`，§2.7f）
單獨跑是雙向平手 —— 修好 `#282`／`#308`／`#287`，弄壞 `#94`／`#100`，
而 `hints_for()` 的 docstring 自己寫著「修好與弄壞來自同一個機制，
不存在只留好處的參數設定」。**那句話對單張選票成立，對聯集不一定。**

`#94` 的病是「模型看到 `total_spent` 就一張表答完，不 JOIN `orders`」＝**選表選少了**，
而那正是聯集治得了的形狀。所以：

```
B 臂（今天的 production）= b0 ∪ b1 ∪ b2      三張都沒提示
C 臂                    = h0 ∪ b1 ∪ b2      只有第 0 張帶提示，seed 與 b0 相同
```

只補「第 0 票的帶提示版本」，b1／b2 沿用 §2.7i 的票庫 —— 305×3 次而不是 915×3 次。

#### 漏斗層：假說成立，抵銷真的發生了

```
              錨點     +KMB    平均表數
B 三票無提示  97.4%   98.5%     2.57
C 第0票帶提示 98.0%   99.2%     2.61

【對照：單票】h0 vs b0    變好 8 / 變差 11   p = 0.82  ← 完整重現 §2.7f 的雙向平手
【主結果】  C vs B(聯集)  變好 4 / 變差  1   p = 0.19
```

**11 個「變差」被聯集吃掉 10 個**（`#94`／`#100` 都在被吃掉的那群裡），
變好的是 `#282`／`#284`／`#292`／`#308` —— 四題全是 §9 的穩定錯或硬幣。
機制預測完全命中。但 4:1 只有 5 個事件，p = 0.19：**方向對，證據不足。**

#### e2e：5 好 7 壞，否決

跑之前依 §7.10 的結論預測「漏斗 4:1 到 e2e 會縮水」。實際縮到負的：

```
78 題 / 140 組   持平 66｜C 好 5 題｜B 好 7 題｜雙尾 p = 1.0
防禦題 6 格全部 4/4
```

**提示每次只多給一張表，那張表對不對就是全部：**

| | 題 | C 多給的那張表 | B → C |
|---|---|---|---|
| 救回 | `#284` | `product_profiles` | **0/3 → 3/3** |
| | `#282` | `shipment_profiles` | 1/3 → 3/3 |
| | `#292` | `payment_profiles` | 1/3 → 3/3 |
| 弄丟 | `#258` | `product_specs` | **3/3 → 0/3（三輪全輸，同一張表）** |
| | `#202` | `order_profiles` | 3/3 → 1/3 |
| | `#249` | `return_profiles` | 1/1 → 0/1 |
| | `#248` | `customers` | 3/3 → 2/3 |

**弄丟的四題全部在 §7.10 那張「多給表 → 伸手拿母表」清單上。**
這是同一條雙向軸第一次在**同一個介入的同一次量測裡**兩端都拿到實例：
多給的那張表是答案就救回，是近義母表就死掉。

`#258` 是決定性的：**三輪都輸、都是 `product_specs`、B 三輪都對** ——
那不是硬幣，是可重現的回歸。一題這樣就足以否決上線，不必等 p 值。

#### 這一次的對照組不合格，方法論要改

```
雜訊對照組 105 組：持平 105、零歧異     但 B 臂在對照組是 105/105 = 100.0%
```

**零歧異不是雜訊小，是抽樣抽錯了。** 我從「兩臂輸入相同」的題裡**均勻抽**，
而那群題正是三張選票一致的題 ＝ 沒有歧義 ＝ 簡單題，全部貼在天花板上，
**對照組沒有檢定力**，測不出雜訊有沒有偏。
（有驗到的是：105 組裡 65 組兩臂 SQL 逐字不同，所以兩臂確實獨立跑，不是共用結果。）

> **下次的雜訊對照組要按「該題的歷史正確率」分層抽樣，不能均勻抽。**
> §2.7i 那次剛好抽到 283 題全體所以有代表性，那是運氣不是設計。

#### 判決

`COLUMN_HINT_K` 維持 `0`。`FILTER_VOTES=3` 不受影響 —— 它是獨立驗收過的（§2.7i）。

**這是第六次「探針全勝、全庫不兌現」，但它是六次裡診斷最完整的一次**：
第一次同時拿到救回與弄丟的逐題機制、同一張表、可重現。§10 那條紀律要更新 ——
不是「不要再提第六次」，而是**「提第七次之前，先解決 `#258` 這一類：
提示挑對表的時候是解藥，挑到近義母表的時候是毒藥，而它們用的是同一個餘弦分數。」**
欄位餘弦本身已被證明不是那個區分訊號（§2.7g、§5.5），這次是第四個證據。


### 2.7k 第七次：欄位提示的毒性來自資料矛盾，不是「多給一張表」（2026-09-02）

§2.7j 否決了欄位提示（`COLUMN_HINT_K=2`），但它留下了一條非常具體的前提：

> 「提第七次之前，先解決 `#258` 這一類 —— 挑對表時是解藥、挑到近義母表時是
> 毒藥，而兩者用的是同一個欄位餘弦分數。」（§10）

**這一輪不是又提了一次同樣的旋鈕，是那條前提被別的工作拆掉了。**
§7.12 把 35 條同義欄位矛盾對齊之後，「挑到近義母表」不再等於「拿到不同的答案」。
`#258` 的 `product_specs.is_discontinued` 與 `product_profiles.lifecycle_stage`
現在 φ=+1.000。所以假說是：**毒性的來源是資料矛盾，不是多給一張表。**

#### 第一輪：§2.7j 的原班人馬，n=8 兩臂

```
                     A(K=0)      B(K=2)
解藥組（§2.7j 救回的四題 ＝ 三輪 e2e 還在錯的四題）
  #284               1/8    →    8/8      +7
  #308               0/8    →    0/8       0
  #282               4/8    →    8/8      +4
  #292               1/8    →    8/8      +7
                     6/32   →   24/32

毒藥組（§2.7j 弄丟的四題）
  #258               8/8    →    8/8       0   ← 點預測：當時 3/3 → 0/3
  #202               8/8    →    8/8       0
  #249               8/8    →    8/8       0
  #248               4/8    →    6/8      +2
                    28/32   →   30/32

陷阱組（快照陷阱，不受修資料影響）
  #94                8/8    →    6/8      -2
  #100               8/8    →    8/8       0
```

**`#258` 是這張表唯一重要的一格。** §2.7j 時它三輪全輸、都是多給了
`product_specs`；現在多給同一張表，八次全對。中間唯一的變化是那兩欄不再矛盾。

`#94` 的 −2 是**另一種毒，而且它該留著**：兩次失敗都回
`customer_profiles.total_spent` 的 165,250 而不是即時 SUM ——
那是閘門 [9] **宣告為刻意**的快照陷阱，`column_hints.py` 的 docstring 早就寫死
這個機制。修資料不會、也不該解掉它。**兩種毒必須分開講：**

```
毒一 近義母表的同義欄位給出不同答案     ← §7.12 修掉了
毒二 寬表的快照欄位是刻意的陷阱          ← 設計如此，永遠都在
```

#### 但第一輪不能拿來下結論 —— 它踩在 §5.4 上

那 10 題是 §2.7j 用「哪幾題有差」挑出來的，正是 §5.4 禁止的子集。
迴歸到平均會同時灌水兩邊：解藥組本來就低（6/32）、毒藥組本來就高（28/32）。

> **可信的是機制**（`#258` 是點預測，它守住了）；
> **不可信的是幅度**（+18/80 不能拿去推全庫）。

#### 第二輪：分層對照組，而且刻意讓迴歸對提示臂不利

抽 §2.7j 名單**以外**的 18 題，主軸是「三輪 e2e 全對」——
那些題**只能往下掉**，所以迴歸到平均是**對提示臂不利**的，
與第一輪的偏誤方向相反。

```
                        A(K=0)     B(K=2)
三輪全對·窄表（靶）      63/64  →   64/64      +1
   #10 #27 #45 #91 #102 #105 #136 #139
三輪全對·寬表            32/32  →   32/32       0    #262 #285 #304 #305
三輪全對·其他            16/16  →   16/16       0    #238 #246
防禦題                   16/16  →   16/16       0    #68 #70
歷史不穩                  5/16  →    8/16      +3    #89 4→7、#296 1→1
                        ------------------------
                        132/144 →  136/144
```

**這個實驗是設計來抓傷害的，而它抓不到。** 64 格的窄表靶一格都沒掉
（`#139` 7/8 → 8/8 在雜訊內），防禦題 16/16 不動 ——
「多給欄位資訊會讓守衛失守」這個最該擔心的方向沒有發生。

`#296` 兩臂都 1/8、失敗模式逐字相同（回 1 列 `[9, '訂單何時出貨']`）——
它與提示無關，是題目理解問題，不該算進任何一邊。

#### 兩個實驗合起來

```
選出來的 10 題（§5.4 偏誤對提示臂有利）    50/80  → 68/80
獨立 18 題（偏誤對提示臂不利）            132/144 → 136/144
```

**兩個方向相反的偏誤下都沒有變壞，這比任何一邊單獨看都強。**


#### 第三輪：全量 e2e，三輪 × 兩臂（2026-09-03）

A 臂沿用 2026-09-02 早上那三輪 —— 同一份資料、同一份 pipeline 程式碼、
`COLUMN_HINT_K` 未設（＝0）。之後只動過 `eval_score.py` 的**報表**，
改完重新對帳三輪，正確率與錯題名單逐字相同。**省下 4 小時配額。**
代價要標註：兩臂不是交錯跑的（A 在上午、B 在下午），中間隔了幾小時的 NIM 端點。

```
                  R1      R2      R3      平均
A (K=0)         97.4%   97.7%   97.1%   97.41%
B (K=2)         98.7%   98.1%   98.1%   98.27%   +0.86pp

三輪都錯      A {#284, #308}  →  B {#308}
過度拒答      A 每輪 1/305    →  B 每輪 0/305
防禦題        兩臂三輪全部 4/4
逐題          救回 11 題（+14 格）｜弄丟 4 題（−6 格）
```

**B 臂最差的一輪（303）贏過 A 臂最好的一輪（302）** —— 六輪完全不重疊。

#### 先驗證臂是活的，再看數字

`hints_for()` 在 K=2 回傳非空**只證明旋鈕會動，不證明它在 e2e 行程裡生效**。
兩個日誌檢查都無效（`eval_stability.py` 呼叫 `log.remove()`；
`test_runner.py` 的 loguru 因 rotation `PermissionError` 沒寫進檔案）。
**25 題的選表差異也分不出來** —— 臂內雜訊 2~4/25、跨臂 3~5/25，重疊。

真正的證據是資料本身：

```
#284   A 三輪   rows=0  rows=0  rows=0   （empty，product_profiles 三輪全漏）
       B 第一輪 rows=73 ok               （73 ＝ GT 列數，選表含 product_profiles）
```

> 這一步花了一小時等待，但它擋掉的是「跑完四小時才發現量的是同一臂」。

#### 四題回歸，沒有一題能歸給提示

```
#46   兩臂選表完全相同，SQL 幾乎逐字相同
#147  兩臂選表完全相同（28 vs 27，邊界差一）
#225  兩臂選表完全相同（0.6 vs 60.0，「率」要不要乘 100）
#286  B 多給 gift_cards，但失敗模式是 §0 記著的硬幣
      「3 次失敗全是 33 列 vs 32 列＝沒濾」——與 gift_cards 無關
```

**三題的選表一模一樣，提示不可能是原因**；剩下那題的失敗模式是既有的擲硬幣，
`3/3` 在 A 臂本來就是運氣。**§2.7j 當年的毒（伸手拿近義母表）一例都沒有重現。**

#### 統計壓在 0.05 上，撐住結論的是機制不是 p 值

```
輪次層級   六輪完全分離，精確排列檢定 p = 1/C(6,3) = 0.050
逐題符號   救回 11 / 變動 15，單尾 p = 0.0592
```

兩個都是邊緣，不是碾壓。真正可信的是**事前的點預測都兌現了**：
說 `#284` 會被救回 → 它 0/3 → 3/3；說毒性已消失 → 四個回歸沒有一個走那條路。
反過來說，如果只看 +0.86pp 而不看機制，這個效應量在 §5.2 的紀律下**不夠格下結論**。

#### 覆蓋率的異議已經回答了，`COLUMN_HINT_K` 仍未改預設值

分層對照組那一輪的保留是「母群 146 題只測了 8 題」——**全量三輪 × 兩臂把
309 題全部測過了，那條異議不再成立。** 剩下的保留只有一條，而且是統計不是覆蓋：
兩個檢定都壓在 0.05 上（見上）。

**旋鈕維持 `0`，等人決定。** 這一節記錄的是量測，不是決定 ——
§10 的欄位提示那一列已同步更新為「否決理由不成立，待決」。

順帶推翻三輪重取當下的判斷「全量 e2e 已經量不動任何東西」：
那句話的前提是剩餘缺陷只值 0.6pp，而實測效應是 **+0.86pp**，
高於三輪量出來的 ±0.3pp 雜訊底線。**判斷「量不動」之前要先知道效應有多大，
而那正是還沒量的東西** —— 這個循環是我自己踩進去的。

> **這一節真正的教訓不在旋鈕上。**
> 欄位提示被否決六次，而最後一次的否決理由（`#258` 中毒）
> **根本不是檢索層的問題** —— 是資料層有兩欄在說不同的話。
> §10 的否決清單裡，可能還有別條的理由是同一種借來的：
> **修掉一個別層的缺陷之後，要回頭重測否決清單，而不是只更新那一層。**

---


### 2.7l 縮 `CANDIDATE_N` 換目錄長度 —— 否決，而且量錯了一次（2026-09-03）

§2.7k 開了欄位提示之後目錄從 3,096 漲到 5,568 字元，遠超 §8.9 量過的範圍。
假說：**縮 `CANDIDATE_N` 把目錄長度換回來**。實測是反的。

```
                        R1      R2      R3      平均
A  N=40 k=0           97.4%   97.7%   97.1%   97.41%
B  N=40 k=2           98.7%   98.1%   98.1%   98.27%   ← 最好
C  N=25 k=3           97.7%   97.1%   96.8%   97.20%   ← 比不開提示還差
```

三個事前假設全部被推翻：

```
N=25 拿掉了需要的表？   C 弄丟的 13 題裡 0 題，最深才第 19 名        ✗
目錄太長害的？          C 的目錄更短（3,909 vs 5,568）而它更差        ✗ 假設方向反了
k=3 稀釋了選表？        定題 n=8：13 題 k=2 80/104 vs k=3 75/104，
                        p = 0.363，而偏誤還是對 k=3 有利的            ✗
```

#### 這一節最貴的部分：一個無效的統計

我先做了「C vs B 逐題配對符號檢定」，得到**救回 4 題、弄丟 13 題、p = 0.0245**，
並據此宣稱 `#189` 是「全新的穩定錯（3/3 → 0/3）」。**那個檢定是無效的。**

定題 n=8 之後看到：**那 13 題裡有 8 題在 `k=2` 就已經是硬幣**

```
#119 3/8   #138 4/8   #296 4/8   #189 5/8   #287 5/8   #89 5/8   #63 7/8   #94 7/8
```

它們在 B 臂三輪拿到 3/3 是**運氣**。一枚 4/8 的硬幣連中三次是 6%，池子裡有八枚 ——
出現幾個「3/3 → 1/3」是必然的。拿三輪的逐題對錯做配對檢定，
等於**把硬幣當成穩定題在數**。

> §5.2 擋的是「單輪總分判斷不了改動」。這次踩到的是它的延伸：
> **n=3 的逐題計數也不能拿來做配對檢定。**
> 逐題比總分「看起來更嚴謹」，但**解析度並沒有比較高** ——
> 分母從 309 變成 3，雜訊反而更大。
> 要做逐題配對，每題至少要有 n=8，否則就只能比輪次層級的總分。

#### 還站得住的與拿不出的

```
✓ 輪次層級 B(305,303,303) vs C(302,300,299) 完全分離，排列 p = 0.050
✗ 逐題歸因 —— 差距分散在別處，不在那 13 題身上，機制未知
✓ 提示的好處在兩臂都完好：#284 #292 #282 #260 在 k=2 與 k=3 各 32/32
```

**`CANDIDATE_N` 維持 `40`、`COLUMN_HINT_K` 的候選值維持 `2`。**
`N=25` 不再追 —— 沒有機制假說的常數調整正是 §10 那條紀律要擋的東西。

#### 副產品：`products` 在「點名商品」的問句上排到 47~62 名

追 KMB 有沒有越權時量到的（KMB 沒有越權，補的三張都是合法的橋）：

```
N=40 就已經補不回來的：#2 products@48、#21 products@62、#40 products@47
```

`#21`「從來沒有買過『原子習慣』這本書的客戶有誰？」——**問句直接點名一本書，
`products` 卻排第 62。** 這些題一直在「檢索漏給」清單上卻是綠的，
因為 LLM 多選的表讓 KMB 把 `products` 當橋補了回來。

> **救援機制在撐住一個 99% 的假象。** 候選召回這個指標測的是 LLM 的選單，
> 不是最終送進 SQL 生成的表集合 —— 兩者被 KMB 隔開了，
> 所以上游的缺陷不會在下游的分數上出現。這是一個還沒修的缺陷，不是一個好消息。

---


### 2.7m `products` 排到第 62 名 —— 以及一個差點被我當成成功的過擬合（2026-09-03）

追 §2.7l 的副產品時挖到的。**缺陷在尾巴，不在中心** —— 這一點決定了閘門怎麼寫:

```
products 被 GT 需要的 60 次：中位數 5.0（正常）｜最差 62（#21）
```

用平均或中位數當判準會**完全漏掉它**。分佈長這樣:

```
products 被 GT 需要的 60 次裡，56 次排前 25 名，4 次掉到 40 / 47 / 48 / 62

壞掉的：#21「從來沒有買過『原子習慣』這本書的客戶」  #2「『運動鞋』總共賣出多少雙」
        #40「買過『iPhone 15』但沒買過『AirPods』」  #39「同時買過…」
正常的：#6 哪個商品總營收最低  #14 庫存量介於 30 到 70  #15 單價高於 20000 的商品
```

**問句說「商品」時 `products` 排第 2，說「原子習慣」時排第 62。**
它的檢索文件從來沒有宣告「具體商品名住在這裡」——
76 字元裡還有一半花在 `category` 反正規化這種使用者問句永遠不會長得像的說明上。

對照 `orders`（150 字元）就懂了:**它直接寫「查待處理、已付款…的訂單都用這張表」、
「訂單張數、客單價、期間營收等統計用這張表」——它宣告自己回答哪些問題。**
這正是 `sync_table_comments.py` 的 docstring 早就記下的因果:
「寫得好的表檢索找得到，偷懶的表就找不到」，當時的病人是 `order_items`。

#### 差點犯的錯:把題目的字串抄進註解

第一版註解舉的例子是「原子習慣」「運動鞋」「iPhone 15」——**那正是那三題問句裡的字串。**
零 LLM 一測，四題變成 3 / 1 / 1 / 1，漂亮得可疑。改善幅度跟
「註解逐字抄了測試題多少」成正比:

```
                          #21   #2   #40  #39
現況                       62    48   47   40
只講規則不舉例             33    40   36   20
舉例＝題目裡的商品名        3     1    1    1   ← 洩題
舉例＝題目沒出現過的商品名  35    23    8    3   ← 誠實的數字
```

**大約一半的改善是背答案。** 保留組（`被討厭的勇氣`／`空氣清淨機`／`MacBook Air`，
都在資料庫裡、都不在任何題目裡）才是可以拿去推廣的版本。

> **表註解裡舉例可以，但例子只能取自資料，不能取自題目。**
> 這條線很細:兩種寫法都「符合資料」，差別只在有沒有看過測試集。
> 防呆的辦法是**每一版都跑保留組**——換掉例子再量一次，
> 差多少就是過擬合了多少。

#### 實際效果（保留組版，已套用）

```
GT 表排名平均    3.70 → 3.11
候選召回 @40    302/305 → 305/305   ← 100%
候選召回 @25    299/305 → 301/305
變好 ≥3 名 23 個｜變差 ≥3 名 1 個（#90 products 7→10，仍在前 10）
```

走的是權威來源那條鏈:`db/init_db.py` 的 `CREATE TABLE` →
`sync_table_comments.py --apply`（純 ALTER，一個位元的資料都沒動）→
`gen_ddl.py --write`。八項閘門全綠、GT 自檢 309/309。

#### 閘門的盲區

`check_comment_coverage.py` 只掃 18 張寬表，查的是「註解有沒有涵蓋各欄位群」。
**`products` 這種核心窄表不在它的射程內**，而它要查的問題也不一樣 ——
不是「欄位概念齊不齊」，是**「這張表被需要的時候，排第幾名」**。
`products` 擺在那裡兩個月沒人看見，因為沒有任何指標在看它。


#### 換一批新題目再測一次 —— 泛化是真的，代價也是真的

前面的保留組只換掉了**註解裡的例子**，沒有換題目。真正的檢驗是拿
**全新的問句**去問，而且要含註解沒有宣告的形狀。20 題自寫題（值全部取自
資料庫、且不在題庫與註解裡），事前登記四組與各自的預測：

```
                                      需要的表最差排名   products 自己的排名
                                       舊  →  新          舊  →  新
A 引號點名商品（保留組名字）           27.4 →  7.8        27.4 →  7.8
B 需要 products 但沒點名                2.0 →  2.0         2.0 →  1.8
C 完全不需要 products                   1.2 →  1.2        49.2 → 39.4
D 點名的是別的表的值（風險組）          4.0 →  4.2        47.6 → 34.4
```

**A 證明泛化是真的**：`「機械鍵盤」63 → 18`、`「快煮壺」43 → 10`、
`「iPad Pro」22 → 6` —— 這些商品名從沒寫進註解、題庫裡也沒有。
學到的是「商品名住在 `products`」這個事實，不是那四題。

**C 與 D 證明代價也是真的**：`products` 變成「任何被點名的東西」的吸子。
最乾淨的一例：

```
「宏達貿易」的聯絡電話是多少？    suppliers 2 → 3     products 16 → 2
```

問供應商的電話，`products` 排第 2 還把 `suppliers` 擠掉一名。
今天不痛（`suppliers` 還在第 3），但**機制已經證實，而且會隨著
「有名字的表」變多而惡化** —— 門市、供應商、活動、方案、倉庫，
每一張都要來搶同一句泛稱。

#### 嵌入不懂否定 —— 寫「不是 X」會讓文件更像 X

想收窄邊界，試了兩個變體（排名數字**越小 = 越被吸過去**）：

```
                                              A 需要的表   D 的 products
舊                                              27.4          47.6
現行（泛稱「被點名的東西」）                     7.8          34.4
E 只講品名、不講泛稱                             9.0          37.8
F 明寫「門市/供應商/活動/方案的名稱不在這裡」     8.0          25.4  ← 更嚴重
```

**F 是最有價值的那一格。** 我寫了一句否定句想劃清邊界，
結果 `products` 在「問供應商」的題上排得**更前面**。
理由很簡單但事前沒想到：**那句話把「門市/供應商/活動/方案」四個詞
加進了 `products` 的文件**，而嵌入模型不做否定。

> **在檢索文件裡劃邊界，只能用「我是什麼」，不能用「我不是什麼」。**

#### 在這裡停手

`現行` 與 `E` 的差距是「A 讓出 1.2 名換回 3.4 名漏出」，
而這是在**我自己寫的 20 題**上量的。再調下去就是
[[comment-examples-from-data-not-questions]] 那個過擬合換套衣服。
**註解維持現行版本，不再微調。**

#### 這一輪改變了值索引的判決依據

§2.7 當初擱置值檢索（變體 C）的理由是「損失 2 題，還沒有大到值得多維護一個索引」。
**那個判斷比的是召回百分點，而這一輪量到的是另一條軸**：

```
表註解   買到商品名題      代價是 products 變成所有「被點名」問句的吸子
         要靠人盯著失敗才發現缺口
值索引   知道「宏達貿易」在 suppliers.name、「iPad Pro」在 products.name
         不需要泛稱、不吃檢索文件的預算、不需要有人盯
```

閘門 [12] 今天紅燈 0，所以成本判斷**暫時仍然成立**。
但觸發條件現在是可量的：**擴表後閘門一冒紅燈，或 D 組那種漏出開始讓
必要表掉出候選，表註解這條路就到頂了。**

#### 所以補了第 [12] 項閘門：`check_table_retrievability.py`

發現 `products` 的過程是「某一題失手 → 追失敗清單 → 才看到排第 62 名」。
**那個過程不可靠**：它需要有人盯著失敗、需要那張表剛好被某一題用到、
還需要那一題剛好因此答錯 —— 而 `#21` 一直是綠的，因為 KMB 把 `products`
當橋補了回來。**缺陷被下游的救援機制遮住了**（§7.2 的「錨點 @40 = 98.9%、
+KMB = 100.0%」就是這個遮蔽的原始紀錄）。

新閘門**逐表算「被 GT 需要時的 dense 排名」，不看對錯、不給 KMB 的救援算分**：

```
紅燈  最差排名 > CANDIDATE_N     掉出候選層，後面兩段救不回來
黃燈  中位數   > CANDIDATE_N / 2  還沒掉出去，但在邊緣
灰    GT 從來不需要這張表         量不到 —— 單獨報，不算通過（§8②）
```

判準綁在既有常數 `CANDIDATE_N` 上，**沒有引進新的可調參數**（§10「停止調常數」）。

**上線前先餵它已知的壞例子**（[[silent-pass-is-not-a-pass]] 的教訓：
新工具第一次報全綠時先懷疑工具）。拿改註解前的排名快照重跑同一套判準：

```
改註解前  🔴 products  被需要 60 次｜中位數 5.0｜最差 62（#21）   → exit 1
改註解後  ✅ 紅燈 0、黃燈 0                                      → exit 0
```

**中位數 5.0 這一格是這支工具最重要的設計證據** ——
如果判準寫成「平均排名」或「中位數」超標才紅燈，它**抓不到 `products`**。
缺陷藏在尾巴，所以紅燈條件必須是**最差值**。

---

### 2.8 其他被否決的檢索方案

| 方案 | 判決 | 依據 |
|---|---|---|
| **向量資料庫** | 否決 | 只有 **93 個向量**，暴力比對就是精確解 —— 80 個向量時 2048 維餘弦加排序**實測 36 ms**，快取當時 2.97 MB。ANN 是拿精確度換速度，我們離那門檻四五個數量級。要變大**下一步是 numpy 不是 Milvus**，那能撐到萬張表 |
| **Hybrid BM25 + RRF** | 否決 ⚠️ 未在 80 張表重測 | 當時每一個 K 都比純 dense 差。機制理由今天仍成立：中文字元 bigram 在短文件上雜訊太大，而今天檢索文件中位只有 **60 字元**，比當時更短 —— **條件往更不利於 BM25 的方向走** |
| **選表改用 20B 小模型** | 否決 ⚠️ 未在 80 張表重測 | 當時召回略低而且**120B 反而比較快**。錯的性質不同才是重點：20B 漏的是問題裡明講的東西。重測要 `--funnel` × 2 |
| **多次取樣取聯集**（MCS-SQL） | ~~否決~~ → **採用（2026-08-28 推翻舊判決，見 §2.7i）** | 舊判決「+KMB 零增益、3× 呼叫，聯集治隨機不治系統」是 80 張表 / 單輪 / 只看總分下量的，兩個地方錯了：當時 schema 裡**沒有會被系統性漏掉的那一群表**（`*_profiles` 是 86→93 才進來的），而單輪總分**量不出雙向抵銷**（§5.2）。93 張表重測：+KMB 97.1%→98.5%（9 救 0 丟 p=0.0020）、e2e 子集 7 救 0 丟（p=0.0156）、防禦題 6 格 4/4。`FILTER_VOTES` 預設已改 3 |
| **打散候選順序** | **採用** | 固定順序與打散順序的召回區間完全不重疊，而且零成本。這推翻了「照相似度排序等於免費給 LLM 一個先驗」—— 那個先驗是**淨負的**。今天 production 用問題的 CRC32 當 seed：同一題永遠同一種順序（可重現），不同題順序不同（不固化成新偏誤） |
| **階層式選表** | 擱置 | 觸發條件：候選目錄大到 needle-in-haystack 開始咬。今天每題送 40 張 ≈ **3,300 字元**（§2.7f 實測 A 臂 3,325；表數不影響，`CANDIDATE_N` 是常數，長的是註解），離那裡還很遠；線性外推約 200 張表 |

**這一輪最重要的結論**：四個檢索演算法方案（A/B/C/D）全部沒有兌現，
**修好 `#104` 的是一句表註解**（0/6 → 6/6，而且 `payments` 的相似度排名沒變 ——
證實問題從頭到尾都在「LLM 看不到那個概念」，不在檢索排序）。

方向與 LinkedIn 報告的 44pp「幾乎完全來自知識圖譜，不是更好的 prompt 或更聰明的
agent」一致 —— 業界（LinkedIn、Pinterest、Snowflake）在表註解不夠分辨近義表時，
補的不是更長的文字，是**「這張表實際被怎麼用」的證據**（query log 熱度、
常見 join pattern、verified queries、synonyms）。**本專案缺的正是這一層。**

⚠️ 反方向的證據也要記：EDBT 2026 的 schema linking 分析與一份 BIRD 上的研究說
**「即使配上高召回的 embedding 檢索器，schema linking 也沒有幫助」**。

### 2.9 失效時的降級

檢索層是加分項，不是單點故障：

| 失效 | 行為 |
|---|---|
| 嵌入 API 掛掉（實測會偶發 502） | 跳過相似度，**LLM 選表照跑**（它讀表註解不需要向量），只損失 ∪ 第 1 名這道保險 |
| LLM 回不出合法 JSON / 選了不存在的表 | 退回純相似度 Top-K |
| 兩者都失效 | 退回全部 93 張表 —— 退化成 Prompt 變長，不是答不出來 |

另外 DDL 一律附上「其他表的表名」（只有名字，約 250 字元）。資料庫裡 93 張表全都在，
EXPLAIN 驗的是真實 DB —— **模型只要知道那張表存在就能用它。真正的風險是它不知道，
然後拿看得到的表硬湊一個答案。** 實測 `#24` 就是檢索漏了 orders/order_items，
而模型靠這份表名清單自己正確補上了 JOIN。

---

## 3. KMB：為什麼補橋接表要用圖論

「陳先生上個月買了哪些手機」語意上只命中 customers 與 products，但少了
orders、order_items 就接不起來 —— 而問題裡**沒有任何字詞會命中這兩張表**。
橋接表只能靠外鍵結構找。

用 KMB 而不是 SPH：近似保證相同（皆 2−2/k），差別在搜尋次數（O(k) vs O(k²)）。
兩者在 80 張表都是微秒級 —— **這是為變大做的準備，不是現在的優化。**

80 張表的 K-sweep（253 題，零 LLM 成本，`eval/eval_retrieval.py --k`）：

| 取前 K | 錨點召回 | +KMB | KMB 補了多少 | 平均表數 | 淨度 |
|---|---|---|---|---|---|
| 1 | 49.4% | 49.4% | **+0**（單錨點的數學下限） | 1.0 | 84.6% |
| 3 | 76.3% | **86.6%** | **+10.3pp** | 3.7 | 39.8% |
| 5 | 83.0% | 92.9% | +9.9pp | 6.2 | 25.2% |
| 8 | 88.1% | **95.7%** | +7.6pp | 9.7 | 16.2% |
| 14 | 94.5% | 98.8% | +4.3pp | 16.2 | 9.9% |
| 24 | 97.6% | 99.6% | +2.0pp | 26.7 | 6.0% |
| 40（＝候選上限） | 99.2% | 100.0% | +0.8pp | 42.7 | 3.7% |
| **production 漏斗** | **97.6%** | **99.2%** | **+1.6pp** | **2.0** | **86.1%** |

規律很乾淨：**錨點召回愈低的那一列，KMB 補得愈多。**
所以要砍它時看的指標是**錨點召回**，不是端到端。

它防的是「兩張錨點表在 Prompt 裡 JOIN 不起來」，而那種錯**不會被 EXPLAIN 擋下來**，
模型會硬湊一個能跑的 JOIN。

#### production 裡 KMB 補幾題：**4 題（+1.6pp）**

這個數字掛「未知」很久了，2026-08-21 用 `--funnel` 量到了，而且**它落在上表的規律上** ——
production 的錨點召回 97.6%，比 dense K=3 的 76.3% 高得多，所以 KMB 只補 1.6pp
而不是 10.3pp。**規律是「錨點愈準，KMB 愈閒」，兩端都對得上。**

這也回答了「KMB 值不值得留」：它今天只救 4 題，但那 4 題是**其他任何一層都救不回來的**
（橋接表在語意上不存在），而它的成本是微秒級的圖搜尋。

具體例子：`#256` 問「哪些客戶既預約過服務又申請過保固」，而 `warranty_claims`
**沒有 customer_id**，要接到客戶只能經由 `orders` —— 那條路徑在任何語意相似度裡都不存在。

### 3.1 權重記在「表」上而不是邊上

「經過 browse_logs 很貴」本來就是**表**的性質，設定量也從 O(邊) 降到 O(表)。
今天 93 張表裡有 10 張行為表成本 3（browse_logs、reviews、carts、cart_items、
wishlists、wishlist_items、product_qna、search_logs、campaign_clicks、
product_recommendations），其餘 83 張走 `_DEFAULT_COST_REVIEWED`（§6.1 第 5 項）。
**全庫 14 張 `*_profiles` 全部維持成本 1** —— 它們是掛在主體旁的一對一檔案表，
不是「行為 ≠ 購買」那一族。但那個 1 是閘門第 5 項逼出來的決定，不是漏掉的。

這些是**行為表**：看過 ≠ 買過、放進購物車 ≠ 買過。沒有權重時 KMB 會抄近路，
把「購買」的問題接到「瀏覽」的表上。

**W=3 是唯一可行的值**，兩個邊界夾出來：要大到輸給 3 跳的購買路徑（3 < 3+1），
要小到 carts 自己當錨點時跳數決勝仍會選對橋。W=4 在真實 schema 上就壞了
（`['carts','products']` 會繞經 orders）。`test_schema_graph.py` 有 17 條迴歸檢查鎖住它。

`tools/check_table_costs.py` 用行為 vs 交易兩組概念探針掃描漂移，加新表時跑一次。

---

## 4. Prompt 層：只用 Few-Shot，不加業務規則

**這是一條硬規定。** 規則表達不了情境相依的邊界，而且會跟既有的 few-shot 打架。

實例：規則說「總營收用 `SUM(o.total_amount)`」並把逐項計算標為不建議，
但 few-shot 4 對同一個詞用的是逐項計算 —— **Prompt 自我矛盾**。
解法是刪掉規則，改加一個「類別粒度營收」的 few-shot，把邊界寫在它的 `reasoning` 裡。
（這種矛盾是**檢查**就能看出來的，刪掉它不需要統計證據。）

今天的規模：few-shot 24 則 / 12,472 字元、business_rules 10 條 / 6,221 字元。

### 4.1 遷移性有兩層，只有一層會遷移

| 類型 | 例子 | 會遷移嗎 |
|---|---|---|
| **謂詞級** | `is_deleted = 0`、狀態值對應 | **會** — `#144` 2/4 → 6/6 |
| **結構級** | JOIN 後主體收斂去重的形狀 | **只在形狀完全對齊時** |

`#56`（佔比題分母誤用 `COUNT(*)`，得 24.38% 而非 98%）是形狀對齊成功的樣本：
4/8 → 8/8。三個刻意的選擇 ——
**不用評估題本身當範例**（那是 teaching to the test，用不同的表靠形狀遷移）、
**把錯的寫法也寫進 reasoning**（這類錯誤的特徵是「看起來完全合理」，
只說正解不足以讓模型認出自己正在犯它）、**接在同族既有範例後面**。

反面樣本是 `#138`（收件地址城市 ≠ 居住城市）：試了四條 few-shot，形狀愈補愈接近，
錯誤形狀確實在移動（未收斂 48 列 → 收斂到姓名 29 列），但分數動不了。
最後定位出原因**不是知識缺口**：

> 問句自己點名了兩個城市（「收件地址城市**和**居住城市不同」），
> 模型把 `a.city` 放進投影是在**回應問句**，而那個來自「多方」的欄位讓 DISTINCT 失效。
> 這是**問句語意與答案粒度衝突**，few-shot 補得了知識缺口，補不了這個。

`#138` 因此永久凍結。解凍條件不是「再寫一條 few-shot」，而是出現**第三個實例、
且該實例的問句沒有點名多邊欄位** —— 那才代表這是可教的通則，而不是這一題的特性。

同族的 `#63` 是「再寫一次沒有槓桿」的決定性反證：同一個教訓
（`DISTINCT name` 會併掉同名不同人）**寫在 few-shot 和表註解兩個地方**，仍然 4/8。

### 4.2 教訓一旦真的送達，就會往相鄰形狀溢出

2026-08-20 發現 **`reasoning:` 從來沒有進過 Prompt** —— YAML 裡 22 則 reasoning、
4,649 字元全是死的，模型只拿到 SQL，得自己反推那是在示範哪一條教訓。
（見 §8 ①，這是「寫了沒接上下游」的第四個實例，而且它讓
「以後都從 few-shot 解決」這條方針在結構上一直做不到。）

修好之後 `#93` 從 1/8 → 8/8，257 題 e2e 的失敗題從 9 掉到 4。**但 `#89` 變差了。**

`#89`「訂單張數最多的前 5 位客戶」回 52 列而非 19 列 —— 模型把 few-shot #4 的
「『最高』可能有並列，不可用 LIMIT 1，要用 `DENSE_RANK()`」套到了「前 5 位」上。
`DENSE_RANK() <= 5` 取的是前 5 個**不同的張數**（14 位客戶 52 列）。

受控 A/B（同一 process、monkeypatch 換回舊 renderer、每臂 n=16。
**n=8 不夠** —— 同一份程式碼三次 n=8 量到 2/8、3/8、6/8）：

| | OFF（只有 question + SQL） | ON（含 reasoning） |
|---|---|---|
| 補邊界前 | 14/16 | **7/16** |
| 補邊界後 | 12/16 | 10/16 |

> **教訓沒送出去的時候不會溢出；一旦真的送達，它就會往相鄰形狀擴散。**
> `business_rules` 早就有「邊界」段落，few-shot 從來沒有 —— 因為它的 reasoning
> 根本沒進過 Prompt，不需要邊界。現在需要了。

補的邊界（寫在 reasoning 裡，`business_rules` 維持 10 條不動）：

```
極值（最高/最多/哪一個最…）→ 問的是第一名這個位置，WHERE rnk = 1
top-N（前 5 位/前 10 名）  → 問的是一份 N 筆的清單，ORDER BY … , <唯一鍵> LIMIT N
rnk <= N 當成 top-N 是錯的：DENSE_RANK 排的是「不同的值」。
```

補完仍有約 20pp 差距（Fisher p ≈ 0.09）。**到此收手** —— 再往下就是對著單一題目
調字句，那是「一直改 K」的同一個錯誤。`#89` 記為**已知代價，不是待辦**。
反向檢查確認邊界沒弄壞真正需要 `rnk = 1` 的題：`#93`/`#121`/`#242` 各 12/12。

**同樣的成對邊界寫法也用在 `#93` 上。** `#93`（總消費中位數）五輪 e2e 錯的都是同一個值
45,259 —— 不是算錯中位數，是算在 `customer_profiles.total_spent`（每日結算快照）
而非即時 `SUM(orders.total_amount)`。那句「與即時 SUM 可能不同」的欄位註解
**確實在 Prompt 裡**，模型看到了然後 7/8 次選了快照。
**系統從來沒有教過「問句沒指定來源時，事實表才是權威來源」** ——
沒教過的東西，模型每次做同一個選擇，這就是「穩定偏誤」的成因。

而只教「一律走 orders」會弄壞 `#146`（題目明講要快照）。所以教訓寫成謂詞級的
**「看問句有沒有指定來源」**，並劃出射程 —— `tier`/`ltv_score`/`risk_flag`/`gender`
這些寬表獨有、事實表算不出來的欄位不在射程內。

**代價**：few-shot 文字 5,443 → 11,887 字元，`#93` 的 prompt 約 21.5k → 28k，
few-shot 佔比 42% —— 正好落在稀釋定律的射程內，所以**單題變綠不能收工**，
必須再跑一輪完整 e2e 對照。單題修好、整體變差，是這個專案已經踩過的形狀。

### 4.3 防禦題：拒答是未解問題，動作必須窄

四題防禦題斷言「93 張表的 schema 裡沒有這個概念」：`#68` 運費、`#70` 利潤率、
`#140` 庫存週轉率、`#141` 信用評分。今天 4/4。

研究界對這個失效有名字且說它未解（AbstentionBench：加大模型沒用，
**推理型模型反而更不知道何時該不答**；LatentRefusal：拒答 prompt-sensitive，
**恰好在模型幻覺時失效**）。所以不要期待修到 100%。

實際漏接過一次：`#140` 自己發明了 `SUM(quantity) / capacity AS turnover_rate` ——
SQL 合法、EXPLAIN 會過、**使用者看不出那個數字是憑空來的**。

處理方式是**窄規則 + 正向邊界**。257 題裡有 19 題可回答的題目提到相鄰詞
（「手續費率」「平均評分」「總消費金額」），任何粗暴的關鍵詞拒答都會誤傷它們，
所以規則裡明寫：

```
擋的是「週轉」這個沒有定義的比率，不是庫存與出貨這兩個有資料的量。
「每個倉庫的容量是多少」「哪些商品庫存是 0」→ 直接答。
```

這是一個**只能否證不能確證**的實驗（`#140` 加規則前探針就已經 8/8，只有 e2e
漏過一次；要區分 91% 與 100% 需要 n≈30 以上）。它能確認的是更重要的那一半：
**四題邊界／相鄰題全部 8/8，沒有過度觸發。**
代價可測為零、收益不可測時，代價為零就是採用的理由。

`#68`／`#141` **沒有加規則** —— 沒觀測到漏接，加規則是純推測還要付誤觸風險。
**觸發條件：`eval_score.py` 的防禦題那一行出現它們，才回來加。**

### 4.4 Prompt 預算與「專有名詞污染」（實測：沒有訊號）

今天 few-shot 24 則 / 12,472 字元，平均每則 519 字元，最長 973（`#93` 那則）。
在 `#93` 這種題上 few-shot 佔 prompt 約 42% —— **這已經落在稀釋定律（§2.5）的射程內**，
所以「few-shot 再變多要不要檢索」是一個真問題，記在 §10 的擱置區。

一個常見的提案是**把 few-shot SQL 裡的真實表名抽象成 `<表A>`**，
理由是「別讓專有名詞污染我要教的 SQL 形狀」。先量了有沒有這個現象：

```
few-shot 24 則，SQL 裡出現過的表：8 張（cart_items、customer_profiles、customers、
                                order_items、orders、payments、products、reviews）
最新 257 題 e2e：產出 SQL 用到「檢索沒給的表」的題數 = 15
  其中 few-shot 出現過的表  customers 5、products 2、orders 1   共 8 次
  few-shot 沒出現過的表     promotions 2、suppliers 2、subscriptions 2、
                            shipments 2、categories 1           共 9 次
```

**看不到污染訊號。** 用到的 8 張表裡有 5 張根本不在 few-shot 裡，
而出現的那 3 張（customers / products / orders）正好是全庫 FK 度數最高、
被需要次數最多的三張（§7.5）—— 模型會用它們是因為它們真的常常需要，
不是因為 few-shot 提過。而且「用了檢索沒給的表」本來就是 §2.9
`_other_tables_line` **設計上要讓它發生**的行為。

依 §5.2：**在沒有錯誤的地方做改動，分不出改好了還是本來就沒壞。**
所以不抽象化。而且抽象化與 §4.1 的核心發現衝突 ——
`#56` 之所以成功，正是因為用了**不同的真實表、同一個形狀**；
把表名換成 `<表A>` 等於把「形狀對齊」賴以成立的具體性拿掉。
真實表名同時還在教「哪張表是什麼意思」，那是這個專案花最多力氣買到的鑑別力。

**觸發條件**：`eval_lucky.py` 或對帳出現「模型用了 few-shot 裡的表、
而該題正解是另一張近義表」的實例，才回來重估。

---

### 4.5 全量注入 24 則 few-shot 的溢出代價：量到 ~0，所以條件式注入否決（2026-08-27）

§4.2 立了「教訓一旦送達就會往相鄰形狀溢出」，`#89` 是那條的實例。
既然溢出是真的，一個自然的下一步是**條件式注入**（few-shot 向量檢索，
每題只給最相關的 3 則），把溢出壓回去。

**先量代價再決定做不做。** 兩臂 e2e，A = 24 則（production，12,472 字元）、
B = 0 則，逐題配對。要的不是總分（§5.2），是**雙向的兩格**：

    A 錯 / B 對  = 溢出代價        ← 條件式注入能買回來的上限
    A 對 / B 錯  = few-shot 的貢獻  ← 條件式注入絕對不能弄丟的

第一階段 309 題 × 2 臂 × 2 輪（`scratchpad/fewshot_ab.py`，
monkeypatch `SchemaParser.get_few_shot_text`，不動 production）：

| 輪 | 正確 | API 掛 | 秒 |
|---|---|---|---|
| A#1 | 300/309 | 0 | 1114 |
| B#1 | 290/309 | 0 | 989 |
| A#2 | 288/309 | 8 | 1326 |
| B#2 | **15/309** | **288** | **12308** ← 整輪被 rate limit 打爛，作廢 |

`workers=3` 連跑四輪撐不住。**B 臂實際只有一輪有效**，所以第一階段的產出
只能當候選集：溢出側 6 題、貢獻側 17 題。

第二階段對那 6 題**重新抽樣** n=8 兩臂（`scratchpad/fewshot_stage2.py`）：

| 題 | A（24 則） | B（0 則） | Fisher 兩尾 |
|---|---|---|---|
| `#152` | 7/8 | 8/8 | 1.000 |
| `#189` | 7/8 | 8/8 | 1.000 |
| `#264` | 3/8 | 4/8 | 1.000 |
| `#270` | 4/8 | 6/8 | 0.608 |
| `#296` | 2/8 | 6/8 | 0.132 |
| `#309` | 6/8 | 6/8 | 1.000 |

**一題都沒有活下來**，而且最大的 `#296` 那個 p 還是**樂觀的** ——
這 6 題是用「兩臂結果不同」挑出來的，選擇偏誤還沒扣（§5.4）。

**比 p 值更硬的是失敗模式。** 把兩臂的錯誤內容攤開看：

    #264  A 3/8 回 [[0]]／B 4/8 回 [[0]]     ← 一模一樣
    #270  A 4/8 回 [[1,5,0]]／B 6/8 回 [[1,5,0]]  ← 一模一樣（漏 JOIN stores 拿 name）
    #296  A 2/8 回 [[9,'訂單何時出貨']]／B 也出現同一個回答

> **few-shot 沒有在這些題引入任何一種「自己的」錯法。**
> 兩臂錯得一樣、只是頻率不同 —— 那是擲硬幣，不是溢出。
> `#89` 那種可歸因的溢出（DENSE_RANK 從極值題套到 top-N 題）這次一個都沒抓到。

**而貢獻側是可歸因的**，四個都能指出機制：

| 題 | B 臂（沒有 few-shot）做了什麼 |
|---|---|
| `#93` 中位數 | 反覆寫 `PERCENTILE_CONT(...) WITHIN GROUP`，MySQL 沒有這個函數，重試耗盡 → `no_sql` |
| `#147` 快照對帳 | 同樣耗盡重試 → `no_sql` |
| `#8` 哪個月訂單最多 | `GROUP BY MONTH(order_date)` —— 跨年合併；A 用 `DATE_FORMAT('%Y-%m')` |
| `#151` AT_RISK 客戶 | 只回 `customer_id`；A 回 id/name/email |

方言、口徑、輸出粒度 —— 全是 §4.1 說的**謂詞級**教訓，正是會遷移的那一層。

**判決：條件式注入否決。** 要買的是量到 ~0 的溢出，要賭的是 17 題有機制的貢獻。
**這不是「few-shot 不會溢出」** —— §4.2 的 `#89` 仍然成立，而且射程邊界
（`extremum` vs `top-N`）就是為它寫的。結論是**現在這 24 則的溢出已經被邊界壓住了**，
沒有留下可用機制買回來的殘量。要重開這條，先拿出一題「A 穩定錯、B 穩定對、
而且兩臂錯法不同」的證據。

**順帶第三次應驗 §5.4。** 第一階段最強的兩個候選是 `#152`、`#189`（A 0/2、B 1/1），
看起來像穩定失敗；重新抽樣是 **7/8 與 7/8**。用「哪些題兩臂不同」挑出來的集合，
挑出來的那批資料一格都不能再用。

## 5. 評估方法

### 5.1 GT 永遠先修正才跑測試

**硬規定。** GT 錯了，模型的錯與 GT 的錯會混在一起，無法歸因。

三條從實際踩坑長出來的規則：

- **GT 只選「回答問題所需的最少欄位」。** 比對規則對多回欄位寬容、對少回不寬容 ——
  容忍度是單向的，所以過度指定一定會製造假錯誤。（`#71` 就是這樣被判錯的：
  GT 多要了一個 `COUNT(*)`，而系統的答案是對的。）
- **不要指定表示格式。** `#36` 只收 `'2026-02'`，把等價的 `YEAR()/MONTH()` 兩欄判成錯 ——
  那是在考格式不是考語意。已補 `alt_sql`。
- **新題的第一次探針，要把「模型錯」與「GT 錯」當成同等可能的假設。**
  這一點被驗證了三次（`#71`、七個平手題、`#188`/`#189` 的 GT 忘了 `is_deleted = 0`
  這個專案自己的慣例），**三次都發生在新寫的題目上** ——
  既有題目經過多輪 e2e 洗過，新題沒有。

寫 GT 時最常撞到的是**平手題**：「工單最多的客戶」36 張單分屬 36 位客戶全部並列 1、
「圖片最多的商品」五個並列 4 張。答案會是任意平手，一律改成計數或清單題。
同名不同人也要先查（37 個 `customer_id` 只對應 33 個相異姓名）。

### 5.2 單輪評估分不出差異

MoE 模型在 temperature=0 仍然不確定。**同一份設定連跑兩次的檢索召回是
99.3% 與 98.6%** —— 任何小於約 1 個百分點的「改善」都讀不出來。

連續三輪完整 e2e 最能說明問題：

```
三輪分數：154 / 154 / 154      每輪錯題數：5 / 5 / 5
曾經錯過的題：9 題（union），三輪錯題只重疊兩題
```

**分數三次一模一樣，失敗的身分卻一直換。** 這是「錯誤預算」型的系統，
不是「某幾題不會」。所以：

```
報告格式：穩定通過 N 題｜硬幣池 M 題（附各自的 k/8）｜穩定錯 K 題｜防禦題 x/4
```

**判斷任何改動有沒有效，比較的是硬幣池的組成與各題的 k/8，不是分數。**

**漏斗評估同樣沒有解析度（2026-08-26 補）。** 兩次把 A/B 跑成 A/A 的意外，
換來了同設定重複執行的區間：

```
錨點召回   拆欄臂 n=10   93.4 ~ 96.1%     ← 2.7pp
           現行臂 n=5    93.4 ~ 94.8%     ← 1.4pp
```

那一天靠**單輪**全庫漏斗下的三個判斷（B 首句 −2.0pp、C' +1.0pp、
以及「A 自己 spread 只有 0.3pp」）全部泡在這個區間裡 ——
最後那句是拿兩個樣本當區間，**兩個樣本量不出區間**。
所以漏斗的判準改成**逐題配對 + 符號檢定**：總分把 305 題的雜訊加總，
配對讓每題自己跟自己比，跑次雜訊被消掉。

解析度下限：n=8 分得出 3/8 → 7/8，分不出 7/8 → 8/8（要 20+ 次）；
要把 2/8 與 5/8 分開需要 n≈40 —— **在硬幣題上做改動，驗收比改動貴得多**，
這本身就是「先不要動」的理由。效果小於解析度時，改用「機制是否消失」判斷
（看失敗時回傳的具體數值）。

**探針條件比完整 e2e 寬鬆。** 探針 5/8、4/8 的題在 257 題的 e2e 裡連錯四輪 ——
若探針機率是真的，那只有 2% 與 6%。**跨條件比較必須明講是哪一種條件量的。**

**零 LLM 的量測沒有這個問題**，所以優先用它：`--k`、`eval_retrieval_variants.py`、
`eval_column_recall.py` 三支都是確定性的、可以隨便重跑。
本文件裡的檢索數字絕大多數來自這三支，正是這個原因。

### 5.3 檢索層要獨立評估

`eval/eval_retrieval.py` 不跑 pipeline、不呼叫生成模型，只量「表選對了嗎」。

理由：**端到端正確率會低報檢索失敗。** `#62` 檢索漏了 `products`，模型改用
`oi.unit_price > 30000`（成交價）而 GT 用 `p.price`（售價）—— 兩者語意不同，
只因為種子資料裡這兩欄永遠相等才碰巧判對。

`eval/eval_lucky.py` 把這件事自動化：答案對、schema 不對 → 標成僥倖候選。
它是旗標不是判決 —— 同一題本來就有多種正確寫法，涵蓋任一條 `alt_sql` 就放行。
**確認是正常寫法就要立刻補進 GT**，因為常態誤報會讓稽核工具失去意義。

三個指標必須一起看（召回、平均表數、淨度）—— 召回與表數是對立的，
單看任何一個都會做出錯誤取捨。§3 的 K-sweep 就是活例子：K=40 召回 100%，
淨度只有 3.7%。

### 5.4 挑出來的子集合不能再拿來做檢定

一度對「兩輪都通過」的題抽 20 題 × 8 次，得到「零個隱藏硬幣」。
**第三輪 e2e 當場推翻** —— 兩題前兩輪都過、第三輪錯，而且不在那 20 題裡。

錯在**抽樣母體本身有條件**：一題失敗率 25% 的脆弱題通過兩輪的機率只有 56%，
母體已經被過濾成偏強韌的一群。

同理，我一度為「探針比 e2e 寬鬆」算出 p = 0.0152 —— **那個檢定也有選擇偏誤**。
那些題是因為在 e2e 錯過才被選進池子的，所以條件在「進了池子」之下，
e2e 的失敗必然被過度呈現。最清楚的例子是 `#43`：探針 8/8，
它進池子唯一的理由就是 e2e 錯過一次 → e2e 顯示 1/2。

> **任何用「哪些題錯了」挑出來的子集合，都不能再用「那些題錯幾次」去檢定。**

同理，**只對錯題取樣有偏誤** —— 那只找得到剛好落錯的硬幣，找不到剛好落對的。
要估硬幣池得對**通過的題**隨機抽樣。

### 5.5 探針只能否決，不能放行 —— 靶子選在依變項上（2026-08-26）

**這是 §5.4 的第三次應用，而且這次被違反的不是檢定，是實驗設計本身。**

五次介入、五次探針大勝、五次全庫不兌現：

| 節 | 介入的層 | 探針結果 | 全庫驗收 |
|---|---|---|---|
| §2.7d | 候選目錄（概念群 D 臂） | 25/32 | 40 題回歸 A 115/120 vs D 114/120 |
| §2.7e | 候選目錄（LLM 宣告 mapping） | 乾淨 6 題 +11/48 | 未上線（8/11 票來自單題） |
| §2.7f | 候選目錄（欄位提示 E 臂） | 誘餌 3/32 → **32/32** | 305 題錨點 95.7 vs 95.4；7 題 n=8 **A 39/56 vs E 35/56** |
| §2.7g | 選表 system prompt（第 5 條原則） | 誘餌 2/32 → **17/32** | 窄表對照當場 35/40 → 28/40 |
| §2.7h | 檢索文件（拆關係宣告句） | 12 題三組全贏 | 305 題 × 6 輪配對 **14:14 / 9:9，p = 0.57** |

**先排除一個誤診：這不是樣本太小。** 用 Fisher 兩側精確檢定重算：

```
§2.7f 誘餌 A 3/32 → E 32/32     p = 0.00001
§2.7g 誘餌 A 2/32 → G 17/32     p = 0.00007
§2.7f 全庫 7 題 A 39/56 → E 35/56   p = 0.55
```

**探針量到的效應是真的**，統計上毫無疑問。它在那四題上確實把 3/32 變成 32/32。
問題是那個效應**在建構上就不可能不出現**：

> **誘餌四題之所以被選進探針，正是因為它們敗於「模型不肯用寬表」這個機制。
> 任何把該機制反轉的介入，在那四題上必然大勝 —— 那是選題方式決定的，不是介入的功勞。**
> 探針看不到的是代價，因為代價分散在沒被選進來的另外 ~290 題上。

這直接推翻一條已經寫進待辦的修法。§2.7d 第 4 項寫「回歸關卡的票數要從 3 提到 8，
否則量到的是雜訊」——

> **把 n 從 8 提到 40 不會有任何幫助**，只會讓 p 值更小、結論一樣不可遷移。
> 那是拿治變異數的藥去治偏誤。真正要換的是**對照組**。

對照組同樣被挑過，而且看得見後果：§2.7f 的對照是「10 題手挑的窄表題」，
而全庫爆掉的 `#94`／`#100` 屬於「寬表欄位是刻意陷阱」這一類 ——
**那一類根本不在對照組裡**。§2.7d 用過「隨機抽 40 題、固定 seed」，方向是對的，
但票數只有 3。**隨機對照 + 足夠票數這兩件事從來沒有同時做過。**

#### 因此的硬規定

1. **探針（手挑靶題、n ≤ 8）只能否決，不能放行。** 探針贏了只代表「值得花錢驗」，
   探針輸了才是結論。§2.7d 早就寫了這句話，然後 §2.7f 在同一份文件裡下了「打穿了」。
2. **對照組必須是隨機抽樣（固定 seed），不准手挑。** 手挑的對照組只涵蓋你想得到的
   失敗類型，而會咬人的正是你沒想到的那一類。
3. **驗收一律逐題配對 + 符號檢定**，不比總分（§5.2）。
4. **一個介入只有在全庫配對上贏才算贏。** 建議規格：隨機 40 題 × n=8 = 320 次呼叫，
   比 305 題 `--funnel` 便宜，而且同時看得到收益與代價。

#### 這條紀律唯一一次被正確執行過的地方

§2.4 的 ∪ Top-1（值 3 題、+1.2pp）落在漏斗的跑次雜訊（1.4~2.7pp）裡，
照理應該被降級 —— **但它不用**，因為那兩臂**共用同一組 LLM 呼叫**，
聯集是離線算出來的。它在結構上就是配對比較，雜訊自動消掉。

> **本文件唯一還站得住的 LLM 層正面結論，正好是唯一一個做了配對的實驗。**

---

## 6. 閘門與標準流程

### 6.1 八項閘門：`tools/check_schema_pipeline.py`

**預設 `exit 1`。** 這支的存在理由見 §8 ① —— 同一類 bug 咬了四次，四次都不報錯。

```
[1] DDL 覆蓋   [2] DDL 新鮮度   [3] 註解同步   [4] regex 可解
[5] KMB 成本   [6] 覆蓋與宣告   [7] 沒有空表   [8] prompt 鏈
```

前七項守 **schema 鏈**（DB → DDL → parser），第八項守 **prompt 鏈**
（YAML → renderer → Prompt）：每個欄位挑最長的一非空行當探針，看它在不在
renderer 的輸出裡。**不比對完整內容** —— renderer 本來就會加縮排與前綴，
比對整串只會一直假警報，而**常態誤報的檢查等於沒有檢查**。

**驗它會響**（閘門不示範會響就是裝飾）：把 renderer 換回改動前的版本 →
抓到 25 個漏掉的欄位，正是它該抓的那個 bug。

第 5 項的做法值得單獨記：`table_cost()` 對沒列到的表回傳預設 1，
所以**新表永遠不會報錯**（症狀是 KMB 靜默繞錯路：拓撲對、語意錯）。
修法是把預設值也明列成 `_DEFAULT_COST_REVIEWED`，強制
`_TABLE_COST ∪ _DEFAULT_COST_REVIEWED == 全庫 93 張表名`。加表就一定要來做一次決定
—— **決定「1」也可以，但必須是決定過的 1。**

擴到 80 張表的過程中這些守門員實際擋下的事（都不是裝飾）：第 6 項兩次擋下沒有
配題的新表（共 31 張）、第 5 項逼出六張「行為 ≠ 購買」的新表設成本 3、
歧義稽核抓到 `job_runs.status` 三次命中、重複鍵檢查當場抓到 `#89` 的裁決被覆寫。

### 6.2 評估框架的四個洞與對應工具

| 洞 | 工具 | 怎麼跑 |
|---|---|---|
| 防禦題會被加表**靜默作廢** | `tools/check_defence_gt.py` | `eval_score.py` 自動跑 |
| 端到端**低報**檢索與生成失敗 | `eval/eval_lucky.py` | `eval_score.py` 自動跑 |
| 單輪計分**掩蓋**擲硬幣的題 | `eval/eval_stability.py` | 手動（會打幾百次 API，值不值得是人的判斷） |
| 欄位裁剪沒有守門員 | `eval/eval_column_recall.py` | 動裁剪前，或 schema 變寬後 |

前兩支合併進 `eval_score.py` 是零額外成本的（`judge()` 本來就要跑一次每題），
而防禦題稽核放在**對帳之前**，因為它最便宜且**它報警時後面的分數要重讀**。

第一個洞值得展開，因為它是唯一一種**會被加表靜默作廢的 GT**：

> **一般題斷言「答案是什麼」，防禦題斷言「概念不存在」。**
> 前者只依賴用到的那幾張表，後者依賴**整個 schema 的補集** ——
> 93 張表裡加任何一張都可能讓它失效，而失效時
> **系統答對了卻被判錯，看起來像模型退步**。

判準是**業務定義在不在，不是欄位在不在**：加寬表帶進 `birth_year`／`gender`
之後 `#69`／`#71` 改為一般題（`2026 - birth_year` 無歧義），但 `#70` 利潤率
維持防禦（`supply_price` 有了，但一商品多進貨價、公式未定義）。
防禦題因此從 6 題剩 4 題。

做法：GT 裡宣告 `absent:` 與 `probes:`，程式掃 INFORMATION_SCHEMA；
**裁決過的命中寫進 `acknowledged:` 就不再報警，未裁決的命中才是警報** ——
所以加表時只會看到新出現的東西，不會被既有命中淹沒。

它上線就抓到 `#141` 的 note 過期（寬表帶進了 `credit_limit`、`risk_flag`、
`churn_risk_score`）—— 依判準仍維持防禦，但理由整段重寫。
**誘餌從「無中生有」變成「就在眼前」，這題反而更有價值。**

⚠️ 這支**看不到「守衛該開火卻沒開火」**（那是模型沒守規矩，不是 GT 過期）。
唯一會出聲的地方是 `eval_score.py` 單獨印的防禦題那一行 ——
**這是誠實性指標，不該被平均進正確率裡稀釋掉。**

### 6.3 標準流程

```bash
python tools/check_schema_pipeline.py         # 八項閘門，紅了不要往下走
python tools/check_derived_consistency.py     # 閘門 [9]：縱向，衍生欄 vs 從母表現算
python tools/check_question_ambiguity.py      # 閘門 [10]：題目層的多來源歧義
python tools/check_predicate_collisions.py    # 閘門 [11]：橫向，兩張平行表的同義謂詞
python tools/check_table_retrievability.py    # 閘門 [12]：逐表，被需要時排第幾名
python eval/eval_gt_check.py                  # GT 自檢：SQL 跑得動、alt_sql 同值
python eval/test_runner.py                    # 完整 e2e（309 題）→ eval/results/
python eval/eval_score.py                     # 對帳 + 防禦題稽核 + 僥倖偵測
python eval/eval_stability.py --ids <上一行印出的錯題>
```

**前五支排在最前面，因為它們不打 LLM、幾秒跑完**，而它們擋下的正是
「跑完 309 題才發現量到的是 GT 或 DDL 的問題」。曾經因此浪費掉一整輪
（12 題全錯，起因是 `gen_ddl.py` 沒重跑）。

改檢索層時**先跑零 LLM 的三支**（`--k`、`eval_retrieval_variants.py`、
`eval_column_recall.py`），它們確定性、可重跑、幾分鐘出結果。

加表時額外的檢查：跑完 e2e 要逐一檢查防禦題有沒有被新 schema 作廢；
每加 10 張表，8 張要有題目（§7.3）。

**重建資料庫之後必跑的兩步**（順序不能反）：

```bash
python tools/fix_derived_consistency.py --apply   # 對齊（35 條：縱向 14 ＋ 橫向 21）
python tools/check_derived_consistency.py         # 閘門 [9]：忘了上一步這裡會紅
python tools/check_predicate_collisions.py        # 閘門 [11]：平行表的同義謂詞
```

`customer_profiles` 的 6 個衍生欄位**結構上無法**在 `init_db_ext` 裡算完 ——
它們的母表是 `init_db.py` 跑完之後才建的（§7.9）。所以對齊注定是獨立的一步，
而閘門 [9] 的 `DERIVED` 名單就是「忘了跑」的守門員：**不管有沒有題目引用，一律紅燈。**

閘門 [11] 排在這裡的理由不同：它守的不是「忘了跑對齊」，而是
**「兩張平行的表對同一件事各擲各的骰子」**。那種欄位沒有母表可以現算，
所以閘門 [9] 看不到它（§7.11）；而它一旦發生，題目就沒有唯一答案了。

**加表或改 schema 之後，另外跑一次探測器**（不是閘門，不擋任何東西）：

```bash
python tools/check_synonym_columns.py             # 全庫掃同義欄位，人來看清單
```

它不問句驅動也不限謂詞，補的正是閘門 [11] 的兩個洞：沒人問過的配對、
數值與字串欄位（§7.12）。誤報率高是**刻意的** —— 「值不一樣」不等於缺陷，
判準是「有沒有哪個業務問題因此有兩個答案」，那一步只能人判。
看它的輸出時先看第一行的「SQL 失敗 N 組」：**N 不是 0 就不算掃過**。

---

## 7. 規模：93 張表的天花板在哪裡

> ⚠️ **§7.1~§7.6 的數字是 86 張表 / 275 題那一輪量的**，逐節標明；
> 93 張表的量測在 §7.8（dense 層代價）與 §7.9（衍生量矛盾）。
> §0.1 已經證實兩者的差距是「被擠出候選 0 題、平均位移 +0.17 名」。

### 7.1 這個規模的漏斗長什麼樣

```
93 張表  →  ① 相似度砍到 40 張（候選涵蓋 43%）
         →  ② LLM 選表，錨點召回 95.7%（93 張表 / 305 題，§2.7f 的 A 臂，單輪）
         →  ③ KMB 補橋，+KMB 97.4%；平均帶進 2.02 張表 / 1,295 字元（80 張表量的）
```

⚠️ **那個 95.7% 是單輪，不能跟 86 張表的 98.0% 直接相減** —— §5.2 量到同設定
重跑的區間是 93.4~96.1%（n=10），兩個數字整個泡在雜訊裡。漏斗的判準是
逐題配對 + 符號檢定，不是總分。

第一段**今天真的在執行** —— 每題砍掉 53 張，而且砍掉的那些後面兩段救不回來。
這是整個漏斗唯一的硬天花板（§7.2）。

一個值得記的規模性質：**schema 從 41 張長到 86 張的過程中，
每題進 prompt 的表數一直是 2.0 張。** 資料庫翻倍，送進 prompt 的內容沒有變多 ——
這正是這一層要買的東西，而它只有在表夠多的時候才量得出來。

**寬表是另一個維度，這一層擋不住。** 六張寬表讓表數 +7.5%，
表級召回幾乎沒動（98.9% vs 99.2%）；但同一批題目讓**欄位**召回從 100% 掉到 98.0%
（§2.6）。**表變多與表變寬是兩個不同的壓力，只有前者被漏斗處理。**

### 7.2 候選天花板：86 張表 / 275 題損失 3 題（93 張表未重跑）

```
錨點召回 @40 = 98.9%      ← 3 題的必要表落在 dense 第 41 名之外（275 題）
+KMB     @40 = 100.0%     ← KMB 全數補回
```

**這 3 題是整個漏斗的召回上限，後面兩段再準都救不回來，而且掉了不會報錯。**

好消息是**需要的深度成長得比表數慢**：80 張表 / 253 題是 99.2%，
86 張表 / 275 題是 98.9% —— 表多了 6 張、題多了 22 題，天花板只掉 0.3pp。
K=20（涵蓋 23%）就有 97.1% / 99.3%，K=40（涵蓋 47%）才 98.9% / 100.0%，
**後半段的候選深度買到的是最後那 1.8pp。**

`CANDIDATE_N` 一度被改成隨表數走的函數（`max(40, 0.8 × 表數)`），**改回固定 40 了**：

> 這一段漏斗被建立的目的，就是「不管資料庫長到多大，送進 LLM 的表數有上限」。
> `0.8 × 表數` 沒有上限，它只是換一個比較慢的斜率把同一個問題往後推。
> 而且它把「dense 召回不夠」偽裝成「候選開大一點就好」—— 相似度在原理上看不到
> 結構必要性（§2.3），**開大候選買到的是雜訊，不是召回**。
> 用一個沒驗證的字元預算去保護另一個沒驗證的公式，不如留一個有驗證的常數。

護欄 `_warn_if_candidate_n_binds()` 每次載入表註解都會響（因為 93 > 40），
只報事實：每題砍掉 53 張、涵蓋 43%、候選目錄約 2,880 字元（訊息是動態算的）。
**守門員是召回那個數字，不是這個常數；護欄的價值不在修好天花板效應，
在於讓一件無聲的事出聲。**

#### 先確認一件事：KMB 救不了，這兩題非得靠錨點

`products` 在這兩題裡是**葉節點不是橋** —— 它掛在 `order_items` 旁邊，
不在任何兩個錨點之間的路徑上。Steiner tree 只收終端點**之間**的節點，
所以實測三組錨點都補不進來：

```
KMB(['orders','order_items'])                        → products 沒進來
KMB(['orders','order_items','customers'])            → products 沒進來
KMB(['order_items','orders','customers','profiles']) → products 沒進來
```

**中介表可以外包給 KMB，問題主體不行。** 這跟 §2.3 動態門檻失敗的理由是同一條：
r=0.85 漏最多的是 customers 與 products，那是問題主體，KMB 設計上就補不了。

#### 這 2 題有兩個量過的解，樞紐表保留席勝出

漏掉的是 `#21` 與 `#40`，**兩題漏的都是 `products`** —— 而 products 的 FK 度數是
全庫第二高（19，僅次於 customers 的 21）。它不是冷門表，是被相似度壓下去的樞紐表（§7.5）。

| 解法 | 候選@40 | 代價 |
|---|---|---|
| 現況 | 99.2% | — |
| **保留 2 席給 FK 度數最高的表**（customers、products） | **100.0%** | 40 個名額裡的 2 個，**Top-1 零損傷** |
| C 值檢索 β=0.05（§2.7） | **100.0%** | 要多維護一個 733 值的索引；Top-1 +1.2pp |

保留席的做法是：**FK 度數最高的 R 張表無條件進候選、不參與排序競爭**，
其餘 40−R 個名額照相似度排。

**但 R 不是愈大愈好，掃出來的曲線是非單調的：**

```
R=0（現況）  99.2%   漏 [21, 40]
R=1          98.8%   漏 [2, 21, 40]   ← 比不做還差
R=2         100.0%   漏 []
R=3 / R=4   100.0%   漏 []
```

R=1 只保留 customers，卻**擠掉了第 40 名**，而 `#2` 需要的正是那一張 ——
**保留席不是免費的，它花的是名單最後一格。** 要嘛保到夠（R=2 把真正會漏的
products 也放進來），要嘛不要開始。這個非單調正好是「不要憑感覺挑常數」
（§2.3）的又一個實例：R=1 看起來是最保守的一步，實測是最差的一步。

**這是目前成本最低的解**：確定性、零 LLM、零嵌入、不影響任何既有指標，
而且它修的是 §7.5 診斷出來的**機制**（樞紐表在「沒被提到」時被需要），
不是那兩題本身。

**今天仍然不做**，理由只有一個：損失 2 題，這個改動在 e2e 上量不出效果（§5.2）。
**觸發條件：必要表落在候選外的題數 ≥ 5，或候選@40 掉到 98% 以下。**
重測是零成本的 —— `eval_retrieval.py --k` 看候選@40，
`eval_retrieval_variants.py --only C` 看 C 那條。

### 7.3 「干擾表」這個詞是錯的

93 張表裡有 11 張宣告 `never_answered: true`，其餘 82 張都有 GT 覆蓋（實測 0 張漏）。
這個比例是刻意的 —— 曾經有 46% 的表在測試集裡一次都沒當過正確答案，
而那低估了問題：

> **一個學會「這些表永遠別選」的檢索器，在那樣的測試集上會拿滿分。**

看一眼清單就知道沒有一張是「沒用的表」：`invoices` 是財務每月要對的帳、
`support_tickets` 是客服日報、`order_returns` 是營運看的退貨率。
**是題目集把它們釘死在干擾的角色上。**

同一張 `payment_attempts`：問「上個月成功付款金額」它是干擾，
問「哪些訂單刷卡失敗過」它是唯一正解而 `payments` 變成干擾。

> **干擾不是表的屬性，是「表 × 題目」的二元關係。**

這句話反覆咬人：稽核工具兩次把干擾實作成「表的屬性」——
第一次把補過題的表誤判成高風險（命中 12 → 修一行變 2），
第二次因為「所有 GT 都沒用到的表」這個定義而**直接跳過 `coupons`，一題都不比**，
於是漏掉了 `#72` 那個真實歧義。

**兩種表都要，比例約 8:2**（真實企業的樣子）：

| 類型 | 例子 | 測什麼 |
|---|---|---|
| 業務表（會被問到，82 張） | `invoices`、`support_tickets` | 在 93 張表裡撈出一張**長得跟鄰居很像的正解表** |
| 基礎設施表（永不作答，11 張） | `schema_migrations`、ETL 暫存表 | 稀釋律下，**永不相關的表會不會把正解擠掉** |

擴表規則因此是：**每加 10 張，8 張要有題目**（閘門第 6 項強制）。
2026-08-21 的六張寬表全部配題（22 題），而**配題才是這次擴充的實質內容**：
六張表建完但 GT 還沒補的那段時間，`eval_column_recall` 照樣回 100% ——
表加了、欄位加了、量測完全沒動，因為**沒有題目會去要那些欄位**。
補完 22 題之後同一支程式立刻掉到 95.2%（§2.6）。**加表不配題，等於沒加。**
刻意留作永久干擾的表在 `tools/distractor_seed_plan.yaml` 標 `never_answered: true`，
免得未來把它當成覆蓋缺口又補一次。

加表造成的真實歧義要靠**改問句**消歧，不是改模型：
`#72`「使用了折扣碼」模型走 `coupons`（券碼）、GT 走 `order_promotions`（檔期）——
兩個都說得通，改成「套用了**促銷活動**的折扣」之後 8/8。
**模型比我更貼字面**，那不是模型的錯。

### 7.4 表必須有資料，五條可檢查的準則

全庫今天**空表 0 張**（閘門第 7 項）。零列的表有兩個壞處：

**① 必然的假通過。** `expect: empty` 的題目「回 0 列就算對」，而**任何對零列表的
查詢都回 0 列**。這比 `#62` 更嚴重 —— `#62` 需要「兩欄剛好相等」的巧合，
這一條不需要任何巧合。

**② 對整類方法瞎掉。** 熱度先驗、值檢索這些「用資料當訊號」的方法在全空的表上
會**假性滿分**。這不是推論 —— 灌完資料之後方案 A 的表面收益從 +5.1pp 砍到 +1.9pp，
**在同一份程式碼上用開關驗證的**（而今天 80 張表上它已經是負的，§2.7）。

`tools/seed_distractor_data.py` 每條準則都有對應的 assert：

1. **列數比例合理，且不能一律偏小。** `payment_attempts`(239) 必須多於 `payments`(164)
   —— 每筆付款至少一次嘗試。干擾表一律小的話，**列數本身就變成免費的答案洩漏**。
2. **值域與正解表重疊**（否則值檢索一秒分辨得出來，量到的有效性是假的）。
3. **參照完整性**（FK 已建，程式再檢一次好給出可讀的錯誤）。
4. **時序一致**（子事件不早於父事件）。
5. **查得出「像樣但錯」的答案**，而不是靜悄悄的空集合。

**六張寬表（2026-08-21）又補了四條**，四條都是被實際擋下來才寫進 `assert_criteria()` 的：

**① 小表不能靠機率。** 三個各 p≈0.5 的服務旗標獨立抽，8 家門市的期望值是 1.0 ——
實際抽到 **0**；供應商暫停往來 p=0.3 × 8 家期望 2.4，也抽到 **0**。
維度表本來就只有 8~10 列，**題目要問的條件必須用保證式配置寫進去**，
在寫入前調整已生成的列（不動亂數序列）。

**② 非空不等於有鑑別力。** 舊判準是「不是空集合、不是全集」。
第一版通過了，但「維修門市 ∧ 有全遠端員工」= 6 = **全部**維修門市 ——
形式上是真子集，實際上另一半條件對答案毫無影響，模型只查一張表也全對。
現在的判準是：**交集要同時小於兩邊各自的大小**，而且單一條件涵蓋率**上限 75%**
（8 家供應商有 7 家準時率不達標時，那個條件已經退化成「幾乎全部」）。

**③ 一致性要對齊母表狀態，不只自己表內的時序。**
`resigned_at` 各擲各的骰子，生出 2 筆「`employees.is_active=1` 卻有離職日」的資料，
而當時的檢查只驗「離職不早於到職」，完全通過。
**跨表的狀態欄位要由母表決定，不能各自隨機。**

**④ 歧義從資料端消掉，比事後補 `alt_sql` 乾淨。**
有一家門市 `stores.is_active=0`。它若落進答案，模型多寫一個 `is_active = 1`
就與 GT 對不起來 —— 那是題目的歧義不是模型的錯。
配置時直接只挑營業中的門市，這個歧義就不存在了。

順序必須是「**先宣告值域，再稽核，最後才灌**」—— 判歧義需要知道值域，
而值是灌的時候才決定的（雞生蛋）。所以值域先宣告成資料（`distractor_seed_plan.yaml`），
稽核過的才准灌，灌的必須跟稽核的一致。

歧義稽核在 80 張表逐題比對會爆出 **869 個命中 / 115 題**，完全不可用 ——
`status` / `name` / `quantity` 在 80 張表裡到處都是，「欄位可移植」退化成雜訊。
救援來自 19 筆手工裁決，**理由幾乎逐字相同**（「`warehouse_transfers` 沒有
`order_id`，接不到訂單這個主體」）。同一條推理寫了 19 次，那就該寫進程式：
加**連通性**（干擾表要在 FK 圖 1 跳內接得到本題正解表）＋
**只共用一個欄位降級 LOW** → **869 個 / 115 題 → 12 個 / 10 題**。

（第一版只比欄位名，跑出 26 個 HIGH 幾乎全是誤報 —— `customer_id`/`created_at`
在任何正規化 schema 裡到處都是，**拿它們判歧義等於宣告「所有子表都可以互換」，
那不是歧義，那是正規化**。真正的歧義需要語意可替代，而語意不在欄位名裡，**在值裡**。）

誠實的殘留：**同概念、不同欄位名**這一類抓不到（`return_shipments.received_at`
vs `status='RECEIVED'`、`payments.status` vs `payment_attempts.result`）。
字串比對看不見它，補法仍然是把正解表的註解寫到涵蓋該概念，然後實測。

### 7.5 dense 的盲點集中在四張樞紐表

每張表「被需要時的相似度排名」（253 題、80 張表）：

| 表 | 被需要幾次 | 中位排名 | **最差排名** |
|---|---|---|---|
| orders | 68 | 2 | **35** |
| customers | 58 | 2 | 11 |
| products | 57 | 4 | **52** |
| order_items | 33 | 2 | **28** |
| customer_profiles | 16 | 1 | 2 |
| **其餘 63 張表** | 162 | **1** | **11** |

**63 張罕用表最差排名 11，從來不是問題。全部的深層漏失風險集中在四張樞紐表** ——
而 `products` 的最差排名 52 **已經掉出候選（40）之外**，那正是 §7.2 那 2 題的來源。

原因是 §2.3：**相似度看得到主題，看不到結構必要性。**
問「各類別的營收」時 `order_items` 一個字都沒出現在問題裡。
**愈是樞紐的表，愈常在「沒被提到」時被需要** —— 而且它們被需要的次數最多
（前四張合計 216 次，佔全部需求的 57%）。

FK 度數證實了這個診斷 —— 度數最高的三張正是最常被需要的三張：

```
customers 21   products 19   orders 18   employees 6   其餘 76 張 ≤ 3
```

**度數與「被需要次數」幾乎完全對齊，而相似度排名不對齊。**
這就是保留席的依據：結構訊號知道哪些表是樞紐，語意訊號不知道。

**實測 R=2 就把候選@40 補到 100%**（§7.2）—— 那是這個洞今天最便宜的解，
只是還沒到值得動手的門檻。C（值檢索）是同一個洞的另一種補法：
**C 用資料訊號補，保留席用結構訊號補**，兩者都量到 100%，保留席便宜得多。

### 7.6 兩層的退化通道不同，要盯兩個數字

| | dense 層 | LLM 選表層 |
|---|---|---|
| 退化長什麼樣 | 需要的表排名往下掉 | **近義表把正確的表擠掉** |
| 隨規模怎麼漲 | 深度需求成長得比表數慢 | 近義表數量增加 → 咬的次數增加 |
| 用什麼吸收 | 加大 `CANDIDATE_N` | **加大候選只會讓近義表更多** |
| 今天漏幾題 | **1 題**（`#40`，products 排名 43） | **3 題**（`#62` products 排名 13；`#263` order_profiles 排名 10、8/8 都沒選；`#264`/`#279` 各是硬幣） |
| 量它的工具 | `--k`（零 LLM，可隨便跑） | `--funnel`（275 次 LLM 呼叫，會存 picks 供離線重算） |

**86 張表之後這張表的重點變了：dense 層還是漏 1 題，LLM 選表層漏 3 題。**
新增的三題全部是「寬表欄位 vs 既有明細表」的近義競爭（§9），
而它們的正解表**都在候選裡**（`#263` 排名 10）—— 加大候選一題都救不到。
**擴表壓在哪一層，這張表現在有答案了：壓在 LLM 選表層。**

以下是 80 張表那一輪的診斷，機制今天仍然成立：
**當時兩層各漏一題，漏的是同一張表 `products` —— 但病因完全不同。**
把「是不是表註解沒寫好」這個假設拿去證偽，量出三件事：

**① 「商品」這個概念被 26 張表分掉了。** 表名含 `product` 或註解含「商品」的
表有 26 張（product_specs、product_images、product_qna、product_variants…）。
**稀釋定律的第三種形式**：不是一份文件太長、也不是所有文件都太長，
而是**同一個概念散在太多張表上**，於是沒有一張特別像。

**② 改註解對兩題的效果差很多**（同一組問題向量，頭對頭）：

| products 的表註解 | `#40` 排名 | `#62` 排名 | 全域 Top-1 | 誤入無關題前 3 |
|---|---|---|---|---|
| 現況（65 字元） | 43 | 13 | 84.6% | 6 |
| ＋「問句點名具體商品時來這張表」 | 39 | **7** | 85.4% | 10 |
| 重寫聚焦（108 字元） | **19** | **4** | 84.6% | 11 |

`#62` 有救（13 → 4），`#40` 怎麼寫都救不起來（最好只到 19）。
而且代價是實的：誤入無關題前 3 從 6 漲到 11（稀釋鏡像，§2.5）。

**③ `#40` 不是註解問題，是問句裡根本沒有「商品」這個詞。**

```
#40「找出買過『iPhone 15』但沒有買過『AirPods』的客戶」
#21「從來沒有買過『原子習慣』這本書的客戶有誰？」
```

問句給的是商品的**名字**，不是「商品」這個概念。嵌入不知道 iPhone 15 是商品 ——
但**資料庫知道**，因為那就是 `products.name` 裡的一個值。值檢索（§2.7 的 C）
天生就是解這個的：

| | `#21` products 排名 | `#40` products 排名 | 值命中 |
|---|---|---|---|
| baseline | 52 | 43 | — |
| C β=0.05 | 31 | **4** | products ×2 |
| C β=0.1 | **10** | **1** | |

**`#62` 則完全不受影響（13 → 13），因為它一個值都沒命中** ——
「單價超過 30000」在資料庫裡不是一個值。

#### 三種病因，三種解，不要混為一談

| 題 | 層 | 病因 | 有效的解 | 無效的解 |
|---|---|---|---|---|
| `#21` `#40` | 候選層 | 問句點名**實體的值**，不含概念詞 | **值檢索**（排名 43→4） | 改註解（最多到 19）、KMB（葉節點） |
| `#62` | LLM 層 | **問句本身有歧義**：「單價」＝售價還是成交價 | **改問句消歧**（§7.3 的判例） | 值檢索（無值可命中） |

`#62` 是 §5.3 那個僥倖的原型：模型用 `order_items.unit_price`（成交價）、
GT 用 `products.price`（售價），兩者語意不同而種子資料裡永遠相等。
它跟 `#72`「使用了折扣碼」是同一類 —— **模型比我更貼字面，那不是模型的錯**，
正解是把問句改成「售價超過 30000 元」。

**這也把 C 的觸發條件從「漏幾題」換成「題型」**：值檢索的價值隨
「問句點名具體實體（商品名、書名、券碼）」這類題的比例線性成長，
不是隨漏題數成長。擴表配題時如果這類題變多，C 要重新評估。

> 三段漏斗把問題從「會不會被截斷」轉成「近義的東西分不分得開」。
> 那是一筆划算但**不免費**的交易。

### 7.7 擴表時不會壞的東西

- **評估框架**：一般題的 GT 由 sqlglot 從 GT SQL 解出，加表不會讓它失效；
  而且加表會讓題目變難 —— **既有題目正好變成規模的回歸測試**。
  ⚠️ **防禦題例外**（§6.2）。
- **既有資料**：只要守「只 CREATE + INSERT、專屬亂數種子」，既有表一個位元都不會動。
  2026-08-21 加六張表灌 290 列，**80 張表的逐表 SHA 指紋全部一致** ——
  這件事要用指紋證明，不能用「我沒有動它」保證（`add_wide_tables.py` 建表前後各取一次）。
- **向量快取**：key 是文件雜湊，新表只算新表；今天整份 **5.44 MB / 205 筆**
  （2048 維）。**93 筆是現行表註解，其餘 112 筆是歷次實驗的文件變體** ——
  雜湊當 key 的代價就是舊變體不會被清掉，這是刻意的（重跑任何一個被否決的
  臂都不用重新付嵌入成本）。
- **KMB**：表變多它反而更有用（§3）。這是唯一一條「加表之後變好」的。
- **檢索延遲**：80 個向量實測 36 ms。純 Python 到 1,000 張表約 450 ms 都還能用，
  換 numpy 一步就解決（約 2 ms），而那能撐到萬張表。

---

### 7.8 第二批擴表：刻意「語意重疊」，86 → 93 張表（2026-08-25）

第一批六張表各自掛在一個**還沒有寬表**的主體上，彼此不搶題。這批反過來：
每一張都掛在**已經有人在管那個概念**的主體上。

| 新表 | 跟誰搶同一個概念 |
|---|---|
| `payment_profiles` | `order_profiles` 的「結帳與付款情境」—— 同一個概念的兩張寬表 |
| `subscription_profiles` | `customer_profiles` 的「行銷同意設定」—— `#152` 的形狀 |
| `support_ticket_profiles` | `order_profiles` 的「客服接觸」、`order_notes` |
| `shipment_profiles` | `product_profiles` 的易碎／冷鏈（同名，不同層級） |
| `review_profiles` | `reviews`、`faq_votes`（有幫助票數同名） |
| `promotion_profiles` | `coupons`／`order_promotions`／`campaign_profiles` |
| `return_profiles` | `refunds`／`warranty_claims`／`product_profiles` 的可退天數 |

**為什麼一定要重疊**：語意獨立的表永遠不會進候選前段，加了只會讓分母變大
而漏題數不變 —— **分數上升、能力沒動**。那是 §7.4「干擾表全是零列時
熱度先驗假性滿分」的同一個病。

#### 量到的：重疊兌現了，但 dense 層完全沒被打痛

253 題舊題，零 LLM，同一組文件、同一個嵌入：

| | 86 張表 | 93 張表 |
|---|---|---|
| 候選@40 | 98.8% | **98.8%** |
| Top-1 | 48.2% | **46.6%** |
| 正解表最差排名 中位／p90／最大 | 2 / 11 / 57 | **2 / 12 / 62** |
| 排名變差／變好 | — | **30 題 / 0 題** |
| 被擠出候選 | — | **0 題** |

新表**確實在搶席次** —— 它們佔掉全部候選名額的 **8.8%**，
`support_ticket_profiles` 出現在 **69.6%** 舊題的 top-40 裡。設計沒有落空。

但**一題都沒被擠出去**，平均只位移 +0.17 名。原因很單純：

> **正解表的最差排名中位數是 2，離第 40 名還有 38 名餘裕。**
> 候選層不是被「表數」壓垮的，是被「正解表排名」壓垮的 ——
> 而那個排名在絕大多數題目上離天花板遠得很。

擴表前我用餘裕分析算過一個上界：「再加 14 張同等競爭力的表，最壞情況下
6 題會被擠出候選」。**上界是對的，但鬆到沒有用** —— 它假設新表全部排在
正解前面，實際上七張只讓平均位移了 0.17 名。

**推論：擴到 100 張在 dense 層幾乎不用付代價。真正的壓力在 LLM 選表層。**
這與 §7.6 早就記下的結論一致，現在有了第二組獨立證據。

#### 建表時被準則擋了三次，每一次都是真問題

**① 母表的值域用猜的。** `order_returns.status` 實際只有
`RECEIVED / REJECTED / REQUESTED`，我寫的是 `COMPLETED / APPROVED / DONE`，
結果 18 筆全部沒有檢驗紀錄。改成從母表推導：`REJECTED` 就是
「驗過但不符合退貨條件」，反而讓準則 4b 有東西可檢查。

**② 把三態當成兩態。** `subscriptions.status` 是
`ACTIVE 6 / CANCELLED 6 / PAUSED 5`，我把非 ACTIVE 全當成「已取消」，
等於**自己另外發明一組暫停名單** —— 母表明明就有 `PAUSED`，
而我的 `is_paused` 跟它無關。這種資料不會報錯，只會讓
「目前暫停中的訂閱」這一題的答案與母表對不起來。

**③ two_wide 題退化成單表題。** 只給易碎商品貼標籤的話，
「貼標籤」是「易碎」的**子集**，模型只查 `has_fragile_label = 1`
就得到一模一樣的答案 —— 那題根本不需要 join `product_profiles`。
補上「非易碎但也貼了標籤」的實務情形（包裝人員本來就會多貼），
兩邊才互不蘊含：易碎 73、貼標籤 63、交集 51。

> **非空不是判準，「這題真的需要兩張表嗎」才是。**
> 四題雙寬表全部用「單表捷徑會不會給出不同答案」驗過：
> `#284` 73 vs 63、`#293` 68 vs 115、`#305` 5 vs 2、
> `#297` 值域完全不相交（`SILVER/BRONZE/PLATINUM/GOLD`
> vs 受理當下的中文快照 `金卡會員/白金會員/銀卡會員`）。

#### e2e 落點：298/309，而失敗全部集中在同一層

單輪 **298/309 = 96.4%**，防禦題 4/4（語意禁令第 [3] 條的「發明運費公式」風險
沒有兌現）。11 題不符跑 `eval_stability --n 8`：

```
穩定過 1  #51
擲硬幣 5  #184 1/8、#249 7/8、#282 1/8、#286 3/8、#305 1/8
穩定錯 5  #279 #284 #287 #292 #308
```

新題的 5 題失敗**全部**是選表層挑了字面更像的舊窄表（表格見 §0）。
dense 層 0 題被擠出、選表層 5 題全滅 —— 兩層的代價差距不是程度差異，是**全有全無**。

#### 兩題是我自己的 GT 不夠好，先修 GT 再談分數

**`#305` 有僥倖漏洞，而且不是「條件」的鑑別力問題。**
符合條件的兩檔活動門檻完全相同（500/200）、流量來源集合也完全相同，
所以原 GT 那 5 列，**模型只查到其中一檔也會拿到一模一樣的答案而過關**。

> 配題準則管的是**條件**的鑑別力（交集小於兩邊、涵蓋率 ≤ 75%）。
> 這題壞在**輸出欄位**沒有鑑別力 —— 兩個不同實體投影到同一組值。
> **兩者要分開檢查。** `assert_criteria()` 今天只檢查前者。

加上活動名稱後 5 → 10 列，實測少查一檔就掉到 5 列，一定被抓到。
第二次改寫再修掉形狀歧義（8 次裡有 4 次用 `GROUP_CONCAT` 把來源併成一列），
加問「各有幾張」用逐格指標把粒度釘死。

**`#284` 的改寫則是白費工。** 原本以為是粒度歧義（模型以每項商品為粒度、
GT 以每筆出貨），改寫後變成 0/8 —— 但原因不在問句：模型把「標成易碎品」讀成
`product_tags`（行銷自由標籤），資料裡沒有「易碎」這個標籤，於是回 0 列。
易碎是物理屬性不是行銷標籤，**這個讀法產出空集合，是錯的讀法，不是另一種合法讀法**。
所以它歸到誘餌族，不再改問句。

> **判準：問句歧義才改問句，模型挑錯表不改問句。**
> 前者是「兩種讀法都答得出東西」，後者是「一種讀法回空集合」。
> 分不清就會一路把題目改到模型剛好會答為止 —— 那是 teaching to the test。

**2026-08-26 補上第三支（閘門 [10] 量出來的）**：兩種讀法都答得出東西，
但 **schema 自己已經裁決了哪一個算數**（欄位註解寫明「快照／衍生／與即時值可能不同」）
—— 這也**不改問句**。改了會把刻意的陷阱一起毀掉。

```
兩讀法都有答案 + schema 沒說哪個算數  → 問句歧義，改問句或補 alt_sql   （#279 #287）
兩讀法都有答案 + schema 明說了哪個算數 → 模型沒讀註解，不改問句         （#94 #100 那 11 題）
一種讀法回空集合                      → 模型挑錯表，不改問句           （#284）
```

#### 順帶修掉的兩個自傷

`guarantee()` 的輸出原本是硬寫的字串「檢驗不符 4、物流商責任 3」——
那是**宣稱，不是量測**，而實際值是 0。改成逐項實測。
§8 ④「對照組與檢查工具本身也要驗」適用於自己寫的保證式配置。

`return_profiles.compensation_note` 寫死「已補償折價券」，
但 `compensation_type` 有 `POINTS` / `GIFT_CARD` —— 文字與代碼打架。
人看得出來，模型只會照抄。

### 7.9 五題穩定錯裡有三題不是選表問題（2026-08-25）

前七輪都把 `#279 #284 #287 #292 #308` 當成同一族「模型挑了字面更像的窄表」，
方向是往註解上加東西。路線 ② 的三臂探針（93 張表全標概念群、8 票）量到的是：

```
誘餌 9 題   A 23/72   C 26/72   D 32/72      目錄 3,382 → 5,341 → 6,128 字元
對照 6 題   A 46/48   C 48/48   D 48/48
D vs A：變好 [258 264 282 292 152]、退步 [284 308]
```

D 最好，但**淨賺 9 票、打死 2 題、目錄 +81%**，而且 `#279 #287` 三臂全是 0/8。
「加文字」加到目錄快兩倍還打不動的題，值得懷疑的是**題目本身**，不是模型。

#### 掃描：profile 欄位與母表算出來的值對得上嗎

30 項純 SQL 檢查（`tools/check_derived_consistency.py`）。**先報兩個自傷** ——
§8 ④「對照組與檢查工具本身也要驗」在同一支腳本上中了兩次：

| 我以為的缺陷 | 真相 |
|---|---|
| `customer_profiles.order_count/total_spent` 23/50 | **刻意的**。`seed_customer_profiles` docstring 寫明快照落後 30 天、「實測 27 位對不上」，掃描量到的正是 27 |
| `return_profiles.total_days` 0/10 | **檢查式錯了**兩次。先寫成 `transit+approval`（建表是 `completed−requested`），改對之後仍 9/10 —— 建表用 Python `timedelta.days`（滿 24 小時），我寫 `DATEDIFF`（日曆天）。統一成 `TIMESTAMPDIFF(DAY,…)` 後 10/10 |

扣掉這兩項，真正的矛盾長這樣 —— 全是 `R.randint(...)` 冒充衍生量：

```
review_profiles.days_after_delivery      1/97     _pick([1,2,…,45])
promotion_profiles.used_count            0/8      randint(3,80)  合計 359 vs 實際 41
product_profiles.image_count             3/40     randint(1,12)  合計 236 vs 實際 123
shipment_profiles.attempt_count         58/143    合計 294 vs delivery_attempts 193
return_profiles.is_over_policy_window   13/18     常數 0 再由 guarantee() 硬寫兩列為 1
```

#### 危險的不是欄位不一致，是**篩選條件在母表也算得出來而且答案不同**

`#261`（`content_completeness<60`）、`#274`、`#280`（`exception_code<>'NONE'`）、
`#303`（`is_budget_exhausted=1`）的篩選條件母表都沒有，模型沒有第二條路可走 —— **不痛**。
真正壞掉的是這三題：

| 題 | 走 profile | 走母表 | 結果 |
|---|---|---|---|
| `#279` 點擊率最高 | 第 3 檔 | `COUNT(campaign_clicks)` → 第 6 檔 | **不同活動** |
| `#287` 到貨超過 30 天 | 36 列 | `DATEDIFF` → 19 列（取最早則 22） | **不同列數** |
| `#308` 超期退貨 | 2 列 | 日期推導 → 4 列 | **不同列數** |

`#279` 的 0/8 不是選錯表 —— 是**同一個問題有兩個都對的答案**，GT 只認一個。
剩下 `#282`（`is_redirected` 全庫只有一處）、`#284`（`product_tags` 空集合）
才是真正的字面吸引，也才是 C/D 打的靶。**九題誘餌裡有三題根本不在測選表。**

#### 這不是本專案獨有的坑

[AmbiQT](https://arxiv.org/abs/2310.13659)（EMNLP 2023）建 3000+ 題歧義 benchmark，
注入手法之一就是「**加入聚合欄位，與 GROUP BY 現算並存**」—— 也就是我不小心做的事。
差別在它有兩份 gold answer，我只有一份。
[CIDR '26](https://arxiv.org/abs/2601.08778) 量到 BIRD mini-dev **32%** 的題目帶標註錯誤
（金融領域 49%），四類錯誤裡的 E4 就是「T 本身有歧義」。

業界的解法分三層，而**第一選擇不是讓模型選對**：
① 資料端 —— 指定真相來源 + 排程對帳（dbt tests / dbt-expectations / Soda 的典型斷言是
「staging 的 revenue 總和必須等於來源，容差 0.01%」）；
② 語意端 —— semantic layer（Cube / dbt Semantic Layer / LookML）讓指標只定義一次；
③ 題目端 —— BIRD 的 per-question evidence 句。

#### 處置：路徑唯一就改資料，路徑不唯一就改問句

| 欄位 / 題 | 處置 | 為什麼 |
|---|---|---|
| `is_over_policy_window` `attempt_count` `image_count` `used_count` | **對齊資料** | 母表唯一決定（FK 一對一、`requested_at` 與母表 18/18 相同） |
| `#287` `days_after_delivery` | **收窄問句** | `reviews` 沒有 `order_id`：134 則裡 97 則連得上，其中 10 則連到 2~3 個不同到貨日。**母表算不出唯一答案，所以 profile 欄位就是真相來源** |
| `#279` `ctr_pct` | **收窄問句** | `clicks` 合計 134,911（廣告平台尺度）vs `campaign_clicks` 418 列（站內記錄）——**兩個量本來就不同**，對齊會毀掉語意 |
| `order_count` `total_spent` | **宣告不動** | 快照落後是刻意設計 |

`tools/fix_derived_consistency.py` 只下 `UPDATE`，不重跑 `init_db.py`。
實測：4 張表指紋變動、**其餘 89 張完全相同**、309 題 GT 全部可執行且無空集合、
兩支建表腳本的 `assert_criteria` 全數通過、`#308` 由 2 列變 4 列
（超期 7、已核准 10、交集 4，涵蓋率 22%，兩個條件都有作用）。

**建表腳本一起改了，否則重跑會洗掉修正**（memory「Schema 來源要接上產生鏈」的同族）。
刻意寫成 post-pass `derive_from_parents()` 而不是改 `gen_*` 裡那一行 ——
`R.randint(...)` 必須留在原地，**少抽一次整條亂數序列就平移，GT 全毀**。

#### 閘門第 [9] 項：衍生量一致性

> **冗餘可以，矛盾不行。**
> 兩張表記同一件事、**值相同** = 好的檢索測試（93 張表的設計目的）。
> 兩張表記同一件事、**值不同** = 壞掉的 benchmark，因為「對」沒有定義。

`tools/check_derived_consistency.py` 的燈號：
**紅**（exit 1）不一致 + 有 GT 引用 + 未宣告，**或該欄已有腳本負責卻對不上**；
**黃**不一致 + 無 GT 引用 + 未宣告（地雷，下次配題問到就變紅）；
**快**用快照截止點檢查（見下）；**綠**一致或已在 `DECLARED` 寫明理由。
今天是 **一致 29｜已宣告 2｜黃 0｜紅 0**。

這條界線同時回答了「反向指標算不算作弊」：
**讓模型選對表的提示是作弊，讓兩張表講同一句話是資料衛生。**

#### 9 個黃燈一次清完（2026-08-26）

上一輪把黃燈留著，理由是「今天沒有題目引用，不扣分」。那正是 §9 那條教訓
（`#261` 修完沒有人問還有幾個）的翻版 —— **名單就在手上，卻等著它們變紅。**
這次一次清完，而過程買到三件事：

**① 快照族：從「宣告的例外」升級成「可驗證的不變量」。**
`customer_profiles` 的 4 欄（`total_items_bought`／`return_count`／
`coupon_used_count`／`cart_abandon_count`）欄位註解本來就寫著「（快照）」，
值卻是 `rnd.randint(...)`。而 `order_count`／`total_spent` 實測**剛好等於**
anchor − 30 天的截止值（50/50 全中）—— 也就是說截止點一直是可驗證的，只是沒人驗，
它們被寫在 `DECLARED` 裡當成「不檢查」。

```
宣告  只能說「這兩個量本來就不同」          ← 分不開「刻意落後 30 天」與「根本沒在算」
檢查  能說「它等於它該等於的那個量」        ← 下次有人動到快照邏輯會當場報錯
```

所以那 4 欄對齊到**同一個截止點**，不是對齊到即時值 ——
**對齊到即時值會讓同一張表一半欄位是快照、一半是即時，那是更難發現的矛盾。**
`order_count`／`total_spent` 同時從 `DECLARED` 移出（已宣告 4 → 2）。

**② 落差本身也要有守門員。** 檢查保證「快照等於截止點的值」，但保證不了
「快照與即時值不同」—— 而那個差距**就是** `#146`／`#147`／`#148` 的鑑別力來源。
有人把快照對齊到即時值時，檢查照樣全綠而那三題靜默失效。
所以另外印一行：`order_count 與即時值不同 27/50 位｜total_spent 不同 27/50 位`，
**歸零就警告**。這與閘門 [10]「分歧名單變短本身就是警訊」是同一個設計。

**③ `DERIVED` 名單：有人負責的欄位不再是黃燈候選。**
黃燈的語意是「還沒有人負責這一欄」。這 14 欄現在由
`fix_derived_consistency.py` 與 `derive_from_parents()` 負責，
它們對不上只有一個意思：**那一步沒有跑**。所以改成**不管有沒有題目引用，一律紅燈**。

> 這是 §8 ① 那個形狀（寫了沒接到產生鏈）第一次**事前**被堵住，而不是事後補閘門。
> `customer_profiles` 的 6 欄結構上**無法**在 `seed_customer_profiles` 裡算 ——
> 它們的母表（`customer_login_logs`、`coupon_redemptions`）是 `init_db.py`
> 跑完之後才由 `add_distractor_tables.py` 建的，那個函式執行時根本還不存在。
> 所以對齊注定是獨立的一步，而獨立的一步注定會有人忘記跑。

**驗過它會響**（§6.1 的規矩）：把一列 `cart_abandon_count` 加 1 —— 這一欄沒有任何
題目引用 —— 閘門從綠變紅、`exit 1`；跑 `fix_derived_consistency.py --apply` 後回綠。

清完的落點：

```
一致 17 → 29｜已宣告 4 → 2｜黃燈 9 → 0｜紅燈 0
指紋變動 3 張（customer_profiles / review_profiles / support_ticket_profiles），其餘 90 張完全相同
309 題 GT 全部可執行；八項閘門全綠；閘門 [10] 分歧名單仍是 11 題（沒有被順手改掉）
兩支建表腳本的 assert_criteria 全數通過
```

順帶抓到的三個真實矛盾（都不是「數字不好看」，是**指著亂數說那是答案**）：

| 欄位 | 原本 | 打架的對象 |
|---|---|---|
| `last_login_at` | `anchor − randint(0,120)` 天，0/50 | `customer_login_logs` 的表註解自己寫著「最近一次登入時間在 `customer_profiles.last_login_at`」 |
| `has_merchant_reply` | `random()<0.4`（57 列） | `reply_count`（合計 123）與 `merchant_reply_at`（57 筆）各自獨立亂數，而 `review_replies` 只有 33 列 —— **三個欄位彼此都對不起來** |
| `reviewer_review_count` | `randint(1,9)` | 134 則只有 15 則對得上這位客戶真正寫過的則數 |

`last_login_at` 順帶消掉一個定義歧義：改成**只算成功的登入**（失敗的嘗試不是「登入過」），
並把這句話寫進欄位註解。50 位客戶都有成功紀錄，不會產生 NULL。
連帶重算 `last_active_at`，否則會出現「最近活動早於最近登入」這種自相矛盾的列。

### 7.10 多給表的代價長什麼樣：天花板弄丟的 14 題（2026-09-01）

> **2026-09-01 訂正。** 本節第一版的結論是「代價的形狀是**模型伸手去拿母表**」。
> **那個結論是錯的**，而且錯在最前面一步：它假設失手是選表選錯了。
> 把 14 題的模型 SQL 逐句拆開之後，事實正好相反 —— **檢索層一次都沒有失敗。**
> 底下是訂正後的版本，原本那段推論全部刪除。

`FILTER_VOTES=3` 上線時我們接受了 **+9.4% 的表數**（§2.7i），而當時對
「多給表會怎麼壞」只有一個總數（天花板 19 救 / 14 丟），**沒有任何一個實例**。
這一節把那 14 題拆開。零 API，全部從 `ceiling_ab.json` 讀
（A = 現行漏斗，B = 93 張表全送，各 2 輪 / 309 題）。

#### 第一件鐵證：模型的表集合是 GT 的**超集**

把每個失手實例的模型 SQL 與 GT SQL 各自解出表集合來比：

```
模型表集合 ⊇ GT（檢索一張都沒漏）   16 / 18 個失手實例
真的漏了 GT 要的表                    1        （#287 少了 reviews）
互有出入                              1        （#275）
```

**表全都在，模型也全都看到了。**「母表擠進候選害模型選錯」這個故事，
在資料面上不成立 —— 沒有「選錯」，只有「多用」。

#### 第二件鐵證：多出來的表 100% 只出現在 WHERE

| 題 | 多出來的表 | 出現在 SELECT | 出現在 WHERE |
|---|---|---|---|
| `#258` | `product_specs` | ✗ | ✓ `ps.is_discontinued = 1` |
| `#202` | `order_profiles` | ✗ | ✓ `op.delivery_preference = 'STORE'` |
| `#299` | `subscriptions` | ✗ | ✓ `s.status = 'ACTIVE'` |
| `#276` | `supplier_contracts` | ✗ | ✓ `EXISTS(... sc.ends_at ...)` |

**模型很清楚資料放在哪張表** —— 輸出欄位每一題都挑對了。它多做的是**加一個過濾條件**。

`#258` 最乾淨：

```sql
-- 模型（B 臂，兩輪都一樣）
SELECT p.name, pp.name_en, pp.delisted_at, pp.content_owner, pp.return_policy_days
FROM product_specs ps JOIN product_profiles pp ... JOIN products p ...
WHERE ps.is_discontinued = 1        -- ← 只有這一行與 GT 不同
```

輸出欄位與 GT **逐字相同**。

#### 真正的機制：平行表之間的「同義謂詞」衝突

問句要的是「已停產」。schema 裡有兩個欄位都宣稱自己是那件事：

```
product_specs.is_discontinued    COMMENT '是否已停產'          ← 逐字命中問句
product_profiles.lifecycle_stage COMMENT '…／EOL 停產'
```

而資料是這樣的：

```
product_specs.is_discontinued = 1        1 筆
product_profiles lifecycle_stage='EOL'  12 筆
交集                                     0 筆      ← 完全互斥
```

`is_discontinued` 是 `db/init_db_ext.py:648` 的 `1 if random.random() < 0.1 else 0`，
`lifecycle_stage` 在 `tools/add_wide_tables*.py` —— **兩支不同的腳本、各擲各的骰子、
誰也不知道對方存在。** 設計說「這兩張表講同一件事」（§7.8 刻意的語意重疊），
灌資料時沒有把那句話兌現。

**最硬的一條：benchmark 自己就在教模型走那條路。**

```
#127「已經停產的商品有哪些？」   GT → product_specs.is_discontinued = 1      1 個商品
#258「列出所有已停產商品的…」    GT → product_profiles.lifecycle_stage='EOL' 12 個商品
```

同一份 GT、同一個詞「已停產」、兩題的正解是**兩個互斥的集合**。
`#258` 上模型做的事，正是照 `#127` 的正解做的 ——
**它不是選錯表，是答對了另一題。**

`#202` 是同一個病的另一種樣子，而且模型是因為**多做一次交叉驗證**才錯的：

```sql
-- 模型：GT 的表、GT 的謂詞，再 AND 一個看起來像保險的條件
WHERE sp.picked_up_at IS NULL AND op.delivery_preference = 'STORE'   → 0
-- GT
WHERE picked_up_at IS NULL                                           → 2
```

```
orders 200 張｜pref='STORE' 58 張｜有取貨紀錄 46 張｜交集 13
獨立亂數的期望交集 = 58×46/200 = 13.3      實測 13      phi = -0.009
```

**phi ≈ 0。** 兩張記錄同一件事的表在統計上完全獨立。模型愈謹慎，錯得愈徹底。

#### 逐題訂正

| 題 | 第一版寫的（錯） | 實際上 |
|---|---|---|
| `#258` | 伸手拿母表 | 用**兄弟表**的同義謂詞欄位；兩者資料互斥（交集 0） |
| `#202` | 伸手拿母表 | 用對了 GT 的表與謂詞，**多加一個交叉驗證條件**；兩個編碼 phi = -0.009 |
| `#249` | 多用 `order_returns` | **INNER vs LEFT JOIN**；模型把「連物流單都還沒建的退貨」也算進去（8 vs GT 4）——「沒寄出的當然也還沒收到」，這個讀法可能比 GT 更對 |
| `#275` | 多用 `promotions` | `promotion_profiles` 有幾乎同名的欄位（`pause_reason` vs `paused_reason`），各 5 筆。廣告活動與促銷檔期是**兩個真實體**，問句的「哪幾**檔**」是促銷的量詞 |
| `#299` | 多用 `customers`／`subscriptions` | 用對了 `converted_from_trial = 1`，**多加 `s.status='ACTIVE'`**（問句有「現在」） |
| `#276` | 多用 `supplier_contracts` | 用 `supplier_contracts.ends_at` 當「停止往來」的第二種編碼 |

**6 題裡有 3 題（`#202`／`#299`／`#249`）模型用對了 GT 的表，
只是多加了一個條件。**「給模型更多關於表的資訊」對這 3 題結構上不可能有幫助 ——
**能看到的表愈多，可用來當額外 AND 的同義謂詞就愈多。**

#### 代價不在防禦題

```
防禦題（#68 #70 #140 #141）   A 8/8   B（93 張表全送）8/8
```

**連「整份 schema 直接送」都沒有讓守衛失守。** 這推翻了我一直以來的假設 ——
`e2e_union.py` 那道「防禦題一定要照臂給 votes」的護欄（§2.7i）依然該留，
但它守的不是最危險的地方。

#### 反直覺的那一題：表變多讓模型變得**更保守**

```
#274  列出每一檔活動的總預算、實際花費、花費進度、曝光數、點擊數與點擊率
      A 2/2 → B 1/2   失敗那次：誤觸防禦暗號 —— 這題用現有 schema 答得出來
```

所有人的直覺（包括我寫 §2.7i 護欄時的直覺）都是「表愈多，模型愈容易湊出
一個假答案 = 誤觸暗號」。**實際發生的是相反方向**：schema 大到一定程度，
模型反而**不敢答**它明明答得出來的題。

**而防禦題 4/4 這道閘門測不到這個方向** —— 它只查「該拒答的有沒有拒答」，
查不到「不該拒答的拒答了」。今天沒有任何指標在盯過度拒答。
（`SCHEMA_UNSUPPORTED` 的觸發次數是現成的代理指標，還沒接。）

#### 為什麼白名單、AST、EXPLAIN、retry 全部攔不到這 14 題

| 護欄 | 給什麼 | 為什麼這裡無效 |
|---|---|---|
| `context_retriever._other_tables_line()` | 沒被選中的**表名**（約 250 字元），每次都給 | 只回答「這個名字存在嗎」——`product_specs`／`orders` **全都存在** |
| `db_validator._augment_schema_error()` | 出現「找不到欄位/表」時補上完整欄位白名單 | 同上，而且根本沒觸發：SQL 是合法的 |
| `ast_validator` 表/欄位白名單 | 擋幻覺表名 | 沒有幻覺 |
| `graph.py` 的 retry | AST/EXPLAIN/執行失敗 → 重回 `sql_generator` | **這 14 題一次都沒觸發 retry** |

```
#276  系統 22 列 / GT 15 列       ← EXPLAIN 過、跑得出結果、只是答案錯
#299  系統  2 列 / GT  4 列
#202  系統 [[0]] / GT [[2]]
```

**整套 AST + EXPLAIN + 白名單 + retry 守的是「SQL 壞掉」，
而這裡的病是「SQL 很好、表也對，只是多了一個 AND」—— 它不經過任何一道閘門。**
白名單只能回答「這個名字存在嗎」，而**每一個被誤用的欄位都存在、註解也都是真的**。

> **所以「下游給 LLM 更好的資訊」對這一類結構上無效。**
> 缺的不是描述，是**兩個都為真的描述之間的優先權**。
> `is_discontinued`「是否已停產」與 `lifecycle_stage`「EOL 停產」兩句話都是真的；
> 要判定哪一句算數，需要的是 schema 裡**根本不存在的一個事實**：誰是真相來源。
> 這也是 §2.7f／§2.7j 欄位提示「同時救回三題又弄死三題」的原因 ——
> 提示機制沒有故障，餘弦替「已停產」挑出 `is_discontinued` 是**字面上正確**的，
> 挑對表時是解藥、挑到第二個編碼時是毒藥，**而它們用的是同一個分數**。

#### 對指標的影響

漏斗的價值有兩半，而我們一直只量了一半：**召回**（把漏掉的表補回來，
`--funnel` 量的就是這個）與**排除**（把可湊的近義表拿走，沒有任何指標在量）。
§2.7i 的帳看得到這筆錢：**漏斗層 9 題救回 0 題弄丟，e2e 只兌現 7 題。**

⚠️ 但要注意本節訂正之後，那兩題的損失機制**不是**「模型改用了別的表」，
而是「多出來的表提供了額外的 AND」。**漏斗指標會系統性高估聯集這類介入**
這個結論不變，理由換了。

#### 結構事實：選錯表沒有第二次機會

**retry 是重回 `sql_generator`，不是重回選表**（`graph.py:44`／`:63`／`:82`）。
表的集合一旦定了，整題就凍住了。

⚠️ 那能不能讓 retry 看到「選表選了哪幾張」並偏好它們？可以做，但那正是
§2.7g 那條軸：讓模型更聽話地用選中的表，同時讓它在選表漏了的時候更不敢
自己補回來 —— 而 `#24` 正是靠「自己補回來」救的（§2.9）。**先量再改。**
而且依本節的訂正，**這 14 題裡它幫不上忙**：表本來就沒選錯。

### 7.11 閘門第 [11] 項：橫向謂詞衝突（2026-09-01）

§7.10 找出的病 —— 兩張**平行**的表對同一件事各擲各的骰子 —— 現有的閘門
一個都看不到，而且是結構性的看不到：

| | 閘門 [9]（§7.9） | 閘門 [11] |
|---|---|---|
| 方向 | **縱向**：寬表衍生欄 vs 從母表現算 | **橫向**：兩張平行表各自獨立編碼 |
| 前提 | 存在「可現算的母表」 | `is_discontinued` 不衍生自任何東西 |
| 對 `#258` | 綠燈（跑幾次都綠） | 紅燈 |

閘門 [10]（`check_question_ambiguity.py`）方向對了 —— 問句驅動、實跑比對 ——
但它的覆蓋是**手寫的 14 條路，全部來自 `customer_profiles` 快照那一個家族**。

`tools/check_predicate_collisions.py`：**候選用問句驅動，判準用資料。**
純 SQL + regex，零 LLM，全部是 SELECT。

```
互補   交集 = 0 且 A + B = 母體   → A = NOT B，正常
包含   交集 = min(A, B)           → 小的是大的子集，正常
互斥   交集 = 0 且 A + B < 母體   → 這個詞指向兩個不相交的集合
獨立   |phi| < 0.25               → 兩顆互不相干的骰子
一致   其餘                       → 冗餘，正是 93 張表的設計目的
```

**踩到的三個坑，每一個都推翻了我原本的設計：**

1. **「交集 0」本身不是缺陷。** 第一版把 `employee_profiles.resigned_at`
   vs `employees.is_active`（phi = -1.000、A 2 + B 22 = 母體 24）判成紅燈。
   那兩個謂詞是**互補**的 —— 離職 = 非在職。補上互補與包含兩條放行規則。
2. **「表裡有沒有列」不主張任何事實。** `product_qna` 與 `support_tickets`
   都掛在 `customers` 底下、彼此無關，phi ≈ 0 是它們**應該**有的樣子。
   改成**結構上**分兩層：註解寫著「是否已停產」的**欄位**主張那個事實，自動判決；
   表存在性不主張，只列出不判決。**不是調門檻分的。**
3. **字串層調不準，而調它是本專案禁止的事。** 詞組「訂閱」會把
   `opt_out_marketing`（是否退出行銷訊息）和 `subscriptions.ended_at`
   （訂閱結束日）配成一對。原型版單靠字串在 309 題裡標了 **161 題（52%）**，
   與 `check_question_ambiguity.py` 餘弦版的失敗一模一樣。

第 3 點的處置沿用閘門 [10] 的 `EXPECTED_DIVERGENT`：**人工裁決一次，
閘門之後只守迴歸**，新出現的配對一律紅燈。裁決寫在 `ADJUDICATED`，含理由。

**首次執行（2026-09-01）：**

```
候選 532 組｜未裁決 0｜待修 3｜靜音 5｜一致 6｜待人工 59（不擋閘門）

待修 [1] product_profiles.lifecycle_stage='EOL' vs product_specs.is_discontinued
         12 vs 1、交集 0                         → 改資料（§7.10）
待修 [2] order_profiles.delivery_preference='STORE' vs store_pickups.picked_up_at
         phi = +0.006                            → 改問句（#202）
待修 [3] customer_profiles.newsletter_opt_in vs newsletter_subscriptions.unsubscribed_at
         4 位退訂客戶裡 3 位 opt_in 仍是 1        ← **手動翻沒找到，閘門找到的**
```

**它抓不到什麼（要寫下來）**：59 組待人工不擋閘門 —— 21 組是表存在性，
38 組是謂詞無法機械化。後者包含 `#249` 那組：`order_returns.status` 的註解是
純英文 `'(REQUESTED/RECEIVED/REJECTED)'`，沒有中文對照，詞組「收到」對不上代碼。
**兩張表的註解沒有共用中文詞的衝突，這支一律看不見。**

#### 處置（2026-09-01，當天就清完）

判準沿用 §7.9：**路徑唯一就改資料，路徑不唯一就改問句。**

**A 類 — 同一件事、值對不上 → 改資料**（`tools/fix_derived_consistency.py`，只下 UPDATE）

| 欄位 | 真相來源 | 為什麼是它 | 動了幾列 |
|---|---|---|---|
| `product_specs.is_discontinued` | `product_profiles` | `lifecycle_stage` 與 `delisted_at` **兩欄互相佐證**（各 12 筆、互斥集合皆為 0）；`is_discontinued` 只有 1 筆孤證 | 40 |
| `order_returns.status` | `return_shipments.received_at` | 有時間戳，較具體。只修「宣稱收到但沒收到」這個方向 —— `REJECTED` 而 `received_at` 有值是合法的（先收到再拒絕） | 4 |

```
#127 已停產商品   1 列 → 12 列          #168 已收到退貨   10 列 → 6 列
#258 GT 不變（仍走 lifecycle_stage），但模型走 is_discontinued 那條路
     現在得到**逐列相同**的 12 個商品 —— 毒性來源消失

指紋：93 張表只有 order_returns 與 product_specs 變動，其餘 91 張完全相同；
      所有表的**列數都沒變**（純 UPDATE，沒有增刪列）。
```

⚠️ 兩個欄位都**結構上無法**在建表時對齊，與 `customer_profiles` 同型（§7.9）：
`product_profiles` 由 `add_wide_tables*.py` 在 `init_db_ext.py` 之後才建；
`order_returns.status` 是第一波 `RNG` 抽的，`return_shipments.received_at`
是第二波 `RNG_LATE` 抽的。**兩支種子腳本已加指路註解，並寫明不要動 RNG 呼叫順序**
（少抽一次整條序列就平移，309 題 GT 全毀）。

**B 類 — 兩個量本來就不同 → 改問句，不對齊**

| 題 | 改法 | 為什麼不對齊 |
|---|---|---|
| `#202` | 「有幾張訂單**選了**到店取貨…」→「**有到店取貨紀錄的訂單裡**，有幾張還沒有被取走？」 | 「選了」逐字指向 `delivery_preference`「配送偏好：STORE 門市取貨」。偏好 ≠ 實際，對齊會毀掉語意。改用 `#221` 已在用的句式 |
| `#249` | →「**已經寄回的退貨裡**，有幾筆倉庫還沒有收到？」 | 母體含糊：18 筆退貨有 4 筆連物流單都沒建，`LEFT JOIN` 讀法得 8、GT 得 4，**兩種讀法都合理**。不補 `alt_sql` 而是收窄母體 —— 這題要測 NULL 語意，母體含糊會讓它同時測兩件事 |
| `#275` | 「哪幾檔**廣告**」→「哪幾檔**廣告投放活動**」 | `promotion_profiles` 有幾乎同名的欄位（`pause_reason` vs `paused_reason`、`approved_by`、`approved_at`），`is_paused=1` 也剛好各 5 筆。**這兩張表不是矛盾，是兩個真實體**（廣告投放 vs 站上折扣檔期），沿用 §7.9 對 `#279` 的處置 |

`#202` 那組因此改判為**宣告**而非待修。⚠️ 但 `phi = +0.006` 仍然是完全獨立，
真實系統會強正相關 —— 那是「資料不夠像真的」，不是「題目沒有唯一答案」，
優先級低很多，先擱著。

**閘門本身也改了一條**：裁決為「待修」的配對**會回頭看資料**，修好了自己放行
（`product_specs` 那組現在 phi = +1.000，自動轉「已修」）。
沒有這一步，修完之後閘門會永遠紅燈，於是三天後被人忽略。

#### 還沒處置的那一組（閘門找到的，不是手動翻的）

```
[待修] phi=+0.184  母體 50｜A 22｜B 4｜交集 3
       customer_profiles.newsletter_opt_in = 1        『是否訂閱電子報』
       newsletter_subscriptions.unsubscribed_at IS NOT NULL
```

`#237`「目前還有多少位客戶**訂閱著**電子報？」GT 走
`newsletter_subscriptions.unsubscribed_at IS NULL` = **18**，
而 `newsletter_opt_in`『是否訂閱電子報』= **22** —— 4 位已退訂的客戶 opt_in 仍是 1。
註解說的是「現在訂著」，資料是「曾經訂過」。

**GT 零引用 `newsletter_opt_in`，所以修它不會動到任何一題的答案。**

**2026-09-02 裁決：選 (a) 對齊資料。** 真相來源＝`newsletter_subscriptions`
（`subscribed_at`／`unsubscribed_at` 兩個時間戳互相佐證，`opt_in` 是
`init_db_ext.py:782` 的 35% 獨立硬幣，沒有任何欄位佐證它）。
`opt_in` 現在**定義為** `EXISTS(該客戶未退訂的列)`：22 → 18，24 位客戶的旗標重新指派。

修完之後閘門**還是紅的**，而那次紅燈是誤報 —— 值得記下來：

```
[待修] phi=-0.221  母體 50｜A 18｜B 4｜交集 0      <- 對齊之後
```

每位客戶只有一列電子報訂閱（22 列 / 22 人），所以「現在訂著」與「已退訂」
天生互斥，剩下 28 位是從來沒訂過的人，兩邊都不算。
**「互斥」只有在兩個謂詞宣稱切開整個母體時才是缺陷** —— 這一組沒有，
和 `employee_profiles.resigned_at` vs `employees.is_active` 的「互補」誤報
是同一個形狀，只是換了一邊。判決改寫成「宣告」，理由與驗證數字寫進
`ADJUDICATED`；迴歸由 `fix_derived_consistency.py` 守（它每次跑都會報
「要改 0 列」，資料一漂就變非 0）。


### 7.12 不靠題目找矛盾：全庫同義欄位掃描（2026-09-02）

§7.10 與 §7.11 是**順著題目查出來的** —— 手上有 `#258` 失手的 SQL，才回頭
找到 `is_discontinued`。閘門 [11] 把這條路自動化了，但它繼承了同一個限制：

```
候選產生 = 問句的中文詞組  ->  只看得到「有人問過的那些字」
判決     = 布林／時間戳／列舉  ->  只看得到謂詞，看不到數值與字串
```

這一輪反過來做：**不看題目，掃全庫每一對共用母表的表**，找「同一個事實被
寫成兩欄而值對不上」。判準只有兩條，都不需要語意模型：

```
欄名完全相同                          installment_periods / payment_terms / received_at
註解共用一段中文，且型別同族            『最低消費門檻』vs『最低訂單金額門檻』
```

#### 掃描腳本自己的兩個 bug，各藏起一批

第一版報「乾淨」，而它是錯的：

```python
try:
    n, bad = conn.execute(text(sql)).fetchone()
except Exception:
    continue          # <- 例外被吞掉，看起來就像「這組沒問題」
```

兩批表的 collation 不同（`utf8mb4_0900_ai_ci` vs `utf8mb4_unicode_ci`），
字串欄位一比就丟例外，於是 **`supplier_profiles.payment_terms`（8/8 全錯）
與 `order_profiles.invoice_carrier`（164/164 全錯）整整兩組靜靜消失。**
第二個 bug 在兩跳 JOIN：反向找到連結時別名沒跟著換，
`order_profiles.installment_periods` 那組也是這樣不見的。

> 這是 §8② 的第 N 個實例：**不會叫的錯比會叫的錯貴得多。**
> 修法不是加 try 的範圍，是**把失敗印出來並且算進總數** ——
> 現在腳本第一行就報「SQL 失敗 N 組」，N 不是 0 就不准當成掃過了。

#### 真相來源怎麼判：一條規則，三組促銷欄位判到不同邊

同一組表（`promotion_rules` 窄表 vs `promotion_profiles` 寬表）的三個欄位，
兩個判給窄表、一個判給寬表。不是隨便挑的，規則是：

```
有佐證的贏     同一張表裡有別的欄位獨立指向同一個事實
都沒佐證       -> 原始窄表贏（後加的寬表是獨立擲的骰子，§7.9 的形狀）
```

| 欄位 | 佐證 | 判給 |
|---|---|---|
| `stackable` / `is_stackable` | 兩邊都沒有（`is_exclusive` 全 0、`stack_priority` 不分疊不疊都有值） | 窄表 |
| `min_amount` / `min_order_amount` | 兩邊都沒有（而寬表 8 檔有 5 檔是 500，預設值的樣子） | 窄表 |
| `applies_to` / `applies_to_scope` | **寬表有**：`scope='CATEGORY'` 的 3 檔 `included_category` 全有值、`='ALL'` 的 3 檔全 NULL，完全吻合註解宣告的不變量 | 寬表 |

`#194` 與 `#302` 是 `#127`／`#258` 的翻版 —— 同一個詞「疊加」，
`#194` 走窄表、`#302` 走寬表，8 檔裡有 2 檔答案相反。

#### 一共下了 19 條對齊

| 類 | 欄位 | 對不上 | 影響到的題 |
|---|---|---|---|
| 橫向 | `customer_profiles.newsletter_opt_in` | 24/50 | 無（GT 零引用） |
| 橫向 | `promotion_profiles.is_stackable` | 2/8 | `#302` 換列 |
| 橫向 | `promotion_profiles.min_order_amount` | 7/8 | `#305` 換數字 |
| 橫向 | `promotion_rules.applies_to` | 6/8 | 無 |
| 橫向 | `supplier_profiles.payment_terms` | **8/8** | 無 |
| 橫向 | `order_profiles.installment_periods` | 79/200 | 無（`#291` 走 `payment_profiles`） |
| 橫向 | `order_profiles.currency_code` ＋ `exchange_rate` | 10/164 | 無 |
| 橫向 | `order_profiles.invoice_carrier` | **200/200** | 無 |
| 橫向 | `order_profiles.is_split_payment` | 18/200 | 無 |
| 橫向 | `return_profiles.received_at` ＋ `transit_days` | 17／14 of 18 | `#307` 換天數 |
| 橫向 | `product_specs.energy_label` | 7/7 有值的 | 無 |
| 橫向 | `subscriptions.ended_at` | 5 筆 PAUSED | **`#212` 6 → 11** |
| 橫向 | `delivery_attempts.result` | 38/143 | **`#176` 42 → 73**（`#177` 不變） |
| 橫向 | `store_profiles` 七天營業時間 | 1 間停業門市 | 無 |
| 不變量 | `product_profiles.package_weight_g` | 16/40 | `#259` |
| 不變量 | `product_profiles` 外箱長寬高 | 32/40 | `#259` |
| 不變量 | `invoices.carrier`（捐贈發票） | 15/164 | 無 |

幾條值得單獨記的：

- **`supplier_profiles.payment_terms` 8/8 全錯**，因為兩邊用不同詞彙寫同一件事：
  合約寫『月結60天』，檔案寫『NET60』。`貨到付款` 在檔案端的列舉裡**根本不存在**，
  所以順帶把 `COD 貨到付款` 補進欄位註解 —— 否則對齊會產生一個註解說不存在的值。
  同理 `promotion_rules.applies_to` 補上 `BRAND`。兩個註解都走
  `ALTER ... MODIFY COLUMN` ＋ 更新 `wide_table_plan.yaml`／`add_distractor_tables.py`
  ＋ `gen_ddl.py --write`，**不是手改 `semantic_layer.yaml`**（§8①）。
- **`order_profiles.is_split_payment` 對齊後整欄變 0**：它宣稱 18 張訂單分多筆付款，
  但全庫沒有任何一張訂單有 2 筆以上的 `payments`。恆為 0 的欄位沒有用處，
  但它至少不再說謊。注意這與「分期」是兩件事 —— 分期是一筆付款拆成多期
  （`payment_profiles.is_installment`），兩者不該互相對齊。
- **`delivery_attempts.result` 只改值不增刪列**，所以 `#177`（嘗試 3 次的出貨單）
  與 `shipment_profiles.attempt_count` 都不受影響，只有 `#176` 變多。
  38 張 IN_TRANSIT 的出貨單原本**全部**有一筆 `result='DELIVERED'` 的最後嘗試 ——
  而 `delivery_attempts` 的表註解自己就寫著「最終是否送達看 shipments.status」。
- **`#212` 是這一輪唯一答案數字改變且該改的題**：5 筆 PAUSED 的訂閱都有結束日，
  但它們的 `is_paused=1`、`resume_scheduled_at` 也都排好了復訂日 ——
  三個欄位有兩個說「還在」、一個說「結束了」。

安全性同前兩輪：**只有 UPDATE**，93 張表 12 張指紋變動、**列數 0 變動**，
`fix_derived_consistency.py` 再跑一次 35 條全部報「要改 0 列」（可重複執行）。

#### 掃到、但**不改**的六組（附理由）

「值不一樣」不等於「缺陷」。判準：**有沒有哪個業務問題因此有兩個答案。**

| 現象 | 為什麼不是缺陷 |
|---|---|
| 19 台沒放棄的購物車收到挽回信 | 挽回信寄出時是放棄狀態，客戶回訪後 `is_abandoned` 變 0 —— 這正是挽回信的目的。實測寄信時間與購物車最後異動的先後是 9:10，**兩個方向都有，資料不支持也不反駁**，而修它要刪列 |
| 34 張訂單既有出貨單又有門市取貨 | 一張訂單多件商品分開履約是真實的；兩張表都沒有宣告互斥 |
| 4 筆 `REJECTED` 的退貨有收件時間 | 先收到再拒絕退貨是合法流程，§7.10 的裁決本來就這樣寫 |
| 33 張訂單有分期期數但 `is_split_payment=0` | 分期 ≠ 分多筆付款，見上 |
| 5 間門市有服務預約但該服務旗標是 0 | **18 筆全部在錨點之前**（最晚 2026-08-13 vs 錨點 08-15）。旗標是「現在提供不提供」，預約是歷史事件。而且沒有任何題目把兩者 JOIN 起來，所以沒有題目有兩個答案 |
| `subscriptions.ended_at` vs `contract_end_date` 6 筆不同 | 合約到期日 ≠ 實際結束日，提前解約本來就會不同 |

最後一列尤其要記：**如果把 `offers_repair` 之類的旗標「對齊」成有預約就設 1，
`offers_repair` 會從 6/8 變成 8/8 涵蓋率 100%，直接違反配題的鑑別力上限（§7.4）。**
把不是缺陷的東西「修好」，代價是毀掉題目。

#### 修完之後的驗收：17 題 × n=8（2026-09-02）

驗收集合是**事前**選出來的 —— 「GT 的 SQL 直接引用了被改過的欄位」，
不是事後看「哪幾題結果變了」。所以它不觸犯 §5.4（用差異挑子集再在同一份
資料上重測），可以合法地當成驗收。

```
17 題 × 8 次 = 136 次 pipeline｜臂 FILTER_VOTES=3（現行）

穩定過 16｜擲硬幣 1｜穩定錯 0

#127 #169 #170 #176 #193 #194 #195 #212 #249 #258 #259 #276 #290 #291 #305 #307   8/8
#302                                                                              7/8
```

三件值得單獨記的：

- **`#258` 5/8 → 8/8。** 這是整條線的終點：`#258` 是 §7.10 的起點（模型伸手拿
  兄弟表的 `is_discontinued`，而那一欄跟 `lifecycle_stage` 交集 0），
  §7.11 的閘門 [11] 與 §7.12 的全庫掃描都是從它長出來的。
  **沒有動模型、沒有加規則、沒有改問句 —— 只是讓兩條路指向同一件事。**
- **`#194` 與 `#302` 現在同時 8/8 / 7/8。** 修之前它們對「哪幾檔可疊加」
  給出相反答案（窄表 5 檔 vs 寬表 3 檔）。現在兩條路都對。
  `#195`／`#305`（最低門檻）同理。
- **答案數字變過的三題（`#212` 6→11、`#176` 42→73、`#307` 天數）全部 8/8。**
  改資料沒有把原本會答的題弄壞。

`#302` 那一次失手是回 0 列（選表／生成層），與資料無關。
n=8 分不出 7/8 與 8/8（§5.2），視為與 8/8 同一枚硬幣。

> **順帶量到一個 §5.2 的乾淨例證**：這 17 題在 2026-08-25 那一輪
> **單輪 e2e 全部 `ok`**，而同一時期重複取樣說 `#258` 是 5/8。
> 單輪把「穩定答對」與「八次中五次」印成同一個字。
---

## 8. 反覆咬人的四個失敗形狀

這一節是這份文件最貴的部分 —— 前面每一節的具體數字都會隨規模改變，這四條不會。

### ① 寫了，卻沒有接到既有的產生鏈上（五個實例，五次都不報錯；第六個在咬人之前先堵住了）

| 實例 | 症狀 |
|---|---|
| `gen_ddl.py` 沒重跑 | 檢索選對表、生成端看不到欄位 → 誠實回「schema 不支援」，**在對帳表上跟防禦題長得一模一樣** |
| `sync_table_comments.py` 漏了一個來源 | 改了註解不生效 |
| `schema_parser` 的 `\);` regex | 表級 COMMENT 進 DDL 後**直接回空 dict** |
| `get_few_shot_text()` 不渲染 `reasoning` | 22 則 reasoning、4,649 字元全是死的，模型只拿到 SQL 形狀那一半 |
| `eval_score.py` 的分母 | `sum(tally.values())` 把 4 題防禦題數了兩次，正確率**低報 1.5pp** |
| **（2026-08-26，事前堵住的第六個）** | `customer_profiles` 的 6 個衍生欄位**只能**在 `init_db.py` 之外對齊（母表那時還沒建），所以「重建了卻忘記跑對齊」是必然會發生的一步。這次沒有等它咬人 —— 閘門 [9] 加了 `DERIVED` 名單，那 14 欄不管有沒有題目引用一律紅燈（§7.9） |

**共同形狀：新增了一個來源／欄位，卻沒接到下游；東西還在、看起來正常、不會報錯。**

第四個實例的殺傷力最大：「以後都從 few-shot 解決」這條方針
**在 2026-08-20 之前結構上一直是做不到的** ——
過去每一次「加了 few-shot 沒效果」的觀察都要重新看待。
而同一個檔案往上 20 行，`get_rules_text()` 早就修過同一個 bug，註解還留著理由。

第五個實例揭示了為什麼這類 bug 活得久：**它只在有人真的去讀那一行時才咬人。**
文件裡其他的百分比都是照「錯幾題」反推寫的，數字剛好都對；
唯一一處直接照抄輸出的，就錯了那一次。

**應對**：閘門第 1~4、8 項；以及下面這條 ——

> **只警告不阻斷的檢查，等於沒有檢查，除非有人會讀。**
> `_report_drift()` 整整一輪都在警告「DB 有而 YAML 沒有的表」，被當成雜訊忽略了。
> 所以閘門預設 `exit 1`。

（順帶：缺 DDL 曾經是一張**意外的安全網** —— 22 題選到了沒有 DDL 的表，
但 0 題真的用了它們，因為**沒有欄位就寫不出 SQL**。補 DDL 等於把網子拆掉，
所以拆之前先離線量了三個數字才動手，結果零回歸。）

### ② 靜默失敗：不會叫的錯比會叫的錯貴得多

同一個原則在五個層級各出現一次：

| 層級 | 會叫的失敗 | 不會叫的失敗 |
|---|---|---|
| 表 | 答不出來、EXPLAIN 報錯 | — |
| 候選天花板 | — | 必要表落在第 41 名外，**分數會掉但沒有東西會報錯**（§7.2） |
| 欄位 | — | 模型挑一個相近的，SQL 合法、結果錯（§2.6） |
| 防禦題 | — | 加表讓概念變可回答，**系統答對卻被判錯**（§6.2） |
| 誠實性 | — | 守衛沒開火：模型發明公式，**使用者看不出那數字是憑空來的**（§4.3） |
| 評分 | crash | **給你一個看起來合理的錯數字**（§8 ①） |

**設計原則：讓失敗變得有聲。** 具體手法有三個，形狀相同 ——
DDL 附上「其他表的表名」、裁剪時留一行「本表另有 N 欄未列出」、
`_augment_schema_error` 把真實欄位附回去。

> **讓模型知道有東西被省略，比乾淨地騙它說沒有更安全** —— 不知道就會拿手上的湊。

### ③ 外推只能決定「什麼時候回來量」，不能當結論

這個專案的外推被實測推翻過兩次，**而且方向相反**：

| | 外推說 | 80 張表實測 | 錯的方向 |
|---|---|---|---|
| 純 Python 餘弦（外推到 1,000 表） | ~150 ms | 按 80 表的 36 ms 重算 **~450 ms** | **樂觀 3×** |
| dense Top-1（外推到 80 表） | ~57% | **84.6%** | **悲觀 28pp** |

第二條錯得比較有意思：它把 Top-1 衰減建模成表數的線性函數（−0.52pp/表），
但真正在動的是**表註解品質與 GT 覆蓋**（誘餌清零、`payments` 註解、新增 62 題 GT）。
**表數翻倍，Top-1 反而回升，干擾搶 Top-1 從 17 掉到 0。**

> 這份文件裡每一條外推都應該在下一個規模點被標記為「校準過」或「推翻」。
> 「∪ Top-1 有保鮮期」這件事本身沒有被否證，只是**到期日不由表數決定** ——
> 真正的觸發指標是「干擾搶 Top-1」那個數字，它今天是 0。

同一條紀律也適用於**方案的判決**：§2.7 的 C 方案今天從「擱置」變成
「候選天花板的解」，不是因為方案變了，是因為它的兩個前提
（候選召回 100%、Top-1 買不動）在 80 張表都不再成立。
**否決與擱置都有保鮮期，要跟數字一起重讀。**

### ④ 對照組與檢查工具本身也要驗

五個實例：A/B 的對照臂 8 次全 crash（`legacy()` 漏了兩段式範例的
`sql_step1`，`ex["sql"]` 直接 KeyError —— 量到的是 harness 不是系統）、
兩次 heredoc 把跳脫字元吃成真換行寫出語法錯的檔案、
`eval_score.py` 的分母、第一版的盲區偵測器（表驅動，不知道哪張表才對，
40 個候選大部分是誤報 —— 那是設計缺陷不是調參問題），
以及底下這個**最貴的一個**。

#### 三支檢索評估對「答對」的定義跟對帳程式不一致（2026-08-21 修）

題目語意有歧義時 GT 會列 `alt_sql`，**任一相符即通過** —— `eval_score.judge()`
與 `eval_lucky.py` 都是這樣判的。但 `eval_retrieval.py`、`eval_retrieval_variants.py`、
`eval_column_recall.py` 三支**只解主 SQL**，於是把「走了另一條被 GT 認可的路」
判成檢索失敗。當時 279 題裡 41 題有 alt_sql（今天是 309 題裡 50 題）。

修好之後的差別**很大**：

```
漏斗召回   97.6%  →  99.2%       漏 6 題 → 漏 2 題
純屬誤報的 #72 #159 #172 #173     全部是「模型走了 GT 自己認可的另一條路」
```

`#172` 是最清楚的例子：「有多少張訂單曾經被取消過」走 `order_status_history`
或 `order_cancellations` 都對，GT 兩條都收，而檢索評估只認第一條。

**為什麼它活這麼久**：它不會 crash，只會讓分數**偏低**——
而偏低的分數看起來像「還有進步空間」，沒有人會去質疑一個對自己嚴格的指標。
比起會高報的 bug，低報的 bug 更難被發現，因為它不製造矛盾。

（`--funnel` 現在會把 LLM 的選擇存成 `eval/results/funnel_picks_*.json`。
這一次的修正就是靠重播那份存檔驗證的 —— **零額外 LLM 呼叫**。
要花 275 次呼叫才拿得到的東西，一定要存檔。）

還有兩條實驗設計的教訓：

- **串跑會被限流污染。** 把多個變體串在同一支腳本裡連續打 API，跑在後面的變體
  吃到累積的 429，122/153 題退回純相似度 —— 量到的是降級路徑的分數
  而不是那個變體的分數。**跨變體比較必須記錄 LLM 退回次數**，
  否則會把基礎設施的狀態讀成演算法的優劣。
- **以鍵為單位的設定檔，append 是一個會沉默覆寫的動作。**
  `ambiguity_verdicts.yaml` 以題號為鍵，append 12 筆有 10 個題號早就存在，
  `yaml.safe_load` 只留最後一個。已加重複鍵檢查，第二次踩到當場被抓。

### 附帶：文件的過期也是靜默的

這份文件曾經有一張「已知缺陷」表從 21 張表的年代一路留到 80 張表，每一列的 k/8
都還是當年的數字，而讀的人沒有任何線索知道它過期了 —— **跟 `gen_ddl.py`、
`reasoning` 是同一個形狀，差別只在那兩個有閘門會抓，文件沒有。**

所以今天的規則是：**每個數字都要寫明它是哪一個基準量的（93 / 86 / 80 張表，
見開頭的導讀）；不能重測的一律標 ⚠️ 未在該規模重測並附重測指令。**
歷史過程搬到 `ARCHITECTURE_archive.md`。

> **原本的規則是「全文只寫 80 張表的數字」，而它在第二次擴表時就過期了** ——
> 一旦同時存在三個基準，「只寫一個規模」這條規則反而會逼人把不同基準的數字混寫在
> 同一句裡。2026-08-26 的對齊掃出 12 處字面殘留（`80 張表`寫在描述今天行為的句子上），
> **每一處都不會報錯，而且都讀得很順**。這正是本節在講的形狀，只是輪到文件自己身上。

---

## 9. 今天的已知缺陷

#### 新臂（`FILTER_VOTES=3`）重測，2026-09-01 —— 這才是今天的名單

上線聯集之後 §9 整份名單就過期了（舊名單是 `votes=1` 量的）。21 題 × n=8，
835 次呼叫，配速 15 RPM：

```
穩定通過 8   #40 8/8  #62 8/8  #94 8/8  #100 8/8  #261 8/8  #263 8/8  #264 8/8  #282 8/8
硬幣池  11   #63 7/8  #279 7/8  #34 6/8  #138 6/8  #189 6/8  #260 6/8
             #258 5/8  #287 5/8  #292 4/8  #89 3/8  #286 3/8
穩定錯   2   #284 1/8   #308 0/8
```

**三個明確的變化：**

| 題 | 舊臂 | 新臂 | 判讀 |
|---|---|---|---|
| `#282` | 硬幣 1/8 | **8/8 穩定通過** | 聯集把 `shipment_profiles` 補進來了。漏斗、e2e、n=8 三個獨立層都指同一件事，歸因成立 |
| `#292` | **穩定錯 0/8** | 硬幣 4/8 | 同上，`payment_profiles` |
| `#40`／`#62` | 檢索缺陷（80 張表基準，未在 93 張表重跑） | **各 8/8** | 93 張表上已經不是問題，**從缺陷清單移除** |

⚠️ **這是單臂的現況更新，不是 A/B。** 除了上面三列（有機制 + 三層獨立證據），
其餘題與舊數字的差異**不可以歸因給 `votes=3`** —— 舊數字是不同時間量的，
中間還改過 GT 與資料。`#286` 5/8→3/8、`#258` 7/8→5/8、`#189` 7/8→6/8
全部在 n=8 的解析度內，照 `#264` 那條既有判準視為同一枚硬幣。

⚠️ `#89` 3/8 是這份名單裡最低的硬幣，而舊臂**從來沒有 n=8 數字**（§4.2 只標了
「已知代價」）。所以它現在無法歸因 —— 要判斷得先補一次 `FILTER_VOTES=1` 的 n=8。

`#308` 0/8 完全沒動，`#284` 1/8 幾乎沒動 —— **這兩題是今天真正的穩定錯**。
`#284` 只有欄位提示救得動（0/3→3/3），而那個做法連帶弄壞 `#258`，已否決（§2.7j）。

---

**以下逐題機制表是舊臂（`votes=1`）量的**，機制診斷仍然成立，
但括號裡的 n=8 分數請以上面那份為準。

| 題 | 狀態 | 機制 |
|---|---|---|
| `#34` | 硬幣 5/8 | 「比較 7 月和 8 月的訂單數量」把兩個月併成一列 |
| `#189` | 硬幣 7/8 | 有被回覆的評價佔比 |
| `#89` | **已知代價** | few-shot 邊界的殘留，已依停損條件收手（§4.2） |
| `#138` | 硬幣 7/8 | 問句自己點名了多邊欄位，是問句語意與答案粒度衝突，不是知識缺口（§4.1）。**永久凍結** |
| `#261` | ✅ **已修 0/8 → 8/8** | 我自己製造的歧義：`product_profiles.image_count`（快照欄位）與 `product_images`（明細表列數）是同一個概念的兩份資料，實際值還不一樣（2 vs 3）。修法是**改問句**（「內容檔案上登記的」）＋把快照語意寫進欄位註解，與 `customer_profiles` 既有寫法一致。**沒有動模型、沒有加規則。** 同表另外四題無回歸（#259 8/8、#276 8/8、#258 7/8、#260 5/8） |
| `#258`／`#260` | 硬幣 7/8／5/8 | 同一家族的第三種形狀：寬表自己帶了 `name_en`／`name_ja`，模型拿它們當「品名」而不 JOIN 回 `products.name`（`#258` 回日文品名是明確錯；`#260` 回英文品名其實識別得出商品，是 GT 嚴不嚴的問題）。**刻意不補 alt_sql** —— 理由同 `#263` |
| `#263` | ✅ **已修 0/8 → 8/8** | 選表層失敗：5/8 誤觸防禦暗號、2/8 用 `order_status_history`+`employees` 回 137 列。錨點從來沒有 `order_profiles`，因為**「風控覆核」整組概念不在表註解裡**（`check_comment_coverage.py` 抓到的）。補進註解後 dense 排名 10 → 1、e2e 8/8。同表 `#262`／`#265`／`#277` 各 8/8 無回歸 |
| `#264` | 硬幣 6/8 → 5/8 | `order_notes` 搶 `contact_before_delivery`（走 `note LIKE '%打電話%'` 回 0 列）。兩次量測都在 n=8 的解析度內，視為同一枚硬幣 |
| `#279` | ✅ **已修 0/8 → 8/8** | `campaign_clicks`（點擊**數**）搶 `ctr_pct`（點擊**率**）。當時的 A/B **結論是對的**（註解不是原因）**但病因猜錯了** —— 不是模型挑錯表，是`campaign_profiles.clicks` 合計 134,911（廣告平台尺度）與 `campaign_clicks` 418 列（站內記錄）**指到不同的活動**（第 3 檔 vs 第 6 檔）。同一個問題有兩個都對的答案。2026-08-25 收窄問句成「行銷活動檔案上點擊率最高」（§7.9）。**n=8 重測 8/8** —— 沒有動模型、沒有動資料、沒有加規則，收窄問句就夠 |
| `#287` | **硬幣 3/8**（病換了兩次） | 同上族。`days_after_delivery` 是 `_pick([1..45])`，與從 `reviews`/`shipments` 算的在 97 筆可對照資料裡只有 1 筆吻合（36 列 vs 19 列）。**`reviews` 沒有 `order_id`，母表算不出唯一答案 → profile 欄位就是真相來源**，收窄問句。收窄後 n=8 仍是 0/8，但**病換了**：8/8 選對 `review_profiles`、8/8 回 COUNT，值 34 而 GT 36 —— 差的正是 2 則已刪除的評價，**變成 GT 的問題**。再修掉整個 review 家族的慣例不一致（6 題濾 `is_deleted`、4 題不濾）之後 0/8 → 3/8，5 次失敗全是「系統 1 列 / GT 34 列」＝那一輪沒濾 |
| `#308` | **穩定錯 0/8**（病換了） | 同上族。`is_over_policy_window` 是常數 0 再由 `guarantee()` 硬寫兩列為 1，與日期推導打架（2 列 vs 4 列）。這一題母表**能**唯一決定（`requested_at` 與母表 18/18 相同、return→order 一對一），所以改資料不改問句。對齊後 n=8 仍是 0/8，但**病從資料層換到選表層**：6/8 觸發 `SCHEMA_UNSUPPORTED`（選表層根本沒給 `return_profiles`）、2/8 回 4 列（＝對齊後的正確列數）但欄位不符。**資料矛盾修好了，露出底下的選表問題** |
| `#282` | 硬幣 1/8 | 誘餌：問句寫「改過**地址**」，模型選 `addresses`，答案在 `shipment_profiles.is_redirected`。7/8 是**誠實拒答**（拿不到表就答不出來），不是亂答。欄位提示（§2.7f）能把它做到 8/8，但同一個機制會把 `#94`／`#100` 打成 0/8，所以不上線 |
| `#284` | **穩定錯 0/8** | 真·字面吸引：「**標**成易碎品」被讀成 `product_tags`（行銷自由標籤），而資料裡沒有「易碎」這個標籤 → 8/8 都回 0 列。**加註解解不掉，改問句也白費**（改寫後仍 0/8，§7.8）—— 依判準「一種讀法回空集合＝模型挑錯表」，不再改問句。它需要四張表，是 §2.7g 第 5 條原則唯一推得動的一題（0/8 → 4/8），也是最脆的一題 |
| `#286` | 硬幣 5/8 | review 家族統一成「一律濾掉已刪除」之後 3/8 → 5/8。3 次失敗全是「系統 33 列 / GT 32 列」＝那一輪沒濾。**統一慣例沒有把它變成穩定過，因為模型自己就是搖擺的**（§0） |
| `#292` | **穩定錯 0/8** | 誘餌：「**對帳**」→ `invoices`，答案在 `payment_profiles.is_reconciled`。5/8 拒答、3/8 把它讀成「沒有發票」查出 8 列。路線 ② 的 D 臂把它 0/8 → 8/8 —— 但那一輪 +11/48 的增益裡有 **8 票來自這一題**，單題效應撐起整個結論，所以不採信（§2.7e） |
| 檢索 | 2 題（80 張表基準，未在 93 張表重跑） | `#40` 候選層漏 products（排名 43）、`#62` LLM 層漏 products（排名 13）。**兩題都是 products**，已有解、觸發條件見 §7.2 |
| 資料 | 未修 | `order_items.unit_price` 與 `products.price` 在種子資料裡**永遠相等**。兩者語意不同卻無法被任何測試分辨 —— 但重跑 `db/init_db.py` 會讓 309 題 GT 全部失效 |

**七題共用一個機制：新寬表的欄位與既存明細表／既有主表是同一個概念的兩份表述，
而模型偏好那張更「字面」的舊表，或偏好寬表自己帶的那一欄。**
今天的落點是 `#261`／`#263`／`#279` 已修、`#287` 降級為硬幣、
`#258`／`#260`／`#264` 留著。

修掉三題，而且三題用的是**三種不同的解法**，因為病不同（§2.5 結論 3）：

```
#261 誘餌     → 改問句消歧 + 快照語意寫進欄位註解        0/8 → 8/8
#263 盲區     → 概念群補進表註解（壓縮不是追加，+5 字）   0/8 → 8/8
#279 資料矛盾 → 收窄問句（同一問題本來有兩個都對的答案）   0/8 → 8/8
```

**三種病都不是「模型挑錯表」，而三次修法都沒有動模型。**
剩下真正屬於選表層的是 `#282`／`#284`／`#292`，而 §2.7e~§2.7h 四次介入
證明了那一層今天沒有可用的訊號（誘餌題與窄表題在同一條軸上）。

> **`#261` 是同一個病的第一例，而修法沒有被一般化 —— 於是它又長出三題。**
> 那次的診斷完全正確（`image_count` 與 `product_images` 是同概念的兩份資料、
> 實際值還不一樣 2 vs 3），修法也正確（改問句 + 註解寫明快照語意），
> 但它被當成**一題的修正**收掉了，沒有人去問「還有幾欄是這樣」。
> 四個月後 `#279`／`#287`／`#308` 用同一個機制各咬一次，
> 而中間七輪都在調註解。答案是掃描一次就有的：30 項檢查、純 SQL、零 LLM。
>
> **教訓不是「要修這個病」，是「修完一題之後要問它是不是一整類」。**
> 一次性的修正沒有變成閘門，就等於沒修 —— 這是 §6 存在的理由，
> 也是閘門第 [9] 項（§7.9）遲到四個月的原因。

其餘三題（`#258`／`#260`／`#264`）留著：**誘餌型的加字解不掉**
（它們在概念群那一臂的 dense 排名已經是 1，e2e 照樣選舊表），
而它們是這個失敗形狀目前僅有的活例子。 這是 §2.9 近義表競爭從「表 vs 表」
變成「欄位 vs 表」的版本 —— `check_ambiguity.py` 抓不到，因為它比的是欄位名，
一邊卻是「表的列數」。

> **`check_comment_blindspot.py` 這次的預測是反的。** 它點名 `#277`／`#278`
> 有註解盲區，兩題實測都對；真正 0/8 的 `#261`／`#263` 它一個都沒報。
> **它掃的是「問句字面詞有沒有出現在表註解」，而失敗的機制是「另一張表更像」**——
> 兩件事不一樣。這支工具的輸出要當成待查清單，不能當成優先序。

#### 兩個今天完全沒有指標在盯的東西（2026-09-01，§7.10）

1. **過度拒答。** 防禦題 4/4 只查「該拒答的有沒有拒答」，查不到反方向。
   `#274` 在 93 張表全送時誤觸防禦暗號 —— 那題用現有 schema 答得出來。
   現成的代理指標是 `SCHEMA_UNSUPPORTED` 的觸發次數，**還沒接**。
2. **漏斗「排除」那一半的價值。** `--funnel` 量的是召回（把漏掉的表補回來），
   而漏斗另一半的作用是**把可湊的近義表拿走**，沒有任何指標在量。
   後果是漏斗指標會**系統性高估**聯集這類「多給表」的介入
   （§2.7i：漏斗 9 題救回，e2e 只兌現 7 題）。

還沒做（不是設計問題）：`utils/semantic_layer.yaml` 與 24 則 few-shot 全部綁在
現有 93 張表的真實表名，換一套 schema 要整份重寫。

### 資料安全約束（改 schema 前必讀）

- **重跑 `db/init_db.py` 會重新隨機生成資料，309 題 GT 整份失效。**
  schema 要改請走 metadata-only（`ALTER TABLE … COMMENT`）或**純新增**
  （只 CREATE + INSERT，事後跑逐表 SHA 指紋確認既有表一位元都沒動）。
- **新增資料要用獨立亂數源。** `seed_distractor_data.py` 的生成迴圈是
  `for t, spec in plan.items()` —— 走的是 **YAML 的順序，不是程式碼的順序**，
  所以「新的 random 加在最後」不夠。用 `RNG_LATE = random.Random(20260820)`，
  主序列一個數字都不動；否則會靜默作廢 36 題 GT。
- **不要新增會命中防禦題探針的欄位／表**：運費/freight/shipping_fee（`#68`）、
  利潤/profit/margin/supply_price（`#70`）、週轉/turnover/inventory/stock/capacity（`#140`）、
  信用評分/credit_score/額度/risk/score（`#141`）。
  擴到 80 張表的過程中就有兩張表因此被擋掉（`store_inventory`、`shipping_rates`）。

---

## 10. 被否決與擱置的方案（提新方案前先查這張表）

判決欄的 ✅ 表示**在 80 張表或之後的規模重測過**（86 / 93 那兩輪的重測在理由欄裡
標明是哪一個），⚠️ 表示結論來自小 schema、機制理由仍成立但數字未重測。

| 方案 | 判決 | 一句話理由 |
|---|---|---|
| 固定 K 檢索 | ✅ 否決 | K=5 對 141 題慷慨五倍，同時對最難的 12 題召回 0%（§2.3） |
| 動態門檻 / Elbow | ✅ 否決 | r=0.85 用 1.7 錨點換 69.2%，K=3 用 3.0 錨點拿 86.6%（§2.3） |
| Top-6 再剪枝 | ✅ 否決 | 剪枝加不回不在名單裡的表 —— 那 12 題 K=8 召回還是 0%（§2.3） |
| 欄位裁剪 | ✅ 否決（86 張表後理由更強） | 80 張表時 K=4 還能 100%；86 張表 / 22 題寬表題之後**沒有一個 K 是滿分**（K=4 漏 13 題、K=5 漏 9 題），而失敗是靜默的（§2.6） |
| 列數／熱度先驗（A） | ✅ 否決 | 候選召回每個 K 都變差，換到的 Top-1 買的是已經歸零的問題（§2.7） |
| 欄位優先並聯（D） | ✅ 否決 | 80 張表上已經不做事，Top-1 還掉 1.6pp（§2.7） |
| 向量資料庫 | ✅ 否決 | 80 個向量、實測 36 ms。下一步是 numpy 不是 Milvus（§2.8） |
| `CANDIDATE_N` 改成函數 | ✅ 否決 | 沒有上限＝這一層不存在（§7.2） |
| Hybrid BM25 + RRF | ⚠️ 否決 | 每個 K 都比純 dense 差；今天文件中位只有 60 字元，對 BM25 更不利（§2.8） |
| LLM 合成 table docs | ⚠️ 否決 | 稀釋定律，K=3 87.1% → 82.7%（§2.5） |
| 概念群文件（一表 N 份短文件） | ✅ 否決 | 86 張表實測：寬表誤選 6 → 19、@5 −4.0pp。**稀釋來自「機會數」不是長度** —— 拆五份等於給那張表五次取 max 的機會（§2.7b） |
| 把 coverage 報的缺口全部補進註解 | ✅ 否決 | 17 個缺口全補 vs 只補有失敗證據的那一個：**`#263` 排名都變 1**，但全補讓 @5 −1.8pp、27 題排名退步（§2.5） |
| Hubness 校正（CSLS，救概念群用） | ✅ 否決 | λ 掃 0.25~1.0、split-half、現行也當對照：**沒有一組贏過現行**。hub 在這個 schema 裡是 `orders`/`customers`/`products`，**中心性就是相關性**，扣掉等於扣訊號（§2.7c） |
| D 欄位優先並聯 | ✅ 否決（86 張表重測） | 唯一有效的 γ=1.0 讓 @3 掉 2.6pp；γ=0.5 兩臂是無效臂（§2.7） |
| enum 值對照進檢索文件 | ⚠️ 否決 | 同上，87.1% → 86.3%（§2.5） |
| 欄位級嵌入 max-pool | ⚠️ 否決 | Top-1 更準但**錯得跟 LLM 一致**，保險失效（§2.4、§2.5） |
| 候選清單加欄位（六種呈現） | ⚠️ 否決 | 每一種都比不給差，且對寬表題零幫助（§2.5） |
| 概念短語列進嵌入 | ⚠️ 否決 | 限定詞在向量空間是**加強誘餌**（§2.5） |
| 中英對照字典進 COLUMN_COMMENT | ⚠️ 否決 | COLUMN_COMMENT 到不了選表層；生成器本來就會翻譯（§2.8） |
| 選表改用 20B | ⚠️ 否決 | 召回略低、**延遲更高**，錯的性質更不可靠（§2.8） |
| **多次取樣取聯集（B）** | ✅ **採用（2026-08-28 推翻 80 張表時的否決）** | 舊理由「聯集治隨機不治系統」預設了「系統性漏表」不存在 —— `*_profiles` 進來之後它存在了。93 張表：+KMB 97.1→98.5（9 救 **0 丟**，p=0.0020）、e2e 逐題配對 **7 救 0 丟**（p=0.0156，只算兩臂輸入真的不同的 173 組）、**雜訊對照組 6:4 p=0.75 平手**、防禦題 6 格 4/4。代價 +9.4% 表數、3× 選表呼叫（§2.7i） |
| 加業務規則 | **硬性否決** | 規則表達不了情境相依的邊界，會跟 few-shot 打架（§4） |
| **打散候選順序（B′）** | **採用** | 召回區間不重疊，零成本（§2.8） |
| **表註解補強** | **採用** | 四個演算法方案全沒兌現，一句表註解修好 `#104`（§2.5） |
| **窄拒答規則 + 正向邊界** | **採用** | 代價可測為零、收益不可測時，代價為零就是理由（§4.3） |
| **樞紐表保留席** | ✅ **擱置（最便宜的解）** | **R=2（customers、products）→ 候選@40 100.0%**，Top-1 零損傷、零 LLM、零嵌入。⚠️ **R=1 反而更差**（98.8%），要嘛保到夠要嘛不要開始（§7.2） |
| **值／實體檢索（C）** | ✅ 擱置（觸發條件已改寫） | 候選@40 也到 100.0%。它與保留席**修的不是同一種病**：`#40`「買過 iPhone 15」這種「問句點名實體的值」只有它救得動（排名 43→4），改註解與 KMB 都無效。**觸發條件從「漏幾題」改成「這類題型佔比」**（§7.6） |
| **在任何一層加「寬表更值得看」的訊號** | ✅ **否決（總結五次）** | 嵌入層（§2.7b/§2.7h）、目錄層（§2.7e/§2.7f）、指令層（§2.7g）各試過，**五次探針全勝、五次全庫不兌現**。機制是雙向軸：誘餌題失敗＝模型不肯用寬表，窄表題成功＝模型不亂用寬表，同一個旋鈕同時推兩端。**要提第六次，先拿出一個能區分「這題該用寬表」與「這題不該用寬表」的訊號** —— 表註解相似度、欄位餘弦、屬性類型描述三種代理都已證明不是（§2.7g、§5.5）。**§2.7i 的投票聯集不是這一類** —— 它沒有告訴模型「寬表更值得看」，它是把同一個模型問三次再取聯集，買的是變異數不是偏好，所以不會推兩端。**§2.7j 是第六次，也否決了**，但它是六次裡診斷最完整的：第一次在同一次量測裡同時拿到救回（`#284` `#282` `#292`）與弄丟（`#258` `#202` `#249` `#248`）的逐題機制。**要提第七次，先解決 `#258` 這一類 —— 挑對表時是解藥、挑到近義母表時是毒藥，而兩者用的是同一個欄位餘弦分數** |
| **寬表註解的關係宣告句拆出檢索層** | ✅ 否決 | 12 題探針三組全贏（誘餌 3/32→9/32、窄表 34/40→40/40），**全庫 305 題 × 各 3 輪逐題配對是 14:14 / 9:9，p=0.57 / 0.59**。指路標在→忘了寬表、不在→忘了母表，仍是 §2.7g 那條軸（§2.7h） |
| **縮 `CANDIDATE_N` 換回目錄長度** | ✅ **否決（2026-09-03）** | 開了欄位提示後目錄 3,096 → 5,568 字元，遠超 §8.9 量過的範圍，所以想縮 N 換回來。三輪實測 `N=25 k=3` 是 **97.20%**，比 `N=40 k=2` 的 98.27% 差、甚至低於不開提示的 97.41%。三個事前假設全被推翻：弄丟的 13 題**沒有一題**是因為表掉出候選（最深第 19 名）；C 的目錄**更短**卻更差；`k=3` 單獨測是 p=0.363 的平手。**沒有機制就不再調這個常數**（§2.7l） |
| **欄位提示只給三張選票裡的一張** | ✅ **否決（2026-09-01）** | 假說對了一半：漏斗層 4 好 1 壞，`#94`／`#100` 這兩題「單票提示會弄壞」的題**真的被聯集吃掉了**。但 e2e 是 **5 好 7 壞**，而且 `#258` 是 `3/3 → 0/3`、三輪都輸、都是多給了 `product_specs` —— 可重現的回歸，不是硬幣。**提示每次只多給一張表：是答案就救回（`#284` 0/3→3/3），是近義母表就死掉**，同一個餘弦分數決定兩邊（§2.7j）。**⚠️ 2026-09-02 更新：這條的否決理由已經不成立。** §7.12 把 35 條同義欄位矛盾對齊之後，`#258` 在 `COLUMN_HINT_K=2` 下是 **8/8**（當時 3/3 → 0/3）——**毒性來自資料層兩欄說不同的話，不是檢索層多給了一張表。**解藥組 6/32 → 24/32，獨立分層對照組 132/144 → 136/144、64 格窄表靶零回歸、防禦題 16/16。**2026-09-03 全量三輪 × 兩臂**：97.41% → **98.27%（+0.86pp）**，B 臂最差一輪贏過 A 臂最好一輪、六輪完全不重疊；`#284` 0/3 → 3/3、三輪都錯從 2 題降到 1 題；過度拒答 1/305 → 0/305；防禦題兩臂全 4/4。四題回歸**沒有一題能歸給提示**（三題兩臂選表完全相同，一題是既有硬幣）。**狀態：否決理由不成立，待決** —— 兩個檢定都壓在 0.05（輪次排列 p=0.050、逐題符號 p=0.059），`COLUMN_HINT_K` 尚未改預設值（§2.7k） |
| 兩層 DDL + 反應式補完 | 擱置 | 真要裁欄位的那一天走這條，不要賭命中率（§2.6） |
| **few-shot 條件式注入（向量檢索每題給 3 則）** | ✅ 否決 | 先量代價：A（24 則）vs B（0 則）全庫兩臂配對，溢出候選 6 題**重抽 n=8 一題都沒活下來**（最大 `#296` 2/8 vs 6/8，Fisher p=0.13 且未扣選擇偏誤）；`#264`/`#270` 兩臂**錯法一模一樣**＝擲硬幣不是溢出。反向的貢獻側 17 題有機制（`#93` MySQL 沒有 `PERCENTILE_CONT`、`#8` `MONTH()` 跨年合併）。**買 ~0，賭 17**（§4.5） |
| few-shot 表名抽象成 `<表A>` | ✅ 否決 | 量不到污染（15 題裡 5/8 張表根本不在 few-shot 裡），而抽象化會拿掉「形狀對齊」賴以成立的具體性（§4.4） |
| few-shot 也做檢索（只送相關的幾則） | 擱置 | few-shot 已佔 prompt 42%，問題是真的。但**必須依形狀檢索而非題目相似度**，而且成對的教訓不能被拆開（§4.1、§4.2）。觸發條件：few-shot 超過約 20,000 字元 |
| 概念群做成獨立檢索單位 | 擱置（唯一沒被否證的方向） | 解 §2.5 的「一張表一句註解」容量上限。與失敗的欄位級 max-pool 不同：5 個人工概念群 vs 57 個原始欄位。**前置條件是先有夠多寬表**（§2.6） |
| 階層式選表 | 擱置 | 候選目錄今天約 3,300 字元，離 needle-in-haystack 還很遠（§2.8） |
| Cross-encoder reranker | 未量 | 輸入簽名與 dense 相同 (query, 表文字)，**「營收存在 order_items 裡」不在任何一邊的字串裡**（§7.5） |

**三條反覆出現的紀律：**

0. **探針只能否決，不能放行**（§5.5）。這張表上五列「✅ 否決」的證據強度不同：
   零 LLM 的確定性量測（固定 K、elbow、CSLS、概念群、A/D 變體）可以直接信；
   靠手挑靶題 n=8 拿到的**正面**結果一律不算數，因為靶子是選在依變項上的。
1. **方向對不等於時候到。** 擱置的四項全部是「機制講得通、實測方向也對，
   但今天買到的東西太小」—— 在量不出效果的時候做改動，就是做一個無法驗證的改動。
   每一項都寫了觸發條件與零成本的重測方式。
2. **判決本身有保鮮期。** C 方案就是在 80 張表重測時從「沒東西可買」變成
   「有東西可買」的 —— 擱置的理由是當時的數字，數字變了要回來重讀。
