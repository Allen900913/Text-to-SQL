# 架構與設計決策

這份文件記錄的是**為什麼不是別的做法** —— 每個結論附實測數字，包含被否決的方案。
沒有數字支撐的理由不寫。這個專案裡「聽起來很有道理」的想法，實測後約一半是反的。

**怎麼讀**：§0 是今天的狀態，§1~§4 是設計，§5~§6 是評估紀律，§7 是規模、資料衛生與邊界，
**§8 是反覆咬人的失敗形狀（這份文件最貴的部分）**，§9 是今天的缺陷，
§10 是被否決方案總表 —— **想提新方案先查那張表**。

> ⚠️ **託管模型換過一次版。** 供應商停止解析無前綴的模型別名，改用
> `nvidia/nemotron-3-super-120b-a12b` 之後延遲 15s → 38s、準確率掉約 1.7pp，
> 錯的形狀也換了（變成 SELECT 選錯欄位）。**換版之前量的 e2e 數字全部作廢**，
> 本文件只保留換版之後的數字。任何跨時間的比較都要先確認服務端沒有再換過
> —— 而它換版不會發任何警報（[[baselines-die-when-the-model-changes]]）。

---

## 0. 現況

```
schema   93 張表 / 1071 欄位｜全庫 DDL 77,474 字元
         >30 欄 14 張（兩批 *_profiles 寬表）｜最寬 57 欄｜中位表寬 5 欄
GT       309 題｜11 張表宣告 never_answered｜防禦題 4 題｜alt_sql 50 題
prompt   few-shot 24 則 / 12,858 字元｜business_rules 10 條 / 6,381 字元
         enum_fields 52 項（手寫 6 ＋ 產生器 46）
基準     六輪 × 309 題：96.8 ~ 98.7%｜防禦題每輪 4/4｜約 30s/題、一輪約 2.5 小時
穩定錯   0 題（這個專案第一次做到）
```

**已上線的四個旋鈕，每一個都可以位元還原成上線前的行為**（A/B 兩臂必須落得進同一個 commit）：

| 旋鈕 | 值 | 還原 | 依據 |
|---|---|---|---|
| `FILTER_VOTES` | 3（選表投三票取聯集） | `=1` | §2.7 |
| `VALUE_BETA` / `VALUE_EVIDENCE` | 0.05 / 1（值索引） | `=0 =0` | §2.8 |
| `COLUMN_HINT_K` | 2（候選目錄加欄位提示） | `=0` | §2.9 |
| `enum_fields` 52 項 | 預設 | `SEMANTIC_LAYER_PATH=utils/semantic_layer_enum0.yaml` | §4.6 |

閘門十三項全綠：

```
[1]~[8]  schema 鏈（DDL 覆蓋／新鮮度／註解同步／regex 可解／KMB 成本／
                    覆蓋與宣告／沒有空表）＋ prompt 鏈
[9]  縱向衍生量一致性   一致 29｜已宣告 2｜黃 0｜紅 0
[10] 題目層多來源歧義   分歧名單 11 題（迴歸測試用，不是缺陷清單）
[11] 橫向謂詞衝突       待修 0｜已修 1｜未裁決 0｜靜音 7｜一致 8
[12] 逐表可檢索性       紅 0｜🟠 橙 1（products，靠值索引撐著）｜黃 0｜量不到 11 張
[13] 題目形狀可判定性   命中 20 題，全部裁決完畢
```

**今天真正剩下的東西**：沒有穩定錯；硬幣池約 9 題；兩個沒有指標在盯的方向
（過度拒答、漏斗「排除」那一半的價值）；以及一條沒量過的缺口
（檢索漏表而模型**沒有伸手**、直接用手上的表湊出答案）。細節見 §9。

---

## 1. Pipeline 總覽

```
context_retriever → sql_generator → ast_validator → db_validator
       ↑ (retry)                          ↓ (通過)
       └──────────────────  executor_voter → final_summarizer → END
```

| Node | 職責 | 模型 |
|---|---|---|
| `context_retriever` | 從 93 張表裡決定這題要看哪幾張，組出 Prompt | 見 §2 |
| `sql_generator` | 產生 SQL | gpt-oss-120b |
| `ast_validator` | sqlglot 靜態快篩（安全性 ＋ 欄位白名單 ＋ 範圍） | — |
| `db_validator` | MySQL `EXPLAIN` 驗證 | — |
| `executor_voter` | 執行、取結果 | — |
| `final_summarizer` | 結果轉繁體中文 | gpt-oss-20b |

三個非顯然的設計：

- **重試一律回 `sql_generator`，不回 `context_retriever`。** 重試是因為 SQL 寫錯，
  不是表選錯；重跑檢索只會多花一次 LLM 呼叫得到完全相同的表。
  **副作用要知道：表的集合一旦定了，整題就凍住了，選錯表沒有第二次機會。**
- **錯誤訊息會加工**（`_augment_schema_error`）：EXPLAIN 報欄位不存在時，把那張表的
  真實欄位附回去。模型猜錯欄位時不是被打回票，是拿到正確答案再寫一次。
- **`ast_validator` 是 fail-open**：解析不出歸屬的欄位一律放行。誤擋的代價遠高於
  漏擋 —— 漏擋後面有 EXPLAIN 與實際執行接住，誤擋直接吃掉重試預算。
  （`SELECT p.*` 曾被 sqlglot 解析成 `Column(name='*')` 而誤判成幻覺欄位，
  三次重試被同一個誤判吃光。`test_ast_validator.py` 9 項的主體是**誤判**不是漏判。）

---

## 2. 檢索層：為什麼是三段漏斗

### 2.1 問題

全庫 93 張表的 DDL 是 77,474 字元，而 309 題**平均每題只需要 2.0 張表**
（中位數 1 張，超過一半的題只需要一張）。不做檢索，模型每次要讀數十倍於它需要的 schema。

### 2.2 設計

```
使用者問題
   │
   ├─① 相似度收斂    93 張 → CANDIDATE_N = 40 張                table_retriever.py
   │                 （每題砍掉 53 張，候選只涵蓋 43%）
   │
   ├─② LLM 選表      從 40 張候選裡「選」出邏輯上必要的表        table_filter.py
   │                 錨點 = LLM 的選擇 ∪ 相似度第 1 名
   │                 候選順序依問題雜湊**打散**（不照相似度排）
   │
   └─③ KMB 補橋      Steiner tree 近似，補上串接用的中介表      schema_graph.py
```

實測（80 張表那一輪，機制與比例今天不變）：

| | 召回 | 平均帶進的表 | 佔全庫 | 淨度 |
|---|---|---|---|---|
| 純相似度 K=3（＋KMB） | 86.6% | 3.7 | 4.6% | 39.8% |
| **三段漏斗** | **99.2%** | **2.0** | **2.5%** | **86.1%** |

**召回高 12.6pp，同時表數少 46%。** 這兩件事在固定 K 的世界裡是對立的
（K 開大兩者一起上升），能同時改善是因為換掉的不是參數，是**憑什麼決定要幾張表**。

一個值得記的規模性質：**schema 從 41 張長到 93 張的過程中，
每題進 prompt 的表數一直是 2.0 張。** 資料庫翻倍，送進 prompt 的內容沒有變多 ——
這正是這一層要買的東西，而它只有在表夠多的時候才量得出來。

**打散候選順序是零成本的採用**：固定順序與打散順序的召回區間完全不重疊。
這推翻了「照相似度排序等於免費給 LLM 一個先驗」—— 那個先驗是**淨負的**。
今天用問題的 CRC32 當 seed：同一題永遠同一種順序（可重現），不同題順序不同（不固化成新偏誤）。

### 2.3 為什麼不是固定 K、動態門檻、或剪枝 —— 三個都死於同一個根

**相似度知道「這題關於客戶」，但不知道「算總消費必須 JOIN orders」。
那是結構必要性，不是文字相似度，相似度在原理上看不到它。**
所以任何純粹在相似度分數上做的切法都救不了 —— 包含門檻、elbow、加權。

**固定 K：同一個 K 同時犯兩個相反的錯**

| 實際需要 | 題數 | K=3 | K=5 | K=8 | K=14 | K=5 給的浪費倍數 |
|---|---|---|---|---|---|---|
| 1 張 | 144（56.9%） | 95.8% | 98.6% | 99.3% | 100.0% | **5.0×** |
| 2 張 | 86（34.0%） | 62.8% | 75.6% | 86.0% | 96.5% | 2.5× |
| 3 張 | 11（4.3%） | 9.1% | 27.3% | 54.5% | 81.8% | 1.7× |
| 4 張 | 12（4.7%） | **0.0%** | **0.0%** | **0.0%** | 25.0% | 1.2× |

**K=5 對超過一半的題目慷慨五倍，同時對最難的 12 題召回是 0。**
要讓那 12 題全中得開到 K≈40，那時前面 141 題各拿到 40 倍於需要的 schema。
K 開大改善下面惡化上面，K 縮小反之 —— **沒有任何 K 值能同時解決。**

（K 曾經被 3 → 6 → 4 反覆調整，每次都是在同一個資料集上挑最高分。
那不是設計，是過擬合 —— 這是本專案第一條方法論教訓。）

**動態門檻 / Elbow**：餘弦分數根本沒有 elbow。r=0.85 用 1.7 個錨點換到 69.2%，
而 K=3 用 3.0 個錨點拿到 86.6% —— 每一個軸都輸。
更關鍵的是**它漏的是第二個錨點，而那連帶關掉了第三段**：單一錨點時
KMB 在數學上不可能作用（單一終端點的 Steiner tree 就是那個點自己）。

```
K=1   錨點召回 49.4%  →  +KMB 49.4%   ← 一張表都補不回來
K=3   錨點召回 76.3%  →  +KMB 86.6%   ← 兩個以上錨點，KMB 補回 10.3pp
```

**剪枝（Top-6 再讓 LLM 刪）**：剪枝沒辦法把不在名單裡的表加回來 ——
而那 12 題需要 4 張表的難題，問題正是需要的表從沒進過 Top-K（K=8 召回還是 0%）。
所以正確的形狀是「LLM 從候選裡**選**」，不是「從短名單裡**刪**」。

> **這個認識就是三段漏斗的由來：既然缺的是推理，就補一層會推理的東西。**

**`CANDIDATE_N` 也不改成隨表數走的函數。** 曾改成 `max(40, 0.8 × 表數)` 又改回固定 40：
這一段漏斗被建立的目的就是「不管資料庫多大，送進 LLM 的表數有上限」，
`0.8 × 表數` 沒有上限，只是換一個比較慢的斜率把同一個問題往後推。
而且它把「dense 召回不夠」偽裝成「候選開大一點就好」——
**開大候選買到的是雜訊，不是召回。**

### 2.4 ∪ 相似度第 1 名：保險要的是誤差獨立，不是自己準

LLM 偶爾漏掉問題裡**明講**的主體，而那恰恰是相似度最不會錯的部分。

| | 錨點召回 | +KMB | 平均表數 | 淨度 |
|---|---|---|---|---|
| LLM 選表單獨 | 96.4% | 98.0% | 1.8 | 90.7% |
| **∪ 相似度 Top-1** | **97.6%** | **99.2%** | 2.0 | 86.1% |

救回的三題形狀完全一致：**LLM 選了「主體表」，而問題真正要的是掛在它旁邊那張更具體的表。**
餘弦剛好相反 —— 它對字面最貼近的那張表最敏感。**這就是誤差獨立在做事。**

> **冗餘元件要的是誤差獨立，不是個別準確度。**
> 而誤差獨立來自**機制不同**，不是來自輸入不同：餘弦只會比對相似度，
> LLM 會推理「物流狀態 ≠ 訂單狀態」—— 同一份文字，兩種讀法。

這條原則後來反覆用到。欄位級嵌入 Top-1 更準，但它把表註解複製進每個欄位 chunk，
讀的東西跟 LLM 高度重疊 —— 更準，但**錯得更一致**，保險失效。

⚠️ 這道保險也是唯一一個**在結構上就做了配對比較**的實驗：兩臂共用同一組 LLM 呼叫，
聯集是離線算出來的，跑次雜訊自動消掉。**本文件唯一還站得住的 LLM 層正面結論，
正好是唯一一個做了配對的實驗**（§5.5）。

### 2.5 檢索文件與候選目錄裡放什麼：只放表名 + 表註解

檢索文件中位數 **60 字元**（最短 24、最長 167）—— **長度與表寬完全脫鉤**。
全庫最寬的表是 57 欄，它的檢索文件跟一張 4 欄的表一樣短。

這是刻意的。文件長度一旦是表寬的函數，兩條互為鏡像的機制就會同時發作：

> **往所有文件加字 → 大家都變像**（稀釋定律：所有表對任何查詢都有點像）。
> **往一份文件加字 → 那一份到處排第一**（57 欄的寬表誤選 24/139，
> 而肇事者 100% 是餘弦，LLM 一次都沒被騙）。

五個獨立實驗收斂到同一點：

| 加了什麼 | 結果 |
|---|---|
| LLM 合成的「典型問法」 | K=3 87.1% → 82.7% |
| enum 中文狀態值對照 | 87.1% → 86.3% |
| 全部欄位名 + 註解進檢索文件 | Top-1 84.9% → 76.3%，寬表誤選 9 → 24 |
| 欄位進候選目錄（六種呈現） | 錨點召回 94~95% → 87.7~92.9%，對寬表題零幫助 |
| 含限定詞的概念短語 | Top-1 83.9% → 80.6%，寬表誤選 10 → 22 |

三個與規模無關的結論：

1. **檢索文件長度必須與表寬無關。** 第一版修法是「替寬表的欄位清單設上限」，
   那只是壓小症狀還多一個憑感覺的常數。根治是只放表註解 ——
   **寬表問題從「被壓小」變成「結構上不可能發生」**。
2. **限定詞在向量空間裡是加強誘餌，不是警告牌。** 加上「（非即時，落後 30 天）」
   之後誤選反而增加 —— 模型只看到我又多塞了幾個「消費金額」token。
3. **描述寫得更好能救「盲區」，救不了「誘餌」。**
   盲區＝概念根本沒被提到，加字就能解；誘餌＝概念被提到了但不是你要的那一個，
   加字只會讓誘餌更顯眼。**這是機制性的上限，不是措辭問題。**

**所以提升檢索只剩一條路：把 `TABLE_COMMENT` 寫好。**
檢索向量、LLM 候選目錄、DDL 現在共用這同一個權威來源。
註解的唯一來源是 `init_db*.py` 的 `CREATE TABLE`，用
`tools/sync_table_comments.py --apply` 同步（純 metadata，不動一位元資料）。

#### 盲區補強：一個可重複的程序

「付款失敗」與「Email 驗證」兩題是同一個形狀：
**問句的關鍵詞字面出現在錯的那張表上，正解表的註解裡沒有。** 修法：

1. 印出實際餵給生成器的 `schema_ddl`，確認缺的是**表**還是**欄位**
2. 比對正解表與競爭表的 `TABLE_COMMENT`
3. 改 `init_db*.py` 的 COMMENT，`sync_table_comments.py --apply`
4. **補概念家族，不補那一題的關鍵詞**（避免 teaching to the test）
5. 跑稀釋鏡像檢查：新舊註解各嵌入一次頭對頭，看 Top-1／誤選／進前 3／搶 Top-1
6. `eval_stability.py --ids <目標題> + <同表其他題>`，確認修好且無回歸

**往干擾表加指路標沒有用** —— `payment_attempts` 的註解本來就寫著
「成功的付款結果在 payments」而完全無效。**要補的永遠是正解表的盲區。**

**兩支偵測器，方向相反，缺一不可：**

| | 驅動來源 | 抓得到 | 抓不到 |
|---|---|---|---|
| `check_comment_blindspot.py` | **問句**（GT SQL 就是「哪張表才對」的答案） | **誘餌**：關鍵詞字面出現在錯的表上 | 還沒被問到的概念 |
| `check_comment_coverage.py` | **欄位註解**（離線 LLM 歸納概念群） | **盲區**：整組概念在表註解裡不存在 | 誘餌（概念寫了但別張表更像） |

⚠️ **blindspot 的 n-gram 曾經是壞的**：`findall(r"[一-鿿]{2,6}")` 取的是
**貪婪、不重疊的定長切塊**，不是 n-gram。「對帳單勾稽」被切在兩塊中間，
兩塊都不存在於任何註解，於是靜默漏掉 —— **一個概念抓不抓得到，取決於它在問句裡的位元組位置**。
（舊輸出裡那些長得很怪的概念詞「戶姓名」「易幣別」「冊時間」全是切塊的殘骸。）
改成真滑動視窗之後，命中從 67 筆變成 302 筆（22 誘餌 / 280 盲區）。

#### 一張表一句註解的容量上限

**把 869 個欄位註解各當成一句問句**，量它自己的表排第幾（欄位註解是問到該欄位所能
寫出的最貼題措辭，所以是保守下界）：

```
全部         869 欄｜中位 3｜第 90 百分位 27｜排不進前 40 的 51 欄 = 5.9%
寬表 profile 613 欄｜中位 5｜第 90 百分位 28｜排不進前 40 的 34 欄 = 5.5%
其餘 79 張   256 欄｜中位 1｜第 90 百分位 23｜排不進前 40 的 17 欄 = 6.6%
```

**天花板是真的，但只有 5.9%，而且寬表沒有比較慘（5.5% vs 6.6%）。**
後者推翻了「45 欄的表一句註解代表不了全部概念」這個直覺 ——
寬表的註解比較長（91~140 字），反而蓋得比較廣。

但**這個上限的另一半是真的**：全庫 302 筆盲區集中在 15 張 `*_profiles` 寬表
（每張 8~27 筆）。一張表一行註解裝不下 20~50 個欄位的概念，
而稀釋定律說不能把每張表都養胖 —— **所以這 302 筆不可能靠改註解清完。**

而且「有缺口就補」是錯的做法。兩個補法頭對頭：

| 設定 | @3 | @5 | @20 | Top-1 | 寬表誤選 | 目標題排名 | 既有題排名 |
|---|---|---|---|---|---|---|---|
| 現行 | 72.4 | **82.2** | **97.1** | **82.9%** | **16** | 10 | — |
| **窄補**（只補有失敗證據的那一組） | **72.7** | **82.5** | **97.1** | 82.5% | 18 | **1** | 好 6 / 差 2 |
| 全補（17 個缺口全補） | 72.4 | 80.4 | 96.4 | 81.8% | 17 | **1** | 好 6 / **差 27** |

**買到的東西一模一樣，代價差 13 倍。判準是有沒有失敗證據，不是有沒有缺口。**
窄補的寫法也刻意是**壓縮而不是追加**：把「客服接觸紀錄」改寫成
「客服接觸與風控人工覆核」，註解只長 5 個字。

#### 表註解裡的例子只能取自資料，不能取自題目

`products` 曾經在「問句點名具體商品」的題上排到第 62 名 ——
它的檢索文件從來沒有宣告「具體商品名住在這裡」。第一版補救時舉的例子
**正是那三題問句裡的字串**，改善幅度跟「註解逐字抄了測試題多少」成正比：

```
                          #21   #2   #40  #39
現況                       62    48   47   40
只講規則不舉例             33    40   36   20
舉例＝題目裡的商品名        3     1    1    1   ← 洩題
舉例＝題目沒出現過的商品名  35    23    8    3   ← 誠實的數字
```

**大約一半的改善是背答案。** 保留組（商品名都在資料庫裡、都不在任何題目裡）才是
可以拿去推廣的版本。**防呆的辦法是每一版都跑保留組** —— 換掉例子再量一次，
差多少就是過擬合了多少。

**泛化是真的，代價也是真的。** 20 題全新問句（值全部取自資料庫、不在題庫與註解裡）：

```
                                      需要的表最差排名   products 自己的排名
A 引號點名商品（保留組名字）           27.4 →  7.8        27.4 →  7.8
B 需要 products 但沒點名                2.0 →  2.0         2.0 →  1.8
C 完全不需要 products                   1.2 →  1.2        49.2 → 39.4
D 點名的是別的表的值（風險組）          4.0 →  4.2        47.6 → 34.4
```

A 證明學到的是「商品名住在 `products`」這個事實；C 與 D 證明 `products`
變成「任何被點名的東西」的吸子 —— 問供應商的電話，`products` 排第 2 還把
`suppliers` 擠掉一名。**這個代價會隨著「有名字的表」變多而惡化。**

#### 嵌入不懂否定

想收窄邊界時明寫「門市/供應商/活動/方案的名稱不在這裡」，
結果 `products` 在「問供應商」的題上排得**更前面**（漏出 34.4 → 25.4）。
那句話把那四個詞**加進了 `products` 的文件**，而嵌入模型不做否定。

> **在檢索文件裡劃邊界，只能用「我是什麼」，不能用「我不是什麼」。**

這也是後來改走值索引的直接理由（§2.8）：值索引知道「宏達貿易」在 `suppliers.name`，
不需要泛稱、不吃檢索文件的預算、不需要有人盯。

⚠️ **表註解裡舉的例子會把該值踢出值索引**（`_keep` 把出現在任何註解裡的值判為概念詞）。
所以那句泛稱不只是零和，它在扯值索引的後腿；值索引上線後已經刪掉。
**表註解從此不該再舉具體實體名當例子。**

### 2.6 欄位裁剪：沒有一個 K 是安全的

`eval/eval_column_recall.py`，270 可評估題、93 張表。掃「非鍵 Top-K × 寬度閘門 W」：

| K | W | 會裁的表 | 題目欄位全中 | 欄位召回 | 每題省 |
|---|---|---|---|---|---|
| 3 | 0 | 86 | 92.6% | 95.5% | 572 字元 |
| 4 | 0 | 86 | 95.2% | 98.0% | 517 字元 |
| 5 | 0 | 86 | 96.7% | 98.7% | 473 字元 |
| 4 | 12 | 8 | 95.2% | 98.0% | 480 字元 |

**80 張表時 K=4 是 100.0% / 100.0%，現在沒有任何一組是滿分。**
變的不是演算法，是題目：22 題寬表題進來之後，K=4 漏欄的 13 題**全部**是新題。

> **舊題庫給的 100% 是「沒人問過那些欄位」，不是「裁剪安全」。**
> 一個沒有反例的守門員，量到的是題庫的形狀不是系統的能力。

四個不做的理由：

**① 失敗模式比表裁剪糟得多，而且是靜默的**

```
表沒給   → 答不出來 / EXPLAIN 報錯 / 空結果      ← 會叫
欄位沒給 → 模型挑一個看起來像的，SQL 合法、
           EXPLAIN 過、結果錯                    ← 不會叫
```

而欄位層級的「語意相近替代品」遠比表層級密集（`created_at`/`paid_at`、
`price`/`unit_price`/`amount`）。

**② 固定 K 已經死了，不是「將來會死」。** K=4 漏 13 題、K=5 仍漏 9 題，
而要漏的題只要問四個以上欄位就會漏（一題就要 5~6 個非鍵欄位）。
**這是 §2.3 原封不動搬到欄位層級。**

**③ 沒有預算危機。** 每題 scoped DDL 的 p90 是 3,337 字元（寬表題也只到 6,836）。
而「欄位給太多模型會挑錯」這個前提**被否證過三次**，最直接的一次是 57 欄的寬表
整份丟給生成器（約 4,500 字元）而那 14 題一題沒錯。
（兩層 DDL 的觸發條件是 p90 超過 10,000。）

**④ 省下來的是「不會出事的字元」。** 每題省約 500 字元，而多出來的節省
**正好來自新寬表**，也正好是那 13 題漏欄的地方。**省得多與漏得多是同一件事。**

真要做的那一天，已經量清楚的三件事：

- **鍵一律保留，只對非鍵欄位評分。** 763 欄有 182 個是鍵（24%），
  而 `customers.id` 跟任何中文問題都沒有語意相似 —— 它的必要性是結構性的。
  **結構性資訊不能交給語意評分決定。**
- **加寬度閘門**：W=12 只碰 8 張表就拿到 480/517 的節省。風險面積小，
  但**它守不住新寬表** —— 那八張正是漏欄的來源。
- **不要用 Elbow**（餘弦分數沒有斷崖，欄位層級只會更碎）。

而正確的架構不是把賭注押在命中率，是**讓失敗變得有聲**：精簡層（鍵全留 + 非鍵
Top-K + 一行「本表另有 N 欄未列出」）＋ 完整層（模型可主動索取完整清單，
`_augment_schema_error` 已經是它的被動版雛形）＋ AST 白名單不跟著裁。

### 2.7 選表投三票取聯集（`FILTER_VOTES=3`，已上線）

**問題**：`*_profiles` 寬表進來之後，選表層開始**系統性地漏掉**某一群表。

**為什麼是聯集不是多數決**：這一層的錯是**單向**的 —— 漏一張表這題就死了，
多一張表只是 Prompt 長一點。逐題拆開，失手的題分成「三輪都漏同一張」（穩定）與
「三輪漏一次兩次」（硬幣）兩群，**聯集只動得了後者**。
多數決會把硬幣題的少數正確票丟掉，剛好丟掉唯一有價值的那一票。

**實驗設計的關鍵：兩臂共用同一次選票採收。**
`_vote_seeds(query, n)` 的契約是「第 0 顆 seed 與 `votes=1` 走的那一顆位元相同」，
所以一次採收三張票之後 `A = 票0`、`B = 票0 ∪ 1 ∪ 2`，
**選表層零額外 API、零細胞間漂移**，而且附贈一個雜訊對照組（見下）。

```
漏斗層     錨點 95.1% → 97.4%｜+KMB 97.1% → 98.5%｜平均表數 2.35 → 2.57（+9.4%）
           逐題配對 kmb 救回 9 弄丟 0，p = 0.0020
e2e 三輪   95.96% → 97.38%
           全集 305 題配對：救回 12 弄丟 3，p = 0.0352
           「兩臂輸入真的不同」的 102 題子集：救回 7 弄丟 0，p = 0.0156
防禦題     6 個細胞全部 4/4
```

⚠️ `anchor` 那一行不是證據 —— 聯集 ⊇ 票 0，anchor 召回**只可能上升**，
「弄丟 0」是算術保證的。有資訊量的是 `kmb`：Steiner 樹在終端點改變時會換掉橋接節點，
**`+KMB` 不是單調的**，它的「弄丟 0」才是量出來的。

**全集的 12:3 有一半是雜訊。** 兩臂只在「聯集 ≠ 票 0」的題上收到不同輸入；
其餘的題兩臂逐字相同，只能貢獻 MoE 雜訊。按治療分組（不是按結果分組）限制到
102 題之後是 7:0。全集那三題「弄丟」逐輪查票庫，兩臂輸入是逐字相同的 ——
它們不是聯集的代價。**兩邊都要扣，不能只扣對自己不利的那邊。**

**雜訊對照組（這個設計最值錢的地方）**：輸入逐字相同的 283 題理論上必須平手，
不平手就代表 MoE 雜訊本身有偏、主結果要打折。實測 B 好 6 / A 好 4，p = 0.7539。

> **只要兩臂共用輸入，就順手把「輸入相同」那一群報出來。**
> 它幾乎免費，而且是唯一能回答「這個顯著是不是雜訊撿到的」的東西。

**防禦題閘門差點被自己的優化弄假**：票庫只有 305 題，4 題防禦題不在裡面會落回真實選表，
第一版兩臂都用預設 `votes=1` —— **那道閘門就完全沒有測到 B 臂**，
而防禦題正是「多給表」最危險的地方。改成照臂給 `votes`，代價只有 48 次呼叫。

#### 舊判決為什麼是錯的

「多次取樣取聯集」曾被否決，理由是「+KMB 零增益、3× 呼叫，聯集治隨機不治系統」。
那個判決在兩個地方錯了：**當時的 schema 裡沒有「會被系統性漏掉的那一群表」可漏**
（`*_profiles` 是後來才加的），而**單輪總分量不出雙向抵銷**。

我原本還主張這個實驗沒有頭寸可賺：B 夾在 A 與**天花板**（不做 schema linking、
93 張表全送）之間，而 A 與天花板實測平手。拆開天花板那次平手的逐題資料：

```
天花板 vs 現行，全庫 309 題      總分：平手
逐題拆開                        救回 19 題 / 弄丟 14 題 / 持平 276
```

**那不是「沒有東西在動」，是 33 題在動然後互相抵銷。**

> **總分平手有兩種，一種是 null，一種是抵銷。**
> 在拿逐題配對看過之前，不可以用「端點平手」推論「中間沒有頭寸」。

### 2.8 值索引（`VALUE_BETA=0.05`、`VALUE_EVIDENCE=1`，已上線）

**問題**：問句點名**實體的值**（商品名、書名、單號）時，問句裡沒有任何概念詞，
嵌入不知道「iPhone 15」是商品 —— **但資料庫知道，那就是 `products.name` 裡的一個值。**

**它是第四條通道，不是任何一條的超集**：

| 層 | 解的是什麼 | 值索引能取代嗎 |
|---|---|---|
| dense 表向量 | 問句用**概念詞**（營收、客訴、備貨天數） | ❌ 95.7% 的題沒有值可命中 |
| LLM 選表 | **結構必要性**（算營收必須 JOIN `order_items`） | ❌ 值索引不做推理，它只加分 |
| KMB 補橋 | 橋接表在問句裡**一個字都沒有** | ❌ 同上 |
| 值索引 | 問句點名**實體的值** | ✅ 只有這一格 |

**兩個接點，職責不同**：dense 層 `+β·min(命中數, 3)` 把表撈進候選；
LLM 選表層在目錄行後面附上事實。第二個接點幾乎是免費的 ——
原型的迴圈本來就是 `for (t, c) in cols`，最後 `idx[v].add(t)` **把欄位丟掉了**。
撿回來之後：**證據 67 條，表對 25，表且欄位都對 25 —— 表對的時候欄位 100% 也對**，
因為欄位不是猜的，是那個值真的存放的地方。

**報事實，不報建議。** 表層精準度只有 37.3%，本來要用這個數字勸退它。
把命中印出來之後結論反過來了：

```
「運動鞋」= products.name    ／「運動鞋」= search_logs.keyword     ← 兩個都是真的
「書籍」= products.category  ／「書籍」= categories.name           ← 反正規化並存
```

**每一條「假」證據都是真的。量到的是「相關性」，但這條證據不主張相關性。**

> **把命中位置全部列出來，讓 LLM 自己選。**
> 只挑一個報就變成「有主張的錯」，那在別的實驗裡是誘餌題 0/32 的形狀。

**噪音怎麼擋：概念詞不進值索引。** 原型 15.4% 的精準度幾乎全毀在一個地方 ——
`faq_articles` 有個分類值就叫「訂單」，而 305 題裡有 71 題的問句含這兩個字。
判準用既有的權威來源，不引進新常數：

> **這個值如果出現在任何一則 schema 註解裡，它就是概念詞，不收。**
> 「訂單」寫在 `orders` 的表註解裡；「iPhone 15」不在任何註解裡。
> **概念詞本來就是 dense 那一層的工作。**

**兩個原型設計被推翻：**

- **刪掉基數上限。** 原型只收相異值 ≤ 60 的欄位，那是一道無聲的懸崖：
  `products.name` 相異 40，商品目錄長到 61 項整條通道就消失且不報錯；
  被排除的還有 `invoice_no` 164、`tracking_no` 143、`barcode` 148 ——
  **正是值檢索在業界最主要的用途**。刪掉上限後索引 1,551 → 4,593 個值，建索引 5.7 秒。
- **不做磁碟快取。** 快取要正確就得有一個能偵測「資料被 UPDATE 過」的指紋，
  而列數與 schema 註解都抓不到就地修改。**一個會無聲失效的快取比沒有快取糟。**

**目錄增量：稀疏 vs 廣播**

| | `COLUMN_HINT_K=2` | 值證據 |
|---|---|---|
| 觸發 | **每一題、每一張候選表** | 有值命中才出現（13/305 題，4.3%） |
| 目錄增量 | +2,472 字元 | 中位 **+34 字元**、最大 60 |
| 欄位怎麼來 | 餘弦**猜** | 資料裡**查**到的 |

**猜的提示必須廣播，查到的證據天生稀疏。**

**e2e 判平手，仍然採用 —— 依據不是分數。** 兩臂各三輪，區間 94.5~96.1 vs 95.1~96.1
（A 的區間完整包住 B），事前登記寫明「重疊就判平手」，照辦。採用依據是零 LLM 的硬指標：

```
                      純 dense    ＋值索引
候選召回 @40（82 張）    81/82      82/82
閘門 [12] products 最差   62 名      33 名
GT 表排名 平均           3.66       3.28
```

外加一項**負成本**：值索引到位之後 `products` 表註解那句泛稱可以刪掉，
風險組的漏出 34.4 → 48.4。**買的是斜率不是截距** ——
今天 93 張表撈得到的，明天 200 張表撈不到。

**已知邊界**：只有落在候選清單裡的表看得到自己的證據。某個值的另一個歸屬
若在候選層就被砍掉，LLM 只會看到單邊，這一行事實上就變成了指向。

### 2.9 欄位提示（`COLUMN_HINT_K=2`，否決六次後採用）

**問題**：選表那一步**看不到欄位**。`get_table_briefs()` 只給註解，
於是模型得從一句散文去猜某張寬表裡有沒有某個欄位。而全庫 1071 個欄位 100% 都有註解，
且註解與問句幾乎逐字對應：

```
「中途改過地址」   ← shipment_profiles.is_redirected「是否中途改過送件地址」
「跟對帳單勾稽」   ← payment_profiles.is_reconciled「是否已與金流對帳單勾稽」
「超過可退期限」   ← return_profiles.is_over_policy_window「申請時是否已超過可退貨期限」
```

零 LLM 前置量測：八題裡七題正解欄位在全庫排 1~2 名，而且都是**自己表內的第 1 名**
—— 所以 `K=2` 就夠，不要再往上調（目錄 3,096 → 5,568 字元，這條曲線會咬人）。

#### 它被否決六次，而否決理由後來消失了

長期的否決理由是：**修好與弄壞來自同一個機制，不存在只留好處的參數設定。**
「總消費金額」那一族的 `customer_profiles.total_spent` 是**刻意埋的快照落後陷阱**
（欄位註解自己就寫著「與即時 SUM 可能不同」），欄位提示把陷阱變成明顯的捷徑，
於是那兩題 8/8 → **0/8**。門檻也切不開：0.302（錯）vs 0.239（對）。

重測時把陷阱家族擴大到六題（含兩題字面位移風險最高的窄表題），**兩臂全部 3/3，一格沒掉**。
零 LLM 探針確認**提示的文字一個字沒變**：同一個第一名候選、同一個第一欄、同一句註解。

> **改變的是模型讀提示的方式，不是提示本身。**
> 一個被六次實測支持的否決結論，在供應商換版之後失效了 —— 而它失效的方式不會發任何警報。
> **§10 這張表最需要的是定期重跑，不是辯論。**

#### 驗收（分兩段，第一段只能否決不能採用）

第一段是用「已知差異」挑出來的 18 題，所以**通過只代表「沒踩到已知的雷」**；
採用依據只能是第二段的全量。四條事前登記的判準全過之後才進第二段：

```
輪次（六輪 × 309 題）
A（K=0）  96.8 ~ 97.1%   穩定錯 4 題   硬幣池 14 題   防禦題 4/4
B（K=2）  98.1 ~ 98.7%   穩定錯 1 題   硬幣池  9 題   防禦題 4/4
```

**B 臂最差一輪贏過 A 臂最好一輪，六輪完全不重疊。** 淨格數 +13（好 16、差 6）。

**收益有兩條路徑，第二條原本沒預期到：**

```
召回   選表從 [invoices, payments]（漏表）→ [payment_profiles, payments]（涵蓋）
       提示 = is_reconciled（是否已與金流對帳單勾稽），與問句「跟對帳單勾稽」逐字對應
精準   A 選 [order_items, order_returns]，把 order_items.quantity（購買數量）加總 → 8.0
       B 看到 order_returns ▸ quantity（退回數量）之後**只選 order_returns** → 正解 30.0
```

六次否決一直把這個機制當成「多給一張表」，所以量的都是召回。
**精準度那一半從來沒被量過。**

#### 這次撿到的工具：只作用在某一層的介入，自己會生出一組對照

追一題 −2 的時候撞到一個結構事實：

```
生成節點拿到的是 get_ddl_for(scoped) ＋ 外鍵提示 ＋ 其他表那一行
                 —— 沒有候選目錄、沒有欄位提示
欄位提示只活在 table_filter.py 的候選目錄裡
```

> **所以欄位提示只能透過「選表結果不同」影響 e2e。
> 選表結果相同的題，兩臂送進生成器的輸入是位元相同的，任何差異都必然是雜訊。**

把六輪依這條線切開（零 API，`retrieved_tables` 本來就存在結果檔裡）：

```
選表六輪完全一致的 197 題    淨格數  -1     ← 結構上不可能有因果 = 雜訊地板
選表有出入的     112 題      淨格數 +14     ← 效應全部在這裡
```

**這一組不是另外挑的對照組，是同一次實驗自己長出來的** ——
不必多跑一輪、不必事前抽樣分層，而且它與處理組共用同一段時間窗、同一個服務端版本。

**用法**：先確認介入只影響哪一層（`grep` 它的文字進到哪一個 prompt），
用那一層的輸出把題目切成兩組，**逐題歸因之前先問它落在哪一組**。
落在對照組的，機制假說再漂亮都不用查。
⚠️ **這招只對「單層介入」成立** —— 值索引同時進候選目錄與證據行、
prompt 層的介入根本不進選表，那些都不能這樣切。

### 2.10 「讓寬表更值得看」是一條雙向軸 —— 五種介入，同一個結局

同一個目標在**五個不同的層**各試過一次，全部沒有兌現：

| 介入的層 | 做法 | 誘餌題 | 窄表題 | 結果 |
|---|---|---|---|---|
| 嵌入層 | 一張表拆成 N 份概念群短文件 | 目標題排名 10 → 1 | 寬表誤選 6 → **19** | 否決 |
| 嵌入層 | 拆掉寬表註解結尾的關係宣告句 | 3/32 → 9/32 | 34/40 → 40/40 | **全庫配對 14:14，p=0.57** |
| 目錄層 | 加一行「本題可能相關的概念群」 | +11/48 | — | 8/11 票來自單題，否決 |
| 目錄層 | 加一行「本題可能相關的欄位」 | 3/32 → **32/32** | 8/8 → **0/8** | 見 §2.9 |
| 指令層 | 選表 system prompt 加第 5 條原則 | 2/32 → **17/32** | 35/40 → **28/40** | 否決 |

> **誘餌題與窄表題在同一條軸上，方向相反。**
> 誘餌題失敗是因為模型不肯用寬表；窄表題成功是因為模型不亂用寬表。
> 任何把「寬表更值得看」這個訊號調高的手段，都會同時推動這兩端。
> 這不是實作沒調好，是**這個訊號本身分不開這兩件事**。

要贏就得有一個**能區分「這一題該用寬表」與「這一題不該用寬表」**的訊號，
而三種代理（表註解相似度、欄位餘弦、屬性類型的文字描述）**都不是那個訊號**。

⚠️ **欄位提示後來被採用了，但不是因為找到那個訊號** —— 是模型換版之後
陷阱不再發作（§2.9）。**這條軸本身沒有被推翻。**

#### 四個機制，每一個都獨立成立

**① 稀釋來自「機會數」，不是長度。** 概念群拆成五份短文件（中位 66 字元，
跟現行的 62 幾乎一樣），但那張表對每個查詢有**五次機會取最大值**。
`max` 是有偏的：抽五次的最大值系統性地大於抽一次。**換了機制，同一個病。**

**② Hubness 校正救不了它，而且理由很有價值。** CSLS / z-normalisation 是
「機會數偏差」的標準解，λ 掃 0.25~1.0、平均值用 split-half 算、現行臂也套同樣校正
—— **沒有任何一組 (臂, λ) 贏過現行**。λ 越大寬表誤選越少、召回也越差，單調到沒有中間地帶。

> **CSLS 假設 hub 是偽陽性；在這個 schema 裡 hub 大多是真陽性。**
> 「對所有查詢都有點像」的表是 `orders`、`customers`、`products` ——
> 它們**本來就被大多數題目需要**。中心性與真實相關性在這裡是同一件事，
> 所以任何懲罰中心性的校正都會把訊號一起扣掉。

最直接的證據是某個「需要風控覆核表」的題，排名隨 λ 從 10 → 16 → 30 → **70**。

**所以「一表一票」不只是迴避偏差的權宜之計，它是唯一可用的形式。**
「N 份文件 + 去偏」這條路已經關掉了。

**③ 目錄那一行是主張，不是資訊 —— 主張錯了比不說更糟。**

| 配方 | 那一行寫什麼 | 四題誘餌 |
|---|---|---|
| 現行 | 沒有 | 14/32 |
| 只列相關度第 1 的群 | 做一次強主張 | 21/32（猜錯時把正解表推走） |
| 列出全部群名，不挑 | **不做主張** | 16/32（幾乎沒用） |
| 列相關度前 2 的群 | 放寬的主張 | **25/32** |

「純補容量、不做任何相關性主張」我原本以為最安全，結果幾乎沒用 ——
把五個群名全列出來，等於告訴 LLM「這張表什麼都做一點」，
**那讓它看起來更泛用，不是更相關**。

**④ 價值在挑對欄位，不在多給文字。** 同樣加兩欄、但**與問句無關**（挑「最有辨識度」的欄位）
的那一臂，誘餌題是 **0/32**，還把三題推得更差。
**收益不是來自「多了欄位文字」，是來自「挑對了欄位」** —— 精準度是 1/869。

#### 附帶：嵌入自動指派會毒害那一行

用嵌入把欄位指派到最近的概念群，實際印出來長這樣：

```
order_profiles    ▸ 本題可能相關：客服接觸（screen_size、gift_message、support_contact_count）
product_profiles  ▸ 本題可能相關：法規合規（id、product_id、name_en）
```

`screen_size` 被塞進「客服接觸」、`id`／`product_id` 被塞進「法規合規」。
改用 LLM 歸納之後（`check_comment_coverage.py` 本來就回 name + cols）該題回升一票。
**所以那一輪的數字是低估，不是高估。**

### 2.11 其他被否決的檢索方案

| 方案 | 依據 |
|---|---|
| **向量資料庫** | 只有 93 個向量，暴力比對就是精確解 —— 80 個向量時 2048 維餘弦加排序實測 36 ms。ANN 是拿精確度換速度，離那門檻四五個數量級。要變大**下一步是 numpy 不是 Milvus**，那能撐到萬張表 |
| **Hybrid BM25 + RRF** | 每一個 K 都比純 dense 差。中文字元 bigram 在短文件上雜訊太大，而檢索文件中位只有 60 字元 —— 條件往更不利於 BM25 的方向走 |
| **選表改用 20B 小模型** | 召回略低而且**120B 反而比較快**。錯的性質不同才是重點：20B 漏的是問題裡明講的東西 |
| **縮 `CANDIDATE_N` 換目錄長度** | `N=25 k=3` 比 `N=40 k=2` 差，甚至低於不開提示。三個事前假設全被推翻：弄丟的題**沒有一題**是因為表掉出候選（最深第 19 名）；短目錄那臂**更差**；`k=3` 單獨測是平手。**沒有機制就不再調這個常數** |
| **中英對照字典進 `COLUMN_COMMENT`** | `COLUMN_COMMENT` 到不了選表層；生成器本來就會翻譯 |
| **Cross-encoder reranker** | 未量。輸入簽名與 dense 相同 (query, 表文字)，**「營收存在 order_items 裡」不在任何一邊的字串裡** |
| **階層式選表** | 擱置。候選目錄今天約 3,300 字元，離 needle-in-haystack 還很遠；線性外推約 200 張表 |

**兩條擱置中、最便宜的解**（都修 §7.2 診斷出的同一個洞，只是訊號來源不同）：

- **樞紐表保留席**：FK 度數最高的 R 張表無條件進候選。**R=2 → 候選@40 100.0%**，
  Top-1 零損傷、零 LLM、零嵌入。⚠️ **R=1 反而更差**（98.8%，只保留一張卻擠掉第 40 名）
  —— **要嘛保到夠，要嘛不要開始。** 這個非單調正好是「不要憑感覺挑常數」的實例。
- 值檢索已經上線（§2.8），它是同一個洞的資料訊號版。

方向與業界一致：LinkedIn 報告 44pp 的增益「幾乎完全來自知識圖譜，不是更好的 prompt
或更聰明的 agent」；業界（LinkedIn、Pinterest、Snowflake）在表註解不夠分辨近義表時，
補的不是更長的文字，是**「這張表實際被怎麼用」的證據**（query log 熱度、
常見 join pattern、verified queries、synonyms）。**本專案缺的正是這一層。**
⚠️ 反方向的證據也要記：EDBT 的 schema linking 分析與一份 BIRD 上的研究說
**「即使配上高召回的 embedding 檢索器，schema linking 也沒有幫助」**。

### 2.12 失效時的降級

檢索層是加分項，不是單點故障：

| 失效 | 行為 |
|---|---|
| 嵌入 API 掛掉（實測會偶發 502） | 跳過相似度，**LLM 選表照跑**（它讀表註解不需要向量），只損失 ∪ 第 1 名這道保險 |
| LLM 回不出合法 JSON / 選了不存在的表 | 退回純相似度 Top-K |
| 兩者都失效 | 退回全部 93 張表 —— 退化成 Prompt 變長，不是答不出來 |

另外 DDL 一律附上「其他表的表名」（只有名字，約 250 字元）。
**模型只要知道那張表存在就能用它。真正的風險是它不知道，然後拿看得到的表硬湊一個答案。**
實測有題目就是檢索漏了 orders/order_items，而模型靠這份表名清單自己正確補上了 JOIN。

---

## 3. KMB：為什麼補橋接表要用圖論

「陳先生上個月買了哪些手機」語意上只命中 customers 與 products，但少了
orders、order_items 就接不起來 —— 而問題裡**沒有任何字詞會命中這兩張表**。
橋接表只能靠外鍵結構找。

用 KMB 而不是 SPH：近似保證相同（皆 2−2/k），差別在搜尋次數（O(k) vs O(k²)）。
兩者在 93 張表都是微秒級 —— **這是為變大做的準備，不是現在的優化。**

| 取前 K | 錨點召回 | +KMB | KMB 補了多少 | 平均表數 | 淨度 |
|---|---|---|---|---|---|
| 1 | 49.4% | 49.4% | **+0**（單錨點的數學下限） | 1.0 | 84.6% |
| 3 | 76.3% | **86.6%** | **+10.3pp** | 3.7 | 39.8% |
| 8 | 88.1% | 95.7% | +7.6pp | 9.7 | 16.2% |
| 40（＝候選上限） | 99.2% | 100.0% | +0.8pp | 42.7 | 3.7% |
| **production 漏斗** | **97.6%** | **99.2%** | **+1.6pp** | **2.0** | **86.1%** |

規律很乾淨：**錨點召回愈低的那一列，KMB 補得愈多。**
所以要砍它時看的指標是**錨點召回**，不是端到端。

它防的是「兩張錨點表在 Prompt 裡 JOIN 不起來」，而那種錯**不會被 EXPLAIN 擋下來**，
模型會硬湊一個能跑的 JOIN。今天只救 4 題，但那 4 題是**其他任何一層都救不回來的**
（橋接表在語意上不存在），成本是微秒級的圖搜尋。
具體例子：`warranty_claims` **沒有 customer_id**，要接到客戶只能經由 `orders` ——
那條路徑在任何語意相似度裡都不存在。

⚠️ **KMB 也會遮住上游的缺陷。** `products` 排到第 62 名那些題一直是綠的，
因為 LLM 多選的表讓 KMB 把 `products` 當橋補了回來。
**候選召回這個指標測的是 LLM 的選單，不是最終送進 SQL 生成的表集合** ——
兩者被 KMB 隔開，所以上游的缺陷不會在下游的分數上出現。閘門 [12] 就是為此而設（§6.1）。

### 3.1 權重記在「表」上而不是邊上

「經過 browse_logs 很貴」本來就是**表**的性質，設定量也從 O(邊) 降到 O(表)。
今天 10 張行為表成本 3（browse_logs、reviews、carts、cart_items、wishlists、
wishlist_items、product_qna、search_logs、campaign_clicks、product_recommendations），
其餘 83 張走 `_DEFAULT_COST_REVIEWED`。**全庫 14 張 `*_profiles` 全部維持成本 1** ——
它們是掛在主體旁的一對一檔案表，不是「行為 ≠ 購買」那一族。
但那個 1 是閘門逼出來的決定，不是漏掉的。

這些是**行為表**：看過 ≠ 買過、放進購物車 ≠ 買過。沒有權重時 KMB 會抄近路，
把「購買」的問題接到「瀏覽」的表上。

**W=3 是唯一可行的值**，兩個邊界夾出來：要大到輸給 3 跳的購買路徑（3 < 3+1），
要小到 carts 自己當錨點時跳數決勝仍會選對橋。W=4 在真實 schema 上就壞了。
`test_schema_graph.py` 有 17 條迴歸檢查鎖住它。

---

## 4. Prompt 層

### 4.1 只用 Few-Shot，不加業務規則

**這是一條硬規定。** 規則表達不了情境相依的邊界，而且會跟既有的 few-shot 打架。

實例：規則說「總營收用 `SUM(o.total_amount)`」並把逐項計算標為不建議，
但 few-shot 4 對同一個詞用的是逐項計算 —— **Prompt 自我矛盾**。
解法是刪掉規則，改加一個「類別粒度營收」的 few-shot，把邊界寫在它的 `reasoning` 裡。
（這種矛盾是**檢查**就能看出來的，刪掉它不需要統計證據。）

⚠️ **但「改規則的射程」不等於「加規則」。** 補一則規則的適用邊界是允許的，
而且實測是零代價的（§4.3）；**刪掉一則規則的處方則會咬人** ——
曾經以為 few-shot 涵蓋了就把規則移除，某題 7/8 掉到 3/8，
而且錯的是語法合法的靜默錯答。**差別在「補邊界」與「刪處方」。**

### 4.2 遷移性有兩層，只有一層會遷移

| 類型 | 例子 | 會遷移嗎 |
|---|---|---|
| **謂詞級** | `is_deleted = 0`、狀態值對應 | **會** |
| **結構級** | JOIN 後主體收斂去重的形狀 | **只在形狀完全對齊時** |

佔比題分母誤用 `COUNT(*)` 那題是形狀對齊成功的樣本（4/8 → 8/8），三個刻意的選擇 ——
**不用評估題本身當範例**（那是 teaching to the test，用不同的表靠形狀遷移）、
**把錯的寫法也寫進 reasoning**（這類錯誤的特徵是「看起來完全合理」，
只說正解不足以讓模型認出自己正在犯它）、**接在同族既有範例後面**。

反面樣本是「收件地址城市 ≠ 居住城市」那題：試了四條 few-shot，形狀愈補愈接近，
錯誤形狀確實在移動（未收斂 48 列 → 收斂到姓名 29 列），但分數動不了。原因**不是知識缺口**：

> 問句自己點名了兩個城市（「收件地址城市**和**居住城市不同」），
> 模型把 `a.city` 放進投影是在**回應問句**，而那個來自「多方」的欄位讓 DISTINCT 失效。
> 這是**問句語意與答案粒度衝突**，few-shot 補得了知識缺口，補不了這個。

該題因此永久凍結。解凍條件不是「再寫一條 few-shot」，而是出現**第三個實例、
且該實例的問句沒有點名多邊欄位** —— 那才代表這是可教的通則。
同族還有一個決定性反證：同一個教訓（`DISTINCT name` 會併掉同名不同人）
**寫在 few-shot 和表註解兩個地方**，仍然 4/8。

### 4.3 教訓一旦真的送達，就會往相鄰形狀溢出

`get_few_shot_text()` 曾經**不渲染 `reasoning`** —— YAML 裡 22 則、4,649 字元全是死的，
模型只拿到 SQL，得自己反推那是在示範哪一條教訓。
（見 §8 ①；同一個檔案往上 20 行，`get_rules_text()` 早就修過同一個 bug，註解還留著理由。）
**這表示過去每一次「加了 few-shot 沒效果」的觀察都要重新看待。**

修好之後某題 1/8 → 8/8、e2e 失敗題從 9 掉到 4。**但另一題變差了** ——
模型把「『最高』可能有並列，不可用 LIMIT 1，要用 `DENSE_RANK()`」套到了「前 5 位」上，
`DENSE_RANK() <= 5` 取的是前 5 個**不同的張數**。

受控 A/B（同一 process、monkeypatch 換回舊 renderer、每臂 n=16。
**n=8 不夠** —— 同一份程式碼三次 n=8 量到 2/8、3/8、6/8）：

| | OFF（只有 question + SQL） | ON（含 reasoning） |
|---|---|---|
| 補邊界前 | 14/16 | **7/16** |
| 補邊界後 | 12/16 | 10/16 |

> **教訓沒送出去的時候不會溢出；一旦真的送達，它就會往相鄰形狀擴散。**
> `business_rules` 早就有「邊界」段落，few-shot 從來沒有 —— 因為它的 reasoning
> 根本沒進過 Prompt，不需要邊界。現在需要了。

補的邊界寫在 reasoning 裡，`business_rules` 維持 10 條不動：

```
極值（最高/最多/哪一個最…）→ 問的是第一名這個位置，WHERE rnk = 1
top-N（前 5 位/前 10 名）  → 問的是一份 N 筆的清單，ORDER BY … , <唯一鍵> LIMIT N
rnk <= N 當成 top-N 是錯的：DENSE_RANK 排的是「不同的值」。
```

補完仍有約 20pp 差距（Fisher p ≈ 0.09）。**到此收手** —— 再往下就是對著單一題目
調字句，那是「一直改 K」的同一個錯誤。該題記為**已知代價，不是待辦**。
反向檢查確認邊界沒弄壞真正需要 `rnk = 1` 的題：三題各 12/12。

#### 同一個手法解掉最後一題穩定錯：全稱量詞的射程

「有哪些客戶的**每一張訂單**金額都低於 10000 元？」模型寫 `NOT EXISTS`，
把**全庫唯一一位沒有任何訂單的客戶**也選了進來。

**題庫在這件事上是刻意且一致的**：「從來沒有」那一族用 `NOT IN`，GT **確實含**那位客戶；
而中文「他的**每一張訂單**都…」預設他有訂單。兩族的處理不同是對的。

**根因是 prompt 層的射程過度宣稱**：

```
business_rules 標題  「全稱量詞（每一個都 / 全部 / 所有 / 從來沒有）」  ← 宣稱涵蓋「每一個都」
             處方  「先算出有任一成員違反條件的群組，再用 NOT IN 整組排除」
             但 correct_sql／wrong_sql 全都是**群組成員**形狀（城市裡的客戶）
而那則同形狀的 few-shot 只反駁 WHERE，**從頭到尾沒提 NOT IN／NOT EXISTS** —— 蓋不過規則。
```

處置是**兩處都只補射程，不動任何一邊的處方**：few-shot 的 reasoning 補三句
（方向決定聚合：都大於用 MIN、都小於用 MAX；明講 `NOT IN`／`NOT EXISTS` 會把
沒有訂單的人選進來；寫明射程是「主體是個體」），規則改名為「群組全稱量詞」並補反向指路。

```
收益：個體全稱量詞兩題  A  7/16 → B 16/16   兩題都從硬幣變成穩定過
回歸：從來沒有／群組全稱 A 48/48 → B 48/48   一格沒掉
防禦題                   A 32/32 → B 32/32
```

**這次為什麼是零代價的**：回歸組 48/48 → 48/48，說明補射程邊界**沒有稀釋**原本那條規則。
稀釋定律講的是「一則註解裝不下太多概念」，不是「不准把一則規則的適用範圍講清楚」。

### 4.4 防禦題：拒答是未解問題，動作必須窄

四題防禦題斷言「93 張表的 schema 裡沒有這個概念」：運費、利潤率、庫存週轉率、信用評分。

研究界對這個失效有名字且說它未解（AbstentionBench：加大模型沒用，
**推理型模型反而更不知道何時該不答**；LatentRefusal：拒答 prompt-sensitive，
**恰好在模型幻覺時失效**）。所以不要期待修到 100%。

實際漏接過一次：模型自己發明了 `SUM(quantity) / capacity AS turnover_rate` ——
SQL 合法、EXPLAIN 會過、**使用者看不出那個數字是憑空來的**。

處理方式是**窄規則 + 正向邊界**。全庫有 19 題可回答的題目提到相鄰詞
（「手續費率」「平均評分」「總消費金額」），任何粗暴的關鍵詞拒答都會誤傷它們：

```
擋的是「週轉」這個沒有定義的比率，不是庫存與出貨這兩個有資料的量。
「每個倉庫的容量是多少」「哪些商品庫存是 0」→ 直接答。
```

這是一個**只能否證不能確證**的實驗（要區分 91% 與 100% 需要 n≈30 以上）。
它能確認的是更重要的那一半：**四題邊界／相鄰題全部 8/8，沒有過度觸發。**
**代價可測為零、收益不可測時，代價為零就是採用的理由。**
另外兩題**沒有加規則** —— 沒觀測到漏接，加規則是純推測還要付誤觸風險。
觸發條件：對帳的防禦題那一行出現它們，才回來加。

### 4.5 兩個被否決的 Prompt 層方案

**few-shot 條件式注入（向量檢索每題只給 3 則）—— 否決。**
既然溢出是真的（§4.3），一個自然的下一步是把溢出壓回去。**先量代價再決定做不做**：
A = 24 則、B = 0 則，逐題配對，要的不是總分而是雙向的兩格
（`A 錯/B 對` = 溢出代價 = 條件式注入能買回來的上限；`A 對/B 錯` = few-shot 的貢獻）。

溢出候選 6 題重新抽樣 n=8：**一題都沒有活下來**（最大的那題 2/8 vs 6/8，
Fisher p = 0.13，而且**這個 p 還是樂觀的** —— 那 6 題是用「兩臂結果不同」挑出來的，
選擇偏誤還沒扣）。**比 p 值更硬的是失敗模式**：

```
A 3/8 回 [[0]]／B 4/8 回 [[0]]              ← 一模一樣
A 4/8 回 [[1,5,0]]／B 6/8 回 [[1,5,0]]      ← 一模一樣（漏 JOIN 拿 name）
```

> **few-shot 沒有在這些題引入任何一種「自己的」錯法。**
> 兩臂錯得一樣、只是頻率不同 —— 那是擲硬幣，不是溢出。

而貢獻側是**可歸因**的：沒有 few-shot 時模型反覆寫 MySQL 沒有的
`PERCENTILE_CONT(...) WITHIN GROUP` 直到重試耗盡；用 `GROUP BY MONTH(order_date)`
跨年合併；只回 `customer_id` 不回 name/email。方言、口徑、輸出粒度 ——
全是會遷移的那一層。**買的是量到 ~0 的溢出，賭的是 17 題有機制的貢獻。**

要重開這條，先拿出一題「A 穩定錯、B 穩定對、而且兩臂錯法不同」的證據。

**few-shot 表名抽象成 `<表A>` —— 否決。** 先量有沒有污染：

```
few-shot 24 則，SQL 裡出現過的表：8 張
e2e 用到「檢索沒給的表」的 15 題裡
  few-shot 出現過的表  customers 5、products 2、orders 1   共 8 次
  few-shot 沒出現過的表 promotions 2、suppliers 2、…       共 9 次
```

**看不到污染訊號。** 用到的 8 張表裡有 5 張根本不在 few-shot 裡，
而出現的那 3 張正好是全庫 FK 度數最高、被需要次數最多的三張。
而且抽象化與 §4.2 的核心發現衝突 —— 形狀對齊之所以成功，正是因為用了
**不同的真實表、同一個形狀**；換成 `<表A>` 等於把它賴以成立的具體性拿掉。
真實表名同時還在教「哪張表是什麼意思」，那是這個專案花最多力氣買到的鑑別力。

### 4.6 enum_fields：代碼欄位的合法值只活在註解的散文裡

**問題**：模型寫出 `channel = 'APP 行動應用'`，實際值是 `'APP'`。

```
註解    下單通路：WEB 官網／APP 行動應用／PHONE 電話／STORE 門市
實際值  ['APP','WEB','PHONE','STORE']
```

全庫掃描：註解寫成「代碼 中文」的欄位 49 個，其中是 MySQL ENUM 型別的 **0 個**
（`enum_text` 一個都收不到）；原有的 6 個 `enum_fields` 全在窄表，與這 49 個零交集。
**值索引也接不到**：`'APP'` 出現在該欄自己的註解裡（被判為概念詞），而且純 ASCII 且太短。

> 這是「寫了沒接到產生鏈」的另一個形狀（§8 ①）：擴表時 DDL 接上了 `gen_ddl.py`，
> `enum_fields` 沒有人接。

**產生器 `tools/gen_enum_fields.py`。納入與否由註解決定，值域由資料決定** ——
這兩件事分開，繞了兩次才對：

| 判準 | 結果 |
|---|---|
| 註解裡 regex 掃「代碼 中文」 | 48 個，**但 parser 只認得一種註解風格** |
| 純看資料的封閉集合 | 138 個，收進了人名、銀行名、對帳批號 |
| **註解宣告 ∧ 值在資料裡驗得到** | **74 個** |

中間那一版錯在哪值得記：種資料時人名只抽了 4 個、銀行名只抽了 12 個，
在這份資料裡它們**確實**是封閉集合 —— 但那是種子池的產物，不是欄位的語意。

**值域 = 資料 ∪ 註解宣告，兩邊的作用不同：**

```
資料   決定字面形式  —— 病灶是 'APP 行動應用' 不是 'APP'，這一半註解說不清楚
註解   決定值域      —— 某個值現在 0 筆不代表它不合法，砍掉模型會挑一個錯的
```

**這個決定是承重的**：剪成只取資料的話，兩題 `expect: empty` 的題會被無聲改難度（§9 的資料安全約束）。

這個庫有兩種註解風格，parser 兩種都要吃：

```
A（原窄表）     動作 (INSERT/UPDATE/DELETE)           括號、斜線、沒有中文
B（*_profiles）  出價策略：CPC 單次點擊／CPM 千次曝光    全形／、代碼後接中文
```

作法是**拿資料裡的真值去註解裡找**，不切段、不猜哪個 token 是代碼。
（切段的版本會把「DECLINED 銀行拒絕、TIMEOUT 逾時未回應」的 TIMEOUT
吃進上一個代碼的中文裡，然後誤報成「資料多出 TIMEOUT」。
詞邊界也必須含連字號，否則 `zh-TW` 會被挖出一個不存在的 `TW`。）

#### 只收 46 欄，不收 74 欄 —— 為的是留下對照組

把 74 欄按「模型會不會寫錯**字面值**」分三級（`--minset` 可重現）：

| 級 | 欄數 | 註解長相 | 收不收 |
|---|---|---|---|
| **R1 黏連** | **44** | `下單通路：WEB 官網／APP 行動應用` | 收 —— 病灶就是這個形狀 |
| **R2 未列** | **2** | `流量來源，例如 google／facebook`（資料還有 `instagram`／`line`） | 收 —— 模型無從得知 |
| R3 乾淨 | 28 | `工單狀態 (OPEN/PENDING/CLOSED)` | **不收** —— 沒有可黏的中文 |

| | Prompt **位元不變** | 會變 | 新增字元（平均／中位／最大） |
|---|---|---|---|
| 全量 74 | 159 題（51%） | 150 | 275 ／ 0 ／ 2,037 |
| **最小 46** | **223 題（72%）** | **86** | 162 ／ 0 ／ 1,764 |

那 223 題是**免費的雜訊地板**：enum 的剪裁發生在檢索**之後**，不會回饋到檢索，
所以它們兩臂的 Prompt 一個位元都不會變，任何差異定義上就是雜訊。
**全量版只給 159 題的地板，卻要多量 64 題 —— 多花的是量測成本，換到零機制收益。**

#### 否決過的另一條路：直接改 `COLUMN_COMMENT`

把 `APP 行動應用` 改成 `APP=行動應用`，44 欄一次解決，整個 DDL 只多約 145 字元，
而且**從源頭消滅黏連**，機制上比 enum 乾淨。**否決理由是它驗不起來**：

- 欄位註解餵給 `column_hints` 的嵌入文件（`HINT_K=2` 是開著的）
  → 變成**檢索層 ＋ Prompt 層的雙層介入**
- DDL 進每一題的 Prompt → **沒有任何一題位元不變，免費對照組整個消失**
- （`value_index._keep` 那條不受影響：代碼本來就在註解裡，加不加 `=` 都一樣被排除）

> **機制上比較好的那條路，恰好是最難證明「不會變差」的那條。**

#### 驗收：非劣性，不是改善

事前登記寫明**這一次量的是「不會變差」**：暴露率只有 1/1851，效應天花板是
120 個樣本裡的 1 個，功率不足以宣稱改善。

```
變動組 86 題 × 6   A 503/516 = 97.5%｜B 502/516 = 97.3%｜B − A = −1 格
對照組 33 題 × 6   A 193/198 = 97.5%｜B 196/198 = 99.0%｜B − A = +3 格

[1] 非劣性   B−A = −1  ≥ −20 格            過
[2] 雜訊地板 |B−A| = 3 ≤ 10 格             過
[3] 防禦題   四題兩臂全 6/6                 過
[4] 機制     非法字面值 A 1 次 → B 0 次     過
```

**變動組差 1 格，而對照組（Prompt 位元相同）差了 3 格 —— 效應比它自己的雜訊尺還小。**
所以正確的說法不是「B 略差」，是**量不出差別**；能講的只有「排除了大於 4pp 的跌幅」。

**判準 [4] 是唯一拿到正面證據的地方，而且它是機制不是分數**：
A 臂寫出 `channel = 'APP 行動應用'`，B 臂 0 次。

差的三題**沒有一題有 enum 機制**：一題在 23 輪裡只有 2 輪拿得到 minset 的 enum
（其餘輪次兩臂 Prompt 位元相同）；一題是追完的 75% 硬幣（失敗簽章恆為少濾 `is_deleted`，
兩臂都出現過）；一題是既有的穩定錯（與 prompt 無關）。

⚠️ **翻預設值的依據是「非劣 ＋ 機制被堵住」，不是分數。** 這與欄位提示
（區間完全不重疊才採用）不同，門檻低是因為**成本結構不同**：
enum 有 72% 的題一個字都不加（中位增量 0），欄位提示是每題每表都要付。

---

## 5. 評估方法

### 5.1 GT 永遠先修正才跑測試

**硬規定。** GT 錯了，模型的錯與 GT 的錯會混在一起，無法歸因。

- **GT 只選「回答問題所需的最少欄位」。** 比對規則對多回欄位寬容、對少回不寬容 ——
  容忍度是單向的，所以過度指定一定會製造假錯誤。
- **不要指定表示格式。** 只收 `'2026-02'` 會把等價的 `YEAR()/MONTH()` 兩欄判成錯 ——
  那是在考格式不是考語意。
- **新題的第一次探針，要把「模型錯」與「GT 錯」當成同等可能的假設。**
  這一點被驗證了三次，**三次都發生在新寫的題目上** ——
  既有題目經過多輪 e2e 洗過，新題沒有。

寫 GT 時最常撞到的是**平手題**：「工單最多的客戶」36 張單分屬 36 位客戶全部並列 1、
「圖片最多的商品」五個並列 4 張。答案會是任意平手，一律改成計數或清單題。
同名不同人也要先查（37 個 `customer_id` 只對應 33 個相異姓名）。

**四條處置判準（哪一種病用哪一種修法）：**

```
問句歧義（兩種讀法都答得出東西）+ schema 沒說哪個算數  → 改問句或補 alt_sql
兩種讀法都有答案 + schema 明說了哪個算數               → 不改問句（模型沒讀註解）
一種讀法回空集合                                       → 不改問句（模型挑錯表）
GT 多要了問句沒問的欄位                                → 收窄 GT
```

**收窄 GT 比補 alt_sql 收斂。** 對帳本來就「忽略欄位名稱與順序、允許系統多帶欄位」，
所以把 GT 投影收窄到「問句真的問了的東西」之後，帶與不帶那一欄的兩種寫法**同時**通過。
補 alt_sql 是列舉第二種主張，收窄 GT 是撤回一個從來沒被問句支持的主張 —— 後者不會有第三種。

**可列舉的補 alt_sql，不可列舉的收窄問句。** 「同一欄的編號或名稱」是封閉的兩個元素，
補 alt 就結案；「有沒有…」的明細／筆數／布林是三種**不同形狀**，**形狀空間不是有限集合**。

**改資料與改 GT 可以重算，改問句不行。** 對帳是計分時把 GT SQL 與模型 SQL
**各執行一次**，所以資料與 GT 一改，存著的每一輪兩邊一起重算，零 API。
**但改問句不一樣：存著的答案是回答舊問句產生的，對那一題作廢。**

⚠️ **YAML 重複鍵會靜默吃掉修改。** 補 `alt_sql` 時插在既有的那份前面，
PyYAML 只留最後一個，而「兩式都 10 列」看起來還很正常。
以題號為鍵的設定檔（`ambiguity_verdicts.yaml` 也是）**append 是一個會沉默覆寫的動作**。
已加嚴格載入器掃全檔。

### 5.2 單輪評估分不出差異

MoE 模型在 temperature=0 仍然不確定。**同一份設定連跑兩次的檢索召回是
99.3% 與 98.6%** —— 任何小於約 1 個百分點的「改善」都讀不出來。

連續三輪完整 e2e 最能說明問題：

```
三輪分數：154 / 154 / 154      每輪錯題數：5 / 5 / 5
曾經錯過的題：9 題（union），三輪錯題只重疊兩題
```

**分數三次一模一樣，失敗的身分卻一直換。** 這是「錯誤預算」型的系統，
不是「某幾題不會」。今天的六輪也一樣：同臂各輪錯題重疊只有 38%~67%，
**單輪報「錯 13 題」時，其中七八題下一輪就會自己好。**

```
報告格式：穩定通過 N 題｜硬幣池 M 題（附各自的 k/8）｜穩定錯 K 題｜防禦題 x/4
```

**判斷任何改動有沒有效，比較的是硬幣池的組成與各題的 k/8，不是分數。**

**這條紀律是雙向的。** 它平常擋「分數變好了所以有效」，但也擋
**「分數沒變所以沒用」** —— 修完 35 條資料矛盾之後三輪平均與修之前一模一樣（97.4%），
而被改到的 17 題在修之前單輪 e2e 也全部 `ok`，真正的差別只在重複取樣下看得到
（某題 5/8 → 8/8）。**同一條紀律，兩個方向都要守。**

**漏斗評估同樣沒有解析度。** 同設定重複執行的區間：拆欄臂 n=10 是 93.4~96.1%、
現行臂 n=5 是 93.4~94.8%。曾經靠**單輪**全庫漏斗下的三個判斷全部泡在這個區間裡
（其中一個還是拿兩個樣本當區間 —— **兩個樣本量不出區間**）。
所以漏斗的判準改成**逐題配對 + 符號檢定**：總分把 300 多題的雜訊加總，
配對讓每題自己跟自己比，跑次雜訊被消掉。

**解析度下限**：n=8 分得出 3/8 → 7/8，分不出 7/8 → 8/8（要 20+ 次）；
要把 2/8 與 5/8 分開需要 n≈40 —— **在硬幣題上做改動，驗收比改動貴得多**，
這本身就是「先不要動」的理由。效果小於解析度時，改用「機制是否消失」判斷
（看失敗時回傳的具體數值）。

#### n=3 的逐題計數也不能拿來做配對檢定

曾經對兩臂做「逐題配對符號檢定」得到救回 4 / 弄丟 13、p = 0.0245，
並據此宣稱某題是「全新的穩定錯（3/3 → 0/3）」。**那個檢定是無效的**：
定題 n=8 之後看到那 13 題裡有 8 題本來就是硬幣（3/8、4/8、5/8…），
它們在另一臂拿到 3/3 是**運氣**。一枚 4/8 的硬幣連中三次是 6%，池子裡有八枚 ——
出現幾個「3/3 → 1/3」是必然的。

> **逐題比總分「看起來更嚴謹」，但解析度並沒有比較高** ——
> 分母從 309 變成 3，雜訊反而更大。
> 要做逐題配對，每題至少要有 n=8；否則只能比輪次層級的總分。

**探針條件比完整 e2e 寬鬆。** 探針 5/8、4/8 的題在完整 e2e 裡連錯四輪 ——
若探針機率是真的，那只有 2% 與 6%。**跨條件比較必須明講是哪一種條件量的。**

**零 LLM 的量測沒有這個問題**，所以優先用它：`--k`、`eval_retrieval_variants.py`、
`eval_column_recall.py` 三支都是確定性的、可以隨便重跑。
本文件裡的檢索數字絕大多數來自這三支，正是這個原因。

### 5.3 檢索層要獨立評估

`eval/eval_retrieval.py` 不跑 pipeline、不呼叫生成模型，只量「表選對了嗎」。

理由：**端到端正確率會低報檢索失敗。** 有題檢索漏了 `products`，模型改用
`oi.unit_price`（成交價）而 GT 用 `p.price`（售價）—— 兩者語意不同，
只因為種子資料裡這兩欄永遠相等才碰巧判對。

`eval/eval_lucky.py` 把這件事自動化：答案對、schema 不對 → 標成僥倖候選。
它是旗標不是判決 —— 同一題本來就有多種正確寫法，涵蓋任一條 `alt_sql` 就放行。
**確認是正常寫法就要立刻補進 GT**，因為常態誤報會讓稽核工具失去意義。

三個指標必須一起看（召回、平均表數、淨度）—— 召回與表數是對立的，
單看任何一個都會做出錯誤取捨（K=40 召回 100%，淨度只有 3.7%）。

⚠️ **但漏斗召回不能單獨判生死，它兩個方向都會失真：**

```
高估「多給表」的介入   它只量召回（把漏掉的表補回來），
                       量不到漏斗另一半的作用（把可湊的近義表拿走）
                       —— 聯集在漏斗層 9 題救回，e2e 只兌現 7 題
低估「拿掉表」的介入   欄位提示的召回率比對照**低 1.0pp**，e2e 卻**高 1.4pp**
```

那 1pp 全部集中在六題「消費金額」家族，而它們兩臂都全對，SQL 全是走
`SUM(o.total_amount) FROM orders` 的即時路徑 —— **漏掉 `orders` 沒有代價，
因為它的欄位在 24 則 few-shot 裡被反覆示範過。**

> **召回指標失真的方式很具體：它把每張表當等價。**
> 漏掉 `orders` 不扣分，漏掉 `payment_profiles` 會 ——
> 差別在於那張表的欄位有沒有被 few-shot 反覆示範過。

### 5.4 挑出來的子集合不能再拿來做檢定

一度對「兩輪都通過」的題抽 20 題 × 8 次，得到「零個隱藏硬幣」。
**第三輪 e2e 當場推翻** —— 兩題前兩輪都過、第三輪錯，而且不在那 20 題裡。
錯在**抽樣母體本身有條件**：一題失敗率 25% 的脆弱題通過兩輪的機率只有 56%，
母體已經被過濾成偏強韌的一群。

同理，為「探針比 e2e 寬鬆」算出的 p = 0.0152 也有選擇偏誤 ——
那些題是因為在 e2e 錯過才被選進池子的。最清楚的例子是某題探針 8/8，
它進池子唯一的理由就是 e2e 錯過一次。

> **任何用「哪些題錯了」挑出來的子集合，都不能再用「那些題錯幾次」去檢定。**

同理，**只對錯題取樣有偏誤** —— 那只找得到剛好落錯的硬幣，找不到剛好落對的。
要估硬幣池得對**通過的題**隨機抽樣。

這條被違反過三次，最貴的一次是拿「兩臂結果不同」的 6 題當溢出證據，
重新抽樣 n=8 一題都沒活下來（§4.5）。
**合法的例外是事前按治療分組**：「聯集 ≠ 票 0 的 102 題」與「GT 直接引用了被改欄位的
17 題」都是事前、按輸入而非按結果選的，可以合法當驗收。

### 5.5 探針只能否決，不能放行 —— 靶子選在依變項上

五次介入、五次探針大勝、五次全庫不兌現（表在 §2.10）。
**先排除一個誤診：這不是樣本太小。** Fisher 兩側精確檢定：

```
誘餌 3/32 → 32/32     p = 0.00001
誘餌 2/32 → 17/32     p = 0.00007
全庫 7 題 39/56 → 35/56   p = 0.55
```

**探針量到的效應是真的**，統計上毫無疑問。問題是那個效應**在建構上就不可能不出現**：

> **誘餌四題之所以被選進探針，正是因為它們敗於「模型不肯用寬表」這個機制。
> 任何把該機制反轉的介入，在那四題上必然大勝 —— 那是選題方式決定的，不是介入的功勞。**
> 探針看不到的是代價，因為代價分散在沒被選進來的另外約 290 題上。

這直接推翻一條曾經寫進待辦的修法：**把 n 從 8 提到 40 不會有任何幫助**，
只會讓 p 值更小、結論一樣不可遷移。**那是拿治變異數的藥去治偏誤。**

對照組同樣被挑過，而且看得見後果：某次的對照是「10 題手挑的窄表題」，
而全庫爆掉的兩題屬於「寬表欄位是刻意陷阱」這一類 —— **那一類根本不在對照組裡**。
另一次雜訊對照組是**均勻抽**「兩臂輸入相同」的題，而那群題正是三張選票一致的題
＝ 沒有歧義 ＝ 簡單題，全部貼在天花板上（105/105 = 100.0%），**對照組沒有檢定力**。

#### 硬規定

1. **探針（手挑靶題、n ≤ 8）只能否決，不能放行。** 探針贏了只代表「值得花錢驗」。
2. **對照組必須是隨機抽樣（固定 seed）並按「該題的歷史正確率」分層，不准手挑、不准均勻抽。**
   手挑的對照組只涵蓋你想得到的失敗類型，而會咬人的正是你沒想到的那一類；
   均勻抽會抽到一堆貼天花板的簡單題。
3. **驗收一律逐題配對 + 符號檢定**，不比總分。
4. **一個介入只有在全庫配對上贏才算贏。**
5. **能用「單層介入自帶對照組」就用它**（§2.9）—— 那是唯一免費且與處理組共用
   時間窗的對照。

### 5.6 儀器要驗它在量什麼

三次踩到同一個形狀：**儀器沒有在量它宣稱在量的東西，而且它報綠燈。**

| 儀器 | 壞法 |
|---|---|
| A/B 的對照臂 | `legacy()` 漏了兩段式範例的 `sql_step1`，8 次全 crash —— 量到的是 harness 不是系統 |
| 「這一層有沒有作用」靠 grep 日誌 | 評估腳本開頭就 `log.remove()`，**該行日誌結構上不可能出現**，於是恆為 0 而印「通過」 |
| A/B 的臂靠環境變數 assert | 跑到一半改了被測檔案，同一個旗標在前後兩版指向不同模式 |

> **A/B 的臂要釘在版本上，不是釘在環境變數上。**
> 環境變數相同不代表行為相同 —— Python 匯入後模組就定住，
> 一個臂的實際模式由「它的行程什麼時候啟動」決定。
> **assert 要驗行程內的實際生效模式**（把生效模式與實際載入的檔案印進結果檔），
> 不是驗傳進去的旗標。**永遠不要在 A/B 跑到一半改被測的檔案。**

同一條也適用於檢查工具本身：

- 掃描腳本 `except: continue` 把 collation 例外吞掉，兩批表的字串欄位一比就丟例外，
  **整整兩組矛盾靜靜消失，而輸出報「乾淨」**。
  修法不是加 try 的範圍，是**把失敗印出來並算進總數** ——
  現在第一行就報「SQL 失敗 N 組」，**N 不是 0 就不准當成掃過了**。
- 保證式配置的輸出原本是硬寫的字串「檢驗不符 4、物流商責任 3」——
  那是**宣稱不是量測**，而實際值是 0。改成逐項實測。
- 三支檢索評估對「答對」的定義跟對帳程式不一致（只解主 SQL，不看 `alt_sql`），
  把「走了另一條被 GT 認可的路」判成檢索失敗：漏斗召回 97.6% → **99.2%**，漏 6 題 → 漏 2 題。
  **它活這麼久是因為它不會 crash，只會讓分數偏低** —— 而偏低的分數看起來像
  「還有進步空間」，沒有人會去質疑一個對自己嚴格的指標。
  **比起會高報的 bug，低報的 bug 更難被發現，因為它不製造矛盾。**
- 串跑會被限流污染：多個變體串在同一支腳本連續打 API，跑在後面的變體吃到累積的 429，
  122/153 題退回純相似度 —— 量到的是降級路徑的分數。
  **跨變體比較必須記錄 LLM 退回次數。**

**要花數百次呼叫才拿得到的東西，一定要存檔。** `--funnel` 現在會把 LLM 的選擇存成
`funnel_picks_*.json`，上面那個 `alt_sql` 的修正就是靠重播存檔驗證的，零額外呼叫。

---

## 6. 閘門與標準流程

### 6.1 十三項閘門

**預設 `exit 1`。** 這些工具的存在理由見 §8 ① —— 同一類 bug 咬了六次，六次都不報錯。

> **只警告不阻斷的檢查，等於沒有檢查，除非有人會讀。**
> `_report_drift()` 整整一輪都在警告「DB 有而 YAML 沒有的表」，被當成雜訊忽略了。

**`check_schema_pipeline.py` 前八項**：前七項守 **schema 鏈**（DB → DDL → parser），
第八項守 **prompt 鏈**（YAML → renderer → Prompt）：每個欄位挑最長的一非空行當探針，
看它在不在 renderer 的輸出裡。**不比對完整內容** —— renderer 本來就會加縮排與前綴，
比對整串只會一直假警報，而**常態誤報的檢查等於沒有檢查**。

第 5 項的做法值得單獨記：`table_cost()` 對沒列到的表回傳預設 1，
所以**新表永遠不會報錯**（症狀是 KMB 靜默繞錯路：拓撲對、語意錯）。
修法是把預設值也明列成 `_DEFAULT_COST_REVIEWED`，強制
`_TABLE_COST ∪ _DEFAULT_COST_REVIEWED == 全庫 93 張表名`。
**決定「1」也可以，但必須是決定過的 1。**

**後五項**，每一項守的是前一項結構上看不到的東西：

| 閘門 | 守什麼 | 為什麼前面的看不到 |
|---|---|---|
| [9] 縱向衍生量一致性 | 寬表衍生欄 vs 從母表現算 | — |
| [10] 題目層多來源歧義 | 同一個問題有兩條路、答案不同 | [9] 用 GT 的 SQL 判斷「有沒有題目引用」，而危險的正是 GT **沒**用、模型會用的那條路 |
| [11] 橫向謂詞衝突 | 兩張**平行**表對同一件事各擲各的骰子 | [9] 的前提是「存在可現算的母表」，而 `is_discontinued` 不衍生自任何東西 |
| [12] 逐表可檢索性 | 這張表被需要時排第幾名 | 下游的 KMB 會把它當橋補回來，**缺陷被救援機制遮住** |
| [13] 題目形狀可判定性 | 問句決定得了 GT 的 SELECT 清單嗎 | [10] 檢查答案的**來源**，[13] 檢查答案的**形狀** |

三個設計細節：

- **[10] 的判準必須是執行，不能是相似度。** 第一版用餘弦門檻，在 305 題上標出一堆
  差 0.001~0.003 的雜訊，而**用已知的那一題去調門檻正是專案禁止的調常數**。
  改成實際跑兩條 SQL 比對結果，不一致本身就是訊號，不需要門檻。代價是替代路徑要手寫。
- **[12] 的紅燈條件必須是「最差值」，不能是平均或中位數。**
  `products` 被需要 60 次的中位數是 5.0（正常），最差是 62（掉出候選）——
  **缺陷藏在尾巴，平均型判準完全抓不到它。**
  判準綁在既有常數 `CANDIDATE_N` 上，沒有引進新的可調參數。
  另外有一級 🟠 **橙燈**：production 過得去、但純 dense 掉出候選（靠值索引撐著），
  不 `exit 1` 但一定點名 —— **當年的問題不是「有救援」，是沒有人把救援印出來。**
- **[13] 量的是「暴露」不是「缺陷」。** 我曾試著收窄它的判準，把命中從 14 題壓到 2 題，
  而留下的正好是最差的兩題、看起來完美 —— **拿一個已經確認是真缺陷的題回頭測就破了**
  （它符合新規則的豁免條件）。而那題與另一題沒事的題句構幾乎一模一樣。
  **暴露會不會發作是擲硬幣；任何讓命中清單變短的規則，都是在對某一組快照過擬合。**
  所以裁決是逐題寫理由，不是改判準。

**新工具第一次報全綠時先懷疑工具**：閘門上線前要餵已知的壞例子確認它會響。
[12] 拿改註解前的排名快照重跑 → 正確變紅並 `exit 1`；
[9] 把一列沒有任何題目引用的欄位加 1 → 從綠變紅。
另外 [13] 會列出「`ADJUDICATED` 裡已經不再命中」的題並講明它們現在不受保護 ——
**裁決過的理由是紀錄，不是保護傘。**

### 6.2 評估框架的四個洞

| 洞 | 工具 | 怎麼跑 |
|---|---|---|
| 防禦題會被加表**靜默作廢** | `tools/check_defence_gt.py` | `eval_score.py` 自動跑 |
| 端到端**低報**檢索與生成失敗 | `eval/eval_lucky.py` | `eval_score.py` 自動跑 |
| 單輪計分**掩蓋**擲硬幣的題 | `eval/eval_stability.py` | 手動（會打幾百次 API） |
| 欄位裁剪沒有守門員 | `eval/eval_column_recall.py` | 動裁剪前，或 schema 變寬後 |

前兩支合併進 `eval_score.py` 是零額外成本的，而防禦題稽核放在**對帳之前**，
因為它最便宜且**它報警時後面的分數要重讀**。

第一個洞值得展開，因為它是唯一一種**會被加表靜默作廢的 GT**：

> **一般題斷言「答案是什麼」，防禦題斷言「概念不存在」。**
> 前者只依賴用到的那幾張表，後者依賴**整個 schema 的補集** ——
> 加任何一張表都可能讓它失效，而失效時
> **系統答對了卻被判錯，看起來像模型退步**。

判準是**業務定義在不在，不是欄位在不在**：加寬表帶進 `birth_year`／`gender`
之後兩題改為一般題（`2026 - birth_year` 無歧義），但利潤率維持防禦
（`supply_price` 有了，但一商品多進貨價、公式未定義）。防禦題因此從 6 題剩 4 題。

做法：GT 裡宣告 `absent:` 與 `probes:`，程式掃 INFORMATION_SCHEMA；
**裁決過的命中寫進 `acknowledged:` 就不再報警，未裁決的命中才是警報** ——
所以加表時只會看到新出現的東西，不會被既有命中淹沒。
它上線就抓到一題的 note 過期（寬表帶進了 `credit_limit`、`risk_flag`、`churn_risk_score`）
—— 依判準仍維持防禦，但理由整段重寫。
**誘餌從「無中生有」變成「就在眼前」，這題反而更有價值。**

⚠️ 這支**看不到「守衛該開火卻沒開火」**。唯一會出聲的地方是對帳單獨印的防禦題那一行
—— **這是誠實性指標，不該被平均進正確率裡稀釋掉。**

### 6.3 標準流程

```bash
python tools/check_schema_pipeline.py         # 八項閘門，紅了不要往下走
python tools/check_derived_consistency.py     # [9] 縱向：衍生欄 vs 從母表現算
python tools/check_question_ambiguity.py      # [10] 題目層的多來源歧義
python tools/check_predicate_collisions.py    # [11] 橫向：兩張平行表的同義謂詞
python tools/check_table_retrievability.py    # [12] 逐表：被需要時排第幾名
#   預設就是 production 組態（值索引開）。要看「沒有值索引會怎樣」：VALUE_BETA=0 …
python tools/check_question_shape.py          # [13] 問句決定得了 GT 的 SELECT 清單嗎
python tools/check_comment_blindspot.py       # 問句驅動：正解表在目錄裡找不到
#   ⚠️ 明細只印 40 筆 —— 不要用 grep 在全庫輸出上找某一題，會得到假的「沒問題」。
#   要看一張表的全部：--table <表名>
python eval/eval_gt_check.py                  # GT 自檢：SQL 跑得動、alt_sql 同值
python eval/test_runner.py                    # 完整 e2e（309 題）→ eval/results/
python eval/eval_score.py                     # 對帳 + 防禦題稽核 + 僥倖偵測
python eval/eval_stability.py --ids <上一行印出的錯題>
```

**前六支排在最前面，因為它們不打 LLM、幾秒跑完**，而它們擋下的正是
「跑完 309 題才發現量到的是 GT 或 DDL 的問題」。曾經因此浪費掉一整輪
（12 題全錯，起因是 `gen_ddl.py` 沒重跑）。

改檢索層時**先跑零 LLM 的三支**（`--k`、`eval_retrieval_variants.py`、
`eval_column_recall.py`），它們確定性、可重跑、幾分鐘出結果。

**重建資料庫之後必跑**（順序不能反）：

```bash
python tools/fix_derived_consistency.py --apply   # 對齊 35 條（縱向 14 ＋ 橫向 21）
python tools/check_derived_consistency.py         # 忘了上一步這裡會紅
python tools/check_predicate_collisions.py
```

`customer_profiles` 的 6 個衍生欄位**結構上無法**在建表時算完 ——
它們的母表是後面的腳本才建的。**所以對齊注定是獨立的一步，而獨立的一步注定會有人忘記跑。**

**加表或改 schema 之後另外跑一次探測器**（不是閘門，不擋任何東西）：

```bash
python tools/check_synonym_columns.py             # 全庫掃同義欄位，人來看清單
```

它不問句驅動也不限謂詞，補的正是閘門 [11] 的兩個洞：沒人問過的配對、數值與字串欄位。
**誤報率高是刻意的** —— 「值不一樣」不等於缺陷，判準是「有沒有哪個業務問題因此有兩個答案」，
那一步只能人判。

加表時額外的檢查：跑完 e2e 要逐一檢查防禦題有沒有被新 schema 作廢；
**每加 10 張表，8 張要有題目**（閘門 [6] 強制）。

---

## 7. 規模、資料衛生與邊界

### 7.1 候選天花板：整個漏斗唯一的硬天花板

```
錨點召回 @40 = 98.9%      ← 3 題的必要表落在 dense 第 41 名之外
+KMB     @40 = 100.0%     ← KMB 全數補回
```

**這幾題是整個漏斗的召回上限，後面兩段再準都救不回來，而且掉了不會報錯。**

好消息是**需要的深度成長得比表數慢**：80 張表 / 253 題是 99.2%，
86 張表 / 275 題是 98.9% —— 表多了 6 張、題多了 22 題，天花板只掉 0.3pp。
K=20（涵蓋 23%）就有 97.1% / 99.3%，**後半段的候選深度買到的是最後那 1.8pp。**

護欄 `_warn_if_candidate_n_binds()` 每次載入表註解都會響（93 > 40），只報事實：
每題砍掉 53 張、涵蓋 43%。**守門員是召回那個數字，不是這個常數；
護欄的價值不在修好天花板效應，在於讓一件無聲的事出聲。**

**KMB 救不了這幾題**：`products` 在那些題裡是**葉節點不是橋**，
掛在 `order_items` 旁邊，不在任何兩個錨點之間的路徑上。Steiner tree 只收終端點**之間**的節點。
**中介表可以外包給 KMB，問題主體不行。**

### 7.2 dense 的盲點集中在四張樞紐表

每張表「被需要時的相似度排名」：

| 表 | 被需要幾次 | 中位排名 | **最差排名** |
|---|---|---|---|
| orders | 68 | 2 | **35** |
| customers | 58 | 2 | 11 |
| products | 57 | 4 | **52** |
| order_items | 33 | 2 | **28** |
| **其餘 63 張表** | 162 | **1** | **11** |

**63 張罕用表最差排名 11，從來不是問題。全部的深層漏失風險集中在四張樞紐表。**

原因是 §2.3：**相似度看得到主題，看不到結構必要性。**
問「各類別的營收」時 `order_items` 一個字都沒出現在問題裡。
**愈是樞紐的表，愈常在「沒被提到」時被需要** —— 而且它們被需要的次數最多
（前四張合計佔全部需求的 57%）。

FK 度數證實了這個診斷 —— 度數最高的三張正是最常被需要的三張：

```
customers 21   products 19   orders 18   employees 6   其餘 76 張 ≤ 3
```

**度數與「被需要次數」幾乎完全對齊，而相似度排名不對齊。**
結構訊號知道哪些表是樞紐，語意訊號不知道 —— 這就是樞紐表保留席的依據（§2.11）。

### 7.3 兩層的退化通道不同，要盯兩個數字

| | dense 層 | LLM 選表層 |
|---|---|---|
| 退化長什麼樣 | 需要的表排名往下掉 | **近義表把正確的表擠掉** |
| 隨規模怎麼漲 | 深度需求成長得比表數慢 | 近義表數量增加 → 咬的次數增加 |
| 用什麼吸收 | 加大 `CANDIDATE_N` | **加大候選只會讓近義表更多** |
| 量它的工具 | `--k`（零 LLM，可隨便跑） | `--funnel`（會存 picks 供離線重算） |

**擴表的代價 100% 落在 LLM 選表層**，這有三組獨立證據：

```
86 → 93 張表   dense 候選@40  98.8% → 98.8%，被擠出候選 0 題，平均位移 +0.17 名
               LLM 選表       新增 5 題全滅，全部是「挑了字面更像的舊窄表」
```

擴表前我用餘裕分析算過一個上界：「再加 14 張同等競爭力的表，最壞情況下 6 題會被擠出候選」。
**上界是對的，但鬆到沒有用** —— 它假設新表全部排在正解前面，實際只位移了 0.17 名。
原因很單純：**正解表的最差排名中位數是 2，離第 40 名還有 38 名餘裕。**
候選層不是被「表數」壓垮的，是被「正解表排名」壓垮的。

**三種病因，三種解，不要混為一談：**

| 病 | 有效的解 | 無效的解 |
|---|---|---|
| 問句點名**實體的值**，不含概念詞 | **值索引**（排名 43→4） | 改註解（最多到 19）、KMB（葉節點） |
| **問句本身有歧義**（「單價」＝售價還是成交價） | **改問句消歧** | 值檢索（無值可命中） |
| 正解表的概念**不在表註解裡**（盲區） | 補概念家族進表註解 | 值檢索、加候選 |

### 7.4 擴表時不會壞的東西

- **評估框架**：一般題的 GT 由 sqlglot 從 GT SQL 解出，加表不會讓它失效；
  而且加表會讓題目變難 —— **既有題目正好變成規模的回歸測試**。⚠️ 防禦題例外（§6.2）。
- **既有資料**：只要守「只 CREATE + INSERT、專屬亂數種子」，既有表一個位元都不會動。
  **這件事要用逐表 SHA 指紋證明，不能用「我沒有動它」保證。**
- **向量快取**：key 是文件雜湊，新表只算新表。整份 5.44 MB / 205 筆，
  其中 93 筆是現行表註解、其餘 112 筆是歷次實驗的文件變體 ——
  雜湊當 key 的代價就是舊變體不會被清掉，這是刻意的（重跑任何一個被否決的臂
  都不用重新付嵌入成本）。
- **KMB**：表變多它反而更有用。這是唯一一條「加表之後變好」的。
- **檢索延遲**：80 個向量實測 36 ms。純 Python 到 1,000 張表約 450 ms 都還能用，
  換 numpy 一步就解決（約 2 ms），能撐到萬張表。

### 7.5 「干擾表」不是表的屬性，是「表 × 題目」的二元關係

93 張表裡有 11 張宣告 `never_answered: true`，其餘 82 張都有 GT 覆蓋。
這個比例是刻意的 —— 曾經有 46% 的表在測試集裡一次都沒當過正確答案，而那低估了問題：

> **一個學會「這些表永遠別選」的檢索器，在那樣的測試集上會拿滿分。**

看一眼清單就知道沒有一張是「沒用的表」：`invoices` 是財務每月要對的帳、
`support_tickets` 是客服日報。**是題目集把它們釘死在干擾的角色上。**
同一張 `payment_attempts`：問「上個月成功付款金額」它是干擾，
問「哪些訂單刷卡失敗過」它是唯一正解而 `payments` 變成干擾。

這句話反覆咬人：稽核工具兩次把干擾實作成「表的屬性」——
一次把補過題的表誤判成高風險（命中 12 → 修一行變 2），
一次因為「所有 GT 都沒用到的表」這個定義而**直接跳過某張表、一題都不比**，
於是漏掉了一個真實歧義。

**兩種表都要，比例約 8:2**：業務表（會被問到，82 張）測「在 93 張表裡撈出一張
長得跟鄰居很像的正解表」；基礎設施表（永不作答，11 張）測「稀釋律下，
永不相關的表會不會把正解擠掉」。

**加表不配題，等於沒加。** 六張寬表建完但 GT 還沒補的那段時間，
`eval_column_recall` 照樣回 100% —— 表加了、欄位加了、量測完全沒動，
因為**沒有題目會去要那些欄位**。補完 22 題之後同一支程式立刻掉到 95.2%。

加表造成的真實歧義要靠**改問句**消歧，不是改模型：某題「使用了折扣碼」
模型走 `coupons`（券碼）、GT 走 `order_promotions`（檔期）—— 兩個都說得通，
改成「套用了**促銷活動**的折扣」之後 8/8。**模型比我更貼字面，那不是模型的錯。**

### 7.6 表必須有資料，準則要 assert 不能靠感覺

全庫空表 0 張。零列的表有兩個壞處：

**① 必然的假通過。** `expect: empty` 的題目「回 0 列就算對」，而**任何對零列表的
查詢都回 0 列**。這比僥倖更嚴重 —— 僥倖需要「兩欄剛好相等」的巧合，這一條不需要任何巧合。

**② 對整類方法瞎掉。** 熱度先驗、值檢索這些「用資料當訊號」的方法在全空的表上
會**假性滿分**。灌完資料之後某方案的表面收益從 +5.1pp 砍到 +1.9pp，
**在同一份程式碼上用開關驗證的**。

`tools/seed_distractor_data.py` 每條準則都有對應的 assert，
而**每一條都是被實際擋下來才寫進去的**：

1. **列數比例合理，且不能一律偏小。** `payment_attempts`(239) 必須多於 `payments`(164)
   —— 每筆付款至少一次嘗試。干擾表一律小的話，**列數本身就變成免費的答案洩漏**。
2. **值域與正解表重疊**（否則值檢索一秒分辨得出來，量到的有效性是假的）。
3. **參照完整性**、**時序一致**（子事件不早於父事件）。
4. **查得出「像樣但錯」的答案**，而不是靜悄悄的空集合。
5. **小表不能靠機率。** 三個各 p≈0.5 的服務旗標獨立抽，8 家門市的期望值是 1.0 ——
   實際抽到 **0**。維度表本來就只有 8~10 列，**題目要問的條件必須用保證式配置寫進去**，
   在寫入前調整已生成的列（不動亂數序列）。
6. **非空不等於有鑑別力。** 「維修門市 ∧ 有全遠端員工」= 6 = **全部**維修門市 ——
   形式上是真子集，實際上另一半條件對答案毫無影響。判準改成：
   **交集要同時小於兩邊各自的大小**，而且單一條件涵蓋率**上限 75%**。
7. **一致性要對齊母表狀態，不只自己表內的時序。** `resigned_at` 各擲各的骰子，
   生出「`is_active=1` 卻有離職日」的資料，而當時的檢查只驗「離職不早於到職」，完全通過。
8. **歧義從資料端消掉，比事後補 `alt_sql` 乾淨。** 有一家門市 `is_active=0`，
   它若落進答案，模型多寫一個 `is_active = 1` 就與 GT 對不起來 —— 那是題目的歧義。
   配置時直接只挑營業中的門市，這個歧義就不存在了。
9. **鑑別力要分兩層檢查：條件的、以及輸出欄位的。**
   某題符合條件的兩檔活動門檻完全相同、流量來源集合也完全相同，
   所以**模型只查到其中一檔也會拿到一模一樣的答案而過關** ——
   準則 6 管的是條件，這一條壞在**輸出欄位**沒有鑑別力（兩個不同實體投影到同一組值）。
10. **雙寬表題要驗「單表捷徑會不會給出不同答案」。** 只給易碎商品貼標籤的話，
    「貼標籤」是「易碎」的**子集**，那題根本不需要 join。補上「非易碎但也貼了標籤」
    的實務情形，兩邊才互不蘊含（易碎 73、貼標籤 63、交集 51）。

順序必須是「**先宣告值域，再稽核，最後才灌**」—— 判歧義需要知道值域，
而值是灌的時候才決定的。所以值域先宣告成資料（`distractor_seed_plan.yaml`），
稽核過的才准灌。

**歧義稽核的判準演化**：第一版只比欄位名，跑出 26 個 HIGH 幾乎全是誤報 ——
`customer_id`/`created_at` 在任何正規化 schema 裡到處都是，
**拿它們判歧義等於宣告「所有子表都可以互換」，那不是歧義，那是正規化**。
第二版逐題比對爆出 869 個命中 / 115 題，完全不可用。救援來自 19 筆手工裁決，
而**理由幾乎逐字相同**（「那張表沒有 `order_id`，接不到訂單這個主體」）——
同一條推理寫了 19 次，那就該寫進程式：加**連通性**（干擾表要在 FK 圖 1 跳內接得到正解表）
＋ **只共用一個欄位降級 LOW** → **869 個 / 115 題 → 12 個 / 10 題**。

誠實的殘留：**同概念、不同欄位名**這一類抓不到
（`return_shipments.received_at` vs `status='RECEIVED'`）。字串比對看不見它。

### 7.7 語意重疊是刻意的：第二批擴表怎麼配

第一批六張寬表各自掛在一個**還沒有寬表**的主體上，彼此不搶題。第二批七張反過來：
每一張都掛在**已經有人在管那個概念**的主體上（`payment_profiles` vs `order_profiles`
的結帳情境、`shipment_profiles` vs `product_profiles` 的易碎／冷鏈…）。

**為什麼一定要重疊**：語意獨立的表永遠不會進候選前段，加了只會讓分母變大而漏題數不變
—— **分數上升、能力沒動**。那是「干擾表全是零列時熱度先驗假性滿分」的同一個病。

建表時被準則擋了三次，每一次都是真問題：

- **母表的值域用猜的。** `order_returns.status` 實際只有 `RECEIVED/REJECTED/REQUESTED`，
  我寫的是 `COMPLETED/APPROVED/DONE`，結果 18 筆全部沒有檢驗紀錄。
  改成從母表推導，反而讓準則有東西可檢查。
- **把三態當成兩態。** `subscriptions.status` 是 `ACTIVE/CANCELLED/PAUSED`，
  我把非 ACTIVE 全當成「已取消」，等於**自己另外發明一組暫停名單**。
  這種資料不會報錯，只會讓「目前暫停中的訂閱」的答案與母表對不起來。
- **two_wide 題退化成單表題**（見 §7.6 準則 10）。

### 7.8 資料矛盾：三種形狀、三個閘門

> **冗餘可以，矛盾不行。**
> 兩張表記同一件事、**值相同** = 好的檢索測試（93 張表的設計目的）。
> 兩張表記同一件事、**值不同** = 壞掉的 benchmark，因為「對」沒有定義。

這條界線同時回答了「反向指標算不算作弊」：
**讓模型選對表的提示是作弊，讓兩張表講同一句話是資料衛生。**

#### 病灶：`R.randint(...)` 冒充衍生量

|欄位|原本|打架的對象|
|---|---|---|
| `review_profiles.days_after_delivery` | `_pick([1..45])` | 從 `reviews`/`shipments` 算，97 筆只有 1 筆吻合 |
| `promotion_profiles.used_count` | `randint(3,80)` | 合計 359 vs 實際 41 |
| `product_profiles.image_count` | `randint(1,12)` | 合計 236 vs `product_images` 123 |
| `customer_profiles.last_login_at` | `anchor − randint(0,120)` 天 | `customer_login_logs` 的表註解**自己寫著**「最近一次登入時間在 `customer_profiles.last_login_at`」 |
| `review_profiles.has_merchant_reply` | `random()<0.4` | `reply_count`、`merchant_reply_at`、`review_replies` **三個欄位彼此都對不起來** |

**危險的不是欄位不一致，是「篩選條件在母表也算得出來而且答案不同」。**
只有 profile 有的條件（`content_completeness<60`）模型沒有第二條路可走，不痛；
真正壞掉的是兩條路都走得通卻指向不同答案的題 —— **那題沒有唯一答案，GT 卻只認一個。**

#### 三個方向，三個閘門，每一個都看不到另外兩個

```
縱向 [9]   寬表衍生欄 vs 從母表現算         前提是「存在可現算的母表」
橫向 [11]  兩張平行表對同一件事各擲各的骰子   is_discontinued 不衍生自任何東西，[9] 看不到
全庫掃描   不看題目，掃每一對共用母表的表     [11] 的候選來自問句中文詞，只看得到有人問過的字
```

**閘門 [11] 的判準：候選用問句驅動，判決用資料。** 純 SQL + regex，零 LLM：

```
互補   交集 = 0 且 A + B = 母體   → A = NOT B，正常
包含   交集 = min(A, B)           → 小的是大的子集，正常
互斥   交集 = 0 且 A + B < 母體   → 這個詞指向兩個不相交的集合
獨立   |phi| < 0.25               → 兩顆互不相干的骰子
一致   其餘                       → 冗餘，正是設計目的
```

**踩到的三個坑，每一個都改了設計：**

1. **「交集 0」本身不是缺陷。** `resigned_at` vs `is_active`（phi = −1.000）是**互補**的
   —— 離職 = 非在職。補上互補與包含兩條放行規則。
   同一個形狀後來又出現一次（「現在訂著」vs「已退訂」天生互斥，剩下的是從來沒訂過的人）
   —— **「互斥」只有在兩個謂詞宣稱切開整個母體時才是缺陷。**
2. **「表裡有沒有列」不主張任何事實。** 兩張都掛在 `customers` 底下、彼此無關的表
   phi ≈ 0 是它們**應該**有的樣子。改成**結構上**分兩層：
   註解寫著「是否已停產」的**欄位**主張那個事實，自動判決；表存在性不主張，只列出不判決。
   **不是調門檻分的。**
3. **字串層調不準，而調它是本專案禁止的事。** 詞組「訂閱」會把
   `opt_out_marketing`（是否退出行銷訊息）和 `subscriptions.ended_at` 配成一對。
   原型單靠字串在 309 題裡標了 **161 題（52%）**。處置沿用閘門 [10] 的做法：
   **人工裁決一次寫進 `ADJUDICATED`（含理由），閘門之後只守迴歸**，新出現的配對一律紅燈。

**閘門要能自己放行。** 裁決為「待修」的配對**會回頭看資料**，修好了自動轉「已修」——
沒有這一步，修完之後閘門會永遠紅燈，於是三天後被人忽略。

#### 最糟的一個實例：benchmark 自己在教模型走錯路

```
product_specs.is_discontinued    COMMENT '是否已停產'          ← 逐字命中問句
product_profiles.lifecycle_stage COMMENT '…／EOL 停產'
is_discontinued = 1  →  1 筆 ｜ lifecycle_stage='EOL'  →  12 筆 ｜ 交集 0（完全互斥）

某題「已經停產的商品有哪些？」   GT → is_discontinued = 1        1 個商品
另題「列出所有已停產商品的…」    GT → lifecycle_stage='EOL'     12 個商品
```

同一份 GT、同一個詞、兩題的正解是**兩個互斥的集合**。
模型在第二題上做的事，正是照第一題的正解做的 —— **它不是選錯表，是答對了另一題。**
兩支腳本各擲各的骰子、誰也不知道對方存在；設計說「這兩張表講同一件事」，
灌資料時沒有把那句話兌現。

另一個形狀是**模型多做一次交叉驗證才錯的**：

```sql
模型  WHERE sp.picked_up_at IS NULL AND op.delivery_preference = 'STORE'   → 0
GT    WHERE picked_up_at IS NULL                                           → 2
```

兩個編碼 `phi = −0.009`，**統計上完全獨立**。**模型愈謹慎，錯得愈徹底。**

#### 處置判準：路徑唯一就改資料，路徑不唯一就改問句

| 判準 | 例子 |
|---|---|
| 母表**唯一決定** → 對齊資料 | `is_over_policy_window`、`attempt_count`、`image_count`、`used_count` |
| 兩個量**本來就不同** → 收窄問句 | `clicks` 合計 134,911（廣告平台尺度）vs `campaign_clicks` 418 列（站內記錄），對齊會毀掉語意 |
| 母表**算不出唯一答案** → profile 欄位就是真相來源 | `reviews` 沒有 `order_id`，134 則裡 97 則連得上、其中 10 則連到 2~3 個不同到貨日 |
| 落差是**刻意的設計** → 宣告不動，但要有可驗證的不變量 | 快照落後 30 天 |

**真相來源怎麼判：有佐證的贏；都沒佐證則原始窄表贏**（後加的寬表是獨立擲的骰子）。
同一組表的三個欄位因此判到不同邊 —— 兩個判給窄表（兩邊都沒佐證），
一個判給寬表（`scope='CATEGORY'` 的 3 檔 `included_category` 全有值、`='ALL'` 的全 NULL，
完全吻合註解宣告的不變量）。

**快照族：從「宣告的例外」升級成「可驗證的不變量」。**
四個欄位的註解本來就寫著「（快照）」，值卻是 `randint(...)`；
而另兩個實測**剛好等於** anchor − 30 天的截止值（50/50 全中）——
也就是說截止點一直是可驗證的，只是沒人驗，它們被寫在「不檢查」名單裡。

```
宣告  只能說「這兩個量本來就不同」    ← 分不開「刻意落後 30 天」與「根本沒在算」
檢查  能說「它等於它該等於的那個量」  ← 下次有人動到快照邏輯會當場報錯
```

所以那四欄對齊到**同一個截止點**，不是對齊到即時值 ——
**對齊到即時值會讓同一張表一半欄位是快照、一半是即時，那是更難發現的矛盾。**

**落差本身也要有守門員。** 檢查保證「快照等於截止點的值」，但保證不了
「快照與即時值不同」—— 而那個差距**就是**三題快照題的鑑別力來源。
所以另外印一行「與即時值不同 27/50 位」，**歸零就警告**。
（同一個設計也用在閘門 [10]：**分歧名單變短本身就是警訊。**）

#### 一共下了 35 條對齊，全部只有 UPDATE

```
93 張表 12 張指紋變動、**列數 0 變動**
309 題 GT 全部可執行；八項閘門全綠；閘門 [10] 分歧名單沒有被順手改掉
fix_derived_consistency.py 再跑一次 35 條全部報「要改 0 列」（可重複執行）
```

⚠️ **建表腳本一起改，否則重跑會洗掉修正。** 但刻意寫成 post-pass `derive_from_parents()`
而不是改 `gen_*` 裡那一行 —— **`R.randint(...)` 必須留在原地，
少抽一次整條亂數序列就平移，GT 全毀。**

幾條值得單獨記的：

- **`supplier_profiles.payment_terms` 8/8 全錯**，因為兩邊用不同詞彙寫同一件事：
  合約寫『月結60天』，檔案寫『NET60』。而『貨到付款』在檔案端的列舉裡**根本不存在** ——
  順帶把 `COD 貨到付款` 補進欄位註解，**否則對齊會產生一個註解說不存在的值**。
- **`is_split_payment` 對齊後整欄變 0**：它宣稱 18 張訂單分多筆付款，
  但全庫沒有任何一張訂單有 2 筆以上的 `payments`。恆為 0 的欄位沒有用處，
  但它至少不再說謊。（注意分期 ≠ 分多筆付款，兩者不該互相對齊。）
- **只改值不增刪列**，所以「嘗試 3 次的出貨單」與 `attempt_count` 都不受影響。

#### 掃到、但**不改**的六組

「值不一樣」不等於「缺陷」。判準：**有沒有哪個業務問題因此有兩個答案。**

| 現象 | 為什麼不是缺陷 |
|---|---|
| 19 台沒放棄的購物車收到挽回信 | 挽回信寄出時是放棄狀態，客戶回訪後旗標變 0 —— 這正是挽回信的目的。實測時間先後 9:10，**兩個方向都有，資料不支持也不反駁**，而修它要刪列 |
| 34 張訂單既有出貨單又有門市取貨 | 一張訂單多件商品分開履約是真實的；兩張表都沒有宣告互斥 |
| 4 筆 `REJECTED` 的退貨有收件時間 | 先收到再拒絕是合法流程 |
| 33 張訂單有分期期數但 `is_split_payment=0` | 分期 ≠ 分多筆付款 |
| 5 間門市有服務預約但該服務旗標是 0 | **18 筆全部在錨點之前**。旗標是「現在提供不提供」，預約是歷史事件。而且沒有任何題目把兩者 JOIN 起來 |
| `ended_at` vs `contract_end_date` 6 筆不同 | 合約到期日 ≠ 實際結束日，提前解約本來就會不同 |

最後一列尤其要記：**如果把服務旗標「對齊」成有預約就設 1，涵蓋率會變成 100%，
直接違反配題的鑑別力上限（§7.6）。把不是缺陷的東西「修好」，代價是毀掉題目。**

#### 驗收（事前選出集合，所以合法）

驗收集合是**事前**選的 ——「GT 的 SQL 直接引用了被改過的欄位」，不是事後看「哪幾題結果變了」，
所以不觸犯 §5.4：**17 題 × n=8 = 穩定過 16｜擲硬幣 1｜穩定錯 0。**
其中最重要的一格是某題 **5/8 → 8/8** —— 它是整條線的起點（模型伸手拿兄弟表的
`is_discontinued`，而那一欄跟 `lifecycle_stage` 交集 0）。
**沒有動模型、沒有加規則、沒有改問句 —— 只是讓兩條路指向同一件事。**

#### 這不是本專案獨有的坑

AmbiQT（EMNLP 2023）建 3000+ 題歧義 benchmark，注入手法之一就是
「**加入聚合欄位，與 GROUP BY 現算並存**」—— 也就是我不小心做的事，
差別在它有兩份 gold answer。CIDR 的一份研究量到 BIRD mini-dev **32%** 的題目帶標註錯誤
（金融領域 49%），四類錯誤裡就有「題目本身有歧義」。
業界解法分三層，而**第一選擇不是讓模型選對**：
① 資料端（指定真相來源 + 排程對帳，dbt tests 的典型斷言是「staging 的 revenue 總和
必須等於來源，容差 0.01%」）② 語意端（semantic layer 讓指標只定義一次）
③ 題目端（per-question evidence 句）。

### 7.9 檢索範圍不是邊界，所以 3d 層是聯集不是淘汰

**問題**：模型可以對一張**沒拿到欄位定義**的表寫出正確 SQL ——
DDL 附的「其他表的表名」那一行說「需要時可直接使用」，
而 24 則 few-shot 反覆示範了 `orders`／`customers`／`products` 的欄位。

```
A（K=0）  909 次可解析   越界 5 次（0.6%）  3 題    全部答對
B（K=2）  914 次可解析   越界 15 次（1.6%） 6 題    全部答對
越界引用到的表：orders 12、products 5、customers 3   ← 只有這三張核心窄表
1,823 次取樣裡越界 20 次，**答錯 0 次**
```

**所以這不是幻覺**：3a（全庫白名單）擋掉不存在的表、3b（欄位存在性）擋掉不存在的欄位。
它是「用了真的存在、但這一題沒給它 DDL 的表」。

#### 三層驗證器的性質完全不同，不可以套同一種嚴格度

| 層 | 來源 | 會不會錯 | 處置 |
|---|---|---|---|
| 3a 全庫白名單 | `INFORMATION_SCHEMA` | 人定的，不會錯 | **硬擋**（安全邊界） |
| 3b 欄位存在性 | `INFORMATION_SCHEMA` | 不會錯 | 硬擋 ＋ 把真實欄位附回去 |
| 3d 檢索範圍 | **機器猜的**（召回 98.4~99.3%，已知會漏） | **會漏** | **聯集，不淘汰** |

**攔截的代價是三項實的：**

- `MAX_RETRIES = 2` 是跨節點**共用**的總預算，一次攔截吃掉一半；
- `executor_voter` 是對 `valid_sqls` 投票，淘汰**等於改變投票結果**；
- **拿安全層的嚴格度去執行一個會漏的猜測，是把「檢索的錯」升級成「生成的死路」。**

而攔截原本宣稱的第二個好處**查證後不成立**：「欄位改名時 few-shot 還在教舊名字，
這條路徑不會報錯」—— 零 LLM 探針直接否掉，3b 拿真實 `INFORMATION_SCHEMA` 比對，
schema drift 早就擋住了並且會列出合法欄位。**3d 在這一項上沒有多買到任何東西。**
剩下真正站得住的只有「檢索指標失真」，而那**只需要記錄，不需要攔截**。

#### 處置

`SCOPE_MODE`：**`union`（預設）** 不淘汰，把那張表聯集進 scope、補上它的 DDL、
記進 `scope_extra`；`strict` 淘汰並重試；`off` 不檢查也不記錄。

- **`scope_extra` 無條件記錄** —— 它是這一層唯一站得住的收益（檢索指標校正），
  而且要在 SQL 通過時也記得到，union 模式下通過才是常態。
- **刻意不寫回 `retrieved_tables`** —— 那一欄是檢索層的輸出，混進來的話
  「檢索漏了、生成端救回來」會被記成檢索沒漏。**`scope_extra` 正好就是 §5.3 缺的那個指標。**
- 補的 DDL 是給**之後的輪次**用的，補過那張表就在 scope 裡，不會再次觸發，不會迴圈。

**這與 RSL-SQL 的 backward pruning 是同一個 union**，差別只在
**它對每一題先付一次全庫 call 去製造訊號，這裡等模型真的伸手才付**：

```
全庫 DDL       77,731 字元        剪裁後平均 2,186（中位 990）   → 35.6 倍
越界觸發率      20 / 1823 = 1.1%
```

無條件版 = 對 100% 的題付 35.6 倍 payload ＋ 每題多一次 LLM call，
去換一個 1.1% 才用得到、而且**已經免費救回來**的訊號。
**且它的第一步正是「把完整 schema 丟給模型自己挑表」—— 就是本專案要避免的那件事。**

#### 準確率不必量：等價性證明覆蓋了它

`schema_ddl` 只有 `sql_generator` 讀，而它只在**重試**時才會被再讀一次；
`scope_extra` 不進任何 Prompt。所以：

> **union 與 `off` 唯一可能不同的樣本 = 「有越界」∩「有重試」。**

```
總樣本 1854 ｜ 有越界 20 (1.08%) ｜ 有重試 19 (1.02%) ｜ **越界 ∩ 重試 0**
```

**這 1854 次取樣裡 union 的行為與 `off` 位元相同**，而那六輪本來就是 3d 之前跑的
—— union 的 e2e 準確率已經有 n=6 輪 × 309 題的量測了。
不需要再跑一次 12 題的 A/B 去「驗證」它：那樣的解析度反而更差。

⚠️ 這個等價是有條件的：交集期望值約 0.011%，**它遲早會落到那一格**。
落到時 union 的行為是「重試的 Prompt 多一張真實 DDL」，資訊嚴格多於 `off`，方向是正的。

#### 管線驗收（三個判準，全過）

| 判準 | 門檻 | 實測 |
|---|---|---|
| 管線通 | `scope_extra` 非空 ≥ 1 次 | **14 / 27** |
| 越界的 SQL 必須對 | 14 條全對 | **14 / 14，0 錯** |
| 防禦題 | 12/12 | **12 / 12** |

判準二是唯一可能翻盤的地方 —— 它沒翻。**而且這 14 條的 `retry` 全部是 0**：
它們**第一輪就通過快篩、而且答對**。strict 模式下這 14 條會被淘汰、逼一次重寫
—— **淘汰的是已經正確的 SQL。代價不需要 A/B，這是計數不是比較。**

（管線通那一條刻意寫成「0 次 ⇒ 判『未測到』而非『通過』」，
就是為了不重蹈 §5.6 那個 `log.remove()` 的覆轍：**不能因為沒看到訊號就當作沒問題。**）

---

## 8. 反覆咬人的四個失敗形狀

這一節是這份文件最貴的部分 —— 前面每一節的具體數字都會隨規模改變，這四條不會。

### ① 寫了，卻沒有接到既有的產生鏈上

| 實例 | 症狀 |
|---|---|
| `gen_ddl.py` 沒重跑 | 檢索選對表、生成端看不到欄位 → 誠實回「schema 不支援」，**在對帳表上跟防禦題長得一模一樣** |
| `sync_table_comments.py` 漏了一個來源 | 改了註解不生效 |
| `schema_parser` 的 `\);` regex | 表級 COMMENT 進 DDL 後**直接回空 dict** |
| `get_few_shot_text()` 不渲染 `reasoning` | 22 則、4,649 字元全是死的，模型只拿到 SQL 形狀那一半 |
| `eval_score.py` 的分母 | 把 4 題防禦題數了兩次，正確率**低報 1.5pp** |
| `enum_fields` 沒有人接 | 擴表時 DDL 接上了產生鏈，49 個代碼欄位的合法值只活在註解的散文裡（§4.6） |

**共同形狀：新增了一個來源／欄位，卻沒接到下游；東西還在、看起來正常、不會報錯。**

第四個實例的殺傷力最大：「以後都從 few-shot 解決」這條方針
**在修好之前結構上一直是做不到的** —— 過去每一次「加了 few-shot 沒效果」的觀察
都要重新看待。而同一個檔案往上 20 行，`get_rules_text()` 早就修過同一個 bug。

第五個實例揭示了為什麼這類 bug 活得久：**它只在有人真的去讀那一行時才咬人。**
文件裡其他的百分比都是照「錯幾題」反推寫的，數字剛好都對；
唯一一處直接照抄輸出的，就錯了那一次。

**第一次事前堵住的**：`customer_profiles` 的 6 個衍生欄位**結構上無法**
在建表時對齊（母表那時還沒建），所以「重建了卻忘記跑對齊」是必然會發生的一步。
這次沒有等它咬人 —— 閘門 [9] 加了 `DERIVED` 名單，那 14 欄
**不管有沒有題目引用一律紅燈**（黃燈的語意是「還沒有人負責這一欄」，
有人負責的欄位對不上只有一個意思：那一步沒有跑）。

（順帶：缺 DDL 曾經是一張**意外的安全網** —— 22 題選到了沒有 DDL 的表，
但 0 題真的用了它們，因為**沒有欄位就寫不出 SQL**。補 DDL 等於把網子拆掉，
所以拆之前先離線量了三個數字才動手，結果零回歸。）

### ② 靜默失敗：不會叫的錯比會叫的錯貴得多

| 層級 | 會叫的失敗 | 不會叫的失敗 |
|---|---|---|
| 表 | 答不出來、EXPLAIN 報錯 | — |
| 候選天花板 | — | 必要表落在第 41 名外，**分數會掉但沒有東西會報錯** |
| 欄位 | — | 模型挑一個相近的，SQL 合法、結果錯 |
| 防禦題 | — | 加表讓概念變可回答，**系統答對卻被判錯** |
| 誠實性 | — | 守衛沒開火：模型發明公式，**使用者看不出那數字是憑空來的** |
| 評分 | crash | **給你一個看起來合理的錯數字** |
| 資料 | — | 兩張表對同一件事各擲各的骰子，**題目沒有唯一答案而沒有人喊** |
| 儀器 | — | 檢查腳本吞掉例外，**報一個假的乾淨** |

**設計原則：讓失敗變得有聲。** 三個形狀相同的手法 ——
DDL 附上「其他表的表名」、裁剪時留一行「本表另有 N 欄未列出」、
`_augment_schema_error` 把真實欄位附回去。

> **讓模型知道有東西被省略，比乾淨地騙它說沒有更安全** —— 不知道就會拿手上的湊。

### ③ 外推只能決定「什麼時候回來量」，不能當結論

被實測推翻過兩次，**而且方向相反**：

| | 外推說 | 實測 | 錯的方向 |
|---|---|---|---|
| 純 Python 餘弦（外推到 1,000 表） | ~150 ms | 按 80 表的 36 ms 重算 **~450 ms** | **樂觀 3×** |
| dense Top-1（外推到 80 表） | ~57% | **84.6%** | **悲觀 28pp** |

第二條錯得比較有意思：它把 Top-1 衰減建模成表數的線性函數，
但真正在動的是**表註解品質與 GT 覆蓋**。**表數翻倍，Top-1 反而回升，
干擾搶 Top-1 從 17 掉到 0。**

**觸發條件要寫成可量的指標，不是規模數字。** 稀釋定律的重測條件寫的是
「表寬中位數超過約 30 欄」，而後來中位表寬**還是 5 欄**（79 張窄表壓著），
真正變的是 >30 欄的表從 1 張變成 14 張 —— 重測是**用另一個理由**站得住的，
不是觸發條件到了。**這個差別要記清楚，否則觸發條件會被當成可以模糊解釋的東西。**

同一條紀律也適用於**判決本身**：值檢索從「擱置」變成「上線」，
不是因為方案變了，是因為它的兩個前提在重測時都不再成立。
**否決與擱置都有保鮮期，要跟數字一起重讀。**

### ④ 對照組與檢查工具本身也要驗

見 §5.5（對照組）與 §5.6（儀器）。這一條要往上延伸一層：**連你在測什麼都要驗。**
三輪探針量測都把某三題當誘餌題在打，而它們根本沒有唯一答案（是我自己造的資料矛盾）
—— 扣掉之後同一組數字的解讀完全不同。

### 附帶：文件的過期也是靜默的

這份文件曾經有一張「已知缺陷」表從很小的 schema 一路留到 80 張表，每一列的 k/8
都還是當年的數字，而讀的人沒有任何線索知道它過期了 ——
**跟 `gen_ddl.py`、`reasoning` 是同一個形狀，差別只在那兩個有閘門會抓，文件沒有。**

---

## 9. 今天的已知缺陷

**穩定錯 0 題。** 剩下的是三類：

**① 硬幣池約 9 題。** 同臂各輪錯題重疊只有 38%~67%，**一半以上的錯題是擲硬幣**。
硬幣池吃掉約 2.4pp，這就是為什麼六輪 1854 題只能量到「平手」——
**這個題庫在 n=3 下量不出 1pp 以下的效應。**

其中兩題是有效的評估點，**不要再修**：

- 某題的失敗簽章恆為「36 列 vs 34 列」＝少濾 `is_deleted`，pooled 約 75%。
  三層都查過了：題庫層（觸及該家族的 10 題 GT **10/10 都濾**、10 題問句**沒有一題**
  提到刪除，慣例完全一致）、檢索層（每輪都拿到那張表）、註解層（欄位註解就寫著
  「1 表示已被刪除**不應顯示**」，還有一則 few-shot 專講）。
  **模型沒套用一條它看得到的慣例 —— 那正是要測的東西。**
- 另一題兩臂失敗模式逐字相同，是題目理解問題，與任何 prompt 介入無關。

順帶量到一個機制：舊問句時代模型退化成布林 `EXISTS` 的那幾次，
**同時也把 join 丟掉了**；寫明細或 COUNT 的那幾次 100% 帶著條件。
**形狀一縮，附帶條件跟著掉** —— 這是收窄問句本身的另一個理由。

**② 兩個完全沒有指標在盯的方向：**

- **過度拒答。** 防禦題 4/4 只查「該拒答的有沒有拒答」，查不到反方向。
  93 張表全送時有題誤觸防禦暗號 —— 那題用現有 schema 答得出來。
  **schema 大到一定程度，模型反而不敢答它明明答得出來的題**，
  這與所有人的直覺（表愈多愈容易湊出假答案）相反。
  現成的代理指標是 `SCHEMA_UNSUPPORTED` 的觸發次數，**還沒接**。
- **漏斗「排除」那一半的價值。** 見 §5.3，兩個方向都會失真。

**③ 一條沒量過的缺口：沉默的錯誤範圍。**
檢索漏表、而模型**沒有伸手**、就用手上的表湊出一個看起來合理的 SQL ——
`scope_extra` 是空的，3d 不會動。§5.3 的證據指向它很小，但沒有直接量過。
真要追，**條件式全庫臂**（只對檢索信心低的題開，例如三票不一致者）
比無條件 backward pruning 便宜一個量級。**未做。**

**還沒做（不是設計問題）**：`utils/semantic_layer.yaml` 與 24 則 few-shot
全部綁在現有 93 張表的真實表名，換一套 schema 要整份重寫。

**已知且刻意不修的資料落差**：`order_items.unit_price` 與 `products.price`
在種子資料裡**永遠相等**。兩者語意不同卻無法被任何測試分辨 ——
但重跑 `db/init_db.py` 會讓 309 題 GT 全部失效。

### 資料安全約束（改 schema 前必讀）

- **重跑 `db/init_db.py` 會重新隨機生成資料，309 題 GT 整份失效。**
  schema 要改請走 metadata-only（`ALTER TABLE … COMMENT`）或**純新增**
  （只 CREATE + INSERT，事後跑逐表 SHA 指紋確認既有表一位元都沒動）。
- **新增資料要用獨立亂數源。** 生成迴圈是 `for t, spec in plan.items()` ——
  走的是 **YAML 的順序，不是程式碼的順序**，所以「新的 random 加在最後」不夠。
  用 `RNG_LATE = random.Random(...)`，主序列一個數字都不動；否則會靜默作廢 36 題 GT。
- **不要新增會命中防禦題探針的欄位／表**：運費/freight/shipping_fee、
  利潤/profit/margin/supply_price、週轉/turnover/inventory/stock/capacity、
  信用評分/credit_score/額度/risk/score。擴表過程中就有兩張表因此被擋掉。
- **改資料前先查它是不是在撐一道題。** 「註解宣告了某個值、資料 0 筆」
  不一定是缺陷 —— 有兩題 `expect: empty` 的 GT note 白紙黑字寫著
  「考的是『欄位註解列了某個值 ≠ 資料裡真的有』」。**補資料進去等於刪掉那兩題。**
  GT 是活 SQL 對現行資料現跑，所以改資料不會弄壞計分 —— 傷害是**題意**與配題鑑別力，
  而**那種傷害不會有任何腳本喊**。判定要拿 `eval_ground_truth.yaml` 的 `expect` 對帳：
  有 `expect: empty` 的題打在它上面就是**撐題**（不准動資料），
  有 `expect: rows` 的才是缺陷。

---

## 10. 被否決與擱置的方案（提新方案前先查這張表）

| 方案 | 判決 | 一句話理由 |
|---|---|---|
| 固定 K 檢索 | 否決 | K=5 對 141 題慷慨五倍，同時對最難的 12 題召回 0% |
| 動態門檻 / Elbow | 否決 | r=0.85 用 1.7 錨點換 69.2%，K=3 用 3.0 錨點拿 86.6%；而且漏掉第二個錨點會連帶關掉 KMB |
| Top-6 再剪枝 | 否決 | 剪枝加不回不在名單裡的表 —— 那 12 題 K=8 召回還是 0% |
| `CANDIDATE_N` 改成表數的函數 | 否決 | 沒有上限＝這一層不存在；而且把「dense 召回不夠」偽裝成「候選開大一點就好」 |
| 縮 `CANDIDATE_N` 換目錄長度 | 否決 | 短目錄那臂**更差**，三個事前假設全被推翻。沒有機制就不再調這個常數 |
| 欄位裁剪 | 否決 | **沒有一個 K 是滿分**，而失敗是靜默的。真要做走兩層 DDL + 反應式補完，不要賭命中率 |
| 列數／熱度先驗 | 否決 | 候選召回每個 K 都變差，換到的 Top-1 買的是已經歸零的問題。本專案**沒有 query log，列數不是熱度** —— `payment_attempts`(239) 比 `payments`(164) 多，列數先驗幫的是干擾表 |
| 欄位優先並聯 | 否決 | 唯一有效的參數讓 @3 掉 2.6pp（另兩個參數值是**無效臂**：合成規則取 max 而該項永遠更小，數字與 baseline 一模一樣） |
| 向量資料庫 | 否決 | 93 個向量、實測 36 ms。下一步是 numpy 不是 Milvus |
| Hybrid BM25 + RRF | 否決 | 每個 K 都比純 dense 差；文件中位只有 60 字元，對 BM25 更不利 |
| LLM 合成 table docs／enum 對照／概念短語進檢索文件 | 否決 | 稀釋定律，三個實驗都是負的 |
| 概念群文件（一表 N 份短文件） | 否決 | 寬表誤選 6 → 19。**稀釋來自「機會數」不是長度** |
| Hubness 校正（CSLS） | 否決 | 沒有一組 (臂, λ) 贏過現行。**hub 在這個 schema 裡是真陽性，中心性就是相關性** |
| 把 coverage 報的缺口全部補進註解 | 否決 | 全補 vs 窄補**買到的東西一模一樣，代價差 13 倍**。判準是有沒有失敗證據，不是有沒有缺口 |
| 在任何一層加「寬表更值得看」的訊號 | 否決（五次） | 誘餌題與窄表題在同一條軸上，同一個旋鈕同時推兩端（§2.10）。**要提第六次，先拿出一個能區分「這題該用寬表」與「這題不該用寬表」的訊號** |
| 寬表註解的關係宣告句拆出檢索層 | 否決 | 12 題探針三組全贏，全庫配對 14:14，p=0.57。**做法保留**（`BRIEF_RELATIONS=strip`），因為它證明了「同一段 TABLE_COMMENT 拆給不同職務看」實作上是零成本的 |
| 欄位提示只給三張選票裡的一張 | 否決 | 漏斗 4:1 但 e2e 5:7，而且有一題三輪全輸、都是同一張表 —— 可重現的回歸 |
| 選表改用 20B 小模型 | 否決 | 召回略低而且 120B 反而比較快；20B 漏的是問題裡明講的東西 |
| 中英對照字典進 `COLUMN_COMMENT` | 否決 | 到不了選表層；生成器本來就會翻譯 |
| 加業務規則 | **硬性否決** | 規則表達不了情境相依的邊界，會跟 few-shot 打架。**但補既有規則的射程邊界是允許的，而且實測零代價** |
| few-shot 條件式注入 | 否決 | 溢出量到 ~0（候選 6 題重抽 n=8 一題都沒活下來，而且兩臂錯法一模一樣），貢獻側 17 題有機制。**買 ~0，賭 17** |
| few-shot 表名抽象成 `<表A>` | 否決 | 量不到污染，而抽象化會拿掉「形狀對齊」賴以成立的具體性 |
| 3d 越界引用改成淘汰 | 否決 | 它宣稱的第二個好處（擋 schema drift）3b 早就做到了；而 14 條越界 SQL `retry` 全 0 且全對，淘汰的是**已經正確的 SQL** |
| **多次取樣取聯集** | **採用** | 舊否決理由預設了「系統性漏表」不存在 —— 寬表進來之後它存在了（§2.7） |
| **打散候選順序** | **採用** | 召回區間不重疊，零成本 |
| **表註解補強** | **採用** | 四個檢索演算法方案全沒兌現，一句表註解修好一題 0/6 → 6/6。**但註解裡的例子只能取自資料** |
| **值／實體檢索** | **採用** | e2e 判平手，依據是零 LLM 硬指標與一項負成本（可以刪掉泛稱句）。**買的是斜率不是截距** |
| **欄位提示** | **採用（否決六次後）** | 六輪區間完全不重疊。**翻盤靠的不是新論證，是新量測 —— 提示的文字一個字沒變，改變的是模型** |
| **enum_fields 最小集** | **採用** | 非劣性（不是改善）＋ 機制被堵住。門檻低於欄位提示是因為**成本結構不同**：72% 的題一個字都不加 |
| **窄拒答規則 + 正向邊界** | **採用** | 代價可測為零、收益不可測時，代價為零就是理由 |
| **指路標不進檢索文件（D+）** | **否決** | 305 題上 Top-1 +2.0pp、修好 13 弄壞 7、寬表搶 1 從 24 降到 15，**但保留組不遷移**：10 題競爭配對裡 A 臂失手 5 題，D+ 只修好 1 題（判準是修好一半以上）。機制真實（`review_profiles` 註解寫「星等與評價文字在 reviews」，餘弦只看到 `reviews`，8/8 逐題全中），但新問法上只夠推動排名、推不動 Top-1。**305 題的數字是貼著那批題的** |
| **用途句（usage）** | **採用（保留組驗過）** | 三條事前登記判準全過：拿掉之後商業動詞問法 U1 **5/9 → 0/9**（均名 2.0 → 5.1），實體名詞問法 U2 4/5 → 4/5 幾乎不動，誘餌端 U3 不掉。**U1 崩而 U2 不動，正是「問題用動詞、文件寫名詞」那個機制**，而問句刻意避開了用途句的字面詞。它是會遷移的除錯介入，不是貼題 |
| **值索引改走候選聯集（`VALUE_MODE=union`）** | **採用** | 候選召回@40 兩臂都 100.0%，但 β=0.05 會換掉 3 題的 Top-1 錨點、union 換掉 0 題 —— **∪Top-1 的設計理由是誤差獨立，β 把純餘弦錨點變成混合訊號**。多帶候選 0.01 張。少一個綁在今天嵌入模型餘弦分佈（0.29~0.42）上的尺度常數。⚠️ e2e 未量，跟著下一輪六輪一起看 |
| enum 從檢索文件拿掉（清「拆了沒清源」的殘留） | 否決（理由已更新） | 只有 `orders`/`payments` 兩張、省 79 字元。**原本記的「Top-1 81.0% → 80.0%」是誤讀** —— 逐題拆開，6 題變動裡 5 題的第一二名餘弦差距是 0.0004～0.004（全庫中位 0.047），是硬幣不是證據。扣掉硬幣後可計分的只有 `#50` 一題，仍是退步 |
| enum 句改寫成中文核心業務說明（保留長度、只換內容） | 否決 | 三臂 A 247 / −enum 244 / 中文 244 —— 總分相同，只是換了哪幾題錯。`#8`/`#38`/`#75` 在中文臂**照樣掉**，所以那三題不是靠英文代碼也不是靠長度，是 `orders` 與 `order_profiles` 差 0.0006 的硬幣 |
| `−ptr` 依總分 +6 直接採用 | 否決 | 扣掉 16 題硬幣後淨值是 +4（窄表 GT 修好 6 弄壞 1、寬表 GT 弄壞 1）。**先前記的「6 題寬表 GT 退步」全部是硬幣**。要採用得先有一組射程內的保留組 |
| `holdout_ptr` C2 判準（A 臂失手 5 題修好 3 題） | 作廢（判準缺陷，非結果） | 那 5 題裡有 2 題落後 0.1228／0.1384，而 `−ptr` 的射程 p95 只有 0.0516 —— 推不動。判準等於要求可及的 3 題 100% 全中。C 組是基線沒餘裕，C2 是題目在射程外，兩種都是判準的錯 |
| `VALUE_MAX_TABLES` 2 → 3 | **採用** | 當初只在「2 與不限」之間二選一，沒量中間值。3 的實測：撈到 GT 表的題 12 → 16（`#24`/`#55`/`#95`/`#98`，「3C數位」「家電」住在 categories＋products＋customer_profiles 三張表），**零題失去**；證據字元 373 → 698（每題 1.2 → 2.3，候選目錄本身約 2,880）。候選階段是 `candidates + extra`，單調相加，召回不可能降；LLM 那一段跟著下一輪多輪跑收 |
| `matches()` 上限改數表而非 (表,欄位) 對 | **採用（純正確性）** | 常數叫 `VALUE_MAX_TABLES`、docstring 寫「跨表太多沒有鑑別力」，實作卻數欄位對。`order_status_history` 有 `from_status`/`to_status`，每個訂單狀態值光這張表就佔 2 格。現況下 0 行為變動（四種組合命中集合相同），要等代碼離開註解才生效（`#9 PAID` 就是它救回來的） |
| 只把代碼移出**表**註解 | 否決 | 解鎖 **0 個值**。`_keep()` 看的是表註解與欄位註解的聯集，兩邊都清才有效：只清欄位註解 3/14、兩邊都清 11/14 |
| 代碼移出資料庫註解（欄位 78 欄 ＋ 表註解 enum 句），改由 `enum_fields` 直送生成器 | **採用（五條事前判準全過）** | ① 值索引・含代碼 14 題 GT 命中 **0 → 11**（門檻 ≥9；未中的 `#11` 住 4 張表被 MAX=3 擋、`#155` 的 VIP 不是任何欄位的值、`#159` 是 `expect: empty`）② 其餘 291 題 16 → 16，零損失 ③ 生成器不失血：78 欄的每個值都在 `enum_text()` 裡，0 缺漏 ④ 檢索 Top-1 247 → **249** ⑤ 欄位提示 444 → 442（−0.3pp，14 組變動中 12 組是 k=2 名額互換） |
| `enum` 從 **retrieval** 投影拿掉 | 否決（第二次，理由更新） | `_keep()` 讀的是**資料庫**的 TABLE_COMMENT（＝`ddl` 投影），檢索文件是另一份行程內字串，從來不進 `_keep` —— 拿掉它對解鎖值索引**零貢獻**，卻讓 `orders` 少 48 字元，扣硬幣後淨 −3（`#9`/`#10`/`#50` 全是 `orders` 輸給 `payments`／`order_cancellations`）。能達成目的的最小改動優先 |
| 註解引用資料值字面 | 新增偵測器 `tools/probes/literal_in_comment.py` | `order_cancellations` 的指路標寫「請看 orders.status = 'CANCELLED'」，一句話讓 `CANCELLED` 對 4 題失效。改成「請看 orders 的訂單狀態」後解鎖，且檢索 Top-1 247 → 249。偵測器分三級（有代價／無代價／刻意保留），**命中清單不是待辦清單** —— 其餘 5 處「有代價」沒有量到的收益，不動 |
| 最後 10 個欄位的值域搬出註解（`strip_enum_round2.py`） | **採用（六條事前判準全過）** | E15 的自動判準（≥2 個代碼 token 且含活值）接不住的個案：只有一個代碼的 `hazard_class`、代碼是**別欄**的值的 `discount_value`、以及我上一輪誤列進 `FORBIDDEN` 的兩欄。判準：① 值索引 11 → **11** ② 291 題 16 → **16** ③ GT 表湊不齊候選 0 → **0** ④ `enum_text()` 9,410 → 10,024 字元、活值缺漏 **0** ⑤ 欄位提示 442 → **441**（門檻 ≥440）⑥ 陷阱家族 `#104` **8/8**、`#159` **8/8**（與 A 臂基線相同）。`literal_in_comment.py` 三級全部歸零 |
| `payments.status` / `risk_flag` 列進 `FORBIDDEN` 不清 | **撤銷（上一輪判斷錯誤）** | 理由是「`#104`/`#159` 考『註解列了值 ≠ 資料裡有』，清掉等於拆考點」。查了宣告才發現站不住：`payments.status` 的 arm 是**送**不是 `dup`，`enum_text()` 早就在直送 SUCCESS/FAILED/REFUNDED，註解那份是純重複。**值域不該住在註解裡，它住在 `enums`** —— 陷阱是從 DDL 註解層搬到值域區塊層，不是被拆掉，⑥ 的 8/8 證實模型讀哪一層都一樣。⚠️ `risk_flag` 有真坑：宣告只有 NONE/WATCH，**沒有 HIGH**（[4b] 拿資料驗值域），直接清會讓 HIGH 從模型視野消失 —— 要先補 HIGH 當死代碼 |
| 表註解漏寫欄位語意會不會害檢索不到 | **會，3 題；但補救的是值索引不是表註解** | 量 GT 表在向量檢索的**餘裕**（不是命中率）：中位 2、p90 11、p95 15，`#21`/`#2`/`#40` 掉在 `products` 排 62/48/46。三題都是問句點名**具體資料值**（「原子習慣」「運動鞋」「iPhone 15」），而 `products` 註解**有寫**「品名」—— 散文與具體值之間沒有語意橋，寫得再長也接不住。`VALUE_MODE=union` 已全數接住。全庫聯集平均補進 **0.010 張候選，補進去的全部是這 3 題** —— 這條通道只做一件事，而它做的正是這件事。推論：**每從註解放出一個代碼，就多一個能把表從候選切線外撈回來的鉤子** |
| 閘門 [4b] 把哨兵欄位判為「漏列」 | 修閘門，不修個案 | `defect_code` 的 DF-NN、`mon_open` 的時段字串、`member_tier_required` 的等級名稱都是**開放集合 ＋ 一個哨兵值**，宣告只列哨兵是對的。豁免條件認 `description` 裡的「只列哨兵」—— 用宣告自己說出來的意圖，不在閘門裡寫表名清單（那種清單會跟資料一起過期）。常態亮著的 WARN 會被當背景雜訊，兩個月後真的漏列活值時沒人會注意到 |
| 用 309 題的分數回答「架構準不準」 | **否決 —— 那是訓練集分數** | `VALUE_MAX_TABLES`、`COLUMN_HINT_K`、`FILTER_VOTES`、`VALUE_MODE`、三個投影、兩輪註解改寫，每一個都是看著這 309 題調的。要無偏估計就必須有一組**從沒參與過任何決策**的題。新增 `eval/testset_holdout.yaml`（123 題）＋ `testset_verify.py`（封存前六項檢查）＋ `testset_run.py`（封存後守協定）|
| 驗收集出題看 schema 註解 | 否決，改看**資料卡** | 看註解寫問句，問句用詞就會跟註解逐字對應（開發集 `#282`：問句「中途改過地址」／註解「是否中途改過送件地址」），量到的變成「註解抄得像不像」。`tools/make_data_cards.py` 只出結構＋真實值分布＋外鍵，**零註解**。但寫題的人讀過註解就不可能真隔離，所以再加機械兜底：問句與 GT 表註解的最長共同**連續**子字串 > 6 字就退回重寫。123 題全過 |
| 驗收集跑完看錯題清單 | **用機制擋，不用承諾擋** | ① 對 hash，題庫改過就拒跑（實測竄改一行即攔下）② 預設不輸出逐題，要看得加 `--show-failures` 而該旗標寫進 runlog ③ 每跑一次自動記 commit／分數／日期 ④ 同一個 commit 已跑過就擋（重跑同一版只會誘發挑數字）。封存後才發現 GT 錯的題**作廢**不修 —— 修完再跑等於用結果挑題目 |
| 驗收集含多來源歧義題 | 封存前抓出來改掉 | `customer_profiles` 的統計快照與逐張訂單現算，**27/50 位客戶不同**（`total_spent`／`order_count`／`avg_order_value`／`total_items_bought`）。問句沒指明來源就有兩個都說得通的答案，那種題永遠答不對。8 題改成明講「逐張訂單加總」，另 2 題明講「用客戶檔案裡登記的累計統計」—— 兩條路都要測。`orders.total_amount` vs 明細加總則是 0/200 不同，冗餘不矛盾，不必改 |
| `value_index._keep()` 拿**子字串**比對註解 | **真 bug，已量、未修** | 任何「剛好是表名或欄位名一部分」的代碼會被靜默踢出值索引：`CUSTOMER` 卡在「主要聯絡方式在 customers」、`CATEGORY` 卡在「products.category」、`REFUND` 卡在「退款金額在 refunds」。**改註解修不掉** —— 指路標不可能不寫表名。ASCII 值改用詞邊界（CJK 維持子字串，中文沒有空白）可救回 11 個值（CART／COUPON／CUSTOMER／DONE／IMAGE／LOYAL／POINTS／REFUND／STAFF／STORE／store_pickup），全部住 ≤3 張表。**309 題裡 0 題問句含這些字**，所以是暴露不是缺陷 —— 要修但不急，而且動的是核心函式，要帶自己的判準 |
| `literal_in_comment.py` 只認大寫代碼 | 修偵測器（第二次） | 偵測器跟被偵測的機制用不同判準就一定會漂。改成鏡射 `_keep`：值的來源 = `enums` 宣告過的值（權威來源）∪ 代碼字形（安全網），再套 `_keep` 第 ② 條去掉假警報（`TW`／`OK`／`C2`／`J8` 是純 ASCII < 4 字元，本來就進不了索引）。**不能改成「所有出現在註解裡的資料值」** —— 那會抓到 265 處，而 `業務`／`訂單`／`products` 正是 `_keep` 第 ① 條**故意**要排除的概念詞。新增第四級「機制缺陷」把上一列那類跟真的引用分開 |
| E15 的自動改寫留下一句壞註解 | 修好 ＋ 新增檢查 | `payment_attempts.result` 變成「嘗試結果：OK 成功； 銀行拒絕、逾時未回應…」—— 代碼被拿掉、中文說明留在原地，`OK` 因為只有 2 字元逃過正則。檢查方式：**比對註解與 `enums` 的值說明**，兩邊重複就是改寫沒收乾淨。掃出 51 欄重複，其中 50 欄是設計本意（中文給檢索、代碼給生成器，不同消費端同一批字），只有這 1 欄是壞的 |
| `sync_table_comments.py` 字串預設值沒加引號 | 修好（潛伏 bug） | `INFORMATION_SCHEMA.COLUMN_DEFAULT` 回傳無引號字面，組出 `DEFAULT PENDING COMMENT '…'` → MySQL 1064。潛伏數月，因為在此之前同步碰到的欄位剛好都沒有字串預設值 |
| 欄位註解也分句型 | 否決 | 唯一有機會的假設（`total_spent` 的快照警語讓提示在陷阱題上贏過正解）被推翻：砍警語壓低陷阱題 `#94` −0.035 / `#100` −0.043，但把正解題壓得**更低** `#146` −0.049 / `#147` −0.109。同一條雙向軸 |
| 樞紐表保留席 | 擱置（最便宜的解） | **R=2 → 候選@40 100.0%**，零 LLM、零嵌入。⚠️ **R=1 反而更差** |
| few-shot 也做檢索 | 擱置 | few-shot 已佔 prompt 42%，問題是真的。但**必須依形狀檢索而非題目相似度**，而且成對的教訓不能被拆開。觸發條件：超過約 20,000 字元 |
| 概念群做成獨立檢索單位 | 擱置 | 解「一張表一句註解」的容量上限。與失敗的欄位級 max-pool 不同：5 個人工概念群 vs 57 個原始欄位 |
| 階層式選表 | 擱置 | 候選目錄約 3,300 字元，離 needle-in-haystack 還很遠 |
| 條件式全庫臂（追沉默的錯誤範圍） | 未做 | 只對三票不一致的題開，比無條件 backward pruning 便宜一個量級（§9） |
| Cross-encoder reranker | 未量 | 輸入簽名與 dense 相同，**「營收存在 order_items 裡」不在任何一邊的字串裡** |

**三條紀律：**

0. **探針只能否決，不能放行。** 這張表上「否決」的證據強度不同：
   零 LLM 的確定性量測可以直接信；靠手挑靶題拿到的**正面**結果一律不算數。
1. **方向對不等於時候到。** 擱置的四項全部是「機制講得通、實測方向也對，
   但今天買到的東西太小」—— 在量不出效果的時候做改動，就是做一個無法驗證的改動。
   每一項都要寫觸發條件與零成本的重測方式。
2. **判決本身有保鮮期，而且可能是借來的。** 值檢索從「沒東西可買」變成「有東西可買」；
   欄位提示的否決理由在模型換版後整個消失；欄位提示第六次的否決理由
   （「多給一張近義母表是毒藥」）**根本不是檢索層的問題**，是資料層有兩欄在說不同的話。
   **修掉一個別層的缺陷之後，要回頭重測這張表，而不是只更新那一層。**
