# 架構與設計決策

這份文件記錄的是**為什麼這樣設計**，不是怎麼用。程式碼說明「做了什麼」，
這裡說明「為什麼不是別的做法」—— 包含被否決的方案與否決它們的實測數字。

沒有數字支撐的設計理由不寫進來。專案裡每一個「聽起來很有道理」的想法，
實測之後有一半是反的。

> 📏 **數字的可比性**：檢索的實測數字分兩批。§2.9 的 Hybrid 對照與 §2.10 的
> 稀釋定律是在**補齊三張表註解之前**量的（純 dense 基準 K=3 87.1% / K=4 91.4%）；
> 補齊之後基準變成 K=3 85.6% / K=4 91.4% / K=6 94.2%。每張表**內部**的比較都是
> 同一批、控制變因的，跨表比絕對值才要留意。所有結論的方向都不受影響。

> ⚠️ `README.md` 描述的是舊的 Tool-Calling Agent（`agent.py`），不是現行架構。
> 現行的是本文件描述的 LangGraph Pipeline（`langgraph_sql/`，進入點 `main.py`）。

---

## 1. Pipeline 總覽

```
context_retriever → sql_generator → ast_validator → db_validator
       ↑ (retry)                          ↓ (通過)
       └──────────────────  executor_voter → final_summarizer → END
```

| Node | 職責 | 模型 |
|---|---|---|
| `context_retriever` | 決定這一題要看哪幾張表，組出 Prompt | 見第 2 節 |
| `sql_generator` | 產生 SQL | gpt-oss-120b |
| `ast_validator` | sqlglot 靜態快篩（安全性 + 欄位白名單 + 反模式） | — |
| `db_validator` | MySQL `EXPLAIN` 驗證 | — |
| `executor_voter` | 實際執行、取結果 | — |
| `final_summarizer` | 結果轉繁體中文回答 | gpt-oss-20b |

重試一律回到 `sql_generator`，**不回 `context_retriever`**：重試是因為 SQL 寫錯，
不是因為表選錯，重跑檢索只會浪費一次 LLM 呼叫而得到完全相同的表。

錯誤訊息會被加工後才回傳給模型（`_augment_schema_error`）：EXPLAIN 報欄位不存在時，
把那張表的真實欄位清單一起附上。這是**反應式**的 schema 修復 —— 模型猜錯欄位時
不是單純被打回票，而是拿到正確答案再寫一次。

---

## 2. 檢索層：為什麼是三段漏斗

這是整個專案花最多力氣、也最反直覺的部分。

### 2.1 要解的問題

21 張表的完整 DDL 是 9,278 字元，佔整份 Prompt 一半。但 145 題平均**每題只需要
1.8 張表**（64 題只需 1 張）。模型每次都在讀十倍於它需要的 schema，而且表愈多愈糟。

### 2.2 最終設計

```
使用者問題
   │
   ├─① 相似度收斂    向量檢索，把候選收斂到 CANDIDATE_N 張      table_retriever.py
   │                 （21 張表時等於全部，此段為擴充性保留）
   │
   ├─② LLM 選表      從候選裡「選」出邏輯上必要的表             table_filter.py
   │                 錨點 = LLM 的選擇 ∪ 相似度第 1 名
   │
   └─③ KMB 補橋      Steiner tree 近似，補上串接用的中介表      schema_graph.py
```

139 題實測：

| 設定 | 召回 | 平均表數 | 淨度 |
|---|---|---|---|
| K=4 純相似度 | 91.4% | 4.9 | 35.2% |
| LLM 選表（單獨） | 97.8% | 2.0 | 92.1% |
| **LLM ∪ 相似度第 1 名** | **99.3%** | **2.1** | **90.2%** |

召回升 7.9 個百分點，同時表數少 57%。這兩件事在固定 K 的世界裡是對立的
（K 開大兩者一起上升），能同時改善是因為換掉的不是參數，是**憑什麼決定要幾張表**。

端到端（145 題，同一份 GT）：

| | 正確率 | 平均帶進表數 | DDL 長度 |
|---|---|---|---|
| K=4 純相似度 | 142/145（97.9%） | 4.8 | 約 3,587 字元 |
| 三段漏斗 | **144/145（99.3%）** | **2.08** | **1,041 字元**（完整 9,278） |

⚠️ **端到端只差 2 題，那在雜訊範圍內**（見 §5.2），不該拿來當這個改動的證據。
真正非雜訊的是檢索層本身：召回 +7.9pp、表數 −57%、DDL −89%。
端到端的意義只在於**確認沒有退步** —— 完成度分佈
（ok 132 / empty 7 / schema_unsupported 6）與 GT 完全一致，防禦題 6/6。


> ⚠️ **2026-08 更新**：候選清單原本照相似度排名給 LLM，理由是「免費給一個先驗」。
> 實測否決 —— 那個先驗是**淨負的**，production 已改成依問題雜湊打散順序。
> 固定 n=3 錨點召回 94.8/92.9/93.5 vs 打散 n=2 的 98.1/96.1，區間不重疊。詳見 §7.12。

### 2.3 為什麼固定 K 是死路

139 題按「實際需要幾張表」拆開，同一個 K=4 同時犯了兩個相反的錯：

| 實際需要 | 題數 | 給了幾張 | 浪費 | 召回 |
|---|---|---|---|---|
| 1 張 | 64（46%） | 5.0 | **5.0×** | 100% |
| 2 張 | 57（41%） | 4.7 | 2.4× | 93% |
| 3 張 | 7 | 4.6 | 1.5× | 86% |
| 4 張 | 11 | 4.9 | 1.2× | **36%** |

對將近一半的題目慷慨五倍，對難題又根本不夠。**K 開大改善下面惡化上面，
K 縮小反之 —— 沒有任何 K 值能同時解決。** 這不是調參問題，是「需要幾張表」
本來就因題而異，而固定 K 是一個與題目無關的常數。

（這也是本專案一段彎路的教訓：K 曾經被 3 → 6 → 4 反覆調整，每次都是在同一個
139 題資料集上挑最高分。那不是設計，是過擬合。）

### 2.4 為什麼動態門檻救不了（已實測否決）

自然的下一步是讓分數自己決定切在哪裡（Elbow Method）：

| 設定 | 召回 | 平均錨點 |
|---|---|---|
| r=0.85 | 60.4% | 1.6 |
| r=0.75 | 69.8% | 2.3 |
| K=6 | 95.0% | 6.0 |

大幅變差。餘弦分數根本沒有 elbow —— 多數題目第 2 名就掉到第 1 名的 85% 以下，
但「分數陡降」不等於「後面的表不需要」。

**它漏的是第二個錨點，而那連帶關掉了第三段。** r=0.85 有 **79/139 題（57%）只給
1 個錨點**（r=0.75 是 43 題）。錨點只有一個時 **KMB 在數學上不可能作用** ——
單一終端點的 Steiner tree 就是那個點自己。實測 K=1 是最乾淨的證據：
錨點召回 41.0% → +KMB 41.0%，**一張表都補不回來**。

而且漏掉的表證明問題不在橋接：r=0.85 漏最多的是 `customers` 19 題、`products` 18 題 ——
那是**問題主體**，不在任何兩個錨點之間的路徑上，KMB 設計上就補不了。

**根本原因**：相似度知道「這題關於客戶」，但不知道「算總消費必須 JOIN orders」。
那是**結構必要性**，不是文字相似度，相似度在原理上看不到它。所以任何純粹
在相似度分數上做的切法都救不了 —— 包含門檻、包含 elbow、包含加權。

這個認識是三段漏斗的由來：既然缺的是推理，就補一層會推理的東西。

### 2.5 為什麼是「選」不是「剪」

一個常見的提案是：相似度取 Top-6，再讓 LLM 從中剪掉不需要的。

這只解決上半部。**剪枝沒辦法把不在名單裡的表加回來** —— 上表那 36% 召回的難題，
問題正是需要的表從沒進過 Top-6。所以正確的形狀是「LLM 從候選裡**選**」，
不是「從短名單裡**刪**」。

### 2.6 為什麼還要 ∪ 相似度第 1 名

LLM 偶爾會漏掉問題裡**明講**的主體 ——
`#91`「商品種類數最多」漏 products、`#107`「多少張訂單已送達」漏 orders。
那恰恰是相似度最不會錯的部分。兩者的錯誤型態互補，所以取聯集。

實測只補第 1 名就到頂：

| 設定 | 召回 | 平均表數 | 淨度 |
|---|---|---|---|
| LLM 單獨 | 97.8% | 2.0 | 92.1% |
| **∪ Top-1** | **99.3%** | **2.1** | **90.2%** |
| ∪ Top-2 | 99.3% | 2.7 | 66.0% |
| ∪ Top-3 | 99.3% | 4.0 | 45.5% |

Top-2 之後召回不再上升，只是變胖。

### 2.7 為什麼選表用 120B 不用 20B

一般建議是輔助任務用小模型省錢省時間。實測後不採用：

| | 檢索召回 | 延遲（中位數 / 平均） |
|---|---|---|
| gpt-oss-20b | 97.1% | 1.72s / 2.55s |
| **gpt-oss-120b** | **97.8%** | **1.50s / 1.71s** |

兩點都跟直覺相反：

1. **召回差距不大，但錯的性質不同。** 20B 漏的是問題裡明講的東西
   （`#62`「單價超過 30000 元商品」漏 products、`#63`「哪些客戶」漏 customers）；
   120B 漏的是真正有歧義的題目。前者是不可靠，不是能力的邊際差。
2. **120B 在這個 NIM endpoint 上反而比較快。**「小模型比較快」在這裡不成立。
   基礎設施的實際表現要量，不能推論。

每題多一次呼叫約 1.5 秒是這一層的固定成本，換到召回 91.4% → 99.3%。

### 2.8 為什麼候選清單只給表註解，不給欄位

實測把全部 144 個欄位名與欄位註解加進候選清單（1,070 → 4,195 字元）：
召回持平 98.6%，只是把漏掉的題目換了一批。**付四倍 token 買到雜訊。**

真正該補的是 `TABLE_COMMENT` 本身。`#121`「備貨天數最長的商品」原本漏
`product_suppliers`，因為它的表註解只有「商品供貨關係」六個字 —— 而「備貨天數」
四個字就寫在它的欄位註解裡。把表註解補好就找到了，而且**表註解同時餵給 DDL、
檢索向量與這份候選清單，一次修好三個地方**。

→ 表註解的唯一來源是 `init_db.py` / `init_db_ext.py` 的 `CREATE TABLE`，
   用 `tools/sync_table_comments.py --apply` 同步到現有資料庫
   （`ALTER TABLE ... COMMENT` 是純 metadata 操作，不動一個位元的資料）。

### 2.9 為什麼不做 Hybrid（BM25 + RRF）

在真實的 139 題上量過（不是先導的合成資料 —— 合成資料宣稱 K=3 有 95%，
真實是 87.1%，差很多）：

| 方法 | K=2 | K=3 | K=4 | K=6 |
|---|---|---|---|---|
| **Dense** | **79.9%** | **87.1%** | **91.4%** | 93.5% |
| RRF (Dense+BM25) | 74.8% | 81.3% | 89.2% | 92.8% |
| BM25 | 69.1% | 77.7% | 83.5% | 94.2% |

**每一個 K，加 BM25 做 RRF 都比純 dense 差。** 中文字元 bigram 在 246~864 字的
短文件上雜訊太大，詞彙重疊多半來自機率而非語意。

### 2.10 稀釋定律（三次實測，同一個方向）

「檢索不到是因為描述的詞彙不夠豐富」這個推論很自然，但三次實測都是反的：

| 往檢索文件加東西 | K=3 召回 |
|---|---|
| LLM 合成的「典型問法」 | 87.1% → **82.7%** |
| enum 中文狀態值對照（運送中/已送達） | 87.1% → **86.3%** |
| **只留精確簡潔的表註解** | **87.1%**（最佳） |

**短文件加字會稀釋鑑別力** —— 所有表對任何查詢都變得有點像。這和 Prompt 稀釋
是同一個機制。**檢索文件要的是描述精準，不是詞彙量大。**

（`table_retriever._load_synthesized_docs()` 只為記錄這個負面結果而保留，
  `utils/table_docs.yaml` 已移除。）

### 2.11 選表 prompt 的第 3 條原則 —— 量不出來，所以不動

選表 prompt 有一條「看過不等於買過、放進購物車不等於買過、物流狀態不等於
訂單狀態」。這些話**已經寫在表註解裡**（`browse_logs`「看過不等於買過」、
`cart_items`「放入不等於購買」），而表註解就在候選清單上 —— 等於同一件事寫兩份，
有漂移風險（改了註解，prompt 還說舊的）。

拿掉它實測：

| | 召回 | 平均表數 | 淨度 |
|---|---|---|---|
| 有第 3 條 | 98.6% | 2.1 | 91.1% |
| 拿掉第 3 條 | 99.3% | 2.2 | 87.9% |

看起來拿掉比較好 —— 但**同一份「有第 3 條」的設定，前一次跑是 99.3%**。
配置本身的跑次間變異就已經大於這個差異，所以這個實驗**分不出勝負**（見 §5.2）。

決定是**不動**。量不出差異時去改一個正在運作的 prompt，就是在雜訊上做決策 ——
那正是本專案在 K 值上犯過的錯（§2.3）。redundancy 的漂移風險已記在這裡，
哪天表註解真的改了，回來一起改。

### 2.12 失效時的降級

檢索層是加分項，不是單點故障。每一段失效都只降級不中斷：

| 失效 | 行為 |
|---|---|
| 嵌入 API 掛掉（實測會偶發 502） | 跳過相似度，**LLM 選表照跑**（它讀的是表註解，不需要向量），只損失排序先驗與 ∪ 第 1 名的保險 |
| LLM 回不出合法 JSON / 選了不存在的表 / API 掛掉 | 退回純相似度 Top-K |
| 兩者都失效 | 退回全表 —— 退化成 Prompt 變長，不是答不出來 |

另外 DDL 一律附上「其他表的表名」（只有名字，約 250 字元）。DDL 只是 Prompt，
資料庫裡 21 張表全都在，EXPLAIN 驗的是真實 DB —— 模型只要知道那張表存在就能用它。
真正的風險是它不知道，然後拿看得到的表硬湊一個答案。實測 `#24` 就是檢索漏了
orders/order_items，而模型靠這份表名清單自己正確補上了 JOIN 條件。

### 2.13 為什麼不用向量資料庫

因為要檢索的東西只有 **21 個向量**，而且暴力比對就是精確解。整個檢索是 21 次餘弦、
約 3 ms，存成一個 0.64 MB 的 JSON（`.table_vectors.json`，key 是文件雜湊，
改一個註解只會讓那張表失效重算）。

向量資料庫解決的是 ANN 問題 —— 幾百萬個向量沒辦法逐一比，所以用 HNSW/IVF
**拿精確度換速度**。我們離那個門檻六個數量級，引進它等於白白付出召回。

| 表數 | 現行純 Python | 改 numpy | 需要 ANN？ |
|---|---|---|---|
| 21（現在） | ~3 ms | 0.05 ms | 否 |
| 1,000 | ~150 ms | 1 ms | 否 |
| 10,000 | 太慢 | ~20 ms | 否 |
| 1,000,000 | — | ~2 s | 是 |

真要變大，**下一步是換 numpy 向量化，不是加 Milvus**，而且那一步能撐到萬張表。

更重要的是這條路不會先卡在向量搜尋：schema 到 1,000 張表時，先爆的是 LLM 選表的
候選清單（1,041 字元 → 約 5 萬字元），那時要解的是階層式選表（先選領域再選表），
跟用不用向量資料庫無關。**為一個不會先到的瓶頸引進一個要維運的服務，是把複雜度花在錯的地方。**

### 2.14 為什麼一張表一個向量，不把欄位拆開嵌（已實測否決）

標準做法是每個欄位自己一個 chunk，表分數取所有 chunk 的最大值（max-pool）。
理由很有說服力：一張表 5~20 個欄位，只有一個欄位跟問題相關時，那個訊號在
單一向量裡會被其他欄位稀釋。139 題實測（嵌入是確定性的，這些數字沒有跑次變異）：

| 變體 | Top-1 命中 | 漏斗召回 | 純相似度 K=3 | K=4 |
|---|---|---|---|---|
| **A 表級（現行）** | 88.5% | **99.3%** | 85.6% | **91.4%** |
| B1 欄位級（chunk 不含表註解） | 89.9% | **99.3%** | 79.1% | 82.0% |
| B2 欄位級（chunk 含表註解） | **91.4%** | 98.6% | **87.1%** | 90.6% |
| D 表級 + 欄位型別 | 87.8% | 99.3% | 86.3% | 91.4% |
| C max(表級, 欄位級) | 89.9% | 99.3% | 87.1% | 89.9% |

**方向對，結論相反。** 欄位級確實把 Top-1 拉高 2.9pp，然後漏斗召回**掉了**。

關鍵在於相似度在漏斗裡只剩一個作用 —— 提供 ∪ 的那個第 1 名（§2.6）。
那是**保險**，而保險的價值來自「跟被保對象犯不同的錯」，不是來自自己準。
`#107「有多少張訂單已經送達？」`（只需要 `orders.status='DELIVERED'`）：

```
LLM 選表  → shipments          ✗ 物流狀態 ≠ 訂單狀態
A  top-1  → orders    0.3632   ✓ 救回來
B2 top-1  → shipments 0.3730   ✗ 跟 LLM 犯同一個錯
```

B2 把表註解複製進每個欄位 chunk，讀的東西就跟 LLM 讀的東西高度重疊 ——
更準，但錯得更一致，保險失效。**這是通則：冗餘元件要的是誤差獨立，不是個別準確度。**

B1 最獨立（純欄位訊號）且維持 99.3%，但它當退路時很慘：LLM 那層掛掉會退回純相似度
Top-K（§2.12），那條路 B1 只有 82.0%，比現行低 9.4pp。等於用正常路徑的 0 分改善，
換降級路徑的大幅退步。

**關於「欄位要有名字、type、描述」**：這件事已經做到，只是不在檢索文件，在給生成模型的
DDL —— `unit_price DECIMAL(10,2) COMMENT '購買時的單價'`。型別放進檢索文件實測是負的
（D 87.8% vs A 88.5%）：中文問題不會跟 `DECIMAL(10,2)` 產生語意相似，那串字元只是雜訊。
**檢索層要的是「這張表在講什麼業務」，生成層要的是「這個欄位能怎麼寫」**，
兩層需要的資訊不同，不該共用同一份文件。

⚠️ **這個否決是有條件的，不是永久結論。** max-pool 本來就是為了解「長文件稀釋」而存在，
而現在每張表平均只有 6.9 個欄位、文件中位數 166 字元 —— **根本沒有東西可以稀釋**。
表寬到 100 欄以上時檢索文件會變成數千字元，稀釋定律（§2.10）直接生效，
那時 B2 很可能翻盤。**重測的觸發條件：單表欄位數中位數超過約 30。**

### 2.15 欄位變多會怎樣（還沒發生，但門檻在哪裡先算出來）

現況每欄位約 65 字元 DDL，最寬的 `product_specs` 是 25 欄 / 1,402 字元，
平均 6.9 欄。漏斗平均帶 2.08 張表 = 1,041 字元。往上推：

| 每表平均欄位 | 2 張表的 DDL | 狀況 |
|---|---|---|
| 7（現在） | ~1,000 | 沒問題 |
| 50 | ~6,800 | 還能接受 |
| 200 | ~27,000 | 比現在的**全庫** DDL 還大 |

~~約 100 欄/表是分水嶺~~ —— **這個判準是錯的，見 §2.17**。真正的判準不是
每張表幾欄，是 DDL 有沒有大到會傷害正確率，而那個門檻比字元數推算高得多。

**但欄位裁剪的失敗模式比表裁剪糟得多，所以門檻要訂得更高：**

```
表沒給   → 答不出來 / EXPLAIN 報錯 / 空結果      ← 會叫，看得見
欄位沒給 → 模型挑一個看起來像的，SQL 合法、
           EXPLAIN 過、結果錯                    ← 不會叫
```

`#62`（§6.1）就是這個形狀。而欄位層級的「語意相近替代品」遠比表層級密集
（`created_at`/`updated_at`/`paid_at`、`price`/`unit_price`/`amount`）。

真要做的時候，形狀應該是：

1. **刪「確定用不到的」，不是猜「可能用到的」** —— 反向白名單比正向挑選安全。
2. **被裁掉的欄位不能從 AST 白名單消失** —— 否則模型正確猜到欄位反而被擋。
3. **DDL 留一行「本表另有 N 個欄位未列出」** —— 與 §2.12 附上「其他表的表名」同一個手法。
   **讓模型知道有東西被省略，比乾淨地騙它說沒有更安全**：不知道就會拿手上的湊。

### 2.16 欄位怎麼選：Elbow 已實測否決，鍵不能參與評分

提案是「在連通的表內用動態斷崖各取 Top 4~5 個高分欄位」。GT SQL 裡就有
「這題真正用到哪些欄位」，所以可以直接量（139 題）。**兩個地方都不成立。**

**① Elbow 在欄位分數上比在表分數上更慘**（同 §2.4：餘弦分數沒有斷崖）

| ratio | 題目欄位全中 | 欄位召回 | 平均留幾欄 |
|---|---|---|---|
| 0.95 | 20.9% | 49.9% | 1.3 |
| 0.85 | 36.0% | 65.1% | 2.2 |
| 0.75 | 50.4% | 75.4% | 3.0 |

ratio=0.95 平均只留 1.3 欄 —— **Elbow 產生不出「Top 4~5」這個數量**。

**② 固定 Top-4~5 剛好落在最糟的位置**

| 固定 K | 題目欄位全中 | 欄位召回 | 省下的欄位 |
|---|---|---|---|
| 4 | 82.7% | 93.7% | 33.5% |
| 5 | 95.0% | 98.5% | 17.1% |
| 6 | 98.6% | 99.6% | 8.2% |

K=4 有 17% 的題目缺欄位；到安全的 K=6 只省 8% —— **省不到東西的時候才安全。**

**③ 根本原因：Top-5 漏掉的欄位 71% 是 PK/FK**

```
#25  有哪些城市的客戶從來沒買過「家電」  漏 customers.id     [JOIN鍵]
#98  比較兩個類別的總營收              漏 products.id      [JOIN鍵]
#104 列出所有付款失敗的紀錄            漏 payments.order_id [JOIN鍵]
#142 所有評論的平均評分                漏 reviews.is_deleted（§4.1 教會的軟刪除條件）
```

`customers.id` 跟任何中文問題都沒有語意相似 —— 它的必要性是**結構性**的。
**結構性資訊不能交給語意評分決定**，這和 §2.4 是同一句話，只是從表層級搬到欄位層級。

**修正後的形狀：鍵一律保留，只對非鍵欄位評分**（全庫 144 欄有 44 個是鍵，31%）

| 非鍵取 Top-K | 題目欄位全中 | 欄位召回 | 省下的欄位 |
|---|---|---|---|
| 3 | 98.6% | 99.6% | 23.5% |
| **4** | **100.0%** | **100.0%** | **14.5%** |

每個軸都贏過原始提案：100% 召回而且省得比裸 Top-5 還多。

**④ 每張表都裁，還是只裁寬表？加寬度閘門嚴格較優**

| 寬度閘門 | 會被裁的表 | 題目欄位全中 | 欄位召回 | 平均每題省 |
|---|---|---|---|---|
| 每張都裁 | 21 | 100.0% | 100.0% | **99 字元** |
| 只裁 >8 欄 | 3 | 100.0% | 100.0% | 46 字元 |
| 只裁 >12 欄 | 1 | 100.0% | 100.0% | 37 字元 |

W=12 只碰 1 張表就拿到 37/99 的節省，**風險面積小 20 倍**。原因是 21 張表裡
**有 12 張根本裁不出東西** —— 扣掉鍵之後不到 4 個非鍵欄位。唯一有量的是
`product_specs`（25 欄，一次省 1,300 字元），但它很少被選中。

### 2.17 結論：欄位裁剪不要做（連寬表進來之後也先不要）

**① K=4 在這份資料上夠，但這個數字校準不了。**
243 組（題,表）配對，「從單一張表要用到幾個非鍵欄位」：

| 需要幾個 | 組數 | 累積 |
|---|---|---|
| 0 | 53 | 21.8% |
| 1 | 149 | 83.1% |
| 2 | 38 | 98.8% |
| **3** | 3 | **100.0%** |

最大值是 3，沒有任何一題超過 4。但那是**題目型態的性質，不是 schema 的性質** ——
這 139 題全是聚合分析題（「有幾筆」「總營收」「前五名」），天生只碰 1-2 個非鍵欄位：
一個過濾、一個聚合。而寬表招來的是另一種問題（「這位客戶的完整樣貌」要 10+ 個欄位）。

**固定 K 在欄位上會用跟 §2.3 一模一樣的方式死掉**：需要幾個本來就因題而異，
而這份資料集只採樣到分佈的一端。它能證明 K≥3，**證明不了 K 該是多少**。

**② 省不到東西。** 每題省 37~99 字元，而每題 DDL 是 1,041 字元。

> ⚠️ **加寬表之後這一點已經被削弱**（`eval_column_recall.py` 實測，155 題）：
> K=4／W=12 是 **382 字元/題**，不是 37。因為 `customer_profiles` 57 欄一旦被選中，
> 裁掉就省約 3,250 字元 —— 節省全部集中在那 16 題上。
> **同時 K=4 的餘裕也沒了**：加 14 題寬表題之前非鍵欄位需求上限是 3，現在 K=3 就會漏
> （`#69` birth_year、`#104` payments.amount、`#145` reviews.is_deleted、`#151` customers.name），
> K=4 從「有餘裕」變成「剛好貼齊邊界」。§2.17 ① 說的「固定 K 只採樣到分佈的一端」
> 在一輪之內就應驗了 —— 這反而是**更強的**不做理由：省得多了，但校準不了。

**③ 沒有預算危機 —— 這是對 §2.15 的修正。**
判準不是每張表幾欄，是 DDL 有沒有大到會傷害正確率，而那件事有實測：

```
K=4 純相似度基準：DDL 平均 3,587 字元 → 端到端 142/145 (97.9%)
```

**3,587 字元是實測過不會壞的。** 加一張 60 欄寬表（約 3,900 字元）之後，
漏斗的 DDL 會從 1,041 變成約 4,500 —— 跟一個已知能運作的配置同一個量級。
要逼近當初促成整個檢索層的 9,278 字元，得**同時選中兩張 60 欄寬表**。
那才是真正的觸發條件。

**④ 而失敗是靜默的**（§2.15）：表沒給會叫，欄位沒給模型會挑個相近的湊。

真要做的那一天，已經量清楚的三件事：**鍵不參與評分**、**加寬度閘門**、
**不要用 Elbow**。

**⑤ 寬表進來之後也還是不用做 —— 已經有直接證據。**
`customer_profiles` 被選中時，**57 個欄位整份丟給生成模型**（DDL 約 4,500 字元），
`#146-159` 共 14 題一題沒錯，包含四題刻意埋的快照／註冊時間陷阱。
**「欄位給太多所以模型會挑錯」這個前提，在這個規模上被否證了三次**
（K=4 的 3,587 字元 97.9%、漏斗的 1,041 字元 99.3%、寬表的 4,500 字元 14/14）。

### 2.18 真要裁的那一天：兩層 DDL ＋ 反應式補完，而不是賭「猜得準」

上面四點是「怎麼裁得準」。但 §2.15 的核心觀察是**裁錯不會叫** ——
既然預測式剪枝在失敗時沒有下游守門員，正確的架構不是把賭注全押在命中率，
而是**讓失敗變得有聲**：

1. **精簡層（預設）**：鍵全留（結構性資訊不進語意評分，§2.16）＋ 非鍵 Top-K
   ＋ 一行「本表另有 N 欄未列出」。
2. **完整層（索取式）**：模型寫不出來時可以主動要求某張表的完整欄位清單，再寫一次。
   `_augment_schema_error`（§1）已經是這個機制的雛形 —— EXPLAIN 報欄位不存在時
   把真實欄位附回去 —— 差別只在它是**被動觸發**，要改成**主動索取**。
3. **AST 白名單不跟著裁**：否則模型猜對了反而被擋。

**多一次 round trip，換掉一整類量不到的錯。** 這和 §2.12「附上其他表的表名」
是同一個原則：讓模型知道有東西被省略，比乾淨地騙它說沒有更安全。


---

## 3. KMB：為什麼補橋接表要用圖論

「陳先生上個月買了哪些手機」語意上只命中 customers 與 products，但少了
orders、order_items 就接不起來 —— 而問題裡**沒有任何字詞會命中這兩張表**。
橋接表只能靠外鍵結構找。

用 KMB（Kou–Markowsky–Berman）而不是 SPH（最短路徑啟發式）：兩者近似保證相同
（皆為 2−2/k），差別在搜尋次數 —— SPH 是 O(k²) 次全圖搜尋，KMB 是 O(k) 次。
21 張表時兩者都是微秒級；**這是為資料庫變大做的準備，不是現在的優化。**

**誠實的規模感：這一段在現行漏斗裡幾乎沒在作用。**

| | KMB 補回必要表的題數 | 平均多帶幾張表 |
|---|---|---|
| 三段漏斗（現行） | **1/139（1%）** | 0.09 |
| K=4 純相似度 | 14/139（10%） | ~0.9 |
| r=0.85 動態門檻 | 12/139（9%） | — |

原因是 **LLM 自己就把中介表選了**，即使 prompt 明講「中介表不用選、系統會自動補」。
錨點召回已經 98.6%，只剩 0.7pp 留給 KMB。唯一那題是
`#98`「比較 3C數位 與 家電 的總營收」——錨點 `[order_items, product_categories,
categories, product_specs]`，KMB 補上問題裡完全沒提到的 `products`。

所以 KMB 現在是**安全網不是主力**——但保費可以量，而且趨近於零：139 題總共只多帶
約 12 張表，其中 1 張是必要的。它防的是「兩張錨點表在 Prompt 裡 JOIN 不起來」，
而那種錯**不會被 EXPLAIN 擋下來**，模型會硬湊一個能跑的 JOIN。

而且它保的險會隨規模變貴：現在 21 張表最短路徑通常 1 跳，LLM 憑常識就能補；
表到幾百張、路徑要 3~4 跳時，那已經不是常識問題而是圖論問題。
**這一層是現在便宜、以後才收成。**

要是哪天想砍它，看的指標是**錨點召回**不是端到端 —— 錨點召回一掉，
KMB 立刻從 1% 回到 10%（K=4 那列）。

### 3.1 為什麼權重記在「表」上而不是邊上

「經過 browse_logs 很貴」本來就是**表**的性質，而且設定量從 O(邊) 降到 O(表)。

```python
_TABLE_COST = {"browse_logs": 3, "reviews": 3, "carts": 3, "cart_items": 3}
```

這些是**行為表**：看過 ≠ 買過、放進購物車 ≠ 買過。沒有權重時 KMB 會抄近路，
把「購買」的問題接到「瀏覽」的表上。

**W=3 是唯一可行的值**，兩個邊界夾出來的：
- 要大到輸給 3 跳的購買路徑（3 < 3+1）
- 要小到 carts/cart_items 自己當錨點時，跳數決勝仍會選對橋

W=4（先導實驗的值）在真實 schema 上就壞了：`['carts','products']` 會繞經
orders（成本 4 對 5）。`test_script/test_schema_graph.py` 有 17 條迴歸檢查鎖住
這個行為，把 W 改成 4 會紅。

### 3.2 權重漂移偵測

`tools/check_table_costs.py` 用兩組概念探針（行為 vs 交易）掃描，
標出「degree≥2 且明顯偏行為語意」卻沒被標貴的表。加新表時跑一次。

---

## 4. Prompt 層：只用 Few-Shot，不加業務規則

**這是一條硬規定。** 規則表達不了情境相依的邊界，而且會跟既有的 few-shot 打架。

實例：規則 1 說「總營收用 `SUM(o.total_amount)`」並把逐項計算標為不建議，
但 few-shot 4 對同一個詞用的是逐項計算 —— Prompt 自我矛盾。解法是**刪掉規則**，
改加一個「類別粒度營收」的 few-shot，把粒度邊界寫在它的 `reasoning` 裡。

### 4.1 Few-shot 的遷移性有兩層，只有一層會遷移

| 類型 | 例子 | 會遷移嗎 |
|---|---|---|
| **謂詞級** | `is_deleted = 0`、狀態值對應 | **會** — `#144` 2/4 → 6/6 |
| **結構級** | JOIN 後主體收斂去重的形狀 | **不會** — `#138` 0/4 → 1/6 |

結構級的教訓只在**形狀完全對齊**時才有效：`#24` 是加了同形狀的範例之後才從
0/6 變 5/6。所以碰到結構性錯誤，要編的是形狀對齊的 few-shot，不是通則。

#### 補充：形狀對齊會**移動錯誤的形狀**，未必移動分數（`#138`）

`#138` 又試了一次。這次找到的缺口是：「主體收斂」那一族的四個 few-shot，
篩選條件**全都是「子表欄位 vs 常數」**（`p.name = '機械鍵盤'`、`o.status = 'CANCELLED'`、
`NOT IN`、存在性），而 `#138` 是 **「子表欄位 vs 母表欄位」**（`a.city <> c.city`）。
那是這一族唯一沒被覆蓋的形狀，而且有獨立的陷阱：**當 WHERE 兩邊各來自 JOIN 的
一邊，「符合條件」的單位天然變成那個配對，不是那個主體** —— 模型回 48 列不是
忘了去重，是它認為答案單位就是那一列地址。

補了一個同形狀、不同表的 few-shot（`o.order_date < c.created_at`，57 張訂單 → 20 位客戶）。

| | 未收斂（48 列） | 收斂到姓名（29 列） | 答對 |
|---|---|---|---|
| 加之前（n=8） | 5 | 1 | **0** |
| 加之後（n=16） | 6 | 4 | **5** |

`1/14 → 5/16`，Fisher 單尾 **p = 0.116 —— 不顯著**。按 §5.2 的標準不能宣稱修好。

但**錯誤的形狀移動了，方向正是那個 few-shot 教的東西**：主要失敗從「完全不收斂」
（一筆地址一列）移到「收斂了但用錯鍵」（`DISTINCT c.name`，把同名不同人併掉）。
模型現在知道答案單位是客戶，只是挑錯收斂鍵。

**這是 §4.1 的第三層結論：謂詞級的形狀對齊有效，但它只解決它針對的那一層。**
剩下的「收斂鍵要用 id 不用 name」是**另一個**教訓，而且它**已經被教過**
（「從來沒有下過 PENDING 訂單」那則 few-shot 明講「33 位裡有同名不同人，
加 DISTINCT 只剩 32 列」）。教過而沒生效，再加第六個 few-shot 是在同一面牆上
再撞一次 —— 所以 `#138` 重新凍結，見 §6。

驗收成本的教訓：判斷一個 few-shot 有沒有效，n=8 只分得出 0/8 與 5/8 這種量級。
要把 5/16 與 8/16 分開需要 n≈40 —— **在擲硬幣題上做改動，驗收比改動貴得多。**

### 4.2 `#56`：形狀對齊到底是什麼意思（4/8 → 8/8）

`#56`「有下過訂單的客戶佔比」寫成 LEFT JOIN 之後 `COUNT(DISTINCT o.customer_id) / COUNT(*)`
—— **分子有 `DISTINCT` 收斂回客戶，分母的 `COUNT(*)` 卻數到 join 後的 201 列**，
得 49/201 = 24.38%，正解是 49/50 = 98%。SQL 合法、EXPLAIN 過、只有數字錯。

加的 few-shot 是「被加入過購物車的商品佔全部商品的比例」——
**不同的表、同一個形狀**：主體是實體、條件在一張一對多的表裡、答案是佔比。
`reasoning` 裡把錯法整段寫出來（包含那個錯誤的 SQL 長什麼樣），
再給正解：分子分母各自獨立成純量子查詢，兩邊都不受 JOIN 扇出影響。

三個刻意的選擇：

1. **不用評估題本身當範例** —— 那是 teaching to the test。用不同的表對，
   靠形狀遷移。
2. **把錯的寫法也寫進 reasoning** —— 這一類錯誤的特徵是「看起來完全合理」，
   只說正解不足以讓模型認出自己正在犯它。
3. **接在既有的「主體收斂」三例後面** —— 那三例講的是 SELECT 的收斂，
   這一例講的是**分母**的收斂，是同一條規則的第四個形狀。

驗收（`eval_stability.py --ids 56 --n 8`）：**4/8 → 8/8**。
連帶檢查同族的 `#52` `#53` `#54` `#55` 各 4/4，沒有回歸。

> 這也是 §4.1 那張表的補充證據：結構級的教訓**不是不能遷移**，
> 是只在形狀對齊時遷移。`#138` 修不好不是因為它是結構級的，
> 是因為沒找到對齊的形狀。

---

## 5. 評估方法

### 5.1 GT 永遠先修正才跑測試

**這是硬規定。** GT 錯了，模型的錯與 GT 的錯會混在一起，無法歸因。
本專案曾自己寫錯 5 題 GT（`#116` `#120` `#125` `#138` `#108`）。

**GT 只選「回答問題所需的最少欄位」** —— 比對規則對多回欄位是寬容的，
對少回是不寬容的，容忍度是單向的，所以 GT 過度指定欄位一定會製造假錯誤。

**同理也不該指定表示格式。** `#36`「每個月的平均訂單金額」原本的 GT 只收
`DATE_FORMAT(order_date,'%Y-%m')` 的 `'2026-02'`，把等價的 `YEAR()/MONTH()`
兩欄判成錯 —— 那是在考格式不是考語意。已補 `alt_sql`。

### 5.2 單輪評估分不出差異

MoE 模型在 temperature=0 仍然不確定。單輪 145 題的差異多半是雜訊。
判斷一個修改有沒有效要用**單題重複取樣**：`python eval/eval_repeat.py 98 138 --n 8`。
n=8 能分辨 3/8 → 7/8，分不出 7/8 → 8/8。

**這不是理論上的顧慮。** 檢索層同一份設定連跑兩次是 99.3% 與 98.6%（139 題）。
也就是說：**任何小於約 1 個百分點的「改善」都讀不出來。**
所以本文件裡凡是差距在 1pp 以內的比較，一律記成「分不出勝負」而不是採納 ——
§2.11 就是這樣處理的。反過來，檢索層 91.4% → 99.3% 這種量級才是真的。

### 5.3 檢索層要獨立評估

`eval_retrieval.py` 不跑 pipeline、不呼叫生成模型，只量「表選對了嗎」。
理由：檢索錯了後面再修 Prompt 都沒用；而混在端到端正確率裡量，就分不出
「表沒給對」和「表給對了但 SQL 寫錯」。

三個指標必須一起看 —— 召回與表數是對立的，單看任何一個都會做出錯誤取捨。

---

## 6. 已知缺陷

| # | 問題 | 狀態 |
|---|---|---|
| `#138` | 收件地址城市 ≠ 居住城市，主體去重寫錯 | **重新凍結（第二次），5/16 → 最新 2/8**。機制已在 §6.5 命名：**DISTINCT 被來自「多方」的佐證欄位破壞**（`SELECT DISTINCT c.id, c.name, c.city, a.city`；只留 `c.id, c.name` 就是正確的 34 列）。這個形狀五個 few-shot 都沒示範過，但目前只有一個實例，等第二個再動。解凍後補了缺口形狀「子表欄位 vs 母表欄位」的 few-shot：`1/14 → 5/16`，p=0.116 不顯著，但錯誤形狀從「未收斂 48 列」移向「收斂到姓名 29 列」（§4.1）。**卡住的不再是收斂本身，是「收斂鍵要用 id 不用 name」—— 而那件事已經有專門的 few-shot 教過而沒生效。** 凍結理由因此改變：不是形狀沒對齊，是同一個教訓教兩次不會更有效 |
| `#62` | 「單價超過 30000 元商品」選表漏 products | 檢索層唯一剩下的漏題（99.3% 的那 0.7%）。**端到端計分是「對」的，但那是假的** —— 見下 |
| ~~`#56`~~ | 「有下過訂單的客戶佔比」LEFT JOIN 後分母誤用 `COUNT(*)` | **已修復 4/8 → 8/8**，加了一個同形狀的佔比 few-shot（見 §4.2）。原本是穩定的系統性偏誤：四次錯的答案完全相同（`0.2438`），而上一輪 144/145 時它剛好擲中 —— §5.2 的活教材 |
| `#63` | 「同一張訂單同時有食品和書籍」回 21 列，正解 24 列 | **4/8**。`DISTINCT c.name` 併掉同名不同人。這個教訓**已經寫在 few-shot 和 `customers` 表註解兩個地方**而仍然失效 —— 它是「再寫一次沒有槓桿」的決定性反證（§6.5） |
| `#43` | 「每張訂單都低於 10000 的客戶」單輪回 9 列，正解 8 列 | **8/8 穩定過，不是缺陷**。`NOT IN` 寫法讓全稱條件對「零訂單客戶」空真，但模型多數時候用 `HAVING MAX(...)`。留作觀察 |
| `#34` | 「比較 7 月和 8 月的訂單數量」把兩個月併成一列（GT 要兩列） | **5/8**（重複取樣）。**與 `#56` 的修復無關** —— 控制實驗：移除那個 few-shot 是 6/8，8 次差 1 次分不出勝負（§5.2）。它本來就是擲硬幣題，只是之前每次都剛好擲中，被 `eval_stability.py` 抓出來 |
| 資料 | `order_items.unit_price` 與 `products.price` 在種子資料裡**永遠相等**（0 列不同） | 兩者語意不同（成交價 vs 售價）卻無法被任何測試分辨。重跑 `init_db.py` 會讓 145 題 GT 全部失效，所以沒有動 |

### 6.1 `#62`：為什麼「答對了」不能算數

檢索漏掉 `products` 之後，模型寫的是 `oi.unit_price > 30000`（成交價），
GT 用的是 `p.price > 30000`（售價）。**兩者語意不同**，只是因為種子資料裡
這兩欄永遠相等，才碰巧回傳同一組 15 位客戶而被判為正確。

這件事的意義不在這一題，在於它證明了 §5.3 的必要性：
**端到端正確率會低報檢索失敗** —— 表沒給對的時候，模型會用手上有的表湊一個
形狀相近的答案，而那個答案有機會在現有資料上剛好對。只看端到端就會以為
檢索沒問題。檢索層必須獨立量。

### 6.2 待辦（不是設計問題，是還沒做）

- `_TABLE_COST` 目前是手寫的 4 筆。表變多之後沒辦法手標，
  `tools/check_table_costs.py` 只是偵測器不是解法。
- 新加的表**零 GT 覆蓋** —— `eval_retrieval.py` 量不到它們，加表要同步補題。
- `utils/semantic_layer.yaml` 與 few-shot 綁在現有 schema。

---

### 6.3 新基準（2026-08，41 張表 + 干擾表已灌資料）

四個改動疊在一起之後的第一次完整 e2e（`eval_result_1787105411.json`）：

```
完成率 158/159   正確率 154/159 = 96.9%
錯：#34 #43 #63 #93(error_end) #138
```

**但 154 不是基準，156 也不是。** 對這五題 + `#154` 跑 n=8 探針：

| 題 | 單輪 e2e | n=8 | 歷史 | 判讀 |
|---|---|---|---|---|
| `#43` | ❌ | **8/8 穩定過** | — | 單輪那次是壞運氣 |
| `#34` | ❌ | 6/8 | 5/8 | 不變 |
| `#93` | ❌ | 6/8 | 4/8 | 不變（`payments` 進檢索沒有害到它） |
| `#63` | ❌ | 4/8 | 未量過 | **新發現的硬幣** |
| `#154` | ✅ | **5/8** | 4/8 | **沒有修好** |
| `#138` | ❌ | 2/8 | 5/16 | 不變 |

已知硬幣的期望錯題數 = (2+2+4+6+3)/8 ≈ **2.1 題**，
所以真值約在 **156~157**：這輪的 154 是壞抽樣、上輪的 156 是好抽樣。
**單輪數字一律不可當基準**（§5.2 第 N 次驗證）。

#### 兩個歸因修正

**① `#154` 沒有被修好。** 我一度依單輪 e2e 與冒煙測試宣稱「B′ 打散順序修好了 `#154`」——
探針說 5/8 vs 歷史 4/8，**在雜訊裡**。

但探針指出了更精確的事：`#154` 的瓶頸**已經從檢索移到生成端**。
檢索層探針是 6/6 拿到 `customer_profiles`（§7.12），而 e2e 的失敗全是
**3/8 誤觸防禦暗號** —— 表給對了，防幻覺守衛還是拒答。那是另一個元件的問題。

**② 三題新錯不是任何改動造成的。** `#34` / `#43` / `#63` 的**檢索進出完全相同**
（逐題比對舊新兩份 JSON），變的只有生成器的 SQL；而且**沒有任何一題的檢索碰到干擾表**。
灌 2,276 列資料沒有製造任何回歸。

### 6.4 `#154` 修好了（4/8 → 8/8），以及一個可重複的程序

#### 診斷：跟 `#104` 逐字同構

先修正一個我自己的錯誤歸因。我一度說「`#154` 的表給對了，防幻覺守衛還是拒答」。
直接把 `context_retriever` 的輸出印出來之後：

```
檢索表: ['customers']          ← customer_profiles 沒有進來
DDL 長度: 948
DDL 含 'email_verified_at': False
```

**生成器觸發防禦暗號是正確行為** —— 它手上真的沒有那個欄位（防禦暗號是
`sql_generator` 的第 3 條指示，不是獨立的守衛）。失敗在上游。

那為什麼 LLM 選不到？

```
customer_profiles.email_verified_at 欄位註解：Email 驗證通過時間，NULL 表示未驗證  ← 完美
customer_profiles 表註解：客戶輪廓、分級、分群、活躍度、風險…                    ← 沒有 Email、沒有驗證
customers        表註解：記錄姓名、Email、電話、居住城市…                        ← 有 Email
```

問句說「Email 驗證」，**只有 `customers` 的註解裡有 Email**。
誘餌詞在錯的那張表上、對的那張表沒有 —— 與 `#104`（`payment_attempts` 寫著
「含失敗的」、`payments` 沒有「失敗」）**結構完全相同**。

#### 修法與結果

`customer_profiles` 是 57 欄的寬表，一句註解塞不下 57 個概念（§8.7）。
所以補的是**漏掉的概念家族**，不是那一題的關鍵詞：

```
帳號狀態旗標（Email 驗證、黑名單、員工、VIP）、紅利點數與信用額度、推薦來源也在這張表
```

| 題 | 修之前 | 修之後 |
|---|---|---|
| `#154` | 4/8 | **8/8** |
| `#159` `#146` `#148`（同吃 customer_profiles） | — | 8/8、8/8、8/8（無回歸） |

Fisher 單尾 **p = 0.038**，顯著。

#### 稀釋鏡像的代價：真的有，但先前就存在

新舊註解各嵌入一次做頭對頭比較（同一組問題向量）：

| | 需要它的中位排名 | 其他 139 題中位 | 進前 3 | 搶 Top-1 |
|---|---|---|---|---|
| 舊註解 | 1 | 12 | **34** | 9 |
| 新註解 | 1 | 10 | **40** | 9 |

**這張寬表在改之前就已經到處排第一了**（34/139 進前 3）—— 那是 §8.2 記錄過的
現象，不是這次造成的。改動讓它多 6 題進前 3、搶 Top-1 不變，
而 dense 的排序結果 @5–@40 與干擾搶 Top-1 全部不變、Top-1 命中 75.5→76.8。
**代價是真的，但還沒有兌現成損害。** 下次再加長要重量這四個數字。

#### 名字要記下來：「盲區補強」是一個可重複的程序

`#104` 與 `#154` 用的是同一套動作，兩次都成功、都有前後量測：

1. 印出實際餵給生成器的 `schema_ddl`，確認缺的是**表**還是**欄位**
2. 比對正解表與競爭表的 **TABLE_COMMENT**，找出「問句關鍵詞出現在錯的那張表上」
3. 改 `init_db*.py` 的 `CREATE TABLE ... COMMENT`（唯一來源），
   用 `tools/sync_table_comments.py --apply` 套用（純 metadata，不動一位元資料）
4. **補概念家族，不補那一題的關鍵詞**（避免 teaching to the test）
5. 跑稀釋鏡像檢查：新舊註解各嵌入一次做頭對頭，看四個數字
6. 跑 `eval_stability.py --ids <目標題> + <同表的其他題>`，確認修好且無回歸

**往干擾表加指路標沒有用** —— `payment_attempts` 的註解本來就寫著
「成功的付款結果在 payments」而完全無效。要補的永遠是**正解表的盲區**。

### 6.5 `#138` / `#63` / `#43`：三個不同的機制，不是同一個教訓

我一度把這三題歸成「收斂鍵用 id 不用 name」同一族。**那是錯的。**
把三條 SQL 攤開之後是三件事：

| 題 | 系統 SQL 的關鍵片段 | 機制 | 教過嗎 | n=8 |
|---|---|---|---|---|
| `#138` | `SELECT DISTINCT c.id, c.name, c.city, a.city` → 48 列 | **DISTINCT 被來自「多方」的佐證欄位破壞** | ❌ 沒有 | 2/8 |
| `#63` | `SELECT DISTINCT c.name` → 21 列（正解 24） | `DISTINCT name` 併掉同名不同人 | ✅ 兩個地方 | 4/8 |
| `#43` | `WHERE NOT c.id IN (…)` → 9 列（正解 8） | 全稱條件對「零訂單客戶」空真 | — | **8/8 不是缺陷** |

#### `#138` 的機制值得單獨命名

它的 SQL **有寫 `c.id`** —— 只 `SELECT DISTINCT c.id, c.name` 就是正確的 34 列。
壞掉的只有那兩個佐證欄位，而 `a.city` 來自一對多的**多方**，於是 DISTINCT
失去收斂效果。生成器 prompt 裡**沒有任何鼓勵多帶欄位的指示**（查過），
那是模型自己的習慣；而對帳規則明確容忍多帶欄位，殺死它的是**列數**。

**五個「主體收斂」few-shot 全部只 `SELECT c.name`，沒有一個示範這種壞法。**

#### 為什麼還是不加第六個 few-shot

`#63` 是決定性的反證：**同一個教訓寫在 few-shot 和 `customers` 表註解
（「資料中存在同名不同人的客戶」）兩個地方，仍然 4/8。** 對這一族而言
「再寫一次」已經量過沒有槓桿（§4.1：`#138` 1/14 → 5/16，p=0.116）。

而 `#138` 的新機制目前**只有一個實例**。§4.1 自己記過驗收成本：
要分開 2/8 與 5/8 需要 n≈40。在單一實例上做一個要 40 次呼叫才驗得出來的改動，
就是過擬合。**機制先命名、記錄，等第二個實例出現再動手** ——
這跟 §7.8 的樞紐表保留席、§7.11 的方案 D 是同一條紀律：**方向對不等於時候到。**

### 6.6 把 §6.4 的程序做成偵測器：`tools/check_comment_blindspot.py`

`#104` 與 `#154` 都是「被咬到才發現」。既然結構相同、修法相同，
就該在被咬到之前找出下一個 —— 這跟 `check_defence_gt` / `check_ambiguity` /
`eval_lucky` 是同一個模式：**把一次診斷變成一支會出聲的程式。**

#### 第一版是表驅動的，錯得很徹底

對每張表列出「欄位註解有、表註解沒有」的詞，40 個候選裡大部分是誤報：

```
[誘餌] customer_profiles 缺概念詞「瀏覽」  競爭表 → ['browse_logs']
```

`browse_logs` 對瀏覽題**本來就是正解表**。表驅動的版本**不知道哪張表才對**，
所以對每個 (表, 缺的詞) 都標誘餌，方向可能整個反過來。
這是設計缺陷，不是調參問題。

#### 第二版：問句驅動 —— GT SQL 就是「哪張表才對」的答案

```
對每一題 Q（正解表集合 N，由 GT SQL 解出）
  對 Q 裡的每個概念詞 c
    對每張正解表 T ∈ N
      c ∈ T 的欄位註解 且 c ∉ T 的表註解          → T 在目錄裡被這個詞找不到
      且 c ∈ 某張**非正解**表 U 的表註解           → 誘餌（HIGH）
      否則                                       → 盲區（MED）
```

概念詞從**問句**抽（問句短，n-gram 不會爆炸），再要求它出現在正解表的欄位註解裡 ——
那一步同時濾掉碎片詞：「款方式」不在任何欄位註解裡，「Email 驗證」在。
候選 40 → **17**（誘餌 5、盲區 12）。

#### 它獨立重新發現了已知的汙染 —— 這是最好的驗證

五個誘餌全部是同一件事：**`orders` 的表註解沒有 `CANCELLED`，而 `order_cancellations` 有。**
命中的題號是 `#10` `#22` `#23` `#50` `#54` ——
**與 §7.9 量到的干擾表汙染清單逐題吻合**（那五題在 e2e 裡全部拉進了
`order_cancellations`）。偵測器沒有看過那份清單，是從註解結構自己推出來的。

#### 修 `orders` 註解的結果

補上 `訂單狀態 PENDING/PAID/SHIPPED/COMPLETED/CANCELLED —— 查待處理、已付款、
已出貨、已完成、已取消的訂單都用這張表`：

| | @10 | @14 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|
| 舊註解 | 89.7 | 94.2 | 76.8% | 17 |
| **新註解** | **91.0** | **95.5** | **79.4%** | **14** |

九題（5 誘餌 + 4 同族盲區）的檢索層探針，每題 4 次：

```
正解表齊全 36/36    拉進干擾表 1/36
```

**之前那五題是 5/5 全部拉進 `order_cancellations`，現在 0/4。**
偵測器的第一個發現，一次清掉八題的潛在風險。

稀釋鏡像：`orders` 在不需要它的題目裡進前 3 從 8 → 11、搶 Top-1 維持 0，
而排序結果 Top-1 +2.6pp、干擾搶 Top-1 −3。**代價小於收益。**

#### 修完之後：誘餌 8 → 0，盲區 12 → 7

剩下的 7 個都是「沒有競爭表」的低風險盲區，而且集中在 `customer_profiles`
（已是第二長的註解，再加會踩 §2.10）。`#24` 的「戶姓名」是 n-gram 碎片
（`customers` 註解裡有「姓名」）—— 殘餘噪音，不修。

**這支的正確用法是門檻不是清單：誘餌數應維持 0，加新表時重跑。**

### 6.7 最終基準：不要報單輪分數，報「穩定通過 + 硬幣池」

兩輪完整 e2e（`1787105411` 與 `1787117543`）都是 **154/159**，
但**錯的題只重疊三題**：

```
Run A: 34 43 63 93 138
Run B:    53 63 89 93 138
```

`#53` 與 `#89` 是兩輪之前從沒錯過的題。**每多跑一輪就多暴露幾題硬幣** ——
這代表「穩定通過」的題數比單輪分數少，而單輪分數本身是一個抽樣，不是狀態。

#### 補上一個一直存在的取樣偏誤

到此為止的探針都只對「這輪錯的題」取樣。那**只找得到剛好落錯的硬幣，
找不到剛好落對的** —— 系統性低估硬幣池。

補測：對「兩輪都通過」的題隨機抽 20 題 × 8 次 = 160 次呼叫。

```
穩定過 20 ／ 擲硬幣 0 ／ 穩定錯 0
```

**零個隱藏硬幣。** 用三法則，隱藏硬幣率 95% 上界約 **15%**，點估計 **0%**。

#### 「零個隱藏硬幣」是錯的 —— 第三輪直接推翻

上面那句話只成立於「20 題抽樣裡沒抽到」。第三輪 e2e 立刻打臉：
`#17` 與 `#140` 前兩輪都通過、這輪錯了，而**它們不在那 20 題裡**。

原因是抽樣母體本身有條件：我從「A、B 兩輪都通過」的題裡抽 ——
一題失敗率 25% 的脆弱題，通過兩輪的機率是 56%，
**那個母體已經被過濾成偏強韌的一群。** 從它抽樣估不出全體的硬幣率。

#### 三輪之後真正的結構：分數極穩，錯的題輪流換

| 題 | 錯在第幾輪 | 次數 |
|---|---|---|
| `#93` `#138` | 1, 2, 3 | **3（慢性）** |
| `#34` | 1, 3 | 2 |
| `#63` | 1, 2 | 2 |
| `#17` `#43` `#53` `#89` `#140` | 各一輪 | 1 |

```
三輪分數：154 / 154 / 154      每輪錯題數：5 / 5 / 5
曾經錯過的題：9 題（union）
```

**分數三次一模一樣，失敗的身分卻一直換** —— 這是「錯誤預算」型的系統，
不是「某幾題不會」。反推：2 題慢性 ＋ 約 **12 題脆弱題（各約 25% 失敗率）**，
每輪抽中 3 題。

#### 基準的正確寫法

```
慢性錯 2 題（#93 #138）
脆弱池 ~12 題（已識別 7：#17 #34 #43 #53 #63 #89 #140）
穩定通過 ~145 題
單輪落點 154 ± 1
```

**不要報「154/159 = 96.9%」。** 那個數字三輪一樣，卻掩蓋了「每輪換一批題」
這個真正的狀態。

#### 一個看起來像落差、但檢定不成立的東西

期望 156.3、兩輪 e2e 都是 154。我一度把它解讀成「探針條件比 e2e 寬鬆」，
還算了一個 p = 0.0152 出來。**那個檢定有選擇偏誤，不能用。**

那 7 題是**因為在 e2e 錯過才被選進硬幣池的**。一題真實通過率 p 要進池子，
必須「兩輪至少錯一次」，機率 1 − p²。所以**條件在「進了池子」之下，
e2e 的失敗必然被過度呈現**。最清楚的例子是 `#43`：探針 8/8，
它進池子唯一的理由就是 e2e 錯過一次 → e2e 顯示 1/2。
那個 100% vs 50% 的落差**完全是選擇效應**。

機制證據也不支持「e2e 條件比較嚴苛」：

```
兩輪 retry 分布幾乎一樣（0:136 / 1:22~23）    llm_error = 0
硬幣題平均重試 0.14  vs  全體 0.15           ← 沒有差別
```

而且期望值那個算法是**循環論證**：用兩輪 e2e 的結果定義分層
（152 穩定 / 7 硬幣），再用分層算期望，然後拿它去跟同樣那兩輪比。
**那兩個 154 不能當獨立檢驗。**

唯一乾淨的測試是**跑第三輪**（見下）。這件事本身是一條方法論教訓：
**任何用「哪些題錯了」挑出來的子集合，都不能再用「那些題錯幾次」去檢定。**

#### 報告方式的規定

**以後不要報單輪分數。** 報這三個數字：

```
穩定通過 N 題｜硬幣池 M 題（附各自的 k/8）｜穩定錯 K 題
```

單輪分數只能當「這次抽到什麼」，不能當狀態。判斷任何改動有沒有效，
比較的是**硬幣池的組成與各題的 k/8**，不是分數。

### 6.8 第三輪挖出兩個不是硬幣的東西

跑第三輪的目的是檢驗期望值，結果順手抓到兩個真缺陷 ——
**這本身就是「多跑幾輪」的價值：硬幣會輪流，真缺陷會被輪到。**

#### `#17`：AST 驗證器把合法 SQL 判成幻覺欄位（已修）

```
error_end（retry=2，連 SQL 都沒有）
所有候選 SQL 均未通過 AST 驗證：products 不存在的欄位: *
```

`SELECT p.*` 被 sqlglot 解析成 `Column(name='*', table='p')`，
第 3b 段拿 `'*'` 去比對 products 的欄位清單 → 判成幻覺欄位。
**三次重試被同一個誤判擋光** → `error_end`，這是 `test_runner.classify()`
定義裡最嚴重的結果（連答案都沒有）。

只有**帶表限定**的星號會這樣：

| SQL | sqlglot 解析 | 結果 |
|---|---|---|
| `SELECT p.*` | `Column(name='*', table='p')` | **誤判** |
| `SELECT *` | `exp.Star`，無 Column | 正常 |
| `COUNT(*)` | `exp.Star`，無 Column | 正常 |

這也解釋了為什麼 `#104` 的 `SELECT * FROM payments` 一直沒事。

**這一層是 fail-open 設計**（解析不出歸屬的欄位一律放行），理由是
誤擋的代價遠高於漏擋 —— 漏擋後面還有 EXPLAIN 與實際執行接住，
誤擋卻直接吃掉重試預算。這個 bug 正好違反了它自己的設計原則。

修法是一行 `continue`，並補上 `test_script/test_ast_validator.py`
（9 項，主體是**誤判**不是漏判）。驗證這支測試不是裝飾品：
把修正停用之後 **4/9 失敗**，包含 `#17` 一字不差的錯誤訊息；裝回去 9/9。
修完 `#17` 探針 **8/8**。

#### `#140`：防禦題「答了」—— 守衛該開火卻沒開火

```
expect=schema_unsupported    實際 outcome=ok
SELECT ... SUM(oi.quantity) / w.capacity AS turnover_rate
```

它**自己發明了庫存週轉率的公式** —— 正是生成器規則第 2 條
（「絕不可發明公式」）要擋的那件事。探針 8/8，所以是低頻的漏接不是穩定缺陷。

**但這暴露了 §8.8 稽核的盲點**：`check_defence_gt.py` 檢查的是
「概念有沒有因為加表而變成可回答」—— 它看不到**守衛該開火卻沒開火**。
兩者是不同的失效：前者是 GT 過期，後者是模型沒守規矩。
目前沒有偵測器覆蓋後者；它只會在 e2e 裡以「防禦題答了」的形式出現，
而那在單輪報告裡長得跟一般答錯一樣。

### 6.9 `#140`「守衛沒開火」怎麼處理（外部研究 ＋ 實測）

#### 研究界對這個失效有名字，而且說它未解

- **AbstentionBench（Meta, 2025）** —— 拒答是**未解問題**，加大模型沒用，
  而且**推理型模型反而更不知道何時該不答**。所以不要期待修到 100%。
- **LatentRefusal（2026）** 對這個症狀的描述幾乎是逐字命中：
  > Prompting an LLM to abstain is convenient and training-free, but refusal is
  > **prompt-sensitive and can fail exactly when the model hallucinates**,
  > after the system has already entered executable-code decoding.
- **Query Carefully（2025）** 的做法是**明確的 No-Answer Rules ＋ 正反 few-shot**。
- **Cube / dbt 的語意層論述** —— 讓 agent 從**已定義的 metric 集合**裡選，
  而不是自己寫 SQL；沒被建模的問題**在結構上就答不出來**。

四條路對本專案的可行性：

| 方案 | 這裡能不能做 |
|---|---|
| prompt 通則拒答（現況） | 已在用，已知 prompt-sensitive |
| 前置意圖分類器（RoBERTa） | ❌ 要訓練資料與另一個模型 |
| 潛在訊號探測（hidden activations） | ❌ NIM API 拿不到 |
| **明確 No-Answer Rule ＋ 反例** | ✅ **semantic_layer 已經有一條先例** |

#### 現況的不對稱：一條有教、三條沒教，漏的正是沒教的那條

```
semantic_layer.yaml 裡的 SCHEMA_UNSUPPORTED 規則：只有「利潤率」一條
防禦題三輪結果：#68 3/3   #70 3/3   #141 3/3   #140 2/3
```

`#70`（利潤率）是唯一有規則的，`#140`（週轉率）沒有規則且是唯一漏的。
**但這只是 1 次漏接，不足以證明因果** —— `#68`、`#141` 同樣沒有規則卻 3/3。

#### 動作必須窄：19 題可回答的題目提到相鄰詞

```
#103 手續費率最高的付款方式        ← 同時有「費」和「率」
#142 #145 平均評分   #153 #155 #156 #157 平均…分數
#5 #30 #46 #58 #60 #93 #94 #100 #146 #147  總消費金額
```

**任何粗暴的關鍵詞拒答規則都會把這 19 題推向 `#154` 那種誤觸。**
所以新規則除了照抄「利潤率」那條的結構（點名概念 → 解釋為何無定義 →
`correct_sql` 是暗號 → `wrong_sql` 是實測產出的假公式），
還加了「利潤率」那條沒有的東西：**正向邊界**。

```yaml
**邊界（這些是答得出來的，不要一起擋掉）**：
  「每個倉庫的容量是多少」        → warehouses.capacity，直接答
  「每個倉庫出貨了幾張單／幾件」  → shipments / order_items 聚合，直接答
  「哪些商品庫存最少 / 庫存是 0」 → products.stock，直接答
擋的是「週轉」這個沒有定義的比率，不是庫存與出貨這兩個有資料的量。
```

#### 實測：一個只能否證、不能確證的實驗

| 題 | n=8 | 角色 |
|---|---|---|
| `#68` `#70` `#140` `#141` | 8/8 | 防禦題 |
| `#31`「庫存量最少的商品」 | 8/8 | **邊界**（規則裡明講了「庫存」） |
| `#73`「有哪些商品的庫存是 0」 | 8/8 | **邊界** |
| `#103`「手續費率最高的付款方式」 | 8/8 | 相鄰詞 |
| `#153`「平均流失風險分數」 | 8/8 | 相鄰詞 |

**這無法確認規則有效** —— `#140` 加規則之前的探針**就已經是 8/8**，
只有 e2e 漏過一次；漏接率約 9%，要區分 91% 與 100% 需要 n≈30 以上。

**它能確認的是更重要的那一半：沒有過度觸發。** 四題邊界／相鄰題全部 8/8。
這是一個**只能否證不能確證**的實驗，而它沒有被否證 ——
在「代價可測、收益不可測」的情況下，代價為零就是採用的理由。

#### 沒有一起做的事，以及觸發條件

`#68`（運費）與 `#141`（信用評分）**沒有加規則**。它們三輪 3/3、沒有觀測到漏接，
加規則是純粹的推測，還要付過度觸發的風險。
**觸發條件：`eval_score.py` 的「防禦題」那一行出現它們，才回來加。**

#### 補上守門員：`eval_score.py` 現在單獨報防禦題

`check_defence_gt.py` 稽核的是「概念有沒有因為加表而變成可回答」（§8.8），
**看不到守衛該開火卻沒開火** —— 那在單輪報告裡長得跟一般答錯一模一樣。
現在對帳會單獨印：

```
防禦題: 3/4 正確拒答
  ‼ 守衛沒開火 —— 模型發明了沒有定義的公式:
  #140  每個倉庫的庫存週轉率是多少？
        產出: SELECT ... COALESCE(SUM(oi.quantity), 0) / w.capacity AS turnover_rate ...
  這比一般答錯嚴重：SQL 合法、EXPLAIN 會過，使用者看不出那個數字是憑空來的。
```

**這是誠實性指標，不該被平均進正確率裡稀釋掉。**

## 7. 擴充性：表變多的時候什麼會先壞

用干擾表實驗量出來的（每題候選 = 該題必要的表 ∪ 隨機干擾表，湊到 N 張）。
這不是推論，是把候選集縮小後量退化斜率。

### 7.1 只有相似度那層會隨規模退化

| 候選表數 | 相似度 Top-1 命中（5 種子平均） | LLM 選表召回 |
|---|---|---|
| 4 | 98.6% | — |
| 6 | 97.3% | 99.3% |
| 8 | 95.3% | — |
| 12 | 93.2% | 99.3% |
| 21（現況） | 88.5% | 100.0% |

相似度的斜率穩定在**每多一張表 −0.52pp**，線性。LLM 那層 6 → 21 張**完全沒有退化**。

這正好解釋了漏斗為什麼是對的形狀：**會隨規模退化的那層現在只負責一件小事
（∪ 第 1 名），扛主力的那層對規模不敏感。**

### 7.2 `CANDIDATE_N = 40` 是一顆定時炸彈（已裝上護欄，見 §7.6）

21 < 40，所以第一段**從來沒有真正執行過** —— 候選一直等於全部的表。
表一超過 40 張它立刻變成關鍵路徑，而且用的是唯一會退化的那層。

`table_filter._warn_if_candidate_n_binds()` 現在會在載入表註解時檢查表數，
超過 `CANDIDATE_N` 就 WARNING 並算出建議值。**這個天花板效應本身是無聲的**
（候選漏掉的表後面兩段救不回來，但沒有任何東西會報錯），所以護欄的價值不在
修好它，在於讓它被看見。

dense 的候選召回（need 全部落在 Top-N 內）：

| Top-N | 佔全庫 | 候選召回 |
|---|---|---|
| 8 | 38% | 92.8% |
| 12 | 57% | 97.8% |
| **14** | **67%** | **99.3%** |
| 16 | 76% | 100.0% |

**dense 要 2/3 的表才不漏。** 100 張表配 `CANDIDATE_N=40` 只有 40%，
候選召回會掉到九成上下 —— 而那是**整個漏斗的天花板**，後面兩段再準也救不回來。

40 這個值原本沒有任何數據支撐，是隨手設的。**41 張表的實驗之後它有依據了，
而且刻意維持成固定常數 —— 見 §7.7。**

### 7.3 三段門檻

| 表數 | 要做什麼 | 依據 |
|---|---|---|
| **≤ 40** | **什麼都不用改** | LLM 層實測 6→21 張無退化；`CANDIDATE_N` 仍等於全部 |
| **40 ~ 200** | `CANDIDATE_N` **維持 40，改去修相似度那一層** | 這一格原本寫「往上調」，§7.7 推翻了它：往上調等於這一層不存在。**不要指望 dense 幫忙篩** —— 那正是要修的東西 |
| **> 200** | 階層式選表（先選領域再選表） | 500 張表目錄 = 25,000 字元，needle-in-haystack 開始咬。**這時要的仍然不是向量資料庫**（§2.13） |

### 7.4 ∪ Top-1 有保鮮期

現在 dense Top-1 有 88.5%，錯的那 11.5% 只讓平均多帶 0.1 張表，很划算（§2.6）。
照 −0.52pp/表 外推，100 張表時 Top-1 只剩約 47% —— 一半以上的機會塞一張不需要的表，
再被 KMB 拖出一整條路徑。**它不會降低召回，但會吃掉淨度。**
到時要重量的是這筆交易還划不划算，不是預設它永遠成立。

### 7.5 擴表時不會壞的東西

- **評估框架**：一般題的 GT 由 sqlglot 從 GT SQL 解出，加表不會讓它失效；
  而且加表會讓題目變難 —— **既有題目正好變成規模的回歸測試**。
  ⚠️ **但防禦題例外，見 §8.8** —— 那是唯一一種會被加表 silently 作廢的 GT。
- **向量快取**：key 是文件雜湊，新表只算新表。
- **KMB**：表變多它反而從 1% 回到有用（§3）。

---

### 7.6 真的把表加到 41 張：跨過 CANDIDATE_N 之後量到什麼

§7.1 的退化斜率是用「隨機抽既有表當干擾」模擬出來的。這次是真的加表：
`tools/add_distractor_tables.py --apply` 建了 19 張干擾表，22 → **41 張**，
剛好跨過 `CANDIDATE_N = 40`。

**為什麼是 41 不是 100**：22 → 100 一步跳過去會同時觸發四五件事，一件都
歸因不了。40 → 41 是資訊量最大的一步 —— 第一段漏斗第一次真的執行。

**為什麼不必補一題 GT**：檢索評估只讀 INFORMATION_SCHEMA，不讀資料。所以
干擾表是**純 schema、零資料列** —— 不碰種子、不動既有 GT、`--drop` 可完全還原。
指標是「既有 155 題的召回有沒有掉」，新表全部是干擾物。

**干擾表刻意做成近義表**（「歷程 vs 現值」「快照 vs 即時」「嘗試 vs 結果」）：
`order_status_history` / `product_price_history` / `product_stock_snapshots` /
`payment_attempts` / `delivery_attempts` …。隨便生 19 張不相干的表量不到東西。

| | 22 張 | 41 張 |
|---|---|---|
| 漏斗・錨點召回 | 94.2~95.5%（4 輪） | 93.5 / 94.2% |
| **漏斗・+KMB** | **97.4~98.7%** | **95.5 / 96.8%** |
| K=3 純相似度・錨點 | 76.8% | **71.6%**（兩輪相同） |
| K=3 純相似度・+KMB | 87.1% | **82.6%**（兩輪相同） |
| 淨度 | 85.8% | 82.0~82.5% |

**表數幾乎倍增，漏斗只掉 1.5pp，純相似度掉 4.5pp。** §7.1 的核心主張
（會退化的是相似度那層，不是 LLM 那層）在真實干擾表上被確認。
+KMB 的兩個值域不重疊，那 1.5pp 是真的；錨點召回只差約 1pp，判不出來。
K=3 不打 LLM，兩輪完全相同 —— 這也讓 −4.5pp 那個數字完全可信。

#### 護欄響了，但天花板沒發生

純嵌入（確定性）分開量兩件事：

```
① 候選召回@40 = 100.0%      0 題被 CANDIDATE_N 砍掉必要的表
② 干擾表搶到 Top-1 = 17/155  ∪Top-1 這道保險被汙染
```

41 張表時那一刀只砍掉排名最後的 1 張，而它從來不是需要的。
**所以 −1.5pp 完全來自干擾表的語意混淆，跟候選天花板無關。**
真正發生的是 §7.4 預測的「∪ Top-1 有保鮮期」：

```
#54 CANCELLED 訂單佔比    → order_cancellations
#11 #20 訂單狀態相關      → order_status_history
#14 #17 #31 #73 庫存相關  → product_stock_snapshots
```

**又是語意誘餌**（§8.9）—— 這些表註解一個字都沒寫錯，錯的是它們字面上
完美對上。只是這次誘餌來自**表數**而不是表寬。

#### 一個猜錯的地方：模擬干擾是偏悲觀，不是偏樂觀

事前的預期是「用隨機抽既有表模擬會**低估**退化，因為真實 schema 的困難在近義表」。
實測相反：

```
相似度 Top-1 命中   22 張 83.9% → 41 張 75.5%   實際斜率 −0.44pp/表
§7.1 線性外推                        78.1%      模擬斜率 −0.52pp/表
```

**刻意設計的近義干擾表，傷害沒有比隨機干擾大。** §7.1 那個估計略偏悲觀。

#### 這一輪還修正了一個擔心

原本以為干擾表被選中會讓 `get_ddl_for()` 退回完整 DDL（§8.6 那個坑）。
實測**沒有** —— semantic_layer.yaml 不認識的表就是靜默略過（364 → 364 字元），
不是退回全庫。所以保留這些表的成本比預期低。

### 7.7 `CANDIDATE_N` 是固定值，不是函數（一度改成函數，改回來了）

41 張表的實驗之後，我先把 `CANDIDATE_N` 改成了隨表數走的函數
（`max(40, 0.8 × 表數)`，外加一道 6,000 字元的目錄預算）。理由是 §7.2 的
「dense 要 2/3 的表才不漏」。**改回固定 40 了**，理由如下。

#### 讓上限隨表數成長，等於這一層不存在

這一段漏斗被建立的目的，就是「不管資料庫長到多大，送進 LLM 的表數有上限」。
`0.8 × 表數` 沒有上限 —— 它只是換一個比較慢的斜率把同一個問題往後推。
函數形式的兩個限制真正在做的事，是把「dense 召回不夠」這個問題偽裝成
「候選開大一點就好」，而 §2.4 早就量過：**相似度在原理上看不到結構必要性**，
所以開大候選買到的是雜訊，不是召回。

#### 40 這個數字現在有依據了（三條，但都只到 41 張表）

| 依據 | 數字 |
|---|---|
| §7.2 dense 候選召回 | Top-14/21 = 99.3%、Top-16/21 = 100% → 約 2/3 |
| 41 張表實測候選召回 @40 | **100%**（天花板沒有咬） |
| §8.9 目錄長度 | 40 張表 ≈ 2,400 字元；1,365 字元 → 94.8%、3,093 字元 → 88.4% |

第三條是關鍵：40 張表的目錄落在**還沒開始掉的那一側**，而 0.8 比例在 100 張表
會給出 80 張 ≈ 4,700 字元 —— 直接踩進實測掉 6.4pp 的區間。原本的字元預算
（6,000）是為了擋這件事，但那個數字本身沒有驗證過；用一個沒驗證的常數去
保護另一個沒驗證的公式，不如留一個有驗證的常數。

#### 候選上限 40 vs 41 的 A/B（41 張表）

| 候選上限 | 錨點召回 | +KMB |
|---|---|---|
| 40（真的篩掉 1 張） | 96.1% | 96.8% |
| 41（等於不篩） | 94.2% | 96.1% |

**「開大沒有好處」是成立的；「開大有害」不成立** —— 同組態重跑的變異約 2pp，
這個差距在雜訊裡。這組數字只用來排除「篩掉那 1 張會痛」，不用來主張篩掉更好。

#### 護欄改了語氣

`_warn_if_candidate_n_binds()` 原本會建議「往上調」。現在它只報事實：
砍掉幾張、候選涵蓋幾 %、目錄多少字元，然後要求跑 `eval_retrieval.py --funnel`。
**守門員是那個召回數字，不是這個常數。** 護欄的價值不在修好天花板效應，
在於讓一件無聲的事出聲。

#### 這條路的下一站不是調 `CANDIDATE_N`

外部建議說到極限就「回頭升級純向量相似度（Reranker 或 BM25 混合檢索）」。
BM25 + RRF 已經實測否決過（§2.9），Reranker 沒量過但方向對 —— 因為真正要修的
是相似度那一層的召回，不是候選的數量。在那之前先看 §7.4：41 張表已經讓
∪ Top-1 的保鮮期問題從預測變成現況。

#### 干擾表汙染還沒兌現成錯誤（Top-1 劫持是先行指標，不是死因）

41 張表這一輪 159 題按「干擾表有沒有進到最終 prompt」切開：

| | 題數 | 其中答錯 |
|---|---|---|
| 干擾表進了 prompt | 22 | **0** |
| 沒有干擾表 | 137 | 3 |

三題錯的是 `#93`（中位數，舊病）、`#138`（凍結中，舊病）、`#154`。
**沒有一題是干擾表造成的。** 最像的 `#104` 檢索確實只拿到 `payment_attempts`
（零列的干擾表），但生成器靠 §2.12 的 other-table-names 自己寫回了 `payments`。

所以「Top-1 劫持是 41 張表的死因」這個說法**是錯的**。汙染率 22/159 量到了，
但還沒兌現成任何一題錯誤。它是先行指標：現在去修它，是在優化一個不花錢的指標。

#### 再往下一層：那 3 題錯的裡面，只有 1 題是真的（`eval_stability.py`，n=8）

```
#93   4/8  擲硬幣   中位數
#138  0/8  穩定錯   收件城市 ≠ 居住城市（凍結中）
#154  4/8  擲硬幣   誤觸防禦暗號
```

`#154` 是**擲硬幣不是穩定錯**。單輪看它是「41 張表新增的失敗」，n=8 看它是
一枚本來就在那裡的硬幣 —— 就跟 `#56`、`#34` 的發現方式一樣（§4.2、§6）。

**結論：22 張表 157/159 → 41 張表 156/159 的那一分，落在擲硬幣的雜訊裡。
把表加到 41 張，沒有可歸因的準確度損失。** §7.6 量到的檢索層退化
（Top-1 命中 83.9% → 75.5%）全部被 LLM 選表 + KMB 那兩段吸收掉了 —— 這正是
三段漏斗設計要買的東西，第一次在規模上被驗證。

這也把 §5.2 那條規則又用了一次：**單輪分數的每一題差異，在歸因之前都不算數。**

### 7.8 干擾表到底弄瞎了什麼？（深度曲線 + 樞紐表盲點）

「19 張精心設計的干擾表成功弄瞎了第一層」—— 這個說法要拆成兩半，一半錯一半對。

#### 第一層沒瞎：純相似度的深度需求只從 14 變成 24

155 題，零 LLM 呼叫（`eval_retrieval.py --k`）：

| 取前 K | 佔全庫 | 純相似度召回 | +KMB |
|---|---|---|---|
| 14 | 34% | 94.2% | 99.4% |
| 16 | 39% | 96.8% | 100% |
| 20 | 49% | 98.7% | 100% |
| **24** | **59%** | **99.4%** | 100% |
| 28 | 68% | **100%** | 100% |

對照 21 張表：Top-14/21（67%）= 99.3%。

**表數 21 → 41（+20），達到同樣召回需要的深度 14 → 24（+10）—— 斜率約 0.5 張深度／張表。
而需要的涵蓋比例反而從 67% 降到 59%。** `CANDIDATE_N = 40` 還有 16 張餘裕。

線性外推：深度需求在**約 73 張表**時撞上 40。這是一個量出來的觸發點，
而且重測是零 LLM 成本的 K-sweep —— 這比 §7.3 那張憑感覺畫的門檻表可靠得多。

#### 對的那一半：盲點集中在 4 張樞紐表，而它們是最常被需要的

每張表「被需要時排第幾」（41 張表）：

| 表 | 被需要 | 中位排名 | 最差排名 |
|---|---|---|---|
| orders | 64 | 2 | **20** |
| customers | 53 | 2 | 9 |
| products | 51 | 3 | **27** |
| order_items | 33 | 2 | **16** |
| 其他 18 張 | 1~16 | **1** | **≤ 4** |

**18 張罕用表最差排名 ≤ 4，從來不是問題。全部的深層漏失風險集中在四張樞紐表。**
K=3/5/8/10 漏掉的題目，漏的表 100% 是 products / orders / order_items / customers。

原因是 §2.4：**相似度看得到主題，看不到結構必要性。** 問「各類別的營收」時
`order_items` 一個字都沒出現在問題裡。愈是樞紐的表，愈常在「沒被提到」時被需要。
**干擾表沒有製造這個盲點，只是把它往下壓了幾格**（products 最差壓到 27）。

#### 所以「換更好的檢索器」救不了這個洞

三條路都在算同一件事：query 文字 vs 表文字的相關性。

| 方案 | 狀態 |
|---|---|
| BM25 + RRF | 已實測否決（§2.9）。「營收」與「訂單明細」字面重疊是零，BM25 在這個盲點上更差 |
| Cross-encoder reranker | 沒量過，但輸入簽名與 dense 相同 (query, 表文字)。**「營收存在 order_items 裡」不在任何一邊的字串裡** |
| 加強嵌入文字 | 加詞三次全部讓召回下降（§2.10）；把表註解寫準有效但逐張手工（#121） |

補得起這個洞的是**結構**，而結構只存在兩個地方：FK 圖與 LLM 的世界知識。
兩者都已在 pipeline 裡，而且量得到在做事 —— K=14 時 +KMB 把 94.2% 拉到 99.4%。

**這是三段漏斗第一次在規模上被驗證：檢索層 Top-1 從 83.9% 掉到 75.5%，
端到端一分都沒有穩定地掉（§7.7）。**

#### 真要做的時候做什麼（現在不要做）

不是換檢索器，是**讓樞紐表不必靠相似度掙位子** —— 表數超過 `CANDIDATE_N` 時，
無條件把 FK 度數最高的幾張表放進候選、不參與排序競爭。成本約 4 個名額，
消掉的是上面觀察到的**全部**深層漏失模式。

**但現在做不得**：41 張表配上限 40 每題只砍 1 張，這個改動今天完全量不出效果 ——
做了就是一個無法驗證的改動（§5.2）。觸發點約 73 張表，守門測試是零成本的 K-sweep。

### 7.9 今天的檢索損失全部在 LLM 層，而且它的退化通道跟 dense 不一樣

§7.8 說 dense 沒瞎。那今天漏的表是誰漏的？把 41 張表那一輪**最終 prompt 漏表**的
四題拉出來，對照它們的相似度排名（零 LLM 呼叫）：

| 題 | 漏掉的表 | 相似度排名 | 在候選 40 內 | 機制 |
|---|---|---|---|---|
| `#104` | `payments` | **2** | 是 | **近義干擾表勝出** —— LLM 選了 `payment_attempts` |
| `#154` | `customer_profiles` | **4** | 是 | **欄位藏在寬表裡，表註解看不見**（`email_verified_at`） |
| `#62` | `products` | 10 | 是 | **語意可替代** —— LLM 認為 `order_items.unit_price` 夠用（§6.1） |
| `#159` | `customers` | 2 | 是 | 不是缺陷 —— GT 已加 `alt_sql`，回 `customer_id` 也算對 |

**四題的漏表全部在候選名單的前段（排名 2/2/4/10）。相似度給了，LLM 沒選。**

順帶排除兩個直覺上的嫌疑：

- **不是「照排名選」** —— 漏掉的表排名是 2 和 4，比一堆被選中的還前面。
- **不是漏連接表** —— 中介表本來就不歸 LLM 選（選表 prompt 第 2 條明講
  「系統會自動補路徑」），那是 KMB 的職責，四題裡沒有一張是橋接表。

#### 共同的根：LLM 一張表只看得到一句話

候選清單的一行是 `- payment_attempts: 每次付款嘗試的紀錄…`。要在這一句裡同時
分辨「這張表跟 `payments` 差在哪」與「email 驗證欄位在不在裡面」是不可能的 ——
這就是 §8.7 記過的「一張表一句註解」結構上限，在選表層的表現。

（`#154` 屬於 §8.9 定義的**盲區**而非**語意誘餌** —— 概念整個不在目錄裡。
§8.9 的結論是盲區可以靠把註解寫準修好，但 §8.7 同時記了單句註解的容量上限。
真要修，路線是 §2.18 的兩層 DDL／反應式補完，不是把欄位倒回候選清單 —— 那條
§8.9 已經量過並否決。）

#### 這條退化通道跟 dense 的方向不同，而且**開大候選會幫倒忙**

| | dense 層 | LLM 選表層 |
|---|---|---|
| 退化長什麼樣 | 需要的表排名往下掉 | 近義表把正確的表擠掉 |
| 隨規模怎麼漲 | 深度需求 ≈ +0.5 張／表（§7.8） | 近義表數量增加 → 咬的次數增加 |
| 用什麼吸收 | 加大 `CANDIDATE_N`（今天還有 16 張餘裕） | **加大候選只會讓近義表更多** |
| 41 張表的實測代價 | **0 題** | **2 題**（`#154` 失敗、`#104` 靠 §2.12 救回） |

**這修正了 §7.7 的說法。** 「檢索層退化被 LLM + KMB 吸收掉了」是對的，
但代價是**退化換了地方冒出來** —— 從排名層移到了選擇層。三段漏斗把問題
從「會不會被截斷」轉成「近義的東西分不分得開」，那是一筆划算但不免費的交易。

所以擴表時要盯的不是一個數字是兩個：
`eval_retrieval.py --k`（dense 深度需求，零 LLM 成本）與
`eval_retrieval.py --funnel`（LLM 選表召回，要打 LLM）。**前者今天很寬鬆，
後者才是目前真正在流血的地方。**

### 7.10 給干擾表灌資料：為什麼非做不可，以及它先擋掉了什麼

19 張干擾表原本是 schema-only、零列的。零列有兩個壞處，一個是評估的漏洞、
一個是方法論的盲點：

**① 必然的假通過。** 對帳規則裡 `expect: empty` 的題目「回 0 列就算對」，
而**任何對零列表的查詢都回 0 列**。模型選錯表也會被判對 —— 這比 §6.1 的
`#62` 更嚴重：`#62` 需要「兩欄資料剛好相等」的巧合，這一條不需要任何巧合。
`#104` 差一步就踩到（檢索只拿到 `payment_attempts`，靠生成器自己繞回去才沒觸發）。

**② 對整類方法瞎掉。** 熱度先驗、值檢索這些「用資料當訊號」的方法，在全空的
干擾表上會**假性滿分** —— 不是方法有效，是靶子是空的。

#### 安全性是驗證過的，不是推論

- GT SQL **0 題**參照干擾表（全文比對）→ 灌資料不可能改變任何 GT 結果
- 只往新表 INSERT，不動舊表任何一列
- 亂數用**專屬種子**，不碰 `init_db_ext` 的共用序列（前例：`tools/add_customer_profiles.py`）
- 灌完重跑對帳：**156/159，與灌之前完全相同**

> 我一度說「灌資料會動到 RNG 序列讓 159 題 GT 全部失效」——**那是錯的**。
> RNG 的問題只在「把 random 呼叫插進既有生成序列」時才咬人，獨立腳本無關。

#### 順序必須是「先宣告值域，再稽核，最後才灌」

第一版稽核工具只比欄位名，155 題跑出 26 個 HIGH，**幾乎全是誤報**：

```
#4「新北市客戶平均下幾張訂單」→ customer_login_logs 也有 customer_id、id
```

`customer_id` / `order_id` / `id` / `created_at` 在任何正規化 schema 裡到處都是。
拿它們判歧義，等於宣告「所有子表都可以互換」—— **那不是歧義，那是正規化。**

真正的歧義需要語意可替代，而語意不在欄位名裡，**在值裡**：

| | 值域 | 判定 |
|---|---|---|
| `orders.status='PAID'` vs `order_returns.status` | {REQUESTED, RECEIVED, REJECTED} | 不含 PAID → 查出來 0 列，不是第二個答案 |
| `orders.status='PENDING'` vs `support_tickets.status` | {OPEN, **PENDING**, CLOSED} | 真歧義候選 |

但要知道值域就得先知道會灌什麼值 —— 而值是灌的時候才決定的。**雞生蛋。**
解法是把值域先宣告成資料：`tools/distractor_seed_plan.yaml` 宣告意圖，
`tools/check_ambiguity.py` 稽核，`tools/seed_distractor_data.py` 實作並自我檢查。
**稽核過的才准灌，灌的必須跟稽核的一致。**

裁決結果：22 個 HIGH/MED 收斂成四個模式，**全部裁決為不歧義** ——
因為問句都自帶主體（「賣出」「訂單」「退款」）。三個關鍵事實：
`warehouse_transfers` 沒有 `order_id`（接不到訂單）、`order_returns` 沒有
`unit_price`（接不到金額）、`refunds.reason` 的實際值與 `order_cancellations.reason`
的宣告值域零重疊。裁決寫在 `tools/ambiguity_verdicts.yaml`（不寫回 GT ——
159 筆多行 note 經 yaml round-trip 會整份重排，風險大於收益），
而且工具會反向偵測**過期的裁決**。

#### 干擾表要「合理」，五條可檢查的準則

`tools/seed_distractor_data.py` 每一條都有對應的 assert：

1. **列數比例合理，且不能一律偏小。** `payment_attempts`(239) **必須多於**
   `payments`(164) —— 每筆付款至少一次嘗試，失敗還會重試。干擾表一律小的話，
   **列數本身就變成免費的答案洩漏**，那還是作弊，只是換個變數。
2. **值域與正解表重疊。** `order_status_history.to_status` 刻意與 `orders.status`
   完全相同 —— 否則值檢索一秒分辨得出來，量到的有效性是假的。
3. **參照完整性**（FK 已建，MySQL 硬擋；程式再檢一次好給出可讀的錯誤）。
4. **時序一致**（子事件不早於父事件）。
5. **查得出「像樣但錯」的答案**，而不是靜悄悄的空集合。

共灌 2,276 列。**刻意留兩張空表**（`product_bundles` 模擬尚未上線、
`cart_recovery_emails` 模擬已棄用）—— 真實 warehouse 本來就有空表，
「空」必須是一個**刻意的測試項**，不是建構的副作用。它們同時是熱度先驗
最危險失效模式的樣本：**用熱度當硬過濾會誤殺剛上線的正確新表。**

### 7.11 四個業界方案的實測（A / C / D 在 dense 層，B 在 LLM 層）

方案取自業界與研究界對「LLM 選錯近義表」的標準解（來源見 §7.12）：

| | 方案 | 出處 | 打不打 LLM |
|---|---|---|---|
| A | 熱度／列數先驗 | LinkedIn SQL Bot（候選檢索「以存取熱度過濾」） | 否 |
| B | 打散順序 × N 取聯集 | MCS-SQL（COLING 2025） | **是** |
| C | 值／實體檢索 | CHESS（Stanford） | 否 |
| D | 欄位優先檢索並聯 | RSL-SQL / bidirectional retrieval | 否 |

工具：`eval_retrieval_variants.py`（A/C/D，零 LLM）、`eval_filter_vote.py`（B）。
α/β/γ 是**刻意掃的曲線，不是選出來的參數** —— 看的是有沒有方向，
不是哪個數字最高（155 題分不出 1pp，§5.2）。

#### dense 層結果（155 題，41 張表，干擾表已灌資料）

| 變體 | @3 | @8 | @10 | @14 | @20 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|---|---|---|
| **baseline** | 71.6 | 87.1 | 90.3 | 94.2 | 98.7 | **75.5%** | **17** |
| A 列數先驗 α=0.05 | 70.3 | 87.1 | 89.0 | 96.1 | 98.7 | 77.4% | 14 |
| C 值檢索 β=0.10 | 72.9 | 89.0 | 92.9 | 96.1 | **100.0** | 76.8% | 19 |
| D 欄位並聯 γ=1.0 | 72.3 | 89.0 | **93.5** | **96.8** | 98.7 | 72.9% | **5** |
| D′ RRF k=10 | 71.6 | 89.7 | 91.6 | 96.1 | 98.1 | 68.4% | 12 |
| 對照：只用欄位向量 | 63.2 | 79.4 | 82.6 | 89.7 | 93.5 | 63.9% | **3** |

#### A：灌資料把它的表面收益砍掉六成 —— 這就是灌資料的價值

| A α=0.05 | @3 | @10 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|
| **對照：干擾表當成零列** | 77.4 | 91.6 | **80.6%** | **9** |
| 灌資料後（真實列數） | 70.3 | 89.0 | 77.4% | 14 |
| baseline | 71.6 | 90.3 | 75.5% | 17 |

零列的世界裡 A 看起來 Top-1 **+5.1pp**、汙染腰斬；真實列數下只剩 **+1.9pp**、
17→14（三題，在雜訊裡）。原因很直接：`payment_attempts` 現在有 239 列，
**比 `payments` 的 164 列還多** —— 列數先驗現在幫的是干擾表。

**這正是 §7.10 預測的假性有效，而且是在同一份程式碼上用開關驗證的，不是推論。**
LinkedIn 用的是 query log 的**存取熱度**，那與列數不是同一件事；本專案沒有
query log，所以 A 在這裡**沒有可用的訊號**，不是「方法無效」。

#### C：買到深層召回，買不到 Top-1

@8 +1.9pp、@10 +2.6pp、@20 到 100%，方向乾淨且隨 β 單調。
但 **Top-1 沒動（75.5→76.8），干擾搶 Top-1 反而變差（17→19）**。
機制上說得通：值命中會同時抬升好幾張表，改善的是「有沒有進到候選」，
不是「誰排第一」。

#### D：唯一機制對得上目標題的方案，但今天沒有東西可以買

先排除尺度假象：γ=1.0 時只有 **13.3%** 的表被欄位分數蓋掉，
所以 `max(表分數, 欄位分數)` 是真的並聯，不是換一個評分函數。
兩種融合方式（max 與 RRF）輪廓一致：**深層召回一致上升、干擾搶 Top-1 一致大跌、
Top-1 命中一致下降**。最後一列解釋了為什麼：干擾表的欄位全是
`id` / `order_id` / `status` 這種通用欄位，沒有問題會在語意上打中它們 ——
**欄位訊號天生對干擾表免疫，但它作為主檢索器太弱**（單獨用只有 63.9%）。

四個目標題的實際排名：

| 題 | 目標表 | baseline | D γ=1.0 |
|---|---|---|---|
| `#154` | customer_profiles | 4 | **1** |
| `#62` | products | 10 | 7 |
| `#104` | payments | 2 | 2 |
| `#159` | customers | 2 | 2 |

`#154` 從 4 到 1 —— **寬表盲點正是雙向檢索文獻預測會修好的東西**
（`email_verified_at` 是欄位，塞不進一句表註解，§8.7）。

**但這個勝利不會兌現。** `#154` 的失敗不是因為排名 4 —— 候選有 40 張，
那張表**本來就在裡面**，是 LLM 沒選它（§7.9）。D 改善的是「候選深度召回」，
而候選召回在 41 張表已經是 100%。**方向對，時候不對** ——
跟 §7.8 的樞紐表保留席同一個結論：等 `CANDIDATE_N` 真的開始截斷才有東西可買。

#### B：聯集買不到東西，但**打散順序本身**買得到（而且免費）

`eval_filter_vote.py`，155 題 × 3 次取樣 = 465 次呼叫，一次得到整條曲線。
第 0 次刻意用**固定順序**當對照 —— 沒有它就分不出「聯集有效」與
「光是打散順序就有效」，那是兩個完全不同的結論。

| 設定 | 錨點召回 | +KMB | 平均表數 | 淨度 | 含干擾 |
|---|---|---|---|---|---|
| N=1 固定順序（n=3） | 94.8 / 92.9 / 93.5 | 97.4 / 96.8 / 97.4 | 2.14 | 82.7% | 19 |
| N=1 打散順序（n=2） | **98.1 / 96.1** | **99.4 / 98.1** | 2.19 | 82.8% | 17 |
| N=2 聯集 | 99.4 | **99.4** | 2.29 | 80.8% | 19 |
| N=3 聯集 | 99.4 | **99.4** | 2.37 | 78.5% | 19 |

**① 聯集否決。** 單一次打散呼叫的 +KMB 就已經是 99.4% —— 聯集用 3 倍呼叫
買到 **0 增益**，代價是淨度 −4.3pp、表數 +8%。

原因在 4 題目標探針裡看得一清二楚（4 題 × 6 次）：

| 題 | 單次命中 | N=3 聯集 |
|---|---|---|
| `#62` | 2/6 | ✅ |
| `#159` | 5/6 | ✅ |
| `#154` | 6/6 | ✅ |
| **`#104`** | **0/6** | **❌** |

**聯集只修得動擲硬幣題，而擲硬幣題 KMB 本來就會沿 FK 撿回來**
（`#62` 缺 products，`order_items.product_id` 一條邊就到）。
`#104` 是**系統性**的錯 —— 多抽幾次同一個確定的錯誤答案，還是那個答案。
這是 §2.6 聯集邏輯的邊界：**聯集治隨機，不治系統。**

**② 打散順序採用。** 固定 n=3 與打散 n=2 的區間**完全不重疊**
（固定最高 94.8 < 打散最低 96.1；+KMB 最高 97.4 < 最低 98.1）。
精確排序檢定 3v2 全勝 = **p = 0.10**，是這個樣本數能達到的最好值 ——
不算顯著，但**方向在三個獨立來源上一致**：155 題全域、4 題目標探針
（打散在 3/4 題上單次命中率更高）、以及 LLM listwise 排序的位置偏誤文獻。
而且這個改動**不花任何額外呼叫**。

> 這推翻了 §2.2 的一句話：`format_catalog` 照相似度排序「等於免費給 LLM
> 一個先驗」。量出來那個先驗是**淨負的** —— 它不是免費的先驗，是免費的偏誤。
> 這也修正了我自己在 §7.9 的判斷：我當時說位置偏誤不是主因，理由是漏掉的表
> 排名 2 和 4 都在最前面。那個推理錯了 —— 位置偏誤影響的不只是「排太後面被忽略」，
> 固定順序還會讓**同一組偏誤每次都重現**，於是錯的那幾題穩定地錯。

### 7.13 最後真正修好 `#104` 的，是一句表註解

A / B / C / D **沒有一個**修得動 `#104`：

| | 為什麼不行 |
|---|---|
| A 列數先驗 | `payment_attempts`(239 列) 比 `payments`(164 列) 還多，先驗幫的是干擾表 |
| B 聯集 | 0/6，系統性錯誤不是隨機錯誤 |
| C 值檢索 | 問句寫中文「付款失敗」，值域裡是 `'FAILED'`，**語言對不上** |
| D 欄位並聯 | `payments` 本來就排第 2，dense 沒有問題 |

診斷出來的根因很具體：

```
payments        表註解：…何時付款、用什麼付、付了多少看這張表；付款狀態與訂單狀態意義不同
payment_attempts 表註解：付款嘗試紀錄：每次刷卡嘗試都留一筆，含失敗的。成功的付款結果在 payments
```

`payments.status` 的值域 `(SUCCESS/FAILED/REFUNDED)` **只寫在欄位註解裡**，
而候選清單只給表註解（§2.8）—— **LLM 從頭到尾看不到 `payments` 有「失敗」這個狀態，
而干擾表的註解上明寫著「含失敗的」。** 問句的關鍵詞字面出現在錯的那張表上。

注意 `payment_attempts` 的註解**已經寫了「成功的付款結果在 payments」而且無效**：
往干擾表加指路標沒有用，要補的是**正解表的盲區**。這與 §8.2 的方向一致 ——
不要削弱干擾者，要讓正確答案可被描述。

改法（`init_db_ext.py` 的 `CREATE TABLE ... COMMENT`，再用
`tools/sync_table_comments.py --apply` 套用，純 metadata，一位元資料都不動）：

```
付款狀態 SUCCESS/FAILED/REFUNDED —— 查付款成功、付款失敗、已退款的紀錄都用這張表
```

**結果：`#104` 從 0/6 變 6/6，而 `payments` 的相似度排名沒變（還是第 2）** ——
證實問題從頭到尾都在「LLM 看不到那個概念」，不在檢索排序。

稀釋鏡像（§8.2）的風險已驗證沒有發生：dense 層 @3 / @14 / Top-1 / 干擾搶 Top-1
**四個數字全部不變**，`payments` 在不需要它的 150 題裡中位排名 14，只搶到 1 次 Top-1。

#### 這一輪最重要的結論

**LinkedIn 那個數字的方向在這裡被複製了：44pp「幾乎完全來自知識圖譜，
不是來自更好的 prompt 或更聰明的 agent」。** 本專案的版本是：
四個檢索演算法方案全部失敗，一句表註解成功。

而且它同時解釋了 `#154`（`email_verified_at` 在 50 欄寬表裡，表註解塞不下）——
**兩題是同一個根：§8.7 的「一張表一句註解」容量上限。**
真正的下一步不是換檢索器，是 §2.18 的兩層 DDL／反應式補完，
或 Snowflake 那條 synonyms + verified queries 的 metadata 路線。

#### 決策總表

| 方案 | 判決 | 理由 |
|---|---|---|
| A 列數／熱度先驗 | **否決** | 灌資料後收益從 +5.1pp 砍到 +1.9pp；本專案沒有 query log，列數不是熱度 |
| B 多取樣聯集 | **否決** | +KMB 零增益，3× 呼叫，淨度 −4.3pp |
| **B′ 打散候選順序** | **採用** | +3.4pp 錨點、+1.5pp +KMB，**零成本**（p=0.10，方向三方一致） |
| C 值／實體檢索 | **擱置** | 深層召回 +2.6pp 但買不到 Top-1；中文問句對英文值域，語言不通 |
| D 欄位優先並聯 | **擱置** | 方向對（`#154` 排名 4→1）但候選召回已 100%，今天沒東西可買 |
| **註解補強** | **採用** | `#104` 0/6 → 6/6，dense 零損傷，成本是一句話 |

### 7.14 「中文問句 vs 英文 DB 值」的語言斷層 —— 外部建議，實測否決

外部建議（Gemini）指出方案 C 的失效是 Cross-lingual Entity Resolution 問題，
並提議在 `COLUMN_COMMENT` 寫中英對照字典：
`付款狀態 (SUCCESS 成功 / FAILED 失敗 / REFUNDED 退款)`。

**機制描述是對的，結論不成立。三個可驗證的錯誤：**

#### ① 放錯管道 —— COLUMN_COMMENT 到不了選表層

```
檢索向量文件 = 「表 {name}：」+ TABLE_COMMENT
候選清單一行 = 「- {name}: 」  + TABLE_COMMENT
```

§8.3 已經把欄位文字從檢索文件整個拿掉，§2.8 的候選清單也只給表註解。
**COLUMN_COMMENT 不在這兩條管道裡的任何一條**，只會流進最後給生成器的 DDL。
修好 `#104` 的是 **TABLE_COMMENT**（§7.13），不是 COLUMN_COMMENT。

#### ② 歸因錯了 —— 生成器本來就會翻譯

`#104` 當初的系統 SQL 是 `SELECT * FROM payments WHERE status = 'FAILED'`。
**模型在完全沒有中英對照的情況下自己完成了「付款失敗」→ `FAILED`。**
它從來沒有錯在映射，錯在 `payments` 這個概念沒進候選目錄 —— 那是**盲區**。
字典要解的是一個不存在的問題。

#### ③ 規模與收益都是零（實測）

| | |
|---|---|
| 155 題裡有值命中的 | 34 題 |
| 中文值命中 / 英文值命中 | 22 次 / **21 次**（英文命中是因為問句自己寫英文碼，如「狀態是 PAID」） |
| 真正有語言斷層的 | **5 題**（`#104` `#111` `#113` `#114` `#115`） |
| 其中目前答錯的 | **0 題** |

把中英別名層加進值索引（426 → 443 個值）之後重測：

| 變體 | @3 | @10 | @14 | Top-1 | 干擾搶 Top-1 |
|---|---|---|---|---|---|
| baseline | 71.6 | 89.7 | 94.2 | 75.5% | 17 |
| C 原版 β=0.1 | 72.9 | 92.3 | 96.1 | 76.8% | 19 |
| **C + 中英別名 β=0.1** | 72.3 | **92.3** | **96.1** | **76.8%** | 19 |

**三個指標一個都沒動。** 那 5 題的目標表排名也全部不變：

| 題 | 目標表 | baseline | C 原版 | C + 別名 |
|---|---|---|---|---|
| `#104` | payments | 2 | 2 | 2 |
| `#111` | payments | **1** | 1 | 1 |
| `#113` `#114` `#115` | refunds | **1** | 1 | 1 |

原因：表註解寫的是中文（「付款紀錄表」「退款」），問句寫的也是中文 ——
**dense 在表的層級早就跨過去了，值那一層沒有東西可以補。**
語言斷層只存在於低基數的**狀態碼**，而狀態碼正好是 metadata 寫得下的；
高基數的**實體名**（商品、類別、城市）在本庫本來就是中文，沒有斷層。
**兩者互補，所以這個洞在結構上就不存在。**

#### 它沒提到的風險

§2.10 稀釋定律：往**所有**欄位註解加中英對照，就是往所有文件加文字。
今天安全只因為 §8.3 已把欄位文字排除在檢索文件外 —— 但 §8.9 一直在重新評估
要不要把欄位放回候選目錄。真放回去的那天，這些對照字就變成稀釋來源。
**這條建議與一條可能的未來決策相衝突，而它自己看不到。**

#### 唯一還站得住的殘餘

生成器的 DDL 裡放中英對照，理論上對更難的狀態映射有幫助。
但**生成器目前在這類映射上零錯誤，沒有訊號可以量** —— 依 §5.2，
在沒有錯誤的地方做改動，分不出改好了還是本來就沒壞。
**觸發條件**：`eval_score.py` 出現「表選對、狀態常數寫錯」的失敗時再回來做。

### 7.12 這些方案的出處（2026-08 檢索）

不是憑空想的四個方向，是業界與研究界對「LLM 選錯近義表」的既有答案。
記在這裡是為了下次要往下走時不必重查，也為了讓「我們的做法跟業界差在哪」
可以被檢查。

**業界：漏斗形狀跟本專案幾乎一樣，差別在多疊一層「使用訊號」**

- **LinkedIn SQL Bot** —— embedding 取 20 張候選、**以存取熱度過濾**，
  再由 LLM 評分收斂到 7 張。表召回 78%、欄位召回 56%。
  最值得記的數字：**只給 schema 是 9% 正確，加上知識圖譜後 53%** ——
  這 44pp「幾乎完全來自知識圖譜，不是來自更好的 prompt 或更聰明的 agent」。
  知識圖譜內容：DataHub metadata、query log 的表熱度與常見 join pattern、
  人工領域知識、認證過的範例查詢。
- **Pinterest** —— 10 萬張表；明講「單純的關鍵字比對與表摘要**不夠**」。
- **Snowflake Cortex Analyst** —— YAML semantic model，三個準確度槓桿：
  **synonyms、metrics、verified queries**，回報 90%+。

  → 共同訊息：**表註解不夠分辨近義表時，業界補的不是更長的文字，
    是「這張表實際被怎麼用」的證據。** 本專案缺的正是這一層（沒有 query log）。

**研究界**

- **MCS-SQL（COLING 2025）** —— 隨機打散表順序產生多個 prompt，取**聯集**。
  理由與本專案 §2.6 選 ∪Top-1 逐字相同：「多選幾張表對後續 SQL 生成影響不大，
  漏掉必要的表則會讓查詢根本無法正確生成」。→ 方案 B
- **CHESS（Stanford）** —— 關鍵字 → LSH → 在**實際欄位值**裡找。→ 方案 C
- **RSL-SQL / Rethinking Schema Linking (2510.14296)** —— 表優先→選欄位，
  與欄位優先→反推表，兩條並聯。並明確警告：**激進裁剪雖然精準度變好，
  卻有把正確的表刪掉的風險**。→ 方案 D，同時支持 §7.7 把 `CANDIDATE_N` 留在 40
- **In-depth Analysis of LLM-based Schema Linking（EDBT 2026）** 與一份 BIRD 上的
  開源模型研究 —— **「即使配上高召回的 embedding 檢索器，schema linking 也沒有幫助」**。
  這條是反方向的證據，要一起記。
- **位置偏誤** —— LLM listwise 排序中「排在後段的項目較不會被排高」。
  本專案 `format_catalog` 刻意照相似度排序（§2.2 說那是「免費的先驗」），
  正好是這個偏誤的來源。但**對本專案的四個漏表題它不是主因** ——
  漏掉的表排名 2 和 4，都在清單最前面。

## 8. 寬表：加 `customer_profiles` 之後真的發生了什麼

規劃裡「1~2 張 50~80 欄寬表」是唯一沒驗證的維度（表數 21 已達標）。
2026-08-18 加了 `customer_profiles`（57 欄，1:1 掛在 customers），
全庫變成 **22 張表 / 201 欄**。

**只新增，不重建**：`tools/add_customer_profiles.py --apply` 只做
CREATE TABLE + INSERT，不碰其他 21 張表一個位元，事後跑 `verify_base_tables()`
確認核心 4 張表 sha256 不變 —— 145 題 GT 全部存活，既有 139 題正好變成
「多一張寬表」的對照組。seeding 用專屬亂數種子 `PROFILE_SEED`，不碰
`seed_extended` 的共用序列，所以「就地新增」與「完整重跑」產生相同資料。

### 8.1 兩個陷阱都會咬（`#62` 的教訓）

| 陷阱 | 對不上的比例 |
|---|---|
| `registered_at` vs `customers.created_at`（兩個系統的紀錄） | **49/50** |
| `total_spent` / `order_count`（每日結算快照，30 天延遲） vs 即時 SUM/COUNT | **27/50** |

§6.1 學到的是「兩欄永遠相等 = 這個陷阱不存在」，所以刻意讓它們對不上。

### 8.2 稀釋定律的鏡像：一份文件太長，它會到處排第一

139 題沒有一題需要這張新表，所以它每次進 Top-1 都是誤選。實測 **24/139（17.3%）**。
拆開看肇事者：

```
LLM 主動選的          0/139   ← 一次都沒有
相似度 ∪ Top-1 帶進來的  24/139   ← 全部
```

**LLM 完全沒被騙，被騙的只有餘弦。** 原因是這張表的檢索文件 1,828 字元
（其他表中位數 173），57 個欄位註解幾乎涵蓋所有業務概念。

> §2.10 說「往**所有**文件加字，大家都變像」。這裡是它的鏡像：
> **往一份文件加字，那一份到處都贏。** 同一個機制，兩種表現。

這也是 §7.4「∪ Top-1 有保鮮期」提前實現 —— 但觸發它的不是我預測的「表數變多」，
是**表寬的變異變大**。

### 8.3 解法：檢索文件**只放表註解**，欄位清單整個拿掉

第一版的修法是「替寬表的欄位清單設一個裁剪上限」，把誤選從 24 壓到 13。
但那只是把症狀壓小，還多了一個憑感覺的常數（`_MAX_DOC_COLUMNS = 12`）——
正是 §2.3 說過的錯誤形狀：不該調常數，該換機制。

真正的病因是**文件長度變成表寬的函數**。根治法是讓它與表寬無關：
檢索文件只放表名 + 表註解。139 題確定性測量（嵌入固定，無跑次變異）：

| 檢索文件內容 | 文件中位 | Top-1 命中 | 寬表誤選 | 候選@12 | K=3 | K=4 |
|---|---|---|---|---|---|---|
| **只有表註解（現行）** | **64** | **84.9%** | **9/139** | 95.7% | 85.6% | 90.6% |
| 註解 + 鍵 + 非鍵前 12 欄 | 180 | 81.3% | 13/139 | 97.1% | 85.6% | 90.6% |
| 註解 + 鍵 | 92 | 83.5% | 10/139 | **97.8%** | 84.2% | 89.2% |
| 註解 + 全部欄位 | 180 | 76.3% | 24/139 | 97.1% | **86.3%** | **92.1%** |

**欄位清單唯一的貢獻是候選召回高 1.4pp**，而那一段在表 ≤ `CANDIDATE_N` 時
根本沒作用（§7.2）。退路（K=3/K=4）完全沒受影響。文件長度從 24~1,828 收斂到
24~104，**寬表問題從「被壓小」變成「結構上不可能發生」**。

#### 誤差獨立性：擔心的事沒有發生

檢索文件現在與 LLM 候選目錄讀**完全相同的文字**。照 §2.14 的教訓應該擔心
「錯得一致、∪ Top-1 這個保險失效」。用同一批 LLM 選表結果做控制比較：

| 檢索文件 | ∪Top-1 召回 | LLM 漏掉但被救回 | 幫倒忙 | 平均表數 | 淨度 |
|---|---|---|---|---|---|
| 只有表註解 | 98.6% | **2** | 0 | **2.1** | **87.6%** |
| 註解 + 鍵 + 12 欄 | 98.6% | 2 | 0 | 2.2 | 87.0% |
| 註解 + 全部欄位 | 98.6% | 2 | 0 | 2.3 | 85.2% |

（LLM 單獨不 ∪ Top-1 是 97.1%。）救回的是同樣 2 題，沒有幫倒忙。

> **誤差獨立來自機制不同，不是來自輸入不同。** 餘弦只會比對相似度，
> LLM 會推理「物流狀態 ≠ 訂單狀態」—— 同一份文字，兩種讀法。
> §2.14 的 B2 之所以失效，不是因為「讀了表註解」，而是因為它把表註解
> 複製進每個欄位 chunk，直接改變了排名結果。

**副作用**：`_MAX_DOC_COLUMNS` 這個常數消失了，`build_table_documents()`
不再需要查 INFORMATION_SCHEMA.COLUMNS，直接用 `get_table_briefs()` ——
**檢索、LLM 候選目錄、DDL 現在共用同一個權威來源 `TABLE_COMMENT`。**
要提升檢索只剩一條路：把表註解寫好（§2.8）。

### 8.4 這推翻了 §2.14 的「有條件否決」——方向相反

§2.14 寫「表寬到 100 欄以上時 max-pool 很可能翻盤」。實測是**反的**：

| 做法 | Top-1 命中 | 寬表誤選 |
|---|---|---|
| 表級・完整欄位清單 | 78.4% | 19/139 |
| **表級・寬表截 12 欄** | **84.9%** | **6/139** |
| 欄位級 max-pool | 61.9% | **45/139** |

**57 個欄位等於 57 次僥倖命中的機會。** 寬表讓 max-pool 更糟，不是更好。
稀釋不是靠「把文件拆小」解決的，是靠**讓所有文件長度接近**解決的。

### 8.5 沒有全部修好 —— 誠實的殘留成本

| | 加寬表前（21 表） | 加寬表・全欄位 | 加寬表・只有表註解 |
|---|---|---|---|
| 相似度 Top-1 命中 | 88.5% | 76.3% | **84.9%** |
| 寬表誤選為 Top-1 | — | 24/139 | **9/139** |

**修法把傷害減到 3.6pp，沒有消除。** 一張 57 欄的表放進 22 張表的庫裡，
相似度層還是會退化 —— 因為它的表註解本來就得涵蓋比較多概念。漏斗召回沒有可測的變化（跨次 97.8~99.3%，見 §5.2），
因為扛主力的是 LLM 那層，而它對這件事完全免疫。

### 8.6 順帶抓到的真問題：DDL 有第二個來源

`build_table_documents()` 讀 INFORMATION_SCHEMA（新表自動出現），但 DDL 讀的是
`utils/semantic_layer.yaml`（由 `tools/gen_ddl.py` 生成）。加表之後兩者不同步，
`get_ddl_for({'customer_profiles'})` 找不到就**退回完整 DDL**。

好消息是**這不是靜默的** —— `schema_registry._report_drift()` 有印出
「DB 有但 YAML DDL 沒有的表」。修法是 `python tools/gen_ddl.py --write`
（DDL 9,279 → 13,237 字元）。**加表的標準流程要包含這一步。**

### 8.7 補 GT 覆蓋，順便撞到「一張表一句註解」的結構上限

加了 `#146-159` 共 14 題（`eval_questions_v2.json` 與 `eval_ground_truth.yaml`
同步，總題數 145 → 159）。前四題是陷阱的正面測試：

| 題 | 測什麼 |
|---|---|
| `#146` | 題目明講「檔案的消費快照」→ 要走 `total_spent`，不是即時 SUM |
| `#147` `#148` | 快照與即時**對不起來的客戶有幾位** —— 只有同時用兩張表才答得出來 |
| `#149` | `registered_at`（會員系統）vs `customers.created_at`（交易系統） |
| `#159` | `risk_flag='HIGH'` 是合法列舉值但資料裡剛好 0 筆 —— 測誠實回報空集合 |

**兩題漏了，而且是同一個病因：表註解沒有涵蓋到那個概念群。**

`#149`「會員系統註冊時間」原本 LLM **0/5** 選中這張表 —— 因為 84 字的表註解
完全沒提到那 10 個時間戳欄位。把「會員系統的註冊與最近活動時間」加進去之後
**4/5**（單題重複取樣，§5.2）。代價是確定性測量上 Top-1 命中 85.6% → 84.3%、
誤選 9 → 10。用保險層的 1.3pp 換掉一整組欄位的盲區，划算。

但補完 `#149`，`#154`「還沒通過 Email 驗證的客戶」又漏 —— `email_verified_at`
這個概念同樣不在註解裡。**這裡就停手了**：

> **一張 57 欄的表大約有 5 個概念群，而表註解在被過度選中之前大概只塞得下 3~4 個。**
> 這不是措辭問題，是「一張表一句註解」這個資料結構在寬表上的容量上限 ——
> 每加一個概念，它就在更多不相干的問題上排第一（§8.2）。繼續往裡塞字
> 就變成 teaching to the test，而且沒有原則性的停止點。

真要解，方向是**給寬表額外的概念群摘要**（而不是把所有群塞進同一句），
或 §7.3 的階層式選表。兩者都是設計改動，要另外量，不在這一輪做。

`#159` 漏的是另一回事：LLM 只選了 `customer_profiles` 一張表，**單一錨點時
KMB 在數學上不可能補出 `customers`**（§2.4 的 K=1 證據）。但它預期空集合，
端到端大概仍會判對 —— 又一個 §6.1「端到端低報檢索失敗」的例子。

### 8.8 防禦題是唯一會被「加表」silently 作廢的 GT

加完寬表跑端到端，`#69`「客戶的年齡分布」與 `#71`「客戶的性別比例」判定為錯 ——
但模型答對了，**是 GT 過期**。這兩題原本是防禦題（斷言「schema 沒有這個概念」），
而 `customer_profiles` 帶進了 `birth_year` 與 `gender`。

> **一般題斷言「答案是什麼」，防禦題斷言「概念不存在」。**
> 前者的正確性只依賴用到的那幾張表，後者依賴**整個 schema 的補集** ——
> 所以加任何一張表都可能讓它失效，而且失效時是**靜默的**：
> 系統答對了卻被判錯，看起來像模型退步。

判準沿用 `#70` 立下的那條，而且這次剛好落在它的另一邊：

| | 資料 | 業務定義 | 結論 |
|---|---|---|---|
| `#70` 利潤率 | `supply_price` 有了 | ✗ 一商品多進貨價、公式不在 rules | **維持防禦** |
| `#69` 年齡 | `birth_year` 有了 | ✓ `2026 - birth_year`，無歧義 | 改為一般題 |
| `#71` 性別比例 | `gender` 有了 | ✓ `count / total`，無歧義 | 改為一般題 |

**判準是「業務定義在不在」，不是「欄位在不在」。** 防禦題從 6 題剩 4 題
（`#68` `#70` `#140` `#141`）。

**加表的檢查清單**因此多一條：跑完端到端要逐一檢查防禦題有沒有被新 schema 作廢。

### 8.9 候選目錄要不要附欄位？六種呈現方式對照（已實測否決，但理由跟一開始想的不同）

§8.2 看到「LLM 對 1,828 字元的長註解完全免疫（誤選 0/139）」，很自然會推論
「那就把欄位概念也給它，反正它不怕長」。而且 token 成本確實不高（682 → 3,078 tokens）。
**實測是反的 —— 而且第一版的解釋也是錯的。**

153 題（其中 16 題真的需要寬表）。多輪測量，取值域：

| 候選目錄 | 目錄字元 | 錨點召回 | +KMB | 寬表題找到 | 其餘誤選 |
|---|---|---|---|---|---|
| **只有表註解（現行）** | 1,365 | **94.2~95.5%**（4 輪） | **97.4~98.7%** | 16/16 | 11~13/139 |
| ＋非鍵註解去重短語列 | 3,653 | 90.3~92.9%（3 輪） | 96.1~98.1% | 16/16 | 11~15/139 |
| ＋欄位名（無註解） | 3,660 | 87.7% | 92.9% | 16/16 | 15/139 |
| ＋寬表欄位（name+註解） | 3,093 | 88.4% | 92.9% | 16/16 | 15/139 |
| ＋全部欄位（name+註解） | 6,156 | 88.4~91.0%（3 輪） | 93.5~96.1% | 16/16 | 14~15/139 |

**每一種給法都比不給差，而且沒有一種對寬表題有幫助**（全部 16/16）。

#### 長度不是主因，呈現方式才是

第 2、3 列是一組乾淨的控制對照 —— **3,653 vs 3,660 字元，長度幾乎完全相同，
只換內容，差 5.2pp**：

```
裸欄位名    id、customer_id、total_spent、order_count…        語意近乎零
概念短語列  累計消費金額、訂單筆數、流失風險分數…              有語意、無結構雜訊
```

短語列去掉了主鍵、去掉了 `name（comment）` 的括號堆疊、去掉了跨表重複的
「商品ID」。**它多的不是資訊，是形式。** 所以 §8.9 第一版寫的
「LLM 容不下結構化的細節傾倒／needle-in-haystack」只說對了一半：
**形式雜訊確實會稀釋語意，但那一層是可以靠改寫補回來的**（+5.2pp）。

#### 補不回來的那一層：語意誘餌

短語列補完形式之後仍然停在 90.3~92.9%，比基準低約 3.6pp（+KMB 的差距
0.9pp 則落在雜訊內）。看漏題就知道卡在哪：

```
#30 #46 #93 #94 #100   全部是「總消費金額」→ 被 customer_profiles.total_spent 吸走
#62 單價超過 30000     → 被 order_items.unit_price（購買時的單價）吸走
```

`total_spent` 是 30 天延遲的**結算快照**（§8.1），題目要的是即時 SUM。
**這些欄位註解一個字都沒寫錯，錯的是它們字面上完美對得上。**

> **描述寫得更好能救「盲區」，救不了「誘餌」。**
> 盲區 = 概念根本沒被提到（`#154` Email 驗證），加字就能解。
> 誘餌 = 概念被提到了，但那不是你要的那一個 —— 加字只會讓誘餌更顯眼。
> 這是一條機制性的上限，不是措辭問題。

而候選目錄只有表註解時，`total_spent` 這個誘餌**在物理上不存在**，
LLM 只看得到「每日結算的消費統計快照」這句帶著限定詞的摘要，
它讀得懂那不是即時值 —— 這正好回應 §8.3 的「同一份文字，兩種讀法」：
**摘要保留了限定詞，欄位清單把限定詞丟掉只留下名詞。**

#### 「那要不要只給寬表？」也否決

理由不只是數字（第 4 列一樣糟）：**「只對寬表特別處理」需要一個比
「它比較寬」更好的理由**。歧義本來就不是寬表獨有的 ——
`orders.status` / `shipments.status` / `payments.status` 是這個 schema
刻意埋的同名陷阱。

#### 所以 `#154` 那類漏選目前沒有解

四條路都量過並否決：擴充表註解（有容量上限，§8.7）、欄位進檢索文件
（+2 找到 / +13 誤選，§8.3）、欄位進候選目錄（本節）、概念短語列（本節，
最接近但仍 −3.6pp）。剩下的方向是把概念群做成**獨立的可檢索單位**，
並且**在短語裡保留限定詞**（「每日結算的累計消費金額（非即時）」而不是
「累計消費金額」）—— 後者是這一輪唯一還沒試過、而且有機制上的理由該有效的做法。
那是結構改動，要另外設計與量測。

#### 實驗設計的教訓：串跑會被限流污染

第一次做確認輪時把多個變體串在同一支腳本裡連續打 API，跑在後面的變體吃到
累積的 429，**122/153 題三次重試全部失敗、整題退回純相似度 Top-4** ——
量到的是降級路徑的分數（86.5%），不是那個變體的分數（91.0%）。
**跨變體比較必須記錄 LLM 退回次數**，否則會把基礎設施的狀態讀成演算法的優劣。
`cq2.py` 之後一律 `max_workers=1` 並印出退回計數。

### 8.10 「概念短語列也放進 Embedding」——外部建議，實測否決（但有觸發條件）

提案（來自另一個模型的架構分析）：把含限定詞的概念短語列**同時**放進檢索向量與
LLM 候選目錄，讓「向量負責網羅（High Recall）、LLM 負責避險（High Precision）」。

155 題純嵌入測量（確定性，無跑次變異）：

| 檢索文件 | 中位字元 | Top-1 | 候選@12 | @14 | @16 | 寬表誤選 |
|---|---|---|---|---|---|---|
| **只有表註解（現行）** | 64 | **83.9%** | 96.1% | 98.7% | 100% | **10/139** |
| ＋概念短語列 | 106 | 81.9% | **98.1%** | 99.4% | 100% | 20/139 |
| ＋概念短語列**含限定詞** | 115 | 80.6% | 98.1% | 99.4% | 100% | 22/139 |

**① 「嵌入讀不懂限定詞」——正確，而且比預期更糟。**
加上「（非即時，落後 30 天）」「（非商品現行售價）」之後誤選 20 → **22**、
Top-1 再掉 1.3pp。**限定詞在向量空間裡是加強誘餌，不是加警告牌** ——
模型只看到我又多塞了幾個「消費金額」token。
（這也是原提案的內部矛盾：它一邊論證嵌入看不懂限定詞，一邊建議把限定詞放進嵌入。）

**② 「嵌入盲區導致第一關就被淘汰」——前提在這個系統不成立，兩個獨立理由。**

`customer_profiles` 的相似度排名（22 張表中第幾名）：

| 題 | 只有表註解 | ＋短語列 |
|---|---|---|
| `#154` 還沒通過 Email 驗證 | **2** | 1 |
| `#149` 會員系統註冊時間 | **1** | 1 |

`#154` 用只有表註解就排第 2 —— **它是在 LLM 那一關被漏掉的，不是在檢索這一關。**
其次 `CANDIDATE_N = 40` > 22 張表，**第一段從來沒有淘汰過任何一張表**（§7.2），
候選召回實際是 100%。所以 96.1% → 98.1% 這 +2pp 現在的價值精確地等於零，
而代價是實的：Top-1 −2pp、寬表誤選翻倍。

**③ 「向量網羅 / LLM 避險」的分工正確，但那是現況不是提案**（§2.5、§2.6）。
具體做法兩層都測過，兩層都是負的：LLM 目錄 −3.6pp（§8.9）、嵌入如上。

#### 它不是錯，是早了 —— 觸發條件

**表數超過 `CANDIDATE_N`（40）之後**，第一段才真的開始淘汰，候選召回從死碼變成
**整個漏斗的天花板**（§7.2）。同時 ∪Top-1 的價值照 −0.52pp/表 衰減（§7.4），
100 張表時只剩約 47%。**「+2pp 候選召回換 −3pp Top-1」這筆交易的兩邊會同時翻向另一邊。**
所以這個方案記成**帶觸發條件的待採納**，不是否決。

---

## 9. 評估框架的四個洞（加寬表這一輪暴露出來的，已補上工具）

§5 說的是評估**方法**（GT 先修正、單輪分不出差異、檢索要獨立量）。
這一節記的是評估**框架**本身量不到的東西 —— 都是這一輪才浮出來的。
四支工具都已實作，**加表的標準流程要跑前兩支**。

| 洞 | 工具 | 怎麼跑 |
|---|---|---|
| 防禦題被加表靜默作廢 | `tools/check_defence_gt.py` | **`eval_score.py` 自動跑**（動 schema 時也可單獨跑）|
| 端到端低報檢索失敗 | `eval_lucky.py` | **`eval_score.py` 自動跑**（`--columns` 時單獨跑）|
| 單輪計分掩蓋擲硬幣的題 | `eval_stability.py` | 手動 —— `eval_score.py` 會印出現成指令 |
| 欄位裁剪沒有守門員 | `eval_column_recall.py` | 手動 —— 動裁剪前，或 schema 變寬後 |

**標準流程**（加表或改檢索之後）：

```
python eval/test_runner.py                     # 跑完端到端，印出下一步指令
python eval/eval_score.py eval_result_X.json   # 對帳 + 防禦題稽核 + 僥倖偵測
python eval/eval_stability.py --ids <上一行印出的錯題>
```

前兩支合併是有理由的：`eval_score.py` 本來就要對每一題呼叫 `judge()`，
僥倖偵測搭在上面**不需要重跑任何 SQL**，是零額外成本。而防禦題稽核放在
**對帳之前**，因為它最便宜（不執行 SQL）而且它報警時後面的分數要重讀 ——
防禦題被作廢時，系統答對了卻被判錯。

`eval_stability.py` 刻意**不自動跑**：它會打幾百次 API，什麼時候值得付這個
成本是人的判斷，不該綁在每次對帳上。

### 9.1 防禦題會被加表靜默作廢 → `tools/check_defence_gt.py`

見 §8.8。`#69`／`#71` 過期時**系統答對了卻被判錯**，看起來像模型退步。

做法：防禦題在 GT 裡宣告 `absent:`，列出「這題成立的前提是庫裡沒有這些概念」
與比對用的 `probes`；程式掃 `INFORMATION_SCHEMA` 的欄位名與註解。
**裁決過的命中寫進 `acknowledged:` 就不再報警 —— 未裁決的命中才是警報**，
所以加表時只會看到新出現的東西，不會被既有的已知命中淹沒。

裁決判準是 §8.8 那條：**業務定義在不在，不是欄位在不在。**

它上線就抓到一筆:**`#141`「信用評分」的 note 已經過期** ——
原文寫「21 張表裡沒有任何評分、風險或額度欄位」，而寬表帶進了 `credit_limit`、
`risk_flag`、`churn_risk_score`、`ltv_score`。這題**依判準仍然維持防禦**
（在的是額度／流失風險／終身價值，沒有一個是信用評分，組合公式也未定義），
但理由整段重寫了。誘餌從「無中生有」變成「就在眼前」，這題反而變得更有價值。

### 9.2 端到端會低報檢索與生成失敗 → `eval_lucky.py`

見 §6.1。用 `eval_schema_need.required_schema()` 解模型產出的 SQL，
和 GT（含所有 `alt_sql`）的需求比對：**答案對、schema 不對 → 標成僥倖候選**。
這是旗標不是判決 —— 同一題本來就有多種正確寫法，所以只要涵蓋任一條 alt_sql 就放行。

在最近一輪結果上跑，兩件事同時被抓出來：

```
檢索漏給（與答案對錯無關）  #62 少 products、#63 少 customers、
                            #107 少 orders、#159 少 customers
僥倖候選（答對但 schema 不符）#62  GT 要 products，模型只用 order_items
                            #159 GT 要 customers，模型只查 customer_profiles
```

`#62` 正是 §6.1 記錄的那一題，現在是自動抓出來的。
`#159` 則是 §8.7 預測過會發生的事（「它預期空集合，端到端大概仍會判對」）——
**預測被工具驗證了**。它後來補了一條 `alt_sql`（只查 `customer_profiles`
也是正解，`customer_id` 足以指認客戶）而不再報警 —— **常態誤報會讓稽核工具
失去意義**，所以確認是正常寫法就要立刻補進 GT，不能放著。
剩下 `#62` 是唯一的真陽性。

### 9.3 單輪計分會掩蓋擲硬幣的題 → `eval_stability.py`

見 §5.2 與 `#56`（4/8，四次錯的答案完全相同，上一輪 144/145 時剛好擲中）。
把題目分成**穩定過／擲硬幣／穩定錯**三類，並把重複出現的同一種錯誤分組列出
（同一種錯反覆出現 = 穩定偏誤；每次都不同 = 取樣雜訊）。

不必每題跑 n 次 —— 預設只跑「上一輪錯的」加隨機抽樣 15 題（種子固定，跨輪可比）。

### 9.4 欄位裁剪沒有守門員 → `eval_column_recall.py`

`eval_schema_need.py` 產出的欄位級需求集是免費的（GT SQL 本來就要寫），
但過去只有做實驗時手動用。現在包成 §2.16 量出來的裁剪器形狀
（**鍵一律保留 + 非鍵 Top-K + 寬度閘門 W**）並掃 K×W 的組合，
**欄位召回不是 100% 的設定一律 exit 1**。

它一上線就修正了 §2.17 的一個數字：節省從「37~99 字元」變成 **382 字元/題**
（寬表被選中時裁掉省約 3,250 字元），同時 **K=4 的餘裕消失** ——
詳見 §2.17 ② 的補註。**兩個方向相反，但結論不變：省得多了，卻更校準不了。**


---

## 10. 「干擾表」這個詞是錯的

### 10.1 干擾是「表 × 題目集」的關係，不是表的屬性

補完 §7.10 的資料之後量到一組數字：

```
全庫 41 張表 | GT 有覆蓋 22 張 | 零覆蓋 19 張（46%）
```

那 19 張表在 155 題裡**一次都沒有當過正確答案**。當時的說法是「覆蓋不足，之後
再補」，但這個描述低估了問題 —— 它不只是缺題，它讓評估本身出現捷徑：

> 一個學會「這 19 張永遠別選」的檢索器，在當時的測試集上會拿滿分。

現實中沒有這種捷徑。看一眼那份清單就清楚了：`invoices` 是財務每個月都要對的
帳，`support_tickets` 是客服主管的日報，`order_returns` 是營運看的退貨率，
`supplier_contracts` 是採購的到期提醒。**沒有一張是「沒用的表」。**
是題目集把它們釘死在干擾的角色上。

同一張 `payment_attempts`：

| 問句 | 它的角色 |
|---|---|
| 上個月成功付款金額多少 | 干擾（正解是 `payments`） |
| 哪些訂單刷卡失敗過 | **唯一正解，`payments` 反而變成干擾** |

所以「干擾表」不是一個可以掛在表上的標籤。**它是一個二元關係。**

### 10.2 兩種表都要，但比例不能是 46%

真實資料庫裡兩種表都存在，而它們測的是不同的東西：

| 類型 | 例子 | 測什麼 |
|---|---|---|
| 業務表（會被問到） | `invoices`、`support_tickets` | 在 N 張表裡撈出一張**長得跟鄰居很像的正解表** |
| 基礎設施表（永不作答） | `schema_migrations`、`sessions`、ETL 暫存表 | 稀釋律下，**永不相關的表會不會把正解擠掉** |

真實企業大致是 8:2。我們當時是 **54:46** —— 這個比例在現實裡不存在，而且它讓
評估**偏易**。修正方向不是「刪掉干擾表」，是**把錯誤的標籤拿掉**。

### 10.3 做法：`#160-171`，12 題、四張表、零 schema 變更

只新增題目，不動 `init_db*.py`、不重灌資料，所以不作廢任何既有 GT
（§5.1 的約束仍然成立）。四張表選最像業務主題的：`invoices`、
`support_tickets`、`order_returns`、`payment_attempts`。

零覆蓋 **19 → 15**，覆蓋 22 → 26。

**寫 GT 時實測撞到三件事，都是「先驗證 GT 再測試」擋下來的：**

1. **兩個極值題整組報廢。** `support_tickets` 36 張單分屬 36 位不同客戶，
   「工單最多的客戶」全部並列 1；`order_returns` 退回數量前五名全部並列 3。
   兩題的答案都會是任意平手，改成計數與清單題。
2. **同名不同人。** `#169` 原本要列客戶姓名，實測 37 個 `customer_id` 只對應
   33 個相異姓名 —— 33 列與 37 列兩種答案都說得通。改問「有多少位」。
3. **沒有歧義誘餌，但不是運氣。** `payments.status` 全庫只有 `SUCCESS` 一種值，
   失敗只存在於 `payment_attempts.result`，所以「刷卡失敗」指向唯一一張表。
   這是 §7.10 灌資料時就宣告好的值域在收利息。

### 10.4 順帶暴露 `check_ambiguity.py` 的一個前提失效

補題之後重跑歧義稽核，12 個 HIGH/MED 命中裡有 **10 個是自我比對** ——
`support_tickets` 被判成「`status` 在 `support_tickets` 裡全部都有」的高風險。

原因就是 §10.1：那支工具把「干擾表」當成**表的屬性**，隱含假設
「干擾表永遠不是答案」。`distractor_tables()` 用 DDL 差集算出來的集合，
在補題之前剛好等於「永不作答」，補題把這個巧合打掉了。

修法一行 —— `if d in need_tables: continue`。**命中 12 → 2。**
剩下兩個是真的跨表命中，已裁決：

- `#167` vs `warehouse_transfers.quantity`：調撥是內部搬運不是客戶回流，
  且無 `order_id`，與 `#2/#3/#16` 同一條理由。
- `#170` vs `delivery_attempts.result`：**這一對刻意保留。** 兩張表欄位名與
  值域形狀幾乎孿生，只靠「付款 / 配送」一個動詞區分 —— 檢索層要是答錯，
  錯的是相似度而不是題目，那正是想量到的東西。

### 10.5 擴表的前置條件（為什麼不是先加表）

漏斗目前的實況：

```
全庫 41 張表 | CANDIDATE_N = 40 | 每題實際砍掉 1 張（涵蓋 98%）
```

相似度那一層等於沒有作用，**§7 量到的所有收穫都不是在「表很多」的條件下測到的**。
從這個角度加表是唯一能讓漏斗接受測試的方法 —— 但**先補題再擴表**，理由是：

> 如果現在加 60 張，新的那些同樣會是純干擾，只是把噪音加大，
> 沒有測到任何新東西。先把噪音變成訊號，再放大。

擴表時的配題規則：**每加 10 張，8 張要有題目**（§10.2 的 8:2）。
刻意留作永久干擾的表要在 `tools/distractor_seed_plan.yaml` 註明
「此表設計為永不作答，用於測稀釋律」，免得未來把它當成覆蓋缺口又補一次。

### 10.6 補完題目才發現：那 19 張表**從來沒有進過 DDL**

`#160-171` 第一次跑（n=3）的結果：

```
穩定過 3 | 擲硬幣 0 | 穩定錯 9
```

六題判成「誤觸防禦暗號」，三題從錯的表硬答（`#162` 回 37 而正解 33、
`#166` 回 2 而正解 7、`#170` 回 `[SUCCESS, 164]` —— 那是 `payments`）。

看起來像檢索失敗。**不是。**

```
retrieved_tables : ['invoices']      ← 檢索完全正確
schema_ddl       : 22 張表，不含 invoices
```

`get_ddl_for(['invoices'])` 一張都沒配到 → 觸發它的 fallback「找不到就退回完整
DDL」→ 模型看到 22 張表沒有一張跟發票有關 → **正確地判斷 schema 不支援**。

模型沒有做錯任何事。**六個「誤觸防禦」其實是六個誠實的拒答。**

#### 這是 §7 天花板效應的第三個位置

前兩個是 `CANDIDATE_N`（相似度層砍掉的表後面救不回來）與 LLM 選表。
第三個是 **DDL 本身就是一個沒有人在看的漏斗段**：

| 漏斗段 | 有沒有守門員 |
|---|---|
| 相似度 → 候選 | 有（`_warn_if_candidate_n_binds`、`eval_retrieval.py --funnel`） |
| 候選 → LLM 選表 | 有（`eval_retrieval.py`、`eval_filter_vote.py`） |
| 選到的表 → **DDL** | **沒有** ← 這裡 |

而且它的失敗**偽裝成別的東西**：不是報「找不到表」，是報「schema 不支援」，
在對帳表上長得跟一個誠實的防禦題一模一樣。

#### 死因：新增了 schema 來源，卻沒有接到既有的產生鏈上

`tools/gen_ddl.py` 本來就會輸出**全庫所有表**
（`ordered += [t for t in sorted(by_table) if t not in TABLE_ORDER]`）。
它只是在那 19 張表建立之後**從來沒有重跑過**。

`schema_registry._report_drift()` 一直在警告「DB 有而 YAML 沒有的表」——
那個警告被當成雜訊忽略了整整一輪。**只警告不阻斷的檢查，等於沒有檢查，
除非有人會讀。**

修法：`python tools/gen_ddl.py --write`。DDL **22 → 41 張表**
（13,237 → 19,940 字元）。修完之後 `invoices` 問句拿到 993 字元、
只含 `invoices` 的精準 DDL —— 一般題的 prompt 反而沒有變長。

```
修正前  穩定過  3 | 擲硬幣 0 | 穩定錯 9
修正後  穩定過 11 | 擲硬幣 1 | 穩定錯 0
```

#### 同一個 bug 的第二個實例

`tools/sync_table_comments.py` 的 `SOURCES` 只有 `init_db.py` 與
`init_db_ext.py`，**沒有 `tools/add_distractor_tables.py`** —— 那 19 張表的
註解同樣沒有任何同步路徑。已補進來，並順便加上**欄位註解同步**
（原本只同步表註解）。

欄位的 `ALTER` 走 `MODIFY COLUMN`，型別 / NULL / 預設值 / AUTO_INCREMENT
**全部從 `INFORMATION_SCHEMA` 現況重建**，只有註解那一段來自宣告 ——
不解析 init 腳本的型別文字，就不可能因為解析錯而靜默改到欄位語意。

#### 順帶作廢的一個定義

`check_ambiguity.py` 的 `distractor_tables()` 原本是「DB 有、DDL 沒有」的差集。
DDL 補完之後那個集合變成**空的**，稽核會靜默地什麼都不檢查。
改用 **GT 覆蓋**定義（15 張），同時對上 §10.1 —— 補題會自動把一張表移出集合，
不需要人維護清單。

#### `#169` 剩下的那一次：定義沒有寫在定義該在的地方

`#169` 2/3，錯的那次回 **29**，正解 37。29 是
`COUNT(DISTINCT customer) WHERE result='DECLINED'` —— **模型漏掉了 TIMEOUT。**

不是模型的錯。`payment_attempts.result` 的註解只寫值域
`(OK/DECLINED/TIMEOUT)`，**沒有說哪些算失敗**。改成：

    嘗試結果：OK 成功；DECLINED 銀行拒絕、TIMEOUT 逾時未回應，這兩者都算失敗

這不是新增業務規則（那條禁令仍然有效），是把**枚舉值的語意寫回欄位註解**——
與 §7.13 修好 `#104` 的是同一個機制，差別只在那次改的是表註解。

#### 收尾：`#169` 修完的量測，以及它的回歸風險

```
#169  8/8   有多少位客戶的訂單曾經刷卡失敗過？   （改註解前 2/3，錯的那次回 29）
#170  8/8   付款嘗試的結果分別各有幾次？
#171  8/8   哪些訂單嘗試付款了 3 次？
#104  8/8   列出所有付款失敗的紀錄。            ← 回歸檢查
```

`#104` 是這次唯一真正有風險的一題：它的正解是 `payments.status='FAILED'`，
而我剛剛把 `payment_attempts.result` 的註解寫成「這兩者都算失敗」——
§8.2 的稀釋鏡像說，把一張表的文件寫胖會讓它到處排第一。
**`#104` 維持 8/8，沒有被搶走。** 這是加註解時必須一起量的東西，
不是加完就算數。

#### 現在的狀態與下一步

```
GT 171 題（159 + 12）｜全庫 41 張表｜GT 覆蓋 26 張｜零覆蓋 15 張
DDL 41 張表 / 19,960 字元｜business_rules 10 條｜誘餌 0、盲區 9
單元測試 9 / 15 / 17 全過；防禦題、歧義、註解同步三項稽核全過
```

**基準線已經作廢，需要重跑完整 e2e** —— 題目從 159 變成 171，
舊的「154/159、每輪錯 5 題」不能直接跟新數字比。重跑時照 §9.3 的格式報
（穩定通過 N｜硬幣池 M｜穩定錯 K），不要報單輪百分比。

剩下 15 張零覆蓋的表要照 §10.2 分類：會被問到的補題，
真正的基礎設施表在 `distractor_seed_plan.yaml` 註明「設計為永不作答」。

### 10.7 缺 DDL 是一張**意外的安全網**——拆掉它之前先量了什麼

`#160-171` 補完之後要回答的問題是反過來的：**補 DDL 會不會弄壞舊的題？**

先做零成本的離線分析 —— 三輪 e2e 都記了 `retrieved_tables`：

```
三輪聯集：檢索選到「當時沒有 DDL 的表」的題目 = 22 題
產出 SQL 真的引用那些表（已排除 CTE 別名）  =  0 題
觸發完整 DDL fallback                      =  0 題
```

三個數字合起來說明一件不明顯的事：

那 22 題**從來沒有觸發 fallback**，因為它們同時也選到了核心表。
`get_ddl_for()` 於是回傳**部分 DDL** —— 選了 4 張表、只給 3 張的欄位，
而且沒有任何警告。模型不知道自己少拿了一張。

**然後 0 題真的用了那些表。** 模型看得到全部 41 個表名
（`_other_tables_line` 一直是向 MySQL 取的），卻一次都沒去用 ——
**因為沒有欄位就寫不出 SQL。**

> 缺 DDL 在這 22 題上是一張**意外的安全網**。它的影響不是「沒有影響」，
> 是**擋住了選錯表的後果**。補 DDL 等於把網子拆掉。

#### 拆網之後的實測

| 干擾表 | 題目 | 為什麼危險 |
|---|---|---|
| `product_stock_snapshots`（280 列） | 14 17 27 31 73 77 90 **140** | 拿每日快照取代 `products.stock` |
| `order_cancellations`（17 列） | 10 22 23 50 54 | 取消紀錄只有 17 筆，權威在 `orders.status` |
| `order_status_history`（448 列） | 11 20 | 拿歷程推「現在」的狀態 |
| `customer_contacts`（101 列） | 61 **76** | `#76` 是 `expect: empty` |
| `cart_recovery_emails`（**0 列**） | 131 132 | 用了就回 0 筆 |
| `product_bundles`（**0 列**） | 81 | 同上 |

最危險的是 `#140`：那是**庫存週轉率防禦題**，而一張逐日庫存快照表
正好就是捏造週轉率所需要的東西。缺 DDL 一直在替那道防禦擋子彈。

```
修正前（三輪 e2e 離線重判）  22 題全對 20 題；#17 #140 各錯一次
修正後（n=3 探針）           穩定過 20｜擲硬幣 1（#20）｜穩定錯 0
```

**零回歸**：沒有任何一題從「三輪全對」掉下來。`#17`（AST `p.*`）與
`#140`（週轉率規則）反而都變成 3/3。`#20` 單獨再跑 6 次是 **6/6，
而且 SQL 每次都只用 `customers` + `orders`，一次都沒碰
`order_status_history`** —— 它錯的那次是全稱量詞漏了「至少要有一張訂單」，
與干擾表無關。

沒有翻車最可能的原因是**那些干擾表的表註解寫得夠有防禦性**：
「即時庫存在 products.stock」「訂單是否已取消請看 orders.status」。
那句話在**選表**那一層就發揮了作用。

#### 但那句話生成端從來沒看過

`gen_ddl.py` 原本**不輸出表級 COMMENT**。防禦性註解只存在於檢索目錄
（選表 LLM 看得到），生成 LLM 只看得到欄位。干擾表補進 DDL 之後，
生成端第一次看得到 `stock_qty` 這種欄位，**誘惑到位而警告沒到** ——
等於只剩一道防線。

已補上（DDL 19,960 → 22,322 字元，每張表 +58；實際進 prompt 的只有 scoped
的 1–3 張，約 +120 字元）。**不影響 embedding** —— §2.10 的稀釋律管的是
檢索文件，不是 DDL。

**改的時候撞到一個靜默的 bug：** `schema_parser.get_table_columns()` 的
regex 是 `\);`，加了 `) COMMENT '...';` 之後**一個區塊都對不上，直接回空
dict**。它是 `schema_registry` 連不上 MySQL 時的降級來源，也是
`_report_drift` 的比對基準 —— **壞了不會有人喊**，跟 §10.6 是同一個病。
已修（41 張表 / 299 欄位，與 MySQL 完全一致，漂移警告歸零）。

#### 表註解進 DDL 的效果：**沒有量到**

```
加註解前  穩定過 20｜擲硬幣 1｜穩定錯 0
加註解後  穩定過 20｜擲硬幣 2｜穩定錯 0
```

新出現的 `#81` 硬幣是「最常一起購買」回了 2 列而正解 1 列 —— top-N 的
`LIMIT` 問題，用的是 `order_items` 不是 `product_bundles`，與註解無關。

**所以這個改動不能宣稱有效。** 它與 §6.9 的週轉率規則是同一種決策：
**代價可測為零、收益在可負擔的 n 下不可測**，採用的理由是代價為零加上它
消掉一個結構性的不對稱（同一句警告，選表看得到、生成看不到）。
要證明它有效需要一個**專門把生成端逼到誘惑面前**的實驗 —— 記在這裡當觸發條件：
哪天出現「檢索選對表、生成卻用了鄰居表」的實例，那就是驗收這條的機會。

#### `#81` 待觀察

修正前三輪 OOO，現在 2/3。n=3 分不出 2/3 與 3/3，**不足以判定回歸**。
觸發條件：下一輪完整 e2e 若 `#81` 再錯，用 n=8 查它是不是 `LIMIT` 的問題。

### 10.8 第四輪完整 e2e（171 題）—— 新基準

```
正確 168 / 171   完成率 100%（零 crash、零 error_end）
防禦題 4/4 全部正確拒答     ← #68、#141 沒出現在漏接清單，維持不加規則
新增的 12 題全部通過        ← 探針結論在完整 e2e 下站得住
```

錯 3 題（`#71` 是 GT 缺陷，已補 alt_sql 後通過）：`#84`、`#93`、`#138`。
n=8 探針：

```
穩定過 1（#81 8/8，§10.7 的待觀察項解除）
擲硬幣 4：#71 7/8 | #84 6/8 | #93 5/8 | #138 4/8
穩定錯 0
```

**要更正一個先前的判定：`#93`、`#138` 不是「慢性穩定錯」，是硬幣。**

但更值得看的是這個矛盾：它們在 e2e **已經連錯四輪**。若探針的機率是真的，
`#93` 連錯四次的機率是 0.375⁴ ≈ 2%，`#138` 是 0.5⁴ ≈ 6%。
§5.2 記的「探針條件與完整 e2e 不同」那個未解落差，這輪拿到了強得多的證據：
**連續跑 171 題的條件確實更難，跨條件比較必須明講。**

#### `#71` 是 GT 的缺陷，不是模型的

題目問「性別比例是多少」，系統回 `[F 36.0, M 38.0, OTHER 26.0]` —— 那是
正確答案。GT 的主 SQL 多要了一個 `COUNT(*)`，而比對規則允許系統**多**帶欄位、
不允許**少**帶，於是一個正確答案被判錯。補 `alt_sql`（兩種讀法都對，
所以不改主 SQL）。

這一題另外有一個真的錯法：分母寫成群組數而非總人數，算出 633% / 600% / 433%。
n=8 出現 1 次 —— **同一題有兩個失敗模式，一個是 GT 的、一個是模型的**，
不拆開看就會把它當成單一硬幣。

#### `#84` 與 `#138`：粒度錯位，而且這是第二個實例

```sql
-- #138 系統產出（48 列，正解 34）
SELECT DISTINCT c.id, c.name, c.city, a.city AS address_city
--                                    ^^^^^^ 多的那一邊的欄位
FROM customers c JOIN addresses a ON a.customer_id = c.id WHERE a.city <> c.city
```

模型**有寫 DISTINCT**，但把 `a.city` 放進投影 —— DISTINCT 於是對
(客戶, 地址城市) 去重，一位客戶有兩個不同的不符城市就會出現兩次。

`#84` 是同一件事的另一個形狀：JOIN 一個「一位客戶一天一列」的子查詢，
沒有收斂回客戶，`楊宗憲` 出現兩次。

共同根因：**問句要的粒度是客戶，SQL 的粒度是多的那一邊；
而把多邊欄位放進投影會讓 DISTINCT 失效。**

`#138` 的 note 記著這個機制，觸發條件寫的是「出現第二個實例再處理」——
現在出現了。但同一份 note 也記著：之前加過一個去重 few-shot
（「哪些客戶買過機械鍵盤」）**實測 1/6，沒有遷移**。
那條教的是「DISTINCT 有效」的簡單情形，**完全沒有碰到失效的情形**。

所以重做的關鍵是把**失效的形狀**寫進 few-shot 的 `reasoning`，
而不是再示範一次成功的去重。這是一次**對已知失敗方法的重試**，
只改了一個變數 —— 驗收條件要先講好：`#84` 與 `#138` 各 n=8，
沒有同時上升就承認結構級教訓不遷移，停止再試。

### 10.9 第四條 few-shot：`#84` 好了，`#138` 沒有 —— 依停損條件收手

驗收前先講好的條件是「兩題沒有同時上升就停止」。結果：

```
          加 few-shot 前   加 few-shot 後
#84            6/8             8/8
#138           4/8             5/8      ← n=8 分不出 4/8 與 5/8
無回歸對照   #8 8/8｜#9 8/8｜#149 8/8｜#20 6/8（既有硬幣，非回歸）
```

**`#138` 沒有動 ⇒ 依條件收手。** 保留這條 few-shot（沒有造成任何回歸，
而且 `#84` 現在 8/8），但**不再為 `#138` 寫第五條**。

這一族現在有四條，是一條完整的失敗曲線：

| | 教什麼 | `#138` 的反應 |
|---|---|---|
| `[8]` 機械鍵盤 | JOIN 明細表後收斂 | 1/6 |
| `[9]` 訂單被取消過 | 三種寫法三個答案 | 無改善 |
| `[12]` 訂單早於註冊 | 子表欄位 vs 母表欄位 | 無改善 |
| `[21]` 付款成功過 | **DISTINCT 被顯示欄位破壞**（164 / 112 / 49） | 4/8 → 5/8 |

#### 為什麼 `#138` 特別頑固：問句自己在邀請那個欄位

`#138` 每一次失敗的輸出**完全相同** —— 48 列，SQL 一定帶 `a.city`。
不是隨機漂移，是穩定的選擇。

原因在問句本身：「收件地址城市**和**自己居住的城市不同」**點名了兩個城市**。
模型把它們顯示出來是在回應問句，不是在忽略指示。前四條 few-shot 教的都是
「不要顯示多邊欄位」，但這一題的問句讀起來像是**要求**顯示。

> 這不是知識缺口，是**問句語意與答案粒度衝突**。
> few-shot 補得了知識缺口，補不了這個。

`#138` 因此**永久凍結為已知缺陷**（第三次凍結，這次附上機制與收手理由）。
解凍條件不再是「再寫一條 few-shot」，而是出現**第三個實例、且該實例的問句
沒有點名多邊欄位** —— 那才代表這是一個可教的通則，而不是這一題的特性。

### 10.10 補完第二批：`#172-195`，表覆蓋 22/41 → 38/41

12 張業務表、每張 2 題。零覆蓋剩 3 張，而且**那 3 張不是缺口是測試組**
（`customer_login_logs`、`product_bundles`、`cart_recovery_emails`，
在 `distractor_seed_plan.yaml` 標了 `never_answered: true`）。

**先驗證 GT 再測試又擋掉兩個平手題**：「圖片最多的商品」五個並列 4 張、
「狀態異動最多的訂單」大量並列 3 次，都改成清單或計數。

刻意設計的對照組：

| 題 | 對照 | 測什麼 |
|---|---|---|
| `#174` **某一天**庫存最少 vs `#31` 庫存最少 | 快照表 vs `products.stock` | 差一個時間詞就得換表 |
| `#184` 調**出**最多 | `from_warehouse_id` | 方向反了答案就變 |
| `#188` 還沒被回覆的評價 | 主體是 `reviews` | 反向查詢，子表只用來排除 |

探針 n=3：**穩定過 22｜擲硬幣 0｜穩定錯 2**。

#### 兩題穩定錯，錯的又是 GT

```
#188  系統 97   / 我的 GT 101
#189  系統 22.4 / 我的 GT 24.63
```

模型的 SQL 帶了 `r.is_deleted = 0`。`reviews` 有軟刪除（134 則中 9 則已刪），
而既有的 `#142-145` 四題**全都**帶 `is_deleted = 0`，`semantic_layer` 也有
few-shot 在教。**是 GT 沒跟上專案自己的慣例。**

這是這一輪第三次「錯的是 GT 不是模型」（前兩次：`#71` 多要了一個 COUNT、
`#160-171` 那批的平手題）。三次的共同點：**都是在新寫的題目上**。
既有題目經過多輪 e2e 洗過，新題沒有 —— 所以新題的第一次探針，
要把「模型錯」與「GT 錯」當成同等可能的假設，而不是預設模型錯。

### 10.11 空表不真實：`product_bundles` 與 `cart_recovery_emails` 灌資料

全庫盤點只有兩張真的空表，另外 7 張少於 10 列的是**維度表**
（4 個倉庫、5 種付款方式、8 家供應商）—— 真實企業就是這個量級，不是缺資料。

那兩張空表原本是刻意的，用來測「熱度／列數先驗會不會誤殺零列的新表」。
**但企業不會建一張表然後永遠是空的** —— 這個設定本身不真實，
而它要測的先驗方法（§7.11 的 A 方案）早就實測否決了。

改成**灌資料但不補題**：它們仍然是 `never_answered`，角色從「靠沒有資料
所以不被選」變成「**有資料、內容合理、卻沒人問**」——
那才是真正的稀釋律測試，也更難。

#### 灌之前先擋掉一個會靜默毀掉 36 題 GT 的坑

生成迴圈是 `for t, spec in plan.items()`，走的是**計畫檔的順序**。
把新表插在中間，它抽走的亂數會讓後面每一張表的資料全部改變 ——
`#160-195` 那 36 題的答案全部作廢。

這與 `init_db_ext.py`「新的 random 只能加在最後」是同一條約束，
但更嚴格：**順序由 YAML 決定，不由程式碼決定，所以「加在最後」不夠。**
解法是獨立亂數源 `RNG_LATE = random.Random(20260820)`，主序列一個數字都不動。

再加一個 `--only=t1,t2`，只重灌指定的表。灌完用**逐表 SHA 指紋**對照：

```
order_status_history 448 / payment_attempts 239 / invoices 164
product_stock_snapshots 280 / review_replies 33
→ 既有 17 張表一個位元都沒動 ✅
```

灌入 `product_bundles` 22 列（6 個組合包）、`cart_recovery_emails` 39 列
（`clicked=1` 蘊含 `opened=1`，矛盾 0 筆）。五條準則全過，全庫**空表 0 張**。

#### 順帶抓到自己剛寫進去的一個靜默 bug

`ambiguity_verdicts.yaml` 是以題號為鍵的 YAML。我把 12 筆新裁決直接 append，
其中 **10 個題號早就存在** —— `yaml.safe_load` 只留最後一個，
於是模式一的舊裁決被無聲蓋掉。當下沒有報錯，因為那些表已經不在干擾集合裡了。

已改成合併進原條目（26 條 → 16 條，重複 0）。教訓與 §10.6 同一類：
**以鍵為單位的設定檔，append 是一個會沉默覆寫的動作。**

---

## 11. 規模：把漏斗推到真的會作用的地方

### 11.0 第五輪 e2e（195 題）—— 擴表前的基準

```
完成率 100%（零 crash、零 error_end）
正確 191 / 195 = 98.0%
防禦題 4/4        ← #68、#141 仍未漏接，維持不加規則
錯 4 題：#34、#89、#93、#119
```

兩件值得單獨記的事：

**`#138` 通過了。** 它前四輪 e2e **連錯四次**，這輪過了；`#84` 也過了。
§10.9 那條粒度 few-shot 在 e2e 條件下站得住 —— 這比探針的 8/8 更有說服力，
因為 e2e 是更難的條件（§10.8）。

**新加的 36 題（`#160-195`）全部通過。** 探針結論在完整 e2e 下沒有縮水。

對帳的僥倖偵測標了 `#172`/`#173`：模型走 `order_cancellations`，
GT 走 `order_status_history`。**模型是對的** —— 我自己在 `#172` 的 note 裡就寫過
「本資料集沒有取消後又復原的訂單，兩條路同值」。既然知道兩條都對，
GT 就該接受兩條，否則是在考「猜我心裡想的那張表」。已補 `alt_sql` 並驗證同值。

### 11.1 `tools/check_schema_pipeline.py`：把三次同型 bug 變成一個閘門

2026-08 這一輪連續被同一類 bug 咬三次，**三次都不報錯**：

| | 症狀 |
|---|---|
| `gen_ddl.py` 沒重跑 | 檢索選對表、生成端看不到欄位 → 誠實回「schema 不支援」，在對帳表上跟防禦題**長得一模一樣** |
| `sync_table_comments.py` 漏了一個來源 | 改了註解不生效 |
| `schema_parser` 的 `\);` regex | 表級 COMMENT 進 DDL 後直接回空 dict |

共同形狀：**新增了一個 schema 來源，卻沒接到既有的產生鏈上。**
`_report_drift()` 其實整輪都在警告 —— 但它只 warning 不阻斷。

> **只警告不阻斷的檢查，等於沒有檢查，除非有人會讀。**

所以這支預設 `exit 1`。七項：DDL 覆蓋、DDL 新鮮度、註解同步、regex 可解、
KMB 成本、覆蓋與宣告、沒有空表。加表之前跑它，紅了就不要往下走。

### 11.2 `_TABLE_COST` 的守門員：把「漏掉的 1」與「決定過的 1」分開

`table_cost()` 對沒列到的表回傳預設值 1 —— **新表因此永遠不會報錯**。
加一張「瀏覽 / 收藏 / 加購物車」型別的表卻忘了調成本，KMB 會靜默繞錯路，
症狀是「拓撲對、語意錯」。這與 §11.1 那三個是同一類病。

修法是把**預設值也明列出來**：`_DEFAULT_COST_REVIEWED`。
`_TABLE_COST ∪ _DEFAULT_COST_REVIEWED` 必須等於全庫表名，由 §11.1 的第 5 項強制。
加表就一定要來做一次決定 —— 決定「1」也可以，但必須是**決定過的 1**。

守門員不是裝飾：`unreviewed_tables(全庫 ∪ {'wishlists'})` 回傳 `['wishlists']`。
而第一波擴表確實加了 `wishlists` / `wishlist_items` / `product_qna` 三張
「收藏／提問 ≠ 購買」的表 —— **正是這個守門員存在的理由**，成本都設 3。

### 11.3 第一波 41 → 60：15 張業務表 + 4 張基礎設施表

與 §7.10 那 19 張干擾表的差別，是這一輪學到的東西全部反映在做法上：

| | 2026-08 干擾表（§7.10） | 這一波（§11.3） |
|---|---|---|
| 資料 | 零列 | 有資料 |
| GT | 零題 | 15 張各 2 題 |
| 角色 | 永遠不是答案 | 有時是答案、有時是干擾 |
| 連通性 | 貼著既有表做 | **每張都有 FK 連回骨幹，沒有孤島** |

連通性是刻意的：`employees` 透過 `order_assignments` 接上訂單、`stores` 透過
`store_pickups` 接上 —— 這樣「哪個員工處理的訂單最多」才有路可走，KMB 才有
東西可測。孤島對 KMB 沒有意義，也不像真實 schema。

**兩張表被防禦題擋掉了，不是忘了加：**

```
store_inventory  → 欄位名會命中 #140 的 inventory / stock 探針
shipping_rates   → 直接命中 #68 的運費
```

`wishlists` / `wishlist_items` / `product_qna` 成本設 3 —— 它們都把
`customers` 與 `products` 用 2 跳連起來，比購買路徑短。**§11.2 那個守門員
在加表的第一天就用上了。**

#### 閘門擋住了一次

建完表灌完資料後跑 §11.1，第 6 項紅了：15 張新業務表沒有 GT 覆蓋。
補完 30 題才轉綠 —— 否則就是重演 §10.1 那個「表永遠不是答案」的評估漏洞。

寫 GT 時實測擋掉三個平手題（在職員工最多的部門四方並列、使用最多次的券
兩方並列、被收藏最多的商品兩方並列）。

### 11.4 60 張表：第一次量到漏斗真正的價值

```
表數   候選涵蓋   漏斗召回   純相似度 K=3   差距
41      98%       99.3%       91.4%       7.9pp
60      67%       98.6%       85.5%      13.1pp
```

**41 張時 `CANDIDATE_N = 40` 每題只砍 1 張，第一段幾乎沒作用** ——
所以 §7 所有的收穫都不是在「表很多」的條件下測到的。
60 張時它砍掉三分之一，這是第一次能量到。

結果：漏斗只掉 0.7pp，純相似度崩了 5.9pp。**差距從 8pp 拉開到 13pp。**

### 11.5 第二波 60 → 80：候選只剩一半，漏斗仍是 98.0%

```
表數   候選涵蓋   漏斗召回   純相似度 K=3   差距   平均帶表   佔全庫   淨度
41      98%       99.3%       91.4%      7.9pp    2.1     5.1%   —
60      67%       98.6%       85.5%     13.1pp    2.0     3.3%  85.9%
80      50%       98.0%       86.6%     11.4pp    2.0     2.5%  85.4%
```

表數翻一倍、候選丟掉一半，**漏斗總共只掉 1.3pp**，而且平均帶進去的表
一直是 2.0 張 —— schema 變兩倍，送進 prompt 的內容沒有變多。

> 這證明了 §7 一直宣稱、但**在 41 張表上根本測不出來**的事：
> 三段漏斗的價值不在小 schema，在大 schema。

第二波的 16 張業務表刻意做成既有表的近義詞
（`search_logs` vs `browse_logs`、`warranty_claims` vs `order_returns`、
`product_recommendations` vs `order_items`…），32 題有一半是在考這個鑑別。

`#256` 是最深的一題：`warranty_claims` **沒有 customer_id**，
要問「哪些客戶既預約過服務又申請過保固」必須經由 `orders` ——
那正是 KMB 存在的理由（橋接表要靠外鍵結構找，不是靠語意）。

### 11.6 這一輪三個守門員各擋下了什麼（都不是裝飾）

| 守門員 | 實際擋下的事 |
|---|---|
| §11.1 第 6 項 | **兩次**：第一波 15 張、第二波 16 張新表沒有 GT 覆蓋 |
| §11.2 成本檢視 | 六張「行為 ≠ 購買」的新表被迫做決定，全部設成本 3 |
| 歧義稽核 | `job_runs.status` 三次命中（`#89`/`#111`/`#211`/`#251`） |
| GT 先驗證 | 七個平手題、一個「同名不同人」、一個我自己寫壞的 `ordered` |

還有一個是**同一個坑第二次踩但沒掉下去**：`ambiguity_verdicts.yaml` 以題號
為鍵，append 會沉默覆寫（§10.11）。這次因為加了重複鍵檢查，`#89` 當場被抓到。

### 11.7 80 張表的 e2e：九題錯，其中兩題是我加表弄壞的

```
完成率 100%（零 crash、零 error_end）｜正確 248/257 = 96.5%｜防禦題 4/4
錯 9 題：#34 #72 #89 #93 #121 #168 #207 #216 #248
```

歸因之後只有兩種東西是「真的」：

| 類別 | 題 | |
|---|---|---|
| **加表造成的真實歧義** | `#72` `#168` | 見下 |
| **GT 寫窄了** | `#207` `#121` | `#207` 問「消費金額」回 4900 比 -4900 自然；`#121` 問「哪一個」但資料是兩者並列 |
| **e2e 條件下的偶發** | `#216` `#248` | **n=8 都是 8/8** —— 我第一時間判成「模型真的錯」，探針否決了這個判斷 |
| **既有硬幣** | `#34` `#89` `#93` `#121` | |

#### `#72` 與 `#168`：模型比我更貼字面

```
#72「使用了折扣碼」  → 模型走 coupons（券碼）；GT 走 order_promotions（檔期）
#168「已經收到退貨」 → 模型走 return_shipments.received_at；GT 走 status='RECEIVED'
```

`return_shipments.received_at` 的欄位註解是**我自己寫的**「倉庫收到時間」。
加完表之後那題就有兩個都說得通的答案 —— 已改問句消歧
（「套用了**促銷活動**的折扣」／「退貨狀態是**已收到**」），改完 8/8。

#### 稽核為什麼沒抓到：我在 §10.6 埋的洞

v2 把干擾表定義成「所有 GT 都沒用到的表」。`coupons` 有自己的 GT（`#203`）、
`return_shipments` 也有（`#249`）—— **稽核直接跳過，一題都不比。**

這是 §10.1 那句話反過來咬自己：干擾是「表 × **題目**」的關係，
而 v2 實作成了「表 × **全部題目**」。

#### v3：逐題定義 + 連通性 + 兩欄門檻

改成逐題之後命中 **869 個 / 115 題**，完全不可用 —— 80 張表時
`status` / `name` / `quantity` 到處都是，「欄位可移植」退化成雜訊。

救援來自那 19 筆手工裁決：**理由幾乎逐字相同** ——
「`warehouse_transfers` 沒有 `order_id`，接不到訂單這個主體」。
同一條推理寫了 19 次，那就該寫進程式：

```
+ connected_to()   干擾表要在外鍵圖 1 跳內接得到本題正解表
                   （1 跳是下限：coupons 經由 coupon_redemptions 才碰到
                     orders，那正是 #72 的路徑；放寬到 2 跳全圖連通，失去意義）
+ 只共用一個欄位 → 降級 LOW
                   （每張實體表都有 name，那是正規化不是可替代性）

869 個 / 115 題  →  12 個 / 10 題
```

剩下的十題全部值得人看，包括**我自己在第二波造出來最尖銳的碰撞**：
`order_returns.status='REJECTED'` vs `warranty_claims.status='REJECTED'`，
值域字面完全相同，只能靠問句的「退貨」與「保固」分開。刻意保留。

#### 誠實的殘留：`#168` 那一類抓不到

`return_shipments` 用 `received_at` 不是 `status` —— **同概念、不同欄位名**，
這是本檔開頭就記載的盲點（`payments.status` vs `payment_attempts.result`
也是同一類）。連通性與兩欄門檻都幫不上忙，字串比對看不見它。
補法仍然是 §7.13 那條：**把正解表的註解寫到涵蓋該概念**，然後實測。

### 11.8 80 張表的新基準

```
穩定通過 248 題
硬幣池   4 題（#34 6/8、#89 6/8、#93 1/8、#121 5/8→已改問句待複驗）
穩定錯   0 題
防禦題   4/4
```

**不要報「248/257 = 96.5%」。** 那個數字掩蓋了「4 題在擲硬幣、其中 `#93`
只有 1/8」這個真正的狀態。`#93`（中位數）現在是全庫最差的一題，
而且它的錯誤值每次都一樣（45259 vs 65050）—— 是穩定的系統性偏誤，不是雜訊。

### 11.9 `#93`：整輪唯一的穩定系統性偏誤，追下去發現 few-shot 一直是半殘的

`#93`「客戶總消費金額的中位數大約是多少？」在 §11.8 的硬幣池裡是最差的一題 ——
探針 1/8，而且**五輪 e2e 錯的都是同一個值 45,259**。錯誤值不漂移，就不是雜訊。

**它不是算錯中位數。** 三輪的 SQL 都是正確的 `ROW_NUMBER()` 中位數寫法，
只是算在另一張表上：

| 來源 | 中位數 |
|---|---|
| `customer_profiles.total_spent` | 45,259 |
| `SUM(orders.total_amount) GROUP BY customer_id` | **65,050**（GT） |

`total_spent` 是 §8 加 `customer_profiles` 時**刻意埋的每日結算快照**，
50 位客戶有 27 位跟即時值對不上（`init_db_ext.py` 的 docstring 寫著這是
`#62` 的教訓：恆等的陷阱等於不存在）。欄位註解也寫得很白：

    '累計消費金額（每日結算快照，不含最近數日訂單，與即時 SUM(orders.total_amount) 可能不同）'

實測檢索層完全正常 —— 錨點 `['customers','orders','refunds','customer_profiles']`，
剪裁後 5 張表 7,706 字元，**上面那句註解確實在 Prompt 裡**。
模型看到了、然後 7/8 次選了快照。所以 `#93` 量到的不是檢索缺陷，是
**系統從來沒有教過「問句沒指定來源時，事實表才是權威來源」**。
沒教過的東西，模型每次都做同一個選擇 —— 這就是「穩定偏誤」的成因。

#### 追修法時撞到的東西比 `#93` 本身重要

打算照方針（§10.9「不要再用規則，從 few-shot 解決」）補一組 few-shot 時發現：

```python
def get_few_shot_text(self):
    for ex in self._data.get("few_shot_examples") or []:
        lines.append(f"  Question: {ex['question']}")
        ...
        lines.append(f"  SQL:
          {sql_block}")
```

**`reasoning:` 從來沒有進過 Prompt。** YAML 裡 22 則 reasoning、4,649 字元，
包括 §10.9 那篇 747 字元的粒度失配解說（DISTINCT 為什麼救不回來），
全部是死的。模型只拿到答案，得自己從一份 SQL 反推它示範的是哪一條教訓。

而同一個檔案往上 20 行，`get_rules_text()` **早就修過同一個 bug**，
註解還留著理由：

    # description 說明「為什麼」，是模型判斷規則適用與否的依據；
    # 過去只送 correct/wrong SQL，等於要模型從兩段程式碼反推語意。

修了 rules、漏了 few-shot。**這代表「以後都從 few-shot 解決」這條方針，
在結構上一直是做不到的** —— few-shot 只能傳形狀，傳不了教訓。
這解釋的範圍不只 `#93`：過去每一次「加了 few-shot 沒效果」的觀察，
都要重新看待，因為當時加進去的只有 SQL 形狀那一半。

同型 bug 的第四次（前三次見 §11.1）：**寫了、卻沒有接到下游。**
`check_schema_pipeline.py` 守的是 schema 鏈，守不到 prompt 鏈 ——
它的七項檢查沒有一項會問「YAML 裡的欄位有沒有真的進 Prompt」。

#### 改法

1. `schema_parser.get_few_shot_text()` 渲染 `reasoning`，並支援 few-shot 的
   `wrong_sql` 反例（`business_rules` 早就有，few-shot 沒有）。
2. `semantic_layer.yaml` 加**一組成對**的 few-shot，`business_rules` 維持 10 條不動。

成對是必要的，不是對稱美感：只教「一律走 orders」會把 `#146`
（題目明講「客戶檔案的消費快照」）弄壞 —— 那題的正解就是 `total_spent`。
教訓因此寫成謂詞級的：**看問句有沒有指定來源**，並明確劃出邊界 ——
`tier` / `ltv_score` / `risk_flag` / `gender` / `email_opt_in` / `birth_year`
這些寬表獨有、事實表算不出來的欄位不在射程內，不要一起避開。
（同 §"庫存週轉率"規則的邊界段：擋的是沒定義的比率，不是有資料的量。）

#### 量到的

```
#93   1/8 → 8/8   穩定過
#146  8/8 → 8/8   反面沒被弄壞
#147  7/8         擲硬幣（n=8 分不出 7/8 與 8/8，§5.2）
#148  8/8         穩定過
```

**代價**：few-shot 文字 5,443 → 11,887 字元，`#93` 的 generator prompt
約 21.5k → 28k，few-shot 佔比 **42%**。這正好落在 §2.10 稀釋律的射程內，
所以只看 `#93` 變綠就收工是不夠的 —— 必須再跑一輪完整 e2e 對照 §11.8 的
248/257。單題修好、整體變差，是這個專案已經踩過的形狀。

### 11.10 `#89` 的回歸：教訓一旦真的送達，它的**射程**就得寫清楚

§11.9 把 `reasoning` 接進 Prompt 之後，完整 e2e 的失敗題從 9 掉到 4，
但 `#89`「訂單張數最多的前 5 位客戶，各自的訂單狀態分布是什麼？」變差了。

先排除一個錯誤判斷：**n=8 對 `#89` 不夠**。同一份程式碼三次 n=8 量到
2/8、3/8、6/8 —— 據此說「6/8 掉到 2/8」是站不住的。改用受控 A/B
（同一個 process、monkeypatch `get_few_shot_text` 換成改動前的版本、
每臂 n=16）才拿到能用的數字：

| | OFF（只有 question + SQL） | ON（含 reasoning） |
|---|---|---|
| 補邊界前 | 14/16 | **7/16** |
| 補邊界後 | 12/16 | 10/16 |

（第一版 A/B 的 OFF 臂 8 次全 `row_count=None` —— `legacy()` 漏了
兩段式範例的 `sql_step1` / `sql_step2`，`ex["sql"]` 直接 KeyError。
測到的是 harness 不是系統，那組數字作廢。**A/B 的對照臂本身也要驗。**）

#### 失敗形狀說明了機制

52 這個數字不是隨機的：**52 = 訂單張數 ≥ 5 的客戶的所有（客戶, 狀態）組合**。
張數排行是 `10 / 8 8 / 7 7 7 7 / 6 6 6 / 5 5 …`，
`DENSE_RANK() OVER (ORDER BY 張數 DESC) <= 5` 取的是前 5 個**不同的張數**
（10、8、7、6、5）＝ 14 位客戶 ＝ 52 列。GT 要的是 `LIMIT 5` 的 5 位、19 列。

模型把 few-shot #4 的教訓 ——「『最高』是極值問題，可能有並列，
**不可用 LIMIT 1**，必須用 `DENSE_RANK()`」—— 套到了「前 5 位」上。
而那段 reasoning 正是 §11.9 才第一次送進 Prompt 的。
ON 的 9 次失敗有 8 次是 52 列，OFF 只有 1 次 —— 形狀對得上機制。

#### 這是 few-shot 的一般性風險，不是 `#89` 的個案

**教訓沒送出去的時候不會溢出；一旦真的送達，它就會往相鄰形狀擴散。**
`business_rules` 早就處理過同一件事 —— 週轉率那條規則有一整段「邊界」，
明講「每個倉庫的容量」「哪些商品庫存最少」是答得出來的、不要一起擋掉。
few-shot 從來沒有邊界段，因為它的 reasoning 根本沒進過 Prompt，
不需要邊界。§11.9 之後就需要了。

補在 few-shot #4 的 reasoning 裡（不是新規則，`business_rules` 維持 10 條）：

    極值（最高/最多/哪一個最…）→ 問的是第一名這個位置，`WHERE rnk = 1`
    top-N（前 5 位/前 10 名）    → 問的是一份 N 筆的清單，
                                  `ORDER BY … DESC, <唯一鍵> LIMIT N`
    `rnk <= N` 當成 top-N 是錯的：DENSE_RANK 排的是「不同的值」。

並在「哪一天的訂單數量最多」那則加交叉指向。

#### 收手的位置

補邊界後 ON 合併 17/28（61%）、OFF 合併 26/32（81%），差距約 20pp，
在這個樣本數下 Fisher p ≈ 0.09 —— **達不到顯著，但也不能說沒有**。
兩邊的失敗形狀還不一樣：OFF 全是 18 列（沒加第二排序鍵，第 5 名抓錯人），
ON 是殘留的 52 列。

到這裡停手。再往下就是對著單一題目調字句，那是 §5.2「一直改 K」的同一個錯誤。
`#89` 記為**已知代價**，不是待辦：

反向檢查（確認邊界沒有把真正需要 `rnk = 1` 的題目弄壞）：

```
#93   12/12   #121  12/12   #242  12/12
```

### 11.11 新基準（§11.9 + §11.10 之後）

完整 e2e 257 題：

```
correct 253｜defence_ok 4｜no_sql 1｜wrong 3      失敗題 9 → 4
不符：#34（5/8 硬幣）#45（8/8，那次是 llm_api_error）
      #89（已知代價，見 §11.10）#189（7/8 硬幣）
```

七項閘門全綠、單元測試 9 / 15 / 17 全過。

**這一輪真正的產出不是分數，是發現「以後都從 few-shot 解決」這條方針
在 2026-08-20 之前結構上做不到。** 過去每一次「加了 few-shot 沒效果」的
觀察都要重新看待 —— 當時送到模型眼前的只有 SQL 形狀那一半。
`check_schema_pipeline.py` 守的是 schema 鏈，守不到 prompt 鏈：
它七項檢查沒有一項會問「YAML 裡寫的東西有沒有真的進 Prompt」。

### 11.12 第八項閘門：守 prompt 鏈

§11.9 的 bug 讓 `check_schema_pipeline.py` 的盲區暴露出來：
**前七項守的是 schema 鏈（DB → DDL → parser），沒有一項守 prompt 鏈
（YAML → renderer → Prompt）。** 兩者是同一個形狀 —— 寫了、卻沒接上，
而且不會報錯 —— 差別只在 YAML 裡有欄位很容易讓人以為它有效。

第八項的做法：每個欄位挑**最長的一非空行**當探針，看它在不在 renderer
的輸出裡。不比對完整內容 —— renderer 本來就會加縮排與前綴，
比對整串只會一直假警報，而**常態誤報的檢查等於沒有檢查**（同 §11.8 的
`eval_lucky.py` 教訓）。涵蓋 `few_shot_examples`（question / reasoning /
sql / sql_step1 / sql_step2 / wrong_sql）、`business_rules`
（name / description / correct_sql / wrong_sql）與 `enum_fields`。

**驗它會響**（閘門不示範會響就是裝飾）——把 renderer 換回改動前的版本：

```
現況：                          無 —— 全部送得出去
換回改動前的 get_few_shot_text： 抓到 25 個漏掉的欄位
  few_shot_examples[找出所有姓「陳」的客戶名單].reasoning
  few_shot_examples[哪一個商品的總營收最高？總共賺了多少錢？].reasoning
  …
```

正是它該抓的那個 bug。八項現在全綠：

```
[1] DDL 覆蓋 [2] DDL 新鮮度 [3] 註解同步 [4] regex 可解
[5] KMB 成本 [6] 覆蓋與宣告 [7] 沒有空表
[8] prompt 鏈  few-shot 12,472 字元 / rules 6,221 字元，每個欄位都送得出去
```

順帶記一個工具面的教訓：這一輪有三次是**我自己寫的驗證程式壞掉**，
不是系統壞掉 —— A/B 的對照臂 8 次全 crash（`legacy()` 漏了兩段式範例）、
兩次 heredoc 把 `
` 吃成真換行寫出語法錯的檔案。
**對照組與檢查工具本身也要驗**，否則量到的是 harness 不是系統。

### 11.13 目錄分類（2026-08-20）

之前 40 幾個檔平鋪在根目錄，舊架構（`agent.py` 的 Tool-Calling 版）與現行
LangGraph pipeline 混在一起，看不出哪一條是主線。分成：

```
根目錄   eval_ground_truth.yaml、eval_questions_v2.json、ARCHITECTURE.md、README.md
langgraph_sql/  pipeline
eval/           11 支評估腳本 + results/（跑分產出，gitignore）
db/             init_db.py、init_db_ext.py、docker-compose.yml
tools/          八項閘門、建表腳本、gen_ddl、seed
test_script/    5 支測試
utils/          semantic_layer.yaml
```

**GT 留在根目錄** —— 8 支程式讀它（含 `tools/` 四支閘門），是專案的核心資產，
往下埋一層只是讓四支閘門的相對路徑變難看。

**跨目錄用 sys.path bootstrap，不做成套件。** `tools/` 本來就是這個寫法，
照抄過去即可；換成 `python -m evaluation.stability` 會讓
ARCHITECTURE.md、README、memory 裡幾十條已記錄的指令全部要重寫，
而那些指令是這個專案最常被重跑的東西。指令只從
`python eval_stability.py` 變成 `python eval/eval_stability.py`。

搬遷本身會踩到的三件事（都改掉了）：
  · GT 路徑原本相對 CWD（7 支），改成以 `_ROOT` 為基準
  · 跑分產出與 `glob("eval_result_*.json")` 改指向 `eval/results/`
  · `sync_table_comments.py` 的 `SOURCES` 指向 `init_db*.py` 的絕對路徑要跟著改
    —— 這支漏改的症狀是「改了註解沒生效」且不報錯，正是 §10.6 那個形狀

同時刪掉舊架構那一組（`agent.py` / `main.py` / `test_run.py` /
`generate_questions.py` / `get_answers.py` / `eval_questions.json` /
`sql_review.md` / `experiments/` 等，都在 git 歷史裡）與三個因此變成孤兒的檔
（根目錄 `config.py`、`utils/db_utils.py`、`utils/logger.py`）。

搬完的驗證：八項閘門全綠、單元測試 9/15/17、`eval/eval_gt_check.py` 通過、
`eval/eval_score.py` 讀得到 `eval/results/` 最新結果（253/261、防禦題 4/4）、
`eval/eval_stability.py --ids 242 --n 2` 端到端 2/2。
